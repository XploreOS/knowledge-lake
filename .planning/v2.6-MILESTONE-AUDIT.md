---
milestone: v2.6
audited: 2026-07-18T04:00:00Z
status: passed
scores:
  requirements: 20/20
  phases: 6/6
  integration: 19/19
  flows: 1/1
gaps: {}
tech_debt:
  - phase: milestone-wide
    items:
      - "RESOLVED (2026-07-18): Nyquist validation now reconciled for all 6 phases (17-22) — /gsd-validate-phase was re-run for each; all VALIDATION.md files now show status:validated, nyquist_compliant:true. See per-phase VALIDATION.md audit trails."
      - "RESOLVED (2026-07-18, Phase 22): Milestone success criteria #1/#2 now empirically measured against the real 34-source healthcare corpus via klake quality-audit --full. export_junk_rate fell 33%→0.0% (criterion #2, decisively met). chunk_garbage_rate came out at 45.64% — a different, expected-high metric (live gate-rejection rate of candidates, not garbage reaching the delivered corpus). Human/UAT decision (22-UAT.md, 2026-07-18) resolved the interpretation: criterion #1 is considered MET via export_junk_rate, the metric representing garbage actually reaching the delivered corpus."
  - phase: 20-chunk-substance-gate-export-gate / 21-index-time-dedup (interaction)
    items:
      - "RESOLVED (2026-07-18, Phase 22 D-07): export_rag_corpus() enumerating chunk artifacts pre-dedup (not post-dedup Qdrant points) is now explicitly documented in export.py's module docstring as an accepted design boundary (citation-complete training data vs. deduplicated retrieval), closing the 'undocumented scope decision' finding from the prior audit."
      - "EXPORT-02 (eval dataset versioning) has no cross-phase touchpoint in the traced ingest→export chain — self-contained to Phase 20's dataset-generation code. Not a gap, outside this audit's E2E trace scope; no action needed."
  - phase: milestone-wide (minor, non-blocking)
    items:
      - "Integration checker verified MEAS-02 (must-not-reject fixtures) and PIPE-01 (cache versioning) structurally (files/settings exist, correctly imported) but did not itself re-execute those test suites in this pass. Independently confirmed elsewhere in this session: tests/unit/test_must_not_reject.py (25/25 fixtures pass) and tests/unit/test_chunk_storage.py's PIPE-01 cache-versioning tests both pass — not an open gap, just noting the check method for transparency."
---

# Milestone v2.6 — Data Quality & Enrichment — Audit Report

**Audited:** 2026-07-18 (re-audit; supersedes 2026-07-17 report below the fold)
**Status:** ✓ passed — all requirements satisfied, no critical blockers, no unresolved tech debt

## Summary

All 6 phases (17–22) individually verified. Phases 17-21 verified `passed` with zero gaps; Phase 22 verified with 8/9 truths automatically confirmed and 1 human-interpretive item, which was resolved via UAT on 2026-07-18 (decision: criterion #1 met via `export_junk_rate`). All 20 requirements SATISFIED via 3-source cross-reference (phase VERIFICATION.md + SUMMARY frontmatter + REQUIREMENTS.md traceability table) — Phase 22 introduces zero new requirement IDs, only extends MEAS-01/QUAL-03/EXPORT-01. Integration checker confirmed all 19 requirement-to-code wiring paths are WIRED end-to-end across **both** the Dagster asset-graph path and the CLI/API/MCP (`process_crawled`) path, with no drift between the two call sites and no orphans. Nyquist validation, previously unreconciled for all 5 original phases, was fully reconciled this session — all 6 phases now show `status: validated, nyquist_compliant: true`. Both tech-debt items open after the 2026-07-17 audit (unmeasured success criteria; undocumented export/dedup interaction) are now RESOLVED by Phase 22.

## Requirements Coverage (20/20 SATISFIED)

| Requirement | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|---|---|---|---|---|---|
| CLEAN-01 | 17 | passed | listed (17-01, 17-02) | Complete | **satisfied** |
| CLEAN-02 | 17 | passed | listed (17-01, 17-03) | Complete | **satisfied** |
| CLEAN-03 | 17 | passed | listed (17-01) | Complete | **satisfied** |
| CLEAN-04 | 19 | passed | listed (19-04) | Complete | **satisfied** |
| CLEAN-05 | 19 | passed | listed (19-03) | Complete | **satisfied** |
| CLEAN-06 | 19 | passed | listed (19-02) | Complete | **satisfied** |
| GATE-01 | 18 | passed | `provides:[GATE-01]` (18-01) | Complete | **satisfied** |
| QUAL-01 | 19 | passed | listed (19-01) | Complete | **satisfied** |
| QUAL-02 | 20 | passed | listed (20-01) | Complete | **satisfied** |
| QUAL-03 | 20 | passed | listed (20-01, 20-02) | Complete | **satisfied** |
| QUAL-04 | 17 | passed | listed (17-01, 17-04) | Complete | **satisfied** |
| QUAL-05 | 17 | passed | listed (17-01) | Complete | **satisfied** |
| DEDUP-01 | 21 | passed | listed (21-01, 21-02, 21-04, 21-06, 21-07, 21-08) | Complete | **satisfied** |
| DEDUP-02 | 21 | passed | listed (21-01, 21-02, 21-04, 21-05, 21-08) | Complete | **satisfied** |
| DEDUP-03 | 21 | passed | listed (21-01, 21-03, 21-05, 21-08) | Complete | **satisfied** |
| EXPORT-01 | 20 | passed | listed (20-03) | Complete | **satisfied** |
| EXPORT-02 | 20 | passed | listed (20-03) | Complete | **satisfied** |
| MEAS-01 | 17 (extended 22) | passed | listed (17-04; 22-01/02/03) | Complete | **satisfied** |
| MEAS-02 | 20 | passed | listed (20-02, 20-04) | Complete | **satisfied** |
| PIPE-01 | 20 | passed | listed (20-01) | Complete | **satisfied** |

No orphaned requirements — all 20 REQ-IDs in REQUIREMENTS.md's traceability table are present in at least one phase VERIFICATION.md. Phase 22 correctly introduces zero new REQ-IDs (`grep -E "Phase 22" .planning/REQUIREMENTS.md` returns zero matches, consistent with 22-CONTEXT.md's explicit scope statement) — it extends MEAS-01/QUAL-03/EXPORT-01, all already "Complete" under their originating phases.

## Phase Verification Summary

| Phase | Goal | Status | Score | Gaps |
|---|---|---|---|---|
| 17 — Close the Bypass + Measurement | Cleaned text reaches all downstream consumers; garbage measurable | passed | 25/25 | None |
| 18 — Gate Decouple | Extending boilerplate patterns doesn't trigger re-crawl | passed | 5/5 | None |
| 19 — Section Classifier + Patterns | Junk sections removed at section granularity, clinical content protected | passed | 15/15 | None |
| 20 — Chunk Substance Gate + Export Gate | Garbage chunks rejected before embedding; gold export quality-gated | passed | 5/5 | None |
| 21 — Index-Time Dedup | Duplicate text embedded/indexed once, lineage preserved | passed | 8/8 (3 ROADMAP + all 8 plans' truths) | None |
| 22 — Address Tech Debt: Measure Garbage/Junk Rates E2E | Real measurement of milestone's 2 quantitative criteria against live 34-source corpus | passed (post-UAT) | 8/9 auto + 1/1 UAT | None (was 1 human-interpretive item, resolved 2026-07-18) |

All 6 phases: zero anti-patterns (no TBD/FIXME/XXX/TODO/HACK/stub markers), zero unresolved code-review findings across all phases with review-fix cycles (19, 20, 21, 22 — all independently re-verified as genuinely fixed via revert-and-retest or direct code diff, not merely trusted from narrative).

## Cross-Phase Integration (19/19 WIRED)

Integration checker (spawned this audit, model: sonnet) traced the full pipeline chain by reading actual source — not summaries — across both call sites: `crawl → parse → clean(domain_filters, section classification) → chunk(domain_filters, substance gate) → dedup_chunks() → embed(new-only) → index(duplicate_chunks) → export_rag_corpus(substance_passed)`.

**Dagster asset-graph path** (`dagster_defs/assets.py`): `ingest_raw_document → parsed_document → clean_document → enrich_document → curate_document_asset → chunk_document → dedup_chunks → embed_chunks → index_chunks`. Confirmed via `deps=` edges and `core_pipeline_e2e_job`'s asset selection (which explicitly includes `dedup_chunks`, with an inline comment warning against the exact regression class this would create if omitted).

**CLI/API/MCP path** (`pipeline/process.py::process_crawled`): `parse → clean(domain_filters) → chunk(cleaned_doc, domain_filters) → dedup_chunks(chunks_list) → embed(dedup_result["new"]) → index(dedup_result["new"], duplicate_chunks=dedup_result["duplicates"])`. Structurally identical ordering and data flow to the Dagster path — same `new`/`duplicates` routing, same `domain_filters` resolution pattern. **No drift between call sites.**

| Requirement | Integration Path | Status |
|---|---|---|
| CLEAN-01 | Dagster `clean_document` forwards cleaned `parsed_doc` → `chunk_document` | WIRED |
| CLEAN-02 | `process_crawled`'s `clean()` inserted parse→chunk | WIRED |
| CLEAN-03 | `clean.py` WR-05 parent-scoped SHA256 hash | WIRED |
| CLEAN-04 | `classify_sections()` → `_clean_sections()` → `clean()` | WIRED |
| CLEAN-05 | `BOILERPLATE_PATTERNS.extend()` (additive, preserves Phase 18 gate) | WIRED |
| CLEAN-06 | `DomainFilters.normative_allowlists` → `classify_sections()` | WIRED |
| GATE-01 | `crawl.py`'s `_GATE_BOILERPLATE_PATTERNS` decoupled from `clean.py` | WIRED |
| QUAL-01 | `pipeline/quality/` predicate package, zero I/O | WIRED |
| QUAL-02 | `FineWebQualityFilter` in `_build_fineweb_filter()` (chunk.py) | WIRED |
| QUAL-03 | `_apply_substance_gate` + `domain_filters` at both call sites | WIRED |
| QUAL-04 | `rejection_reason` recorded in raw chunks | WIRED |
| QUAL-05 | Conservation invariant check (`chunk.py`) | WIRED |
| DEDUP-01 | `dedup_chunks()` — both Dagster asset + CLI inline call | WIRED |
| DEDUP-02 | `uuid5` point_id (`index.py`) | WIRED |
| DEDUP-03 | `contributors[]` capped mirror (`index.py`) | WIRED |
| EXPORT-01 | `export_rag_corpus()`'s `substance_passed` gate | WIRED |
| EXPORT-02 | `export_pretrain_corpus()`'s `min_quality_score` filter | WIRED |
| MEAS-01 | `run_quality_audit()` + `run_full_pipeline_audit()`, both CLI-wired | WIRED |
| MEAS-02 | `must_not_reject.yaml` fixtures (structurally confirmed; suite independently re-run earlier this session, 25/25 pass) | WIRED |
| PIPE-01 | `ChunkQualitySettings.filter_config_version` versioning | WIRED |

**Ordering constraint check (ROADMAP.md L3<L4<L5):** substance gate (chunk, Phase 20) → dedup (Phase 21) → embed/index → export gate (Phase 20) confirmed in both call sites — `_apply_substance_gate` runs inside `chunk()`, which produces the list consumed by `dedup_chunks()`; `export_rag_corpus()`'s gate reads `substance_passed` from already-chunked/gated artifacts. **No ordering violations found.**

**Broken flows:** 0
**Orphaned connections:** 0

**Note:** `tree_index_document` is confirmed an intentional parallel branch off `clean_document` (not part of the L3-L5 chain) — correctly excluded from dedup/export ordering constraints per its own docstring; not a defect.

## Findings — All Resolved

The 2026-07-17 audit (Phases 17-21 only) flagged three items. All three are now closed:

1. ~~**Gold export is not dedup-aware.**~~ **RESOLVED (Phase 22, D-07).** `export_rag_corpus()`'s module docstring now explicitly documents the chunk-artifact-scoped (not dedup-collapsed) export design as an accepted boundary — citation-complete training data (export) vs. deduplicated retrieval (index). Zero `export.py` logic change; documentation-only closure, independently confirmed via byte-identical diff of the filter line before/after.

2. ~~**Quantitative success criteria unmeasured.**~~ **RESOLVED (Phase 22).** A real `klake quality-audit --domain healthcare --full --json` run against the live 34-source corpus produced actual numbers: `export_junk_rate` fell 33%→0.0% (criterion #2, decisively met, target <2%); `chunk_garbage_rate` came out at 45.64% (criterion #1's literal reading, against a 28% baseline that used a different measurement basis — post-hoc classification of an ungated legacy corpus vs. this run's live gate-rejection rate of raw candidates). The Pitfall-2/A1 convergence check (`export_junk_rate <= chunk_garbage_rate`) was explicitly verified to hold (0.0% <= 45.64%). The resulting interpretive ambiguity was surfaced honestly (not glossed over) and routed to human verification.

3. ~~**Nyquist validation not reconciled.**~~ **RESOLVED (this session, 2026-07-18).** `/gsd-validate-phase` was re-run for all 6 phases (17-22). All VALIDATION.md files now show `status: validated`, `nyquist_compliant: true`. Zero gaps found during reconciliation — all previously-planned test coverage was confirmed actually implemented and green (full suite: 1181 passed, 3 skipped, 6 xfailed, 0 failed).

## Nyquist Coverage (6/6 COMPLIANT)

| Phase | VALIDATION.md | Status field | nyquist_compliant | Classification |
|---|---|---|---|---|
| 17 | exists | validated | true | **COMPLIANT** |
| 18 | exists | validated | true | **COMPLIANT** |
| 19 | exists | validated | true | **COMPLIANT** |
| 20 | exists | validated | true | **COMPLIANT** |
| 21 | exists | validated | true | **COMPLIANT** |
| 22 | exists | validated | true | **COMPLIANT** |

All 6 phases reconciled this session via `/gsd-validate-phase` (2026-07-18). Zero gaps found across all phases — every requirement's test coverage claimed at planning time was independently confirmed present, correctly named, and green against the live codebase. See each phase's `VALIDATION.md` "Validation Audit 2026-07-18" section for the full per-phase audit trail.

## Human Verification / UAT Resolution

Phase 22's verifier routed one interpretive question to human sign-off rather than resolving it unilaterally (per `22-VERIFICATION.md`, status `human_needed`). This was resolved via `22-UAT.md` on 2026-07-18:

> **Decision:** Criterion #1 is considered **MET**, read via `export_junk_rate` (0.0%, decisively beats the <2% target and is the metric that represents garbage actually reaching the delivered corpus). The literal `chunk_garbage_rate` reading (45.64%) is acknowledged as a different, expected-to-be-high metric (the gate's own live candidate-rejection rate) and is not the criterion-#1 metric.

This decision is consistent with the independent analysis in `22-VERIFICATION.md` (which reached the same conclusion via its own reasoning before routing to human sign-off) and with `STATE.md`'s "Resolved in v2.6" entry. No REQUIREMENTS.md wording change was made as part of this resolution — the decision was recorded via UAT rather than a wording edit; a future maintainer could still choose to revise the wording as a small housekeeping follow-up, but this does not block milestone completion.

## Milestone Success Criteria (from ROADMAP.md)

| # | Criterion | Status |
|---|---|---|
| 1 | `klake process` on 34 sources produces <5% garbage chunks | **Met** (via UAT interpretation — see above; `export_junk_rate` 0.0%, the corpus-quality successor metric to the original 28%-baseline measurement basis) |
| 2 | Gold RAG export contains <2% junk rows | **Met** — `export_junk_rate` 0.0%, down from 33% baseline, decisively beats <2% target |
| 3 | Must-not-reject fixtures pass | **Met** — 25/25 fixtures pass (MEAS-02) |
| 4 | Conservation invariant holds for every source | **Met** — enforced as `RuntimeError`, tested (QUAL-05) |
| 5 | No cross-document artifact corruption (WR-05 extended to clean stage) | **Met** — CLEAN-03, verified with distinct-hash tests |
| 6 | No regressions: 971+ tests pass, `xfail_strict` holds | **Met** — 1181 passed, 0 failed, 3 skipped, 6 xfailed (`xfail_strict` active), confirmed via independent re-run this session |

**6/6 criteria met.** All quantitative targets now have real measurement evidence from a live run against the 34-source healthcare corpus, with the one interpretive ambiguity resolved via documented UAT sign-off.

---

_Audited: 2026-07-18T04:00:00Z_
_Auditor: Claude (gsd-audit-milestone)_

---

## Appendix: Prior Audit (2026-07-17, Phases 17-21 only, superseded above)

_Audited: 2026-07-17T15:30:00Z_
_Status: tech_debt (2 open items — both resolved by Phase 22, see Findings section above)_

The original audit covered only Phases 17-21 (before Phase 22 existed) and scored requirements 20/20, phases 5/5, integration 9/9, flows 1/1, with `tech_debt` status pending the two now-resolved findings above. Full original report superseded; historical detail preserved in git history (commit `44edfe4` and earlier) if needed.
