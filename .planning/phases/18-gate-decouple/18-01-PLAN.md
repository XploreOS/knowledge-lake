---
phase: 18-gate-decouple
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge_lake/pipeline/crawl.py
  - tests/unit/test_gate_signature_pin.py
autonomous: true
requirements:
  - GATE-01
must_haves:
  truths:
    - Adding any pattern to clean.py BOILERPLATE_PATTERNS does not change the return value of _signature() for any existing text
    - _signature(None) and _signature("") both return a deterministic, stable SHA256 hex digest (the markdown-or-empty guard ensures this is frozen)
    - The 4 patterns in _GATE_BOILERPLATE_PATTERNS are applied in this exact order: page-headers, cookie/privacy, navigation, copyright/disclaimer — identical to BOILERPLATE_PATTERNS at freeze date 2026-07-15; output is fully deterministic for any given input
    - _gate_normalize() produces byte-identical output to remove_boilerplate() for the same 4 patterns on identical input (no regression on existing test_recrawl_gate.py)
    - The from knowledge_lake.pipeline.clean import remove_boilerplate line does not appear in crawl.py after this phase
  artifacts:
    - src/knowledge_lake/pipeline/crawl.py — contains _GATE_BOILERPLATE_PATTERNS, _gate_normalize(), updated _signature(), no remove_boilerplate import
    - tests/unit/test_gate_signature_pin.py — contains test_gate_signature_byte_stable and test_gate_decoupled_from_clean_patterns, both passing
  key_links:
    - _signature() calls _gate_normalize() (not remove_boilerplate) — the critical decoupling link
    - _gate_normalize() inlines whitespace normalization verbatim from _normalize_whitespace() in clean.py — exact match prevents regression on existing gate tests
    - test_gate_decoupled_from_clean_patterns temporarily appends to BOILERPLATE_PATTERNS and asserts _signature() is unchanged — proves the decoupling is real, not assumed
---

<objective>
Sever the re-crawl change gate from clean.py by replacing the imported remove_boilerplate() call with a gate-local _gate_normalize() function that holds a frozen copy of the current 4 boilerplate patterns. Lock the resulting signature with a byte-stability pinning test, and prove decoupling by simulating a Phase 19 pattern extension.

Purpose: Phase 19 will extend BOILERPLATE_PATTERNS in clean.py. Without this phase, every source's _signature() output would change after that extension, triggering re-crawl of all 34 sources. This phase makes Phase 19 safe to ship.

Output:
- crawl.py with _GATE_BOILERPLATE_PATTERNS + _gate_normalize() + updated _signature() + removed import (GATE-01, D-01 through D-04)
- tests/unit/test_gate_signature_pin.py with pinning test (D-05) and decoupling assertion (D-06)
</objective>

<execution_context>
@/root/healthlake/.claude/gsd-core/workflows/execute-plan.md
@/root/healthlake/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/root/healthlake/.planning/PROJECT.md
@/root/healthlake/.planning/ROADMAP.md
@/root/healthlake/.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze gate-local boilerplate patterns and replace remove_boilerplate import in crawl.py (D-01 through D-04)</name>
  <files>src/knowledge_lake/pipeline/crawl.py</files>

  <read_first>
    - src/knowledge_lake/pipeline/crawl.py lines 1-119 — module header, imports (line 40: remove_boilerplate), _VOLATILE_PATTERNS (77-90), _suppress_volatile() (93-103), _signature() (106-118)
    - src/knowledge_lake/pipeline/clean.py lines 42-92 — BOILERPLATE_PATTERNS (48-62, the 4 patterns to freeze), _normalize_whitespace() (68-80, copy verbatim), remove_boilerplate() (83-92)
    - tests/unit/test_recrawl_gate.py — all 5 tests; these are the regression guard for this task
  </read_first>

  <behavior>
    - _signature("# Health\n\nContent.") returns the same hex digest before and after this refactor (no behavioral change for any existing test input)
    - _signature("") returns a stable non-empty SHA256 hex digest (the markdown-or-empty guard is preserved)
    - _gate_normalize("Page 1 of 5\n\n# Title\n\nText.") strips the page-header line and returns normalized whitespace — identical to what remove_boilerplate() would have returned for the same input with the same 4 patterns
    - The module does not import remove_boilerplate from clean.py at any scope
  </behavior>

  <action>
    Insert the following in crawl.py after the _suppress_volatile() function (after line 103, before the blank line before _signature()):

    1. Add _GATE_BOILERPLATE_PATTERNS list — a module-private List[re.Pattern] with 4 compiled patterns in this exact order, matching BOILERPLATE_PATTERNS in clean.py at freeze date 2026-07-15 (GATE-01, D-01, D-02):
       - Pattern 1 (page headers/footers): re.compile(r"^(?:Page \d+ of \d+|\d+)\s*$", re.MULTILINE)
       - Pattern 2 (cookie/privacy banners): re.compile(r"(?i)(?:this site uses cookies|accept all cookies|cookie policy)[^\n]*$", re.MULTILINE)
       - Pattern 3 (navigation elements): re.compile(r"(?im)^(?:home|about us|contact|sitemap|skip to (?:main )?content)\s*$")
       - Pattern 4 (copyright/disclaimer lines): re.compile(r"(?i)^(?:disclaimer|copyright \d{4})[^\n]*$", re.MULTILINE)

       Add a block comment above the list explaining: GATE-01 — frozen point-in-time snapshot of BOILERPLATE_PATTERNS (clean.py) at 2026-07-15. Deliberately NOT auto-synced — changes to BOILERPLATE_PATTERNS in clean.py do not propagate here. This mirrors the _VOLATILE_PATTERNS pattern in this same file. The gate uses this frozen copy so extending clean patterns (Phase 19) does not change any source's stored signature and trigger spurious re-crawls.

    2. Add _gate_normalize(text: str) -> str function immediately after the patterns list. This function:
       - Iterates _GATE_BOILERPLATE_PATTERNS and applies pattern.sub("", text) for each pattern in order (matching remove_boilerplate() behavior exactly)
       - Inlines whitespace normalization — copy _normalize_whitespace() from clean.py verbatim: strip trailing whitespace per line with a list comprehension, re.join, re.sub(r"\n{3,}", "\n\n", text), return text.strip()
       - Add docstring: "Gate-local normalization: frozen boilerplate patterns plus whitespace collapse. GATE-01: deliberately decoupled from clean.py. Changes to BOILERPLATE_PATTERNS in clean.py do NOT affect this function. Edit _GATE_BOILERPLATE_PATTERNS explicitly if gate normalization must change."
       - Signature: def _gate_normalize(text: str) -> str

    3. Update _signature() function:
       - Replace the call remove_boilerplate(markdown or "") with _gate_normalize(markdown or "")
       - Update the docstring: replace the paragraph that says "Reuses the silver-stage remove_boilerplate so the gate and the clean stage agree on boilerplate (D-06)" with: "Uses gate-local _gate_normalize() (frozen patterns, GATE-01) so changes to clean.py BOILERPLATE_PATTERNS do not alter stored source signatures." Keep the volatile-token suppression description and SCHED-02 references unchanged.

    4. Remove the import line: from knowledge_lake.pipeline.clean import remove_boilerplate
       The remaining imports from knowledge_lake.pipeline.clean are none — that entire from-import line is removed.

    5. Update the recrawl_source() docstring: replace references to "remove_boilerplate" with "_gate_normalize" where the SCHED-02 normalization step is described (search for "remove_boilerplate" in the docstring body and update the affected sentence).

    Pitfall guard (RESEARCH.md Pitfall 3): The inlined whitespace normalization in _gate_normalize() must match _normalize_whitespace() from clean.py character-for-character. Read clean.py lines 76-80 before writing the inline version. Do not paraphrase — copy the 4-line body exactly.

    Pitfall guard (RESEARCH.md Pitfall 2): Run pytest tests/unit/test_recrawl_gate.py -x after this change. If test_nonce_noise_unchanged or test_unchanged_skips_no_raw fails, the pattern or whitespace logic diverged from remove_boilerplate() — diagnose and fix before proceeding to Task 2.
  </action>

  <verify>
    <automated>uv run pytest tests/unit/test_recrawl_gate.py -x -v</automated>
  </verify>

  <acceptance_criteria>
    - All 5 tests in test_recrawl_gate.py pass
    - grep -c "from knowledge_lake.pipeline.clean import remove_boilerplate" src/knowledge_lake/pipeline/crawl.py outputs 0
    - grep -c "_GATE_BOILERPLATE_PATTERNS" src/knowledge_lake/pipeline/crawl.py outputs at least 2 (definition site + usage in _gate_normalize)
    - grep -c "_gate_normalize" src/knowledge_lake/pipeline/crawl.py outputs at least 2 (definition site + call in _signature)
    - grep -c "remove_boilerplate" src/knowledge_lake/pipeline/crawl.py outputs 0 (no stale references in code or docstrings)
  </acceptance_criteria>

  <done>_GATE_BOILERPLATE_PATTERNS and _gate_normalize() exist in crawl.py; _signature() calls _gate_normalize(); remove_boilerplate import is absent; all 5 tests in test_recrawl_gate.py pass</done>
</task>

<task type="auto">
  <name>Task 2: Create gate signature pinning test with byte-stability and decoupling assertions (D-05, D-06)</name>
  <files>tests/unit/test_gate_signature_pin.py</files>

  <read_first>
    - src/knowledge_lake/pipeline/crawl.py — the updated _signature() and _gate_normalize() from Task 1 (needed to compute the pinned hash)
    - src/knowledge_lake/pipeline/clean.py lines 46-62 — BOILERPLATE_PATTERNS list (used in the decoupling assertion to simulate a Phase 19 extension)
    - tests/unit/test_recrawl_gate.py — test file conventions in this project (imports, async marks, fixture style)
  </read_first>

  <action>
    Create tests/unit/test_gate_signature_pin.py. The file imports _signature from knowledge_lake.pipeline.crawl and BOILERPLATE_PATTERNS plus remove_boilerplate from knowledge_lake.pipeline.clean.

    STEP 1 — Compute the pinned hash. After Task 1 is complete, run this command to get the actual SHA256 hex digest for the known fixture:

      uv run python -c "
      from knowledge_lake.pipeline.crawl import _signature
      fixture = (
          'Page 3 of 10\n\n'
          '# Clinical Guidelines for Hypertension Management\n\n'
          'Evidence-based recommendations for care.\n\n'
          'Skip to main content\n\n'
          'Copyright 2026 HealthOrg. All rights reserved.\n'
      )
      print(_signature(fixture))
      "

    Record the printed hex string. This is _EXPECTED_HASH.

    STEP 2 — Write the test file with this structure:

    Module-level constants:
    - _FIXTURE: a multi-line string that contains all 4 gate-pattern triggers plus substantive content. Use exactly:
      "Page 3 of 10\n\n# Clinical Guidelines for Hypertension Management\n\nEvidence-based recommendations for care.\n\nSkip to main content\n\nCopyright 2026 HealthOrg. All rights reserved.\n"
    - _EXPECTED_HASH: the hex string computed in STEP 1 (hardcoded, no computation at import time)

    Function test_gate_signature_byte_stable():
    - Docstring: "GATE-01: Gate signature must not change when clean.py patterns change. (D-05)"
    - Calls _signature(_FIXTURE) and assigns to actual
    - Asserts actual == _EXPECTED_HASH with failure message per D-05 — the message must say: gate signature changed means re-crawl of all sources, update the pin only if intentional

    Function test_gate_decoupled_from_clean_patterns():
    - Docstring: "GATE-01: Adding a new pattern to BOILERPLATE_PATTERNS does not change the gate signature. (D-06) Simulates Phase 19 extending clean patterns."
    - Records sig_before = _signature(_FIXTURE) and original_len = len(BOILERPLATE_PATTERNS)
    - Appends re.compile(r"(?i)^subscribe to our newsletter[^\n]*$", re.MULTILINE) to BOILERPLATE_PATTERNS inside a try/finally block that pops the appended entry on exit
    - Records sig_after = _signature(_FIXTURE) inside the try block
    - Asserts sig_before == sig_after with failure message: gate signature changed after extending BOILERPLATE_PATTERNS — gate is still coupled to clean.py
    - The finally block restores BOILERPLATE_PATTERNS to original_len via pop() and asserts len(BOILERPLATE_PATTERNS) == original_len

    Imports needed: re, pytest (optional for docstrings), and the two pipeline imports above.

    No async marks needed — both functions are synchronous.

    Pitfall guard (RESEARCH.md Pitfall 1): _EXPECTED_HASH must be the value computed from the actual Task 1 implementation via the STEP 1 command above, NOT a placeholder. If the value is still "0000..." or empty the test will fail on first run.

    Pitfall guard (RESEARCH.md Pitfall 5): Never add an xfail marker. The xfail_strict = true setting in pyproject.toml will cause the build to fail if a passing test is marked xfail.
  </action>

  <verify>
    <automated>uv run pytest tests/unit/test_gate_signature_pin.py tests/unit/test_recrawl_gate.py -x -v</automated>
  </verify>

  <acceptance_criteria>
    - test_gate_signature_byte_stable passes (actual == _EXPECTED_HASH)
    - test_gate_decoupled_from_clean_patterns passes (sig_before == sig_after after BOILERPLATE_PATTERNS is temporarily extended)
    - All 5 tests in test_recrawl_gate.py still pass (no regression from test isolation)
    - _EXPECTED_HASH in the test file is a 64-character lowercase hex string (valid SHA256)
    - BOILERPLATE_PATTERNS has the same length after test_gate_decoupled_from_clean_patterns runs as before (the finally block restores it — grep or inspect the finally block)
    - uv run pytest tests/ -x exits 0 (full suite green)
  </acceptance_criteria>

  <done>test_gate_signature_pin.py exists with hardcoded _EXPECTED_HASH (64-char hex); both test_gate_signature_byte_stable and test_gate_decoupled_from_clean_patterns pass; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None applicable | This phase modifies no data paths, no inputs, no outputs, no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | _gate_normalize() — pattern order or whitespace logic diverges from remove_boilerplate() | low | accept | Regression guard: test_recrawl_gate.py all 5 tests must pass after Task 1. If they fail, normalization diverged — fix before shipping. No security impact; risk is operational (false "changed" signals). |
| T-18-02 | Tampering | _GATE_BOILERPLATE_PATTERNS accidentally synced to BOILERPLATE_PATTERNS at runtime | low | accept | No sync mechanism is introduced. Patterns are a module-level list literal. Static analysis (grep -c "BOILERPLATE_PATTERNS" crawl.py) confirms no runtime reference to the clean.py list. |

**Security surface:** None. This is a pure internal refactoring of a normalization function within crawl.py. No new inputs, no new network calls, no new data paths, no authentication or authorization surfaces. SSRF guards in recrawl_source() are unchanged. SHA256 is used for content fingerprinting only, not for security. ASVS L1 has no applicable categories (V2–V6 all inapplicable per RESEARCH.md Security Domain assessment).

**slopcheck:** No package-manager installs in this phase. No new dependencies. T-{phase}-SC row not applicable.
</threat_model>

<verification>
Full suite regression:

  uv run pytest tests/ -x

Expected: all tests pass, 0 failures. The pinning test asserts byte-stability. The decoupling test asserts BOILERPLATE_PATTERNS independence. The existing test_recrawl_gate.py asserts no behavioral regression in the gate.

Import check (no stale coupling):

  python -c "import knowledge_lake.pipeline.crawl; import knowledge_lake.pipeline.clean; print('OK')"

Should print OK with no ImportError or AttributeError.

Decoupling smoke check:

  python -c "
  from knowledge_lake.pipeline import crawl, clean
  import re
  text = '# Test\n\nContent.\n'
  sig_before = crawl._signature(text)
  clean.BOILERPLATE_PATTERNS.append(re.compile(r'TESTONLY'))
  sig_after = crawl._signature(text)
  clean.BOILERPLATE_PATTERNS.pop()
  assert sig_before == sig_after, 'FAIL: gate is still coupled'
  print('PASS: gate is decoupled')
  "
</verification>

<success_criteria>
Phase 18 is complete when:

1. grep -c "from knowledge_lake.pipeline.clean import remove_boilerplate" src/knowledge_lake/pipeline/crawl.py returns 0
2. crawl.py contains _GATE_BOILERPLATE_PATTERNS (4 compiled patterns) and _gate_normalize()
3. _signature() calls _gate_normalize() not remove_boilerplate()
4. test_gate_signature_pin.py exists with a hardcoded 64-char SHA256 hex _EXPECTED_HASH
5. uv run pytest tests/unit/test_gate_signature_pin.py tests/unit/test_recrawl_gate.py -x exits 0
6. uv run pytest tests/ -x exits 0 (full suite green)
7. A Phase 19 extension to BOILERPLATE_PATTERNS does not change the output of _signature() for any existing text
</success_criteria>

## Artifacts This Phase Produces

### New symbols introduced
- `_GATE_BOILERPLATE_PATTERNS: list[re.Pattern]` — module-private list in `src/knowledge_lake/pipeline/crawl.py`; frozen copy of BOILERPLATE_PATTERNS at 2026-07-15
- `_gate_normalize(text: str) -> str` — module-private function in `src/knowledge_lake/pipeline/crawl.py`; replaces remove_boilerplate() in the gate path

### Modified symbols
- `_signature(markdown: str) -> str` in `crawl.py` — call site changed from `remove_boilerplate(markdown or "")` to `_gate_normalize(markdown or "")`; docstring updated

### Removed symbols / imports
- `from knowledge_lake.pipeline.clean import remove_boilerplate` — removed from `crawl.py` imports entirely

### New test file
- `tests/unit/test_gate_signature_pin.py` — two test functions:
  - `test_gate_signature_byte_stable()` — asserts `_signature(_FIXTURE) == _EXPECTED_HASH` (D-05)
  - `test_gate_decoupled_from_clean_patterns()` — simulates BOILERPLATE_PATTERNS extension, asserts gate signature unchanged (D-06)

### Changed test files
- None — `tests/unit/test_recrawl_gate.py` is a regression guard (must pass unchanged)

## Source Coverage Audit

| Source Type | Item | Status | Plan/Task |
|-------------|------|--------|-----------|
| GOAL | "Extending boilerplate patterns no longer triggers re-crawl of all sources" | COVERED | 18-01 Task 1+2 |
| REQ GATE-01 | Frozen gate-local patterns, not evolving BOILERPLATE_PATTERNS | COVERED | 18-01 Task 1 (D-01/D-02) |
| REQ GATE-01 | Pinning test asserts gate-signature byte-stability across clean-stage pattern change | COVERED | 18-01 Task 2 (D-05/D-06) |
| CONTEXT D-01 | _GATE_BOILERPLATE_PATTERNS frozen inline list in crawl.py | COVERED | 18-01 Task 1 |
| CONTEXT D-02 | NOT auto-synced; deliberate static copy | COVERED | 18-01 Task 1 (comment + no sync mechanism) |
| CONTEXT D-03 | _gate_normalize() replacing remove_boilerplate() in _signature() | COVERED | 18-01 Task 1 |
| CONTEXT D-04 | from .clean import remove_boilerplate removed entirely | COVERED | 18-01 Task 1 |
| CONTEXT D-05 | Pinning test with failure message | COVERED | 18-01 Task 2 |
| CONTEXT D-06 | Decoupling assertion simulating Phase 19 | COVERED | 18-01 Task 2 |
| RESEARCH pitfall 2 | Existing test_recrawl_gate.py regression guard | COVERED | 18-01 Task 1 verify |
| RESEARCH pitfall 3 | Whitespace normalization copied verbatim from clean.py | COVERED | 18-01 Task 1 action |

<output>
When execution is complete, create `.planning/phases/18-gate-decouple/18-01-SUMMARY.md` following the template at `/root/healthlake/.claude/gsd-core/templates/summary.md`.
</output>
