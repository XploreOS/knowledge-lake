---
phase: 20-chunk-substance-gate-export-gate
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - tests/fixtures/must_not_reject.yaml
  - tests/unit/test_must_not_reject.py
autonomous: true
requirements: [MEAS-02]

must_haves:
  truths:
    - "tests/fixtures/must_not_reject.yaml contains at least 20 entries, each with label/text/category, spanning all 5 MEAS-02 categories: icd_code, dosage, loinc, hipaa_ref, cardinality_constraint (D-15)"
    - { statement: "MEAS-02 has no natural 'merge/collide/separate' adjacency semantics — fixture entries are independent, hand-authored clinical text samples, not intervals or ranges that could touch/overlap; the only adjacency-adjacent risk is a future fixture edit reducing a sample below check_terminal_punct_ratio's _MIN_SAMPLE_WORDS=6 boundary or removing its terminal punctuation, silently changing which exemption path it takes — flagged as a backstop: the CI test asserts substance_passed=True end-to-end regardless of WHICH predicate path a fixture takes, so this drift is still caught even though it is not independently distinguished", verification: backstop }
    - "A parametrized pytest test loads the YAML and calls chunk() — the real Plan 20-01 gate function, not just the bare predicate functions in isolation — with domain_filters resolved via DomainLoader.from_name('healthcare').filters, asserting every fixture entry survives with substance_passed=True (D-16, closing RESEARCH.md Pitfall 1's warning that a predicate-only test does not prove the pipeline mechanism)"
    - "CI fails (non-zero exit) if any fixture entry is rejected by the substance gate — a red assertion, not a skipped/xfail test"
    - "The must_not_reject.yaml fixture list is asserted non-empty and >= 20 at test-collection time — pytest.mark.parametrize over an accidentally-emptied YAML file fails loudly (0 collected tests would otherwise pass vacuously) rather than silently passing"
    - "Fixture entries are evaluated in YAML list order via pytest.mark.parametrize (pytest preserves parametrize list order) — no sorting/dedup is applied, so a test failure always attributes a specific label to a specific list position"
  artifacts:
    - "tests/fixtures/must_not_reject.yaml — new fixture file"
    - "tests/unit/test_must_not_reject.py — new parametrized CI test"
  key_links:
    - "tests/fixtures/must_not_reject.yaml -> loaded and parametrized by tests/unit/test_must_not_reject.py"
    - "tests/unit/test_must_not_reject.py -> calls Plan 20-01's chunk() with domain_filters resolved via Plan 20-02's DomainLoader pattern, exercising the same predicate chain a live pipeline run uses"
  prohibitions:
    - statement: "MUST NOT let a passing must-not-reject CI suite be treated as an exhaustive clinical-safety guarantee — the ~20-entry fixture set is a representative sample across 5 categories, not a complete enumeration of protected clinical-code shapes; this scope boundary must be stated explicitly in the fixture file or test module docstring, never silently implied as 'all clinical codes are safe'."
      status: flagged-unverified
---

<objective>
Build the CI safety net for MEAS-02: a hand-labeled `must_not_reject.yaml` fixture set (~20 entries across 5 clinical-content categories) and a parametrized test proving every entry survives the REAL `chunk()` substance gate — with `domain_filters` resolved exactly as Plan 20-02 wired it into production — not just the bare predicate functions checked in isolation.

Purpose: This is the milestone's explicit safety backstop ("Must-not-reject fixtures pass — no clinical codes, dosage instructions, or normative statements dropped"). Testing at the `chunk()` level (not just `run_predicates()` directly) closes RESEARCH.md's own explicitly-flagged gap: a fixture test that only calls the predicate function can pass while the real pipeline still drops the same text, because the pipeline never wires `domain_filters` in. Plan 20-02 already fixed the wiring; this plan is the CI proof that the fix actually protects the fixture set end-to-end.

Output: `tests/fixtures/must_not_reject.yaml` (~20 entries) and `tests/unit/test_must_not_reject.py` (parametrized, calling `chunk()` directly).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-chunk-substance-gate-export-gate/20-CONTEXT.md
@.planning/phases/20-chunk-substance-gate-export-gate/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the must_not_reject.yaml fixture set</name>
  <files>tests/fixtures/must_not_reject.yaml</files>
  <read_first>
    domains/healthcare/filters.yaml (as extended by Plan 20-02 — all 7 normative_allowlists patterns this fixture set must exercise)
    src/knowledge_lake/pipeline/quality/predicates.py (check_terminal_punct_ratio's _MIN_SAMPLE_WORDS=6 boundary and DEFAULT_MIN_TERMINAL_PUNCT_RATIO — informs which fixture texts need an allowlist rescue vs. which naturally clear the thresholds)
    .planning/phases/20-chunk-substance-gate-export-gate/20-RESEARCH.md (Common Pitfalls section — the exact empirically-tested failure/rescue behavior for "ICD-10 E11.9", "See §164.502...", "Meets 2 of 4 SIRS criteria")
  </read_first>
  <action>
    Create `tests/fixtures/must_not_reject.yaml` as a YAML list of at least 20 mapping entries, each with exactly three keys: `label` (a short unique slug, e.g. `icd_diabetes_1`), `text` (the exact clinical string), and `category` (one of `icd_code`, `dosage`, `loinc`, `hipaa_ref`, `cardinality_constraint`). Include at least 4 entries per category (5 categories x 4 = 20 minimum). Author realistic, healthcare-domain-appropriate samples: `icd_code` entries like `"ICD-10 E11.9"` and `"Diagnosis code ICD-10 I10 for essential hypertension"`; `dosage` entries like `"Metformin 500 mg PO BID"` and `"Administer 10 mg IV push"`; `loinc` entries referencing `"LOINC"` codes (e.g. `"LOINC 2345-7 Glucose result"`); `hipaa_ref` entries using the `§\d+\.\d+` pattern (e.g. `"See §164.502 for permitted disclosures."`); `cardinality_constraint` entries using the Plan 20-02 pattern (e.g. `"Meets 2 of 4 SIRS criteria"` and `"3/5 Duke criteria present"`). Add a top-level YAML comment (before the list) stating explicitly that this fixture set is a REPRESENTATIVE SAMPLE across 5 categories, not an exhaustive enumeration of every protected clinical-code shape — this satisfies the prohibition-recall finding above (scope-boundary documentation, not a mechanical check).
  </action>
  <acceptance_criteria>
    - The file parses as valid YAML via `yaml.safe_load`
    - The resulting list has >= 20 entries, each with exactly `label`/`text`/`category` keys
    - All 5 categories (`icd_code`, `dosage`, `loinc`, `hipaa_ref`, `cardinality_constraint`) are represented with >= 4 entries each
    - Every `label` value is unique
    - A leading YAML comment documents the representative-sample (not exhaustive) scope boundary
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml, collections
data = yaml.safe_load(open('tests/fixtures/must_not_reject.yaml'))
assert isinstance(data, list) and len(data) >= 20, len(data)
cats = collections.Counter(e['category'] for e in data)
for c in ('icd_code','dosage','loinc','hipaa_ref','cardinality_constraint'):
    assert cats[c] >= 4, (c, cats)
labels = [e['label'] for e in data]
assert len(labels) == len(set(labels)), 'duplicate labels'
for e in data:
    assert set(e.keys()) == {'label','text','category'}, e
print('OK', len(data), dict(cats))"</automated>
  </verify>
  <done>tests/fixtures/must_not_reject.yaml exists with >= 20 unique, well-formed entries covering all 5 MEAS-02 categories with >= 4 entries each.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parametrized CI test proving the fixture set survives chunk()'s gate</name>
  <files>tests/unit/test_must_not_reject.py</files>
  <read_first>
    tests/unit/test_chunk_storage.py (the `engine`/`_patch_engine`/`fake_storage`/`test_settings`/`_seed_source_and_parsed` fixture pattern — reuse verbatim, this is a real chunk() call requiring the same in-memory-SQLite + mocked-StorageBackend harness)
    tests/unit/test_chunk_substance_gate.py (as created by Plan 20-01 — the exact predicate list / domain_filters wiring shape `chunk()` now exposes)
    src/knowledge_lake/domains/loader.py (`DomainLoader.from_name("healthcare")` — resolves the real, Plan-20-02-extended `filters.yaml`)
    tests/fixtures/must_not_reject.yaml (as created by Task 1)
  </read_first>
  <behavior>
    - Loading `tests/fixtures/must_not_reject.yaml` and asserting its length is >= 20 BEFORE parametrizing — if the file is accidentally emptied, this assertion fails loudly at collection/setup time rather than silently collecting 0 parametrized tests that vacuously "pass".
    - For each fixture entry, building a single-section `ParsedDoc` whose one section's text is the fixture's `text`, calling the real `chunk()` (Plan 20-01) with `domain_filters=DomainLoader.from_name("healthcare").filters` and `settings.chunk_quality.gate_mode="enforce"` (the default), and asserting the resulting chunk list is non-empty AND its single chunk dict has `substance_passed=True`.
    - A fixture text long enough to be split into multiple chunks by `_build_token_chunks` (unlikely given typical fixture text length, but not assumed) still has EVERY resulting chunk pass — the assertion iterates all chunks returned for that fixture, not just the first.
  </behavior>
  <action>
    Create `tests/unit/test_must_not_reject.py` reusing `tests/unit/test_chunk_storage.py`'s exact fixture style (`engine`, `_patch_engine`, `fake_storage`, `test_settings`, `_seed_source_and_parsed`). At module scope, load `tests/fixtures/must_not_reject.yaml` via `yaml.safe_load` (resolve the path via `pathlib.Path(__file__).parent.parent / "fixtures" / "must_not_reject.yaml"`, matching this codebase's existing `DomainLoader`-style `yaml.safe_load()` convention) and assert `len(fixtures) >= 20` as a module-level assertion (fires at collection time, not per-test) so an accidentally-emptied fixture file fails loudly rather than silently collecting zero parametrized tests. Build `pytest.mark.parametrize("entry", fixtures, ids=[e["label"] for e in fixtures])` over the loaded list (preserves YAML list order — pytest's parametrize does not sort). In the parametrized test function, seed a source+parsed artifact via `_seed_source_and_parsed(engine, domain="healthcare")`, build a `ParsedDoc` with `sections=[Section(heading="", text=entry["text"], section_path="§1", page=1, is_table=False)]` (adjust field names/order to match the actual `Section` dataclass signature found in `src/knowledge_lake/plugins/protocols.py`), resolve `domain_filters = DomainLoader.from_name("healthcare").filters` (real filesystem load, not mocked — this is the point: exercising Plan 20-02's actual wiring pattern end-to-end against the real, Plan-20-02-extended `domains/healthcare/filters.yaml`), call `chunk(parsed_id, source_id, parsed_doc, settings=test_settings, domain_filters=domain_filters)`, and assert `len(results) >= 1` and `all(r["substance_passed"] for r in results)` with a failure message that includes `entry["label"]`, `entry["category"]`, and any `rejection_reason` found, so a CI failure is immediately diagnosable.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_must_not_reject.py -x` collects >= 20 parametrized test cases (one per fixture `label`) and all pass
    - Deliberately corrupting one fixture entry's `text` to a nav-junk string (e.g. temporarily, for local verification only — not committed) causes exactly that one parametrized case to fail with a clear label/category/rejection_reason message, proving the test is not vacuously green
    - The test calls the real `chunk()` function (imported from `knowledge_lake.pipeline.chunk`), not a mock, and resolves `domain_filters` via a real `DomainLoader.from_name("healthcare")` call (not a stub) — proving Plan 20-02's wiring pattern, not just Plan 20-01's predicate logic in isolation
    - A module-level assertion (not a per-test assertion) fails collection if the loaded fixture list has fewer than 20 entries
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_must_not_reject.py -x -v</automated>
  </verify>
  <done>Every entry in must_not_reject.yaml survives chunk()'s real substance gate with domain_filters resolved via the real healthcare DomainLoader — CI fails loudly on any regression, and on an accidentally-emptied or malformed fixture file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| tests/fixtures/must_not_reject.yaml (test-only, committed by developers) | Not a production trust boundary — no attacker-controlled input; safety net for a production trust boundary (chunk-level gate) exercised elsewhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-20-08 | Repudiation | must_not_reject.yaml fixture-set completeness | medium | accept | A passing CI suite covers ~20 representative fixtures across 5 categories, not an exhaustive clinical-code enumeration — this scope boundary is documented in the fixture file's leading YAML comment (per the MEAS-02 prohibition above); accepted since expanding fixture coverage further is an ongoing, not Phase-20-blocking, concern. |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this plan. |

</threat_model>

<verification>
Run `pytest tests/unit/test_must_not_reject.py -x -v` — all parametrized cases green. Run `pytest tests/` afterward to confirm no regression against the full suite.
</verification>

<success_criteria>
CI fails if any of the ~20 hand-labeled must-not-reject clinical fixtures is dropped by the real `chunk()` substance gate with `domain_filters` resolved via the real, Plan-20-02-wired `DomainLoader` — closing MEAS-02 end-to-end, not just at the predicate-function level.
</success_criteria>

<output>
Create `.planning/phases/20-chunk-substance-gate-export-gate/20-04-SUMMARY.md` when done
</output>
