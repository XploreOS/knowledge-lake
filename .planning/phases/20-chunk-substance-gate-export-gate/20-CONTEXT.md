# Phase 20: Chunk Substance Gate + Export Gate - Context

**Gathered:** 2026-07-16
**Status:** Ready for planning

<domain>
## Phase Boundary

Reject garbage chunks before embedding and gate gold RAG export on chunk-level quality signals. After this phase, chunks failing a composite substance predicate are rejected (or flagged in report mode), `FineWebQualityFilter` runs at chunk scope, the gold RAG corpus exports only quality content, and CI enforces a must-not-reject fixture set protecting clinical content. Filter configuration is versioned so threshold changes trigger re-processing.

**Requirements:** QUAL-02, QUAL-03, MEAS-02, EXPORT-01, EXPORT-02, PIPE-01

</domain>

<decisions>
## Implementation Decisions

### Substance Gate Wiring (QUAL-03)
- **D-01:** The substance gate runs inside `chunk()` as a post-generation filter. After the raw chunk list is produced, `run_predicates()` (from `pipeline/quality/`, built in Phase 19) is applied to each chunk. Rejected chunks are excluded from the returned list (in enforce mode) or annotated but kept (in report mode). This ensures both Dagster and CLI paths (which both call `chunk()`) have automatic parity — no separate wiring needed.
- **D-02:** Each chunk's `metadata_` dict is annotated with `substance_passed: bool` and `rejection_reason: str | None` before the gate decision. These annotations persist through embed and index and are available to the export gate (EXPORT-01).
- **D-03:** `is_table=True` chunks are always exempt from the substance gate — they bypass all predicates and are marked `substance_passed=True`. This matches QUAL-03's acceptance criteria.
- **D-04:** The conservation invariant (QUAL-05, Phase 17) is checked at the end of `chunk()`: `rejected + kept == total_generated`. This catches broken predicates that silently drop chunks.

### FineWebQualityFilter at Chunk Scope (QUAL-02)
- **D-05:** A `ChunkQualitySettings` Pydantic model is added to the settings hierarchy under `settings.chunk_quality`. It holds chunk-appropriate thresholds for `FineWebQualityFilter` (shorter text, different character distribution than document-level `CurateSettings`). Fields include `min_token_count`, `min_alpha_ratio`, `max_link_density`, `min_stopword_ratio`, and FineWebQualityFilter-specific overrides.
- **D-06:** `FineWebQualityFilter` is instantiated with chunk-scoped params from `ChunkQualitySettings`, not reusing `CurateSettings`. The filter is wrapped as a predicate function compatible with `run_predicates()` from `pipeline/quality/`.
- **D-07:** FineWebQualityFilter handles the "too short" and "no real sentences" audit categories. The composite predicate from `pipeline/quality/` handles the remaining categories (boilerplate, marketing, navigation) via the existing predicates.

### Export Gate (EXPORT-01)
- **D-08:** `export_rag_corpus()` in `pipeline/export.py` filters on chunk `metadata_.substance_passed` instead of (or in addition to) the document-level `enriched.quality_score`. Only chunks with `substance_passed=True` are included in the gold Parquet export.
- **D-09:** Pre-v2.6 chunks without the `substance_passed` flag default to `True` (included) — backward compatible, no migration needed. This is forward-only per scope decision D-2.
- **D-10:** The export logs kept/rejected/total counts at the chunk level, extending the existing `export.rag_corpus.building` structured log.

### Eval Dataset Versioning (EXPORT-02)
- **D-11:** Eval datasets generated by `pipeline/datasets.py` carry a `version` field in their metadata. After v2.6 gate improvements, existing eval sets grounded in boilerplate must be regenerated. The version tag prevents evaluating v2.6 quality against pre-v2.6 eval sets.
- **D-12:** The version value is derived from `filter_config_version` (PIPE-01) — a config change bumps the eval dataset version.

### Report vs Enforce Mode (QUAL-03)
- **D-13:** `ChunkQualitySettings` includes `gate_mode: Literal["enforce", "report"]` with default `"enforce"`. In `report` mode, chunks are annotated (`substance_passed=False`, `rejection_reason` set) but NOT filtered from the output — they pass through to embed/index/export. In `enforce` mode, failing chunks are removed.
- **D-14:** Configurable via env var `KLAKE_CHUNK_QUALITY__GATE_MODE` following the project's nested settings pattern.

### Must-Not-Reject CI Fixtures (MEAS-02)
- **D-15:** A `tests/fixtures/must_not_reject.yaml` file containing ~20 entries, each with `label`, `text`, and `category` (e.g., `icd_code`, `dosage`, `loinc`, `hipaa_ref`, `cardinality_constraint`). Categories map to the healthcare domain's clinical-code allowlist (Phase 19 D-08).
- **D-16:** A parametrized pytest test loads the YAML and asserts each entry passes both the substance gate predicates and FineWebQualityFilter at chunk scope. CI fails if any fixture is rejected.

### Filter Configuration Versioning (PIPE-01)
- **D-17:** `ChunkQualitySettings` includes `filter_config_version: str` (default: `"1.0"`). The chunk stage computes a cache key `sha256(f"{parsed_artifact_id}:{filter_config_version}")`, mirroring the proven `_curation_cache_key` pattern in `curate.py:80`.
- **D-18:** When `filter_config_version` changes, existing chunk artifacts' cache keys no longer match, triggering re-processing on the next pipeline run. The Dagster asset checks for cache hits before running `chunk()`.

### Claude's Discretion

Claude has flexibility on: exact threshold values in `ChunkQualitySettings`, FineWebQualityFilter parameter tuning, YAML fixture content (specific clinical text samples), `ChunkQualitySettings` field naming, cache check implementation details in the Dagster asset, eval dataset version format, and internal wiring of the FineWebQualityFilter wrapper predicate.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Requirements & Scope
- `.planning/REQUIREMENTS.md` §QUAL-02, §QUAL-03, §MEAS-02, §EXPORT-01, §EXPORT-02, §PIPE-01 — Full requirement definitions and acceptance criteria
- `.planning/ROADMAP.md` §Phase 20 — Success criteria and dependency graph
- `.planning/MILESTONE-CONTEXT.md` — Audit evidence (28% garbage, 33% gold junk, 5 categories), root causes, scope decisions

### Prior Phase Context
- `.planning/phases/17-close-the-bypass-measurement/17-CONTEXT.md` — Bypass wiring, conservation invariant infrastructure, rejection recording (QUAL-04/05)
- `.planning/phases/19-section-classifier-patterns/19-CONTEXT.md` — Section classifier (CLEAN-04), quality predicate module (QUAL-01, `pipeline/quality/`), domain-pack filter config (CLEAN-06)

### Pipeline Code (chunk + export)
- `src/knowledge_lake/pipeline/chunk.py` — `chunk()` function (line 263), `chunk_section()` (line 82), `is_table` handling (line 230)
- `src/knowledge_lake/pipeline/export.py` — `export_rag_corpus()` (line 244), `_RAG_CORPUS_FIELDS`, current document-level quality_score gating
- `src/knowledge_lake/dagster_defs/assets.py` — `chunk_document` asset (line 349), `export_rag_corpus` asset (line 811)
- `src/knowledge_lake/pipeline/process.py` — `process_crawled()` CLI path that must also apply the substance gate

### Quality Infrastructure (built by prior phases)
- `src/knowledge_lake/pipeline/quality/` — Pure predicate module (Phase 19): `run_predicates()`, individual predicate functions
- `src/knowledge_lake/pipeline/curate.py` — `_curation_cache_key()` (line 80), `FineWebQualityFilter` usage, `CurateSettings` — pattern to replicate at chunk scope

### Domain Pack
- `domains/healthcare/` — Healthcare domain pack; will contain `filters.yaml` with clinical-code allowlist (Phase 19 D-08)

### Datasets
- `src/knowledge_lake/pipeline/datasets.py` — Q&A eval dataset generation; needs version tagging (EXPORT-02)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `_curation_cache_key()` in `curate.py:80` — Proven config-versioning pattern: `sha256(f"{hash}:{version}")`. Directly reusable for PIPE-01's chunk-level cache versioning.
- `FineWebQualityFilter` in `curate.py` — Already installed and used for document-level curation. Needs chunk-scoped parameter tuning, not a new installation.
- `run_predicates()` in `pipeline/quality/` — Phase 19 builds this composable predicate combinator. Phase 20 wires it into `chunk()` and wraps FineWebQualityFilter as an additional predicate.
- `CurateSettings` in settings — Existing Pydantic model for curation config including `filter_config_version`. `ChunkQualitySettings` follows this exact pattern.

### Established Patterns
- Dagster assets pass dicts with `parsed_artifact_id`, `source_id`, `parsed_doc`, `collection` keys — `chunk_document` destructures these
- `is_table` flag on chunk metadata — tables are atomic and exempt from size limits; same exemption applies to substance gate
- `metadata_` dict on artifacts carries all per-chunk annotations (text, section_path, page, is_table, oversized)
- Content-addressed dedup via `registry_repo.get_artifact_by_hash()` — pattern for cache-hit checks
- Structured logging via `structlog` with domain-specific event names
- Settings hierarchy with `_env_file` override and `KLAKE_*` env var pattern

### Integration Points
- `chunk()` return value → consumed by `embed_chunks` Dagster asset and `export_rag_corpus()`
- `process_crawled()` → calls `chunk()` directly — substance gate is automatically applied
- `export_rag_corpus()` → iterates chunks, reads `metadata_` — needs `substance_passed` filter added
- `datasets.py` → generates eval Q&A from enrichment metadata — needs version tagging
- `pipeline/quality/` → Phase 19's predicate module — Phase 20 is the primary consumer

</code_context>

<specifics>
## Specific Ideas

No specific requirements — the implementation follows naturally from the existing codebase patterns. The substance gate extends `chunk()` with quality filtering using Phase 19's predicate module, the export gate adds a metadata filter in `export_rag_corpus()`, and CI fixtures are YAML-driven parametrized tests.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 20-Chunk Substance Gate + Export Gate*
*Context gathered: 2026-07-16*
