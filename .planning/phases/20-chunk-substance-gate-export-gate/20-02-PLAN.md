---
phase: 20-chunk-substance-gate-export-gate
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/knowledge_lake/dagster_defs/assets.py
  - src/knowledge_lake/pipeline/process.py
  - domains/healthcare/filters.yaml
  - tests/unit/test_process_crawled_clean.py
autonomous: true
requirements: [QUAL-03, MEAS-02]

must_haves:
  truths:
    - "chunk_document (Dagster asset) resolves settings.domain.domain_name via DomainLoader.from_name(...).filters and passes it into chunk(domain_filters=...) — mirrors enrich_document's existing DomainLoader guard (assets.py:452-455) exactly, using .filters instead of .render_prompt('enrich.j2')"
    - "process_crawled() (CLI/API/MCP shared entry point) resolves the same DomainLoader.filters and threads it into its chunk() call — closing RESEARCH.md Pitfall 1's explicitly-flagged gap: a unit-level predicate test alone does not prove the production pipeline protects clinical codes"
    - "When settings.domain.domain_name is None (no domain pack configured), domain_filters resolves to None and chunk() behaves exactly as it did before this plan (no regression for domain-less sources)"
    - "domains/healthcare/filters.yaml gains one new normative_allowlists entry protecting the cardinality_constraint MEAS-02 fixture category, which the existing 6 patterns (ICD-10, LOINC, RxNorm, section-ref, dosage, PO BID) do not cover (RESEARCH.md Pitfall 2, empirically proven: 'Meets 2 of 4 SIRS criteria' fails check_terminal_punct_ratio without it)"
    - "The new filters.yaml pattern uses no nested quantifiers or ambiguous overlapping groups (ReDoS-safe, linear-time) — narrow and specific per DomainFilters' own docstring warning against broad patterns"
  artifacts:
    - "src/knowledge_lake/dagster_defs/assets.py — domain_filters resolution block in chunk_document"
    - "src/knowledge_lake/pipeline/process.py — domain_filters resolution block in process_crawled"
    - "domains/healthcare/filters.yaml — new cardinality-constraint pattern"
    - "tests/unit/test_process_crawled_clean.py — new TestProcessCrawledDomainFilters test class"
  key_links:
    - "settings.domain.domain_name -> DomainLoader.from_name(...).filters -> chunk(domain_filters=...) at both the Dagster and CLI entry points (closes the parity gap Phase 19 explicitly deferred)"
    - "domains/healthcare/filters.yaml's new pattern -> consumed by Plan 20-04's must_not_reject.yaml cardinality_constraint fixtures"
  prohibitions: []
---

<objective>
Close the gap Phase 19 explicitly deferred: `chunk()` gained a `domain_filters` parameter in Plan 20-01, but nothing resolves or supplies it in production. Wire `DomainLoader.from_name(settings.domain.domain_name).filters` into both the Dagster `chunk_document` asset and the CLI/API/MCP-shared `process_crawled()` function, mirroring `enrich_document`'s already-shipped `DomainLoader` guard. Also extends the healthcare pack's `filters.yaml` with a cardinality-constraint pattern the existing 6 patterns do not cover.

Purpose: Without this wiring, the domain-allowlist exemption built in Plan 20-01 is inert in every real pipeline run — a unit test calling the predicate function directly would pass while a live `klake process` run on a source containing bare ICD-10 codes would still drop them (RESEARCH.md Pitfall 1, empirically proven). This plan is the mechanism that makes Plan 20-04's CI fixture guarantee actually true end-to-end, not just at the predicate level.

Output: `chunk_document` and `process_crawled()` both resolve and thread `domain_filters`; `domains/healthcare/filters.yaml` covers all 5 MEAS-02 fixture categories; a new test proves the CLI path's wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-chunk-substance-gate-export-gate/20-CONTEXT.md
@.planning/phases/20-chunk-substance-gate-export-gate/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve domain_filters in the chunk_document Dagster asset and process_crawled()</name>
  <files>src/knowledge_lake/dagster_defs/assets.py, src/knowledge_lake/pipeline/process.py</files>
  <read_first>
    src/knowledge_lake/dagster_defs/assets.py (lines 339-460 — `chunk_document` at line 350 is the exact function to modify; `enrich_document`'s DomainLoader guard at lines 452-455 is the exact pattern to mirror)
    src/knowledge_lake/pipeline/process.py (full file — `process_crawled()`'s function-local-import shape; the existing `chunk(parsed_id, src_id, cleaned_doc)` call site at line 113)
    src/knowledge_lake/pipeline/chunk.py (as modified by Plan 20-01 — confirm the exact `domain_filters` keyword-argument name and position on `chunk()`)
    src/knowledge_lake/domains/loader.py (`DomainLoader.from_name` signature and `.filters` attribute — lines 173-216)
  </read_first>
  <action>
    In `chunk_document` (assets.py, currently spanning lines 350-393), immediately before the existing `chunks = chunk(parsed_artifact_id, source_id, doc, settings=settings)` call, add: `domain_filters = None` then `if settings.domain.domain_name:` followed by a function-local `from knowledge_lake.domains.loader import DomainLoader` and `domain_filters = DomainLoader.from_name(settings.domain.domain_name).filters` — this is the EXACT pattern already used at `enrich_document`'s lines 452-455 for `domain_system_prompt`, substituting `.filters` for `.render_prompt("enrich.j2")`. Change the `chunk(...)` call to also pass `domain_filters=domain_filters`. Do not add a `minio`/`MinIOResource` parameter to `chunk_document`'s signature — `DomainLoader.from_name()` reads from the filesystem (`domains/{name}/`), not S3, so no new resource dependency is needed. Update the asset's `description=` string to mention the new domain-filter resolution, matching the existing docstring convention used elsewhere in this file.

    In `process.py`'s `process_crawled()`, which already uses function-local imports for `parse`/`clean`/`chunk`/`embed`/`index` (per this file's own module docstring), add the identical `domain_filters` resolution block using `get_settings()` from `knowledge_lake.config.settings` (function-local import, matching the existing style) to read `settings.domain.domain_name` — resolve it ONCE, before the per-document `for raw_id, src_id, storage_uri, stored_mime in raw_docs:` loop (not once per document — `settings.domain.domain_name` does not vary per document within a single `process_crawled()` call). Change the existing `chunks_list = chunk(parsed_id, src_id, cleaned_doc)` call (inside the loop) to `chunks_list = chunk(parsed_id, src_id, cleaned_doc, domain_filters=domain_filters)`.
  </action>
  <acceptance_criteria>
    - `chunk_document`'s body resolves `domain_filters` via `DomainLoader.from_name(settings.domain.domain_name).filters` exactly when `settings.domain.domain_name` is truthy, else `domain_filters` stays `None`; the `chunk(...)` call inside `chunk_document` passes `domain_filters=domain_filters`
    - `process_crawled()` resolves `domain_filters` once (outside the per-document loop) via the same `DomainLoader.from_name(...).filters` pattern, and its `chunk(...)` call site passes `domain_filters=domain_filters`
    - `chunk_document`'s function signature is otherwise unchanged (still `clean_document: dict[str, Any], postgres: PostgresResource`)
    - `process_crawled()`'s public signature (`source_id`, `limit`, `collection`) is unchanged
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast, pathlib
for path, fn_name in [('src/knowledge_lake/dagster_defs/assets.py','chunk_document'), ('src/knowledge_lake/pipeline/process.py','process_crawled')]:
    src = pathlib.Path(path).read_text()
    tree = ast.parse(src)
    fn = next(n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name == fn_name)
    body_src = ast.get_source_segment(src, fn)
    assert 'DomainLoader' in body_src, f'{fn_name} does not reference DomainLoader'
    assert 'domain_filters=domain_filters' in body_src, f'{fn_name} does not thread domain_filters into chunk()'
print('OK')"</automated>
  </verify>
  <done>Both chunk_document and process_crawled resolve settings.domain.domain_name via DomainLoader and thread the resulting .filters into their respective chunk() calls.</done>
</task>

<task type="auto">
  <name>Task 2: Add a domain_filters wiring test for process_crawled()</name>
  <files>tests/unit/test_process_crawled_clean.py</files>
  <read_first>
    tests/unit/test_process_crawled_clean.py (full file — the existing `TestProcessCrawledCleanWiring`/`TestProcessCrawledCleanBoundaries` classes and their mocking pattern: `patch("knowledge_lake.pipeline.parse.parse", ...)` etc. patches the SOURCE module each function-local import reads from, not `knowledge_lake.pipeline.process`'s own namespace)
    src/knowledge_lake/pipeline/process.py (as modified by Task 1)
  </read_first>
  <action>
    Add a new test class `TestProcessCrawledDomainFilters` to `tests/unit/test_process_crawled_clean.py` (do not modify the two existing classes). Write two tests: (1) `test_domain_filters_resolved_and_threaded_when_domain_configured` — construct a `Settings` instance with `domain=DomainSettings(domain_name="healthcare")` (or monkeypatch `knowledge_lake.pipeline.process.get_settings` to return such a settings object, matching whichever settings-injection mechanism Task 1 actually used), patch `knowledge_lake.domains.loader.DomainLoader.from_name` with a `MagicMock` returning an object whose `.filters` attribute is a sentinel `object()`, mock `parse`/`clean`/`chunk`/`embed`/`index` exactly as the existing tests in this file do, and assert `mock_chunk.call_args.kwargs["domain_filters"] is sentinel_filters` (or the equivalent positional/keyword form Task 1 actually produced) — proving the CLI path genuinely resolves and threads `domain_filters`, not just that a unit test of the predicate function passes in isolation (RESEARCH.md Pitfall 1's exact warning); (2) `test_domain_filters_none_when_no_domain_configured` — with `settings.domain.domain_name` left at its default `None`, assert `mock_chunk.call_args.kwargs.get("domain_filters") is None` and that `DomainLoader.from_name` is never called — proving no regression for domain-less sources (existing default behavior preserved).
  </action>
  <acceptance_criteria>
    - `TestProcessCrawledDomainFilters` exists with the two tests described above
    - Both tests pass and exercise the REAL `process_crawled()` function (not a mock of `process_crawled` itself) with `parse`/`clean`/`chunk`/`embed`/`index` mocked at their source-module locations, matching this file's established interception pattern
    - Neither test modifies or duplicates the existing `TestProcessCrawledCleanWiring`/`TestProcessCrawledCleanBoundaries` classes
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_process_crawled_clean.py -x -k DomainFilters</automated>
  </verify>
  <done>process_crawled() is proven, via a real (non-mocked) function-level test, to resolve and thread domain_filters when a domain pack is configured, and to leave it None with no DomainLoader call otherwise.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the healthcare pack's filters.yaml with a cardinality-constraint pattern</name>
  <files>domains/healthcare/filters.yaml</files>
  <read_first>
    domains/healthcare/filters.yaml (full file — the existing normative_allowlists list and its narrow-pattern-only convention)
    src/knowledge_lake/domains/models.py (DomainFilters docstring — the explicit warning against broad patterns like ".*")
  </read_first>
  <action>
    Add exactly one new entry to `domains/healthcare/filters.yaml`'s `normative_allowlists` list: `"\\d+\\s*(?:of|/)\\s*\\d+"` (matches phrases like "2 of 4 SIRS criteria" or "2/4 SIRS criteria" — RESEARCH.md Pitfall 2, empirically proven that the bare phrase "Meets 2 of 4 SIRS criteria" fails `check_terminal_punct_ratio` without an allowlist rescue, and that this category is not covered by any of the 6 existing patterns: ICD-10, LOINC, RxNorm, the section-reference pattern, the dosage-mg pattern, or the PO-BID pattern). Do not modify `boilerplate_patterns` or `thresholds` — both remain empty per the existing file. Do not use a broad or nested-quantifier pattern — this one is linear-time (no ReDoS risk) and narrowly scoped to a specific numeric-cardinality phrase shape, matching the file's existing narrow-pattern discipline.
  </action>
  <acceptance_criteria>
    - `domains/healthcare/filters.yaml`'s `normative_allowlists` list grows from 6 to 7 entries; all 6 existing entries are byte-identical and unreordered
    - The new pattern matches "2 of 4 SIRS criteria" and "2/4 SIRS criteria" via `re.search`
    - `DomainFilters.model_validate(yaml.safe_load(...))` still validates the file successfully (no schema break)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import re, yaml
from knowledge_lake.domains.models import DomainFilters
data = yaml.safe_load(open('domains/healthcare/filters.yaml'))
filters = DomainFilters.model_validate(data)
assert len(filters.normative_allowlists) == 7, filters.normative_allowlists
assert any(re.search(p, 'Meets 2 of 4 SIRS criteria') for p in filters.normative_allowlists)
print('OK')"</automated>
  </verify>
  <done>The healthcare pack's clinical-code allowlist covers all 5 MEAS-02 fixture categories (icd_code, dosage, loinc, hipaa_ref, cardinality_constraint), validated against the DomainFilters schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator-controlled settings.domain.domain_name -> DomainLoader.from_name() | Domain pack name comes from Settings (env var / .env), not end-user input; DomainLoader.from_name() already validates it against a path-traversal-safe regex (established Phase 06/19) |
| Domain pack filters.yaml regex patterns -> check_domain_allowlist() evaluation | A new regex pattern added to a trusted, operator-maintained YAML file (not user-supplied at request time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-20-04 | Denial of Service | domains/healthcare/filters.yaml new regex pattern (cardinality-constraint) | low | mitigate | Pattern `\d+\s*(?:of|/)\s*\d+` uses no nested quantifiers or ambiguous overlapping groups — linear-time regex, no catastrophic-backtracking risk; `check_domain_allowlist()` already catches `re.error` per-pattern (Phase 19 established, unchanged by this plan). |
| T-20-05 | Elevation of Privilege | DomainLoader.from_name(settings.domain.domain_name) resolution at new call sites | low | accept | `settings.domain.domain_name` is operator-controlled Settings, never attacker input; `DomainLoader.from_name()` already validates the name against `_DOMAIN_NAME_RE` (path-traversal guard, established Phase 06/19) — this plan adds new CALL SITES of an already-hardened function, no new attack surface. |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this plan. |

</threat_model>

<verification>
Run `pytest tests/unit/test_process_crawled_clean.py -x` — all green, including the two new domain-filter tests and all pre-existing tests in this file. Run `pytest tests/` afterward to confirm no regression.
</verification>

<success_criteria>
Both `chunk_document` (Dagster) and `process_crawled()` (CLI/API/MCP) resolve `settings.domain.domain_name` via `DomainLoader` and thread the resulting `.filters` into `chunk()`, closing the production-wiring gap Phase 19 explicitly deferred; the healthcare pack's `filters.yaml` protects all 5 MEAS-02 fixture categories.
</success_criteria>

<output>
Create `.planning/phases/20-chunk-substance-gate-export-gate/20-02-SUMMARY.md` when done
</output>
