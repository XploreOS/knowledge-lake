---
phase: "14"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge_lake/plugins/protocols.py
  - src/knowledge_lake/config/settings.py
autonomous: true
requirements:
  - RETR-04
  - RETR-06
  - RETR-07
  - RETR-08

must_haves:
  truths:
    - "Hit(id, score) exposes citation_source defaulting to \"chunk\"; existing chunk search is byte-identical (additive default, D-02)"
    - "RetrieverPlugin is a @runtime_checkable Protocol with name: str and search(tree_index, query, *, top_k, mode, settings) -> list[Hit] (D-03)"
    - "Settings exposes a tree_search submodel (TreeSearchSettings) with mode/shortlist_k/max_docs/top_k/concurrency/budget_usd/model_alias and a top-level retriever swap key defaulting to pageindex (D-12, A1)"
    - "retriever is validated by the _validate_swap_key ASVS V5 field-validator so a malicious entry-point name is rejected (A2, mirrors T-13-03)"
  artifacts:
    - src/knowledge_lake/plugins/protocols.py
    - src/knowledge_lake/config/settings.py
  key_links:
    - "RetrieverPlugin.search returns list[Hit] — the shared result contract downstream retriever + orchestrator produce (D-01)"
    - "Settings.retriever feeds resolver.get_retriever (Plan 14-03); Settings.tree_search feeds pipeline.tree_search (Plan 14-04)"
    - "TreeSearchSettings.model_alias is the cheap_model task alias the LLM-nav path uses via f\"openai/{alias}\" (D-06, never a hardcoded provider ID)"
---

<objective>
Establish the Phase 14 contracts that every later wave depends on: the `citation_source` discriminator on `Hit` (D-02), the `RetrieverPlugin` Protocol (D-03), the `TreeSearchSettings` submodel + `retriever` swap key (D-12), and the ASVS V5 validation of `retriever` (A2). All changes are additive — chunk `search()` and existing config callers stay byte-identical.

Purpose: These are the typed seams. Plan 14-03 (retriever builtin) implements `RetrieverPlugin`; Plan 14-04 (orchestrator) reads `Settings.tree_search`. Shipping the contracts first makes Waves 2-3 independent of each other's internals.

Output: Additive edits to `plugins/protocols.py` (Hit.citation_source + RetrieverPlugin) and `config/settings.py` (TreeSearchSettings submodel + tree_search field + retriever swap key + validator tuple entry).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-tree-retrieval/14-CONTEXT.md
@.planning/phases/14-tree-retrieval/14-PATTERNS.md
@src/knowledge_lake/plugins/protocols.py
@src/knowledge_lake/config/settings.py
</context>

<tasks>

<task id="T1" type="auto" tdd="true">
  <name>Task 1: Add Hit.citation_source and RetrieverPlugin to protocols.py (D-02, D-03)</name>
  <files>src/knowledge_lake/plugins/protocols.py</files>
  <read_first>
    - src/knowledge_lake/plugins/protocols.py — Hit dataclass (lines 114-129: id, score, payload=field(default_factory=dict)); VectorPoint.sparse=None additive-default precedent (line 102); TreeNode (598-630) and TreeIndex (633-657) field names the Protocol type-annotates; IndexerPlugin (665-699) — the exact @runtime_checkable + name:str + single-method template to mirror.
    - .planning/phases/14-tree-retrieval/14-PATTERNS.md — "plugins/protocols.py — EDIT" section (exact Hit and RetrieverPlugin shapes).
    - .planning/phases/14-tree-retrieval/14-CONTEXT.md — D-01 (reuse Hit, not TreeHit), D-02 (citation_source field + tree payload keys), D-03 (RetrieverPlugin contract).
  </read_first>
  <behavior>
    - Hit(id="x", score=1.0).citation_source == "chunk" (default preserved so chunk search is unchanged — regression guard).
    - Hit(id="x", score=1.0, citation_source="tree").citation_source == "tree" (tree search can set it).
    - isinstance(obj, RetrieverPlugin) is True for any object exposing a name attribute and a search callable with the D-03 signature (structural @runtime_checkable check).
  </behavior>
  <action>
    Edit src/knowledge_lake/plugins/protocols.py.

    (1) Add one field to the Hit dataclass after payload: citation_source of type str with default "chunk". Because payload already carries a default via field(default_factory=dict), the new field is a defaulted trailing field — dataclass field-ordering stays valid. This mirrors the VectorPoint.sparse = None additive-default precedent at line 102 per D-02. Do NOT add a separate TreeHit type (D-01 overrides the ARCHITECTURE.md TreeHit sketch).

    (2) Add a RetrieverPlugin Protocol immediately after IndexerPlugin (near line 699), mirroring IndexerPlugin's structure verbatim: decorate with @runtime_checkable; declare a name: str class attribute; declare a single method search with the exact signature per D-03 — self, tree_index of type TreeIndex, query of type str, keyword-only top_k int default 5, mode str default "heuristic", settings Any-or-None default None, returning list[Hit]. Body is the ellipsis placeholder like IndexerPlugin.build_index. Add a concise docstring stating the retriever consumes the shared TreeIndex/TreeNode contract and never PageIndex's internal schema (Anti-Pattern 5, D-03). Reuse the already-imported Protocol, runtime_checkable, Any, TreeIndex, Hit symbols — do not add new imports unless Any is not already imported in this file.

    Do not add fenced code blocks inside this action. Keep the change additive — do not alter Hit's existing fields, IndexerPlugin, or any other symbol.
  </action>
  <acceptance_criteria>
    - `python -c "from knowledge_lake.plugins.protocols import Hit; assert Hit(id='x', score=1.0).citation_source == 'chunk'"` exits 0.
    - `python -c "from knowledge_lake.plugins.protocols import Hit; assert Hit(id='x', score=1.0, citation_source='tree').citation_source == 'tree'"` exits 0.
    - `python -c "from knowledge_lake.plugins.protocols import RetrieverPlugin; assert getattr(RetrieverPlugin, '_is_runtime_protocol', False)"` exits 0 (Protocol is runtime_checkable).
    - `grep -c 'class RetrieverPlugin' src/knowledge_lake/plugins/protocols.py` returns 1; `grep -c 'class TreeHit' src/knowledge_lake/plugins/protocols.py` returns 0 (D-01 — no separate TreeHit).
    - Regression: `pytest tests/unit/test_tree_search.py::TestHitContract::test_hit_citation_source_default -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from knowledge_lake.plugins.protocols import Hit, RetrieverPlugin; assert Hit(id='x', score=1.0).citation_source == 'chunk'; assert getattr(RetrieverPlugin, '_is_runtime_protocol', False)" && pytest tests/unit/test_tree_search.py::TestHitContract::test_hit_citation_source_default -q</automated>
  </verify>
  <done>Hit exposes citation_source defaulting to "chunk"; RetrieverPlugin is a runtime_checkable Protocol with the D-03 signature; no TreeHit type added; the citation-source regression test passes.</done>
</task>

<task id="T2" type="auto" tdd="true">
  <name>Task 2: Add TreeSearchSettings submodel + retriever swap key + validator entry to settings.py (D-12, A1, A2)</name>
  <files>src/knowledge_lake/config/settings.py</files>
  <read_first>
    - src/knowledge_lake/config/settings.py — TreeSettings submodel (lines 199-220: Literal mode, budget_usd=5.0, model_alias="cheap_model", schema_version, prompt_version, max_tokens) and SearchSettings (359); _SWAP_KEY_RE regex (385); top-level swap keys embedder/parser/vectorstore (434-443) and indexer: str = "pageindex" (480); the tree/search submodel wiring via Field(default_factory=...) (471-496); the _validate_swap_key field_validator decorator tuple (513); env_nested_delimiter="__".
    - .planning/phases/14-tree-retrieval/14-PATTERNS.md — "config/settings.py — EDIT" section (exact TreeSearchSettings shape, tree_search + retriever wiring, validator tuple).
    - .planning/phases/14-tree-retrieval/14-CONTEXT.md — D-12 (field defaults) and 14-RESEARCH.md Assumptions A1 (model_alias added beyond the D-12 literal list, needed by D-06) and A2 (retriever added to the validator tuple).
  </read_first>
  <behavior>
    - Settings().tree_search.mode == "heuristic", .shortlist_k == 20, .max_docs == 3, .top_k == 5, .concurrency == 5, .budget_usd == 5.0, .model_alias == "cheap_model".
    - Settings().retriever == "pageindex".
    - Constructing Settings(retriever="bad name with spaces") raises a validation error (ASVS V5 — _validate_swap_key rejects names not matching _SWAP_KEY_RE); Settings(retriever="pageindex") is accepted.
    - Env override KLAKE_TREE_SEARCH__MODE=llm resolves Settings().tree_search.mode == "llm" via the existing nested delimiter (no new wiring).
  </behavior>
  <action>
    Edit src/knowledge_lake/config/settings.py.

    (1) Add a TreeSearchSettings(BaseModel) submodel near TreeSettings (line 199), mirroring TreeSettings' shape, with fields per D-12: mode as Literal["heuristic", "llm"] default "heuristic", shortlist_k int default 20, max_docs int default 3, top_k int default 5, concurrency int default 5, budget_usd float default 5.0. Add model_alias str default "cheap_model" per Assumption A1 (LLM-nav in D-06 needs the cheap_model task alias — this is a deliberate addition beyond the D-12 literal list; the executor must include it). Reuse the already-imported Literal / BaseModel / Field symbols.

    (2) Wire the submodel onto Settings: add tree_search: TreeSearchSettings = Field(default_factory=TreeSearchSettings) alongside the existing tree/search submodel fields (near line 477), and add a top-level retriever: str = "pageindex" swap key next to the indexer key (line 480). Both are additive defaults — no existing caller changes (mirrors the additive-default back-compat convention).

    (3) Add "retriever" to the _validate_swap_key field_validator's field-name tuple (line 513) so the retriever swap key is validated against _SWAP_KEY_RE, keeping the tuple alphabetically ordered: crawler, discovery, embedder, indexer, parser, retriever, vectorstore. This is Assumption A2 and mirrors the T-13-03 mitigation that hardened the indexer key. Do not change the regex or the validator body.

    Do not add fenced code blocks inside this action. Keep every change additive.
  </action>
  <acceptance_criteria>
    - `python -c "from knowledge_lake.config.settings import Settings; s=Settings(); assert s.retriever=='pageindex'; ts=s.tree_search; assert (ts.mode,ts.shortlist_k,ts.max_docs,ts.top_k,ts.concurrency,ts.budget_usd,ts.model_alias)==('heuristic',20,3,5,5,5.0,'cheap_model')"` exits 0.
    - `python -c "from knowledge_lake.config.settings import Settings;\nimport pydantic\ntry:\n Settings(retriever='bad name'); raise SystemExit(1)\nexcept pydantic.ValidationError: pass"` exits 0 (invalid swap key rejected — ASVS V5).
    - `grep -c '\"retriever\"' src/knowledge_lake/config/settings.py` returns 1 or more AND the _validate_swap_key decorator line contains retriever: `grep -c 'field_validator(.*\"retriever\".*mode=\"after\")' src/knowledge_lake/config/settings.py` returns 1.
    - `KLAKE_TREE_SEARCH__MODE=llm python -c "from knowledge_lake.config.settings import Settings; assert Settings().tree_search.mode=='llm'"` exits 0 (env override works).
  </acceptance_criteria>
  <verify>
    <automated>python -c "from knowledge_lake.config.settings import Settings; s=Settings(); ts=s.tree_search; assert s.retriever=='pageindex'; assert (ts.mode,ts.shortlist_k,ts.max_docs,ts.top_k,ts.concurrency,ts.budget_usd,ts.model_alias)==('heuristic',20,3,5,5,5.0,'cheap_model')" && grep -Eq 'field_validator\(.*"retriever".*mode="after"\)' src/knowledge_lake/config/settings.py</automated>
  </verify>
  <done>Settings.tree_search exposes the seven D-12/A1 fields with correct defaults; Settings.retriever defaults to "pageindex" and is validated by _validate_swap_key (A2); env override KLAKE_TREE_SEARCH__* resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/env → Settings | KLAKE_* environment variables (incl. KLAKE_RETRIEVER) are attacker-influenceable input that becomes an entry-point name used to load code |
| caller → Hit contract | citation_source is a downstream-rendered discriminator; a wrong value mislabels provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering | Settings.retriever swap key → entry-point resolution | high | mitigate | Add "retriever" to _validate_swap_key so it is checked against _SWAP_KEY_RE (`^[a-zA-Z][a-zA-Z0-9_-]{0,63}$`); rejects path traversal, spaces, and injection in entry-point names (A2, mirrors T-13-03 for indexer). Verified by the invalid-swap-key acceptance test. |
| T-14-04 | Spoofing | Hit.citation_source provenance label | low | accept | Additive default "chunk" keeps existing hits truthfully labeled; only tree search sets "tree". No external input sets this field. Accept at ASVS L1. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this plan (protocols/settings edits only, zero new imports); Package Legitimacy Audit is a no-op per RESEARCH.md. |
</threat_model>

<verification>
After both tasks:
- `python -c "from knowledge_lake.plugins.protocols import Hit, RetrieverPlugin; from knowledge_lake.config.settings import Settings; s=Settings(); assert Hit(id='x',score=1.0).citation_source=='chunk'; assert s.retriever=='pageindex'; assert s.tree_search.model_alias=='cheap_model'"` exits 0.
- `pytest tests/unit/test_tree_search.py::TestHitContract -q` passes (the rest of test_tree_search.py stays RED until Waves 2-3).
- Regression: `pytest tests/unit/test_builtin_plugins.py -q -k "not PageIndexRetriever"` still passes (chunk/indexer paths untouched).
</verification>

<success_criteria>
Wave 1 is complete when Hit carries the citation_source discriminator, RetrieverPlugin is a runtime_checkable Protocol matching D-03, and Settings exposes the tree_search submodel + validated retriever swap key — all additively, with the existing chunk-search and indexer config paths unchanged.
</success_criteria>

## Artifacts this phase produces (Wave 1 contribution)

Modified files:
- `src/knowledge_lake/plugins/protocols.py` — Hit gains `citation_source: str = "chunk"`; new `RetrieverPlugin` Protocol
- `src/knowledge_lake/config/settings.py` — new `TreeSearchSettings` submodel; `Settings.tree_search` field; `Settings.retriever` swap key; `retriever` added to `_validate_swap_key` tuple

New symbols:
- `Hit.citation_source` (field, default `"chunk"`) — D-02
- `RetrieverPlugin` (`@runtime_checkable` Protocol: `name: str`, `search(tree_index, query, *, top_k, mode, settings) -> list[Hit]`) — D-03
- `TreeSearchSettings` (BaseModel: `mode`, `shortlist_k`, `max_docs`, `top_k`, `concurrency`, `budget_usd`, `model_alias`) — D-12 + A1
- `Settings.tree_search` (field) and `Settings.retriever` (swap key, default `"pageindex"`) — D-12

<output>
Create `.planning/phases/14-tree-retrieval/14-02-SUMMARY.md` when done
</output>
