---
phase: 17-close-the-bypass-measurement
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/knowledge_lake/dagster_defs/assets.py
  - tests/integration/test_dagster_assets.py
autonomous: true
requirements: [CLEAN-01]

must_haves:
  truths:
    - "clean_document's Dagster asset threads parsed_doc=parsed_doc into its clean() call and forwards clean_result[\"cleaned_doc\"] (not the raw parsed_doc) under the SAME \"parsed_doc\" dict key"
    - "chunk_document, tree_index_document, and enrich_document require zero code changes — they already destructure clean_document[\"parsed_doc\"] and now transparently receive the cleaned ParsedDoc instead of the uncleaned one"
    - "curate_document_asset and pipeline/curate.py require zero code changes — curate already re-fetches cleaned text from S3 via cleaned_artifact.storage_uri, independent of the in-memory dict-value swap (D-03), verified by a new regression assertion rather than a code change"
    - "After Dagster materialization, result.output_for_node(\"clean_document\")[\"parsed_doc\"] is a different object than result.output_for_node(\"parsed_document\")[\"parsed_doc\"] — proves the uncleaned parsed_doc is no longer forwarded verbatim"
  artifacts:
    - src/knowledge_lake/dagster_defs/assets.py
    - tests/integration/test_dagster_assets.py
  key_links:
    - "clean_document[\"parsed_doc\"] dict key <-> chunk_document/tree_index_document/enrich_document's existing clean_document[\"parsed_doc\"] reads — the value changes, the key and all three consumers' code stay untouched"
    - "This plan's forwarding change is only safe because 17-01's WR-05 hash fix already landed (Pitfall 1 — must not activate forwarding before the hash fix is in place); depends_on enforces this ordering"
---

<objective>
Close the Dagster half of the clean-stage bypass: make `clean_document` forward the cleaned `ParsedDoc` (not the uncleaned original) under the same `"parsed_doc"` dict key its three consumers (`chunk_document`, `tree_index_document`, `enrich_document`) already read. Verify `curate_document_asset` is unaffected (D-03) with a regression test, without touching its code.

Purpose: This is the literal CLEAN-01 acceptance criterion — "after processing a source with known boilerplate, chunk_document receives sections with boilerplate removed." Since all three downstream assets already destructure `clean_document["parsed_doc"]` via the exact same pattern, the fix is a single-value swap inside `clean_document` alone (RESEARCH.md Pattern 1) — zero changes anywhere else in the asset graph.
Output: `clean_document` forwards the cleaned `ParsedDoc`; a new Dagster materialize-test assertion proves both the forwarding change and curate's continued correctness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-close-the-bypass-measurement/17-CONTEXT.md
@.planning/phases/17-close-the-bypass-measurement/17-RESEARCH.md
@.planning/phases/17-close-the-bypass-measurement/17-PATTERNS.md
@.planning/phases/17-close-the-bypass-measurement/17-01-SUMMARY.md
</context>

<flagged_assumptions>
## Flagged Assumptions (unresolved spec-less edge-probe items — never silently dropped)

- **CLEAN-01 (unclassified):** This plan assumes a zero-section `ParsedDoc` reaching `clean_document`
  (a parser that legitimately returns no sections) still materializes successfully through the full
  asset graph — `clean()` logs `clean.zero_sections` (from Plan 17-01) rather than raising, and
  `chunk_document` falls back to its existing "no sections: emit full text as single chunk" path
  (`chunk.py:204-215`). This plan does NOT add a dedicated empty-sections Dagster integration test —
  the real HIPAA Security Rule PDF fixture used by `test_dagster_assets.py` always produces sections,
  so this path is exercised only by Plan 17-01's unit-level zero-section test, not end-to-end. Flagged
  as non-blocking; revisit if a future phase's fixture set needs a genuinely section-less document.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Forward the cleaned ParsedDoc from clean_document</name>
  <files>src/knowledge_lake/dagster_defs/assets.py</files>
  <read_first>
    src/knowledge_lake/dagster_defs/assets.py (clean_document at lines 276-335 — the exact function to modify; chunk_document at lines 349-392, tree_index_document at lines 479-525, enrich_document at lines 407-466 — read to confirm all three already do `clean_document["parsed_doc"]` / `doc = clean_document["parsed_doc"]` and require NO changes; curate_document_asset at line 735 — confirm it takes clean_document as its data input but does not read `["parsed_doc"]` at all)
    src/knowledge_lake/pipeline/clean.py (this phase's Plan 17-01 output — clean()'s new `parsed_doc` kwarg and `cleaned_doc` return key)
    .planning/phases/17-close-the-bypass-measurement/17-PATTERNS.md (Pattern 1 — Replace-in-dict threading, the exact before/after code shape for this task)
  </read_first>
  <action>
    In `clean_document` (`assets.py:276-335`), change the call `clean_result = clean(parsed_artifact_id, source_id, settings=settings)` to `clean_result = clean(parsed_artifact_id, source_id, parsed_doc=parsed_doc, settings=settings)`.

    Change the result dict's `"parsed_doc": parsed_doc,` line (originally forwarding the raw uncleaned object, comment `# forwarded in-memory to chunk_document (Pitfall 7)`) to `"parsed_doc": clean_result["cleaned_doc"],` and update the trailing comment to note the forwarded value is now the CLEANED doc (e.g. `# CLEANED parsed_doc forwarded in-memory to chunk_document/tree_index_document/enrich_document (CLEAN-01, Pitfall 7)`), preserving the existing comment style.

    Do NOT modify `chunk_document`, `tree_index_document`, `enrich_document`, or `curate_document_asset` — verify by re-reading each function body after the edit and confirming zero lines changed outside `clean_document`.
  </action>
  <acceptance_criteria>
    - `grep -n "parsed_doc=parsed_doc" src/knowledge_lake/dagster_defs/assets.py` matches exactly once, inside `clean_document`'s `clean(...)` call.
    - `grep -n "\"parsed_doc\": clean_result\[.cleaned_doc.\]" src/knowledge_lake/dagster_defs/assets.py` matches inside `clean_document`'s return dict.
    - `git diff --stat src/knowledge_lake/dagster_defs/assets.py` shows changes confined to `clean_document`'s function body only.
    - `uv run pytest tests/integration/test_dagster_assets.py::TestDefinitionsLoad -x` passes (fast import/definitions-load sanity — confirms no syntax errors, asset graph still binds).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_dagster_assets.py::TestDefinitionsLoad -x</automated>
  </verify>
  <done>clean_document threads the in-memory parsed_doc into clean() and forwards clean_result["cleaned_doc"] under the same "parsed_doc" key — chunk_document, tree_index_document, enrich_document, and curate_document_asset are byte-identical to before this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize-test proof of cleaned forwarding and curate regression check</name>
  <files>tests/integration/test_dagster_assets.py</files>
  <read_first>
    tests/integration/test_dagster_assets.py (full file — TestDefinitionsLoad at lines 35-72, the materialize fixture/test class containing test_dagster_materialize_produces_artifacts at line 291, test_lineage_resolves_after_dagster_materialize at line 374; note this suite requires the docker-compose stack — PostgreSQL, MinIO, Qdrant — already running in this environment)
    src/knowledge_lake/dagster_defs/assets.py (Task 1's output — clean_document's new forwarding behavior)
    src/knowledge_lake/pipeline/curate.py (curate_document at line 134 — confirms it re-fetches cleaned_artifact.storage_uri from S3 at line 206, independent of the in-memory dict swap — this is why D-03 requires only a test assertion, not a code change)
  </read_first>
  <behavior>
    - Test: after materializing the asset graph, `result.output_for_node("clean_document")["parsed_doc"] is not result.output_for_node("parsed_document")["parsed_doc"]` — proves the uncleaned object is no longer forwarded verbatim (the CLEAN-01 literal acceptance criterion).
    - Test: none of `result.output_for_node("chunk_document")["chunks"]`'s `"text"` values match a full trimmed line of `^(?:Page \d+ of \d+)\s*$` (a defensive, best-effort content check — the real HIPAA Security Rule PDF fixture commonly carries page-footer boilerplate matching this existing BOILERPLATE_PATTERNS entry).
    - Test: `result.output_for_node("curate_document_asset")` has a non-None `"quality_score"` and a `"status"` key present — proves curate continues to function correctly with the now-cleaned clean_document input (D-03 regression check, zero code change to curate.py).
  </behavior>
  <action>
    Extend the existing materialize test class in `tests/integration/test_dagster_assets.py` (either add assertions directly inside `test_dagster_materialize_produces_artifacts` or add a new sibling test method reusing the same `inprocess_result`/materialize fixture pattern already present in the file) with the three assertions described in `<behavior>`. Import `re` at the top of the test module if not already imported, for the boilerplate-pattern content check.

    Keep the assertions additive — do not remove or weaken any existing assertion in this file (e.g. the existing `raw_artifact_id` prefix check, the existing `result.success` check).
  </action>
  <acceptance_criteria>
    - The three new assertions from `<behavior>` are present in `tests/integration/test_dagster_assets.py`.
    - `uv run pytest tests/integration/test_dagster_assets.py -k materialize -x` exits 0 (requires the docker-compose stack up — confirmed running in this environment: postgres, minio, qdrant, litellm services healthy).
    - `uv run pytest tests/integration/test_dagster_assets.py -x` (full file) exits 0 — no regressions to `TestDefinitionsLoad`, `TestResourcesUseEnvVar`, or `test_lineage_resolves_after_dagster_materialize`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_dagster_assets.py -k materialize -x</automated>
  </verify>
  <done>A Dagster materialize test proves clean_document forwards a distinct (cleaned) object rather than the raw parsed_doc, that page-footer boilerplate does not reach chunk text, and that curate_document_asset still produces a valid quality_score — all without any code change to curate.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `clean_document` asset output → `chunk_document`/`tree_index_document`/`enrich_document` in-memory input | Same trust domain (in-process Dagster asset graph, no external input) — the risk is entirely a data-integrity one inherited from the content-hash boundary Plan 17-01 already mitigates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-17-02-01 | Tampering | `dagster_defs/assets.py::clean_document` forwarding | high | mitigate | This plan activates the cross-document lineage-corruption risk described in T-17-01-01 (RESEARCH.md Pitfall 1: "CLEAN-01 is precisely the change that makes it live") — safe only because `depends_on: ["17-01"]` guarantees the WR-05 hash fix landed first. No independent mitigation is added in this plan; it inherits 17-01's fix by wave ordering. |
</threat_model>

<verification>
`uv run pytest tests/integration/test_dagster_assets.py -x` full file green. `uv run pytest tests/unit tests/integration -x` (full suite) has no new failures introduced by this plan.
</verification>

<success_criteria>
`clean_document` forwards the cleaned `ParsedDoc` to `chunk_document`, `tree_index_document`, and `enrich_document` with zero changes to those three functions or to `curate_document_asset`; a materialize test proves both the forwarding change and curate's continued correctness.
</success_criteria>

<output>
Create `.planning/phases/17-close-the-bypass-measurement/17-02-SUMMARY.md` when done
</output>
