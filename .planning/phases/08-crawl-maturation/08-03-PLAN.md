---
phase: 08-crawl-maturation
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - src/knowledge_lake/pipeline/crawl.py
autonomous: true
requirements:
  - CRAWL-01
  - CRAWL-02
  - CRAWL-03

must_haves:
  truths:
    - crawl_source no longer passes source_config=None; it reads from registry per D-02
    - Per-source depth overrides effective_max_pages when present in crawl_config per D-04
    - _crawl_loop applies adaptive backoff on http_status_code 429/403 via PerHostLimiter per D-10/D-11
    - crawl_all_sources(domain=None) returns {total, succeeded, failed, results} per D-06/D-09
  artifacts:
    - src/knowledge_lake/pipeline/crawl.py (source_config fix + adaptive backoff + crawl_all_sources)
  key_links:
    - get_source_crawl_config(session, source_id) returns inner crawl_config dict (source of Pitfall 4)
    - backoff_extra is computed from limiter before resolve_delay call, not after
    - crawl_all_sources() calls crawl_source() (async), so it is also async and uses asyncio.run in CLI
    - Each source failure in crawl_all_sources is caught with Exception, logged, and increments failed count
---

<objective>
Core crawl.py changes: fix the source_config=None bug, wire adaptive backoff into the page-fetch loop, and add crawl_all_sources() for batch crawling.

Purpose: Unblocks the CLI/API surface (Plan 5) and linked-doc ingestion (Plan 4). All three requirements share the same file.
Output: crawl_source reads per-source config; _crawl_loop respects adaptive backoff; crawl_all_sources() function is importable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-crawl-maturation/08-CONTEXT.md
@.planning/phases/08-crawl-maturation/08-RESEARCH.md
@.planning/phases/08-crawl-maturation/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix source_config=None bug and wire per-source depth override (CRAWL-01 D-02/D-04/D-05)</name>
  <files>src/knowledge_lake/pipeline/crawl.py</files>
  <read_first>
    src/knowledge_lake/pipeline/crawl.py (full file — lines 1-490)
    src/knowledge_lake/registry/repo.py (get_source_crawl_config signature just added)
    .planning/phases/08-crawl-maturation/08-CONTEXT.md (D-02, D-04, D-05)
    .planning/phases/08-crawl-maturation/08-RESEARCH.md (Pattern 1, Pitfall 4, CRAWL-01 code example)
  </read_first>
  <behavior>
    - After fix: resolve_delay in _crawl_loop receives the crawl_config sub-dict (not None, not the outer Source.config)
    - Source with crawl_config={'rate_limit_rps': 2} causes resolve_delay to return 0.5s (not the global default)
    - Source with crawl_config={'depth': 5} sets effective_max_pages=5 in crawl_source, overriding settings.crawl.max_pages
    - Source with no crawl_config (get_source_crawl_config returns {}) leaves effective_max_pages unchanged (uses settings default per D-04)
    - Passing source_config={} to resolve_delay still falls through to global default (Pitfall 5: empty dict is valid)
  </behavior>
  <action>
    In crawl_source, after the source_id is resolved (line ~93) and before the job creation, add a registry lookup using a new with get_session() block: call registry_repo.get_source_crawl_config(session, source_id) and store the result in source_crawl_config (a dict, never None). Immediately after, read depth_override = source_crawl_config.get("depth") and if depth_override is not None, set effective_max_pages = int(depth_override). This implements D-04.

    Pass source_crawl_config down to _crawl_loop as a new keyword parameter source_config (add it to the _crawl_loop function signature as source_config: dict with a default of None for backward-compat, then update the call site in crawl_source). Inside _crawl_loop, replace the hard-coded line "source_config = None" (currently at line ~296) with the passed-in source_config parameter reference. The existing "delay = resolve_delay(source_config, robots_crawl_delay, settings.crawl.rate_limit_seconds)" call at line ~297 is then correct — no other change to that line.

    No other changes to this file in this task. Do not yet add adaptive backoff or linked-doc logic (those come in Tasks 2 and Plan 4 respectively).
  </action>
  <verify>
    <automated>pytest tests/unit/test_robots_ratelimit.py tests/unit/test_crawl_all.py -v -x 2>&1 | tail -15</automated>
  </verify>
  <done>
    grep -rn "source_config = None" src/knowledge_lake/pipeline/crawl.py returns 0 results (the bug line is gone). grep -c "get_source_crawl_config" src/knowledge_lake/pipeline/crawl.py returns >= 1. pytest tests/unit/ -v -x passes green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire adaptive backoff into _crawl_loop (CRAWL-03 D-10/D-11/D-12/D-13)</name>
  <files>src/knowledge_lake/pipeline/crawl.py</files>
  <read_first>
    src/knowledge_lake/pipeline/crawl.py (lines 239-360 — _crawl_loop, after Task 1 changes)
    src/knowledge_lake/crawl/ratelimit.py (PerHostLimiter.record_error, reset_errors, backoff_extra)
    .planning/phases/08-crawl-maturation/08-CONTEXT.md (D-10, D-11, D-12, D-13)
    .planning/phases/08-crawl-maturation/08-RESEARCH.md (Pattern 3, Pitfall 1)
  </read_first>
  <behavior>
    - After adapter.fetch_page returns a result with http_status_code in {429, 403}: limiter.record_error(url) is called; the backoff is included in the next resolve_delay call for that host
    - After adapter.fetch_page returns a result with http_status_code not in {429, 403} (success or other failure): limiter.reset_errors(url) is called
    - resolve_delay is called with backoff_extra=limiter.backoff_extra(url, settings.crawl.rate_limit_seconds) per D-12
    - Structured log entry crawl.backoff_applied is emitted when http_status_code is 429 or 403, containing url and backoff_extra value
  </behavior>
  <action>
    In _crawl_loop, update the rate-limit section (the block around line ~295 that calls resolve_delay and limiter.wait). Change the resolve_delay call to pass backoff_extra=limiter.backoff_extra(url, settings.crawl.rate_limit_seconds) as the fourth argument. This is the pre-fetch backoff based on this host's error history.

    After the adapter.fetch_page call returns result (success or failure), add HTTP status code inspection. After the existing "if result.status == 'failed':" block (line ~319), add a block that checks result.http_status_code: if result.http_status_code in (429, 403), call limiter.record_error(url) and emit log.warning("crawl.backoff_applied", url=url, http_status_code=result.http_status_code, backoff_extra=limiter.backoff_extra(url, settings.crawl.rate_limit_seconds)). In all other non-error cases (result.status == "complete"), call limiter.reset_errors(url). Keep the existing status-string checks unchanged — do not alter the "pages_failed += 1" or "pages_complete += 1" logic. The limiter.record_error call is purely for future-request backoff calculation; it does not change how this particular page result is recorded.

    The PerHostLimiter constructor call at the top of _crawl_loop needs no change (the new methods are already on the class from Plan 2).
  </action>
  <verify>
    <automated>pytest tests/unit/test_robots_ratelimit.py -v -x 2>&1 | tail -15</automated>
  </verify>
  <done>
    grep -c "record_error\|reset_errors\|backoff_extra" src/knowledge_lake/pipeline/crawl.py returns >= 3. grep -c "crawl.backoff_applied" src/knowledge_lake/pipeline/crawl.py returns >= 1. pytest tests/unit/ -v -x passes green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add crawl_all_sources() function (CRAWL-02 D-06/D-07/D-09)</name>
  <files>src/knowledge_lake/pipeline/crawl.py</files>
  <read_first>
    src/knowledge_lake/pipeline/crawl.py (end of file — after _name_from_url)
    src/knowledge_lake/registry/repo.py (list_sources_for_crawl_all signature)
    .planning/phases/08-crawl-maturation/08-CONTEXT.md (D-06, D-07, D-09)
    .planning/phases/08-crawl-maturation/08-RESEARCH.md (Pattern 6, CRAWL-02 code example)
  </read_first>
  <behavior>
    - crawl_all_sources(domain=None) returns {'total': N, 'succeeded': M, 'failed': K, 'results': [...]}
    - When a source's crawl_source() raises Exception, the exception is caught, logged as crawl_all.source_failed with source_id and error, and failed count increments; other sources continue
    - When domain='healthcare', list_sources_for_crawl_all is called with domain='healthcare'
    - Each successful source produces a result entry {'source_id': ..., 'status': 'ok', ...result_fields}
    - Each failed source produces {'source_id': ..., 'status': 'failed', 'error': str(exc)}
    - Empty source list returns {'total': 0, 'succeeded': 0, 'failed': 0, 'results': []}
  </behavior>
  <action>
    Add crawl_all_sources as a new async function at the end of crawl.py (after the _name_from_url helper). Signature: async def crawl_all_sources(domain: Optional[str] = None, settings: Optional[Settings] = None) -> dict[str, Any]. The function body: (1) resolve settings with get_settings(); (2) use a with get_session() as session: block to call registry_repo.list_sources_for_crawl_all(session, domain=domain) and store the list; (3) loop over sources sequentially (per D-06 — no parallelism), calling await crawl_source(source.url, settings=s) inside a try/except Exception; (4) build results list with per-source dicts as specified in D-09; (5) return {'total': total, 'succeeded': succeeded, 'failed': failed, 'results': results}.

    Add 'crawl_all_sources' to the module docstring function list at the top of crawl.py. Update the module's Functions section comment to include crawl_all_sources. Import Optional and Any are already imported at the top of the file (confirm before adding).
  </action>
  <verify>
    <automated>pytest tests/unit/test_crawl_all.py -v -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    grep -c "async def crawl_all_sources" src/knowledge_lake/pipeline/crawl.py returns 1. The test_crawl_all_sources_returns_summary, test_crawl_all_sources_failure_does_not_abort, and test_crawl_all_sources_domain_filter stubs from Plan 1 now pass GREEN (no longer xfail). pytest tests/unit/ -v -x passes green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source.config crawl_config → crawl depth | Operator-controlled per-source depth could be set to very large values |
| HTTP 429/403 response → backoff state | Server signals backoff; we respect it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Denial of Service | per-source depth override (D-04) | low | mitigate | depth_override = int(depth_override) — invalid values raise ValueError before assignment; effective_max_pages is already capped by existing max_pages parameter and settings.crawl.max_pages |
| T-08-03-02 | Denial of Service | crawl_all_sources sequential loop | low | accept | Sequential per D-06; per-host adaptive backoff already limits request rate; no amplification beyond single-source crawl rate |
| T-08-03-03 | Spoofing | crawl_all_sources source URL trusted from registry | medium | mitigate | crawl_source already calls validate_public_url(source_url) as first step (line ~78) — SSRF guard is always applied |
</threat_model>

<verification>
grep -rn "source_config = None" src/knowledge_lake/pipeline/crawl.py returns 0.
grep -c "crawl_all_sources" src/knowledge_lake/pipeline/crawl.py returns >= 2 (definition + docstring).
grep -c "record_error" src/knowledge_lake/pipeline/crawl.py returns >= 1.
pytest tests/unit/test_crawl_all.py -v passes with all 3 stubs GREEN.
pytest tests/unit/ -v -x passes green.
</verification>

<success_criteria>
source_config=None bug fixed. Adaptive backoff wired. crawl_all_sources importable. All test_crawl_all.py stubs GREEN. No existing tests regress.
</success_criteria>

<output>
Create .planning/phases/08-crawl-maturation/08-03-SUMMARY.md when done.
</output>

## Artifacts this phase produces

Modified `src/knowledge_lake/pipeline/crawl.py`:
- Function `crawl_all_sources(domain: Optional[str] = None, settings: Optional[Settings] = None) -> dict[str, Any]` (new)
- `crawl_source` — reads source_crawl_config from registry; applies depth override from crawl_config.get("depth")
- `_crawl_loop` — new `source_config: dict` param; `backoff_extra` passed to `resolve_delay`; `limiter.record_error` / `limiter.reset_errors` calls; `crawl.backoff_applied` log event
