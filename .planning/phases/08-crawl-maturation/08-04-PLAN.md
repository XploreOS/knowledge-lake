---
phase: 08-crawl-maturation
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/knowledge_lake/pipeline/enrich.py
autonomous: true
requirements:
  - ENRICH-07

must_haves:
  truths:
    - finish_reason='length' triggers balanced-brace prefix recovery, not tenacity retry
    - Partial result is stored under 'partial:{content_hash}' cache key, not the normal synthetic hash
    - Partial result is never returned as a cache hit for a complete enrichment request
    - EnrichmentResult (or return dict) carries is_partial=True when truncation was detected
    - enrich_document logs enrich.partial_result with finish_reason and content_hash
  artifacts:
    - src/knowledge_lake/pipeline/enrich.py (_extract_longest_valid_prefix + is_partial logic)
  key_links:
    - finish_reason check happens inside _call_llm_for_enrichment BEFORE model_validate_json to prevent tenacity retry (Pitfall 2)
    - partial cache key uses 'partial:' prefix on the synthetic_hash, not on content_hash
    - Complete enrichment cache lookup only checks the non-partial synthetic_hash key
---

<objective>
ENRICH-07: Detect LLM truncation via finish_reason, recover the longest valid JSON prefix, and isolate partial results under a separate cache key so they are never served as complete enrichments.

Purpose: Prevents budget waste from tenacity retries on truncated output; recovers partial metadata rather than losing it entirely.
Output: Extended _call_llm_for_enrichment with truncation detection + prefix recovery; enrich_document stores and flags partial results correctly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-crawl-maturation/08-CONTEXT.md
@.planning/phases/08-crawl-maturation/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _extract_longest_valid_prefix helper to enrich.py (ENRICH-07 D-15)</name>
  <files>src/knowledge_lake/pipeline/enrich.py</files>
  <read_first>
    src/knowledge_lake/pipeline/enrich.py (full file)
    .planning/phases/08-crawl-maturation/08-CONTEXT.md (D-14, D-15)
    .planning/phases/08-crawl-maturation/08-RESEARCH.md (Pattern 4 balanced-brace scan)
  </read_first>
  <behavior>
    - _extract_longest_valid_prefix('{"a": 1, "b": 2}') returns '{"a": 1, "b": 2}' (complete balanced JSON)
    - _extract_longest_valid_prefix('{"a": 1, "b": "truncated') returns '{"a": 1, "b": "truncated' (no balanced close found, returns original per research Pattern 4)
    - _extract_longest_valid_prefix('{"a": 1}extra garbage') returns '{"a": 1}' (returns up to and including last balanced closing brace at depth=0)
    - _extract_longest_valid_prefix('{"outer": {"inner": 1}} junk {"incomplete":') returns '{"outer": {"inner": 1}}' (last complete outermost object)
    - _extract_longest_valid_prefix escapes within strings are not double-counted as brace delimiters
    - _extract_longest_valid_prefix('') returns ''
  </behavior>
  <action>
    Add _extract_longest_valid_prefix(content: str) -> str as a private module-level function in enrich.py, placed immediately after _strip_json_fences. Implement the balanced-brace scan algorithm exactly as described in RESEARCH.md Pattern 4: iterate character by character tracking in_string, escape, and depth; record last_close whenever depth returns to 0 on a closing brace; return content[:last_close + 1] if last_close >= 0, else return content unchanged. Use a single forward scan (O(n)). Include a docstring citing D-15 and noting it handles nested objects and string-embedded braces.

    This function is pure (no I/O, no imports beyond the stdlib). No other part of enrich.py is modified in this task.
  </action>
  <verify>
    <automated>pytest tests/unit/test_enrich.py -v -k "prefix" 2>&1 | tail -15</automated>
  </verify>
  <done>
    grep -c "_extract_longest_valid_prefix" src/knowledge_lake/pipeline/enrich.py returns >= 2 (definition + call site stub). pytest tests/unit/test_enrich.py -v -x passes green (stubs that test prefix logic now pass; others remain xfail).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend _call_llm_for_enrichment to detect truncation and return is_partial flag (ENRICH-07 D-14/D-15/D-17/D-18)</name>
  <files>src/knowledge_lake/pipeline/enrich.py</files>
  <read_first>
    src/knowledge_lake/pipeline/enrich.py (lines 150-210 — _call_llm_for_enrichment + enrich_document)
    .planning/phases/08-crawl-maturation/08-CONTEXT.md (D-14, D-17, D-18)
    .planning/phases/08-crawl-maturation/08-RESEARCH.md (Pattern 4 design constraint, Pitfall 2)
  </read_first>
  <behavior>
    - When response.choices[0].finish_reason == 'length': _call_llm_for_enrichment calls _extract_longest_valid_prefix on the stripped content, then calls model_validate_json on the prefix; returns (result, response, True) — a 3-tuple with is_partial=True
    - When finish_reason == 'stop' (normal): returns (result, response, False) — 3-tuple with is_partial=False
    - When finish_reason == 'length' AND prefix recovery fails (model_validate_json raises): raises the ValidationError directly so the outer except in enrich_document catches it — tenacity is NOT involved (function is no longer decorated with retry for this path)
    - No tenacity retry fires for finish_reason='length' path — the truncation path exits the function without raising before tenacity would intercept
    - enrich_document receives 3-tuple (result, response, is_partial) from _call_llm_for_enrichment
    - enrich_document response dict includes 'is_partial': True when truncation detected (per D-17)
  </behavior>
  <action>
    Change the return type signature of _call_llm_for_enrichment to return a 3-tuple: tuple[EnrichmentResult, object, bool]. The third element is is_partial.

    Inside _call_llm_for_enrichment, after content is stripped (the line calling _strip_json_fences), add a finish_reason check BEFORE the model_validate_json call: read finish_reason = response.choices[0].finish_reason. If finish_reason == "length": call _extract_longest_valid_prefix(content) to get prefix_content; attempt result = EnrichmentResult.model_validate_json(prefix_content) — if this raises ValidationError, re-raise it immediately (do not call model_validate_json again on the full content); if it succeeds, return (result, response, True). If finish_reason != "length": proceed with the existing result = EnrichmentResult.model_validate_json(content) call and return (result, response, False).

    Per D-18 and Pitfall 2: the tenacity retry decorator on _call_llm_for_enrichment stays. The truncation path succeeds (returns a tuple) rather than raising — so tenacity never fires for it. Only genuine RuntimeError or ValidationError (from malformed non-truncated JSON) still triggers retries.

    In enrich_document, update the call site: change "result, _response = _call_llm_for_enrichment(...)" to "result, _response, is_partial = _call_llm_for_enrichment(...)". The is_partial bool is used in Task 3.
  </action>
  <verify>
    <automated>pytest tests/unit/test_enrich.py -v -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    grep -c "is_partial" src/knowledge_lake/pipeline/enrich.py returns >= 3 (return type, variable, usage). test_partial_enrichment stub from Plan 1 now passes GREEN. pytest tests/unit/test_enrich.py -v -x passes green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: enrich_document — partial cache key isolation and is_partial in return dict (ENRICH-07 D-16/D-17/D-18)</name>
  <files>src/knowledge_lake/pipeline/enrich.py</files>
  <read_first>
    src/knowledge_lake/pipeline/enrich.py (lines 205-400 — enrich_document body, after Task 2 changes)
    .planning/phases/08-crawl-maturation/08-CONTEXT.md (D-16, D-17, D-18)
    .planning/phases/08-crawl-maturation/08-RESEARCH.md (Partial-result cache isolation)
  </read_first>
  <behavior>
    - When is_partial=True: the artifact is stored under cache key 'partial:{synthetic_hash}' (not synthetic_hash)
    - When is_partial=False: the artifact continues to use synthetic_hash as the cache key (unchanged path)
    - Complete enrichment cache lookup (Step 3, get_artifact_by_hash with synthetic_hash) ignores any 'partial:{synthetic_hash}' entry — it only checks the plain synthetic_hash
    - Return dict from enrich_document includes 'is_partial': True when truncation was detected
    - Return dict from enrich_document includes 'is_partial': False (or omits the key) for normal complete enrichments
    - enrich.partial_result structured log event is emitted with finish_reason and content_hash when is_partial=True (per D-18)
  </behavior>
  <action>
    In enrich_document, after the LLM call unpacks (result, _response, is_partial), add the following logic:

    Compute partial_synthetic_hash = f"partial:{synthetic_hash}" when is_partial is True. When writing to the registry (Step 5), use the correct key: if is_partial, pass content_hash=partial_synthetic_hash to create_enriched_artifact; otherwise pass content_hash=synthetic_hash (unchanged). Also in Step 5, the second cache check (get_artifact_by_hash before create) should check partial_synthetic_hash when is_partial.

    When is_partial is True, emit the structured log: log.warning("enrich.partial_result", cleaned_artifact_id=cleaned_artifact_id, content_hash=content_hash, finish_reason="length"). This satisfies D-18 (no retry — just log).

    Add 'is_partial': is_partial to the response_dict (both the normal write path and the cached/IntegrityError paths). For the is_partial=False path, 'is_partial': False can be added or the key can be omitted — choose to include it for consistency. For the IntegrityError path (concurrent cache hit), is_partial retains whatever value it has from the earlier computation.

    The complete-enrichment cache lookup (Step 3, get_artifact_by_hash with synthetic_hash) is not changed — it still looks up synthetic_hash only, never partial_synthetic_hash. This is the enforcement per D-16: a partial cache entry is never a hit for a complete request.
  </action>
  <verify>
    <automated>pytest tests/unit/test_enrich.py -v 2>&1 | tail -25</automated>
  </verify>
  <done>
    test_partial_cache_key and test_partial_not_returned_as_complete stubs from Plan 1 now pass GREEN. All three ENRICH-07 stubs are green. grep -c "partial_synthetic_hash\|partial:" src/knowledge_lake/pipeline/enrich.py returns >= 2. pytest tests/unit/ -v -x passes green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response content → JSON prefix extraction | Attacker-influenced document content could produce adversarial JSON that passes partial validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Tampering | _extract_longest_valid_prefix + model_validate_json | medium | mitigate | model_validate_json still validates against EnrichmentResult Pydantic schema even for partial prefix — attacker cannot smuggle extra fields through partial recovery (RESEARCH.md security domain, D-15) |
| T-08-04-02 | Tampering | is_partial flag in return dict | low | accept | is_partial is a boolean set server-side from finish_reason; not an attacker-controlled value |
| T-08-04-03 | Information Disclosure | partial enrichment stored in DB under partial: key | low | accept | Partial results carry the same access control as complete results; no new data is exposed |
</threat_model>

<verification>
pytest tests/unit/test_enrich.py -v passes with all three ENRICH-07 stubs GREEN (test_partial_enrichment, test_partial_cache_key, test_partial_not_returned_as_complete).
grep -c "_extract_longest_valid_prefix" src/knowledge_lake/pipeline/enrich.py returns >= 2.
grep -c "partial_synthetic_hash\|partial:" src/knowledge_lake/pipeline/enrich.py returns >= 2.
grep -c "enrich.partial_result" src/knowledge_lake/pipeline/enrich.py returns >= 1.
pytest tests/unit/ -v -x passes green.
</verification>

<success_criteria>
ENRICH-07 fully implemented. All three Plan 1 stubs GREEN. Partial enrichment never serves as cache hit for complete enrichment. Tenacity does not retry truncated LLM responses.
</success_criteria>

<output>
Create .planning/phases/08-crawl-maturation/08-04-SUMMARY.md when done.
</output>

## Artifacts this phase produces

New/modified symbols in `src/knowledge_lake/pipeline/enrich.py`:
- Function `_extract_longest_valid_prefix(content: str) -> str` (new private helper)
- `_call_llm_for_enrichment` — return type changed from `tuple[EnrichmentResult, object]` to `tuple[EnrichmentResult, object, bool]`; `finish_reason == "length"` path calls `_extract_longest_valid_prefix` and returns `is_partial=True`
- `enrich_document` — unpacks 3-tuple; when `is_partial=True` stores under `"partial:{synthetic_hash}"` key; emits `enrich.partial_result` log; return dict includes `"is_partial"` key
