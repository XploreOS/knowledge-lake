---
phase: 10-hybrid-retrieval
plan: "05"
type: execute
wave: 2
depends_on:
  - 10-03
files_modified:
  - src/knowledge_lake/plugins/builtin/sparse_embedder.py
  - tests/unit/test_sparse_embedder.py
autonomous: true
requirements:
  - RETR-01
must_haves:
  truths:
    - "plugins/builtin/sparse_embedder.py exposes embed_sparse_doc(text) and embed_sparse_query(text), each returning a qdrant_client.models.SparseVector with .indices and .values (D-01)"
    - "The BM25 model is 'Qdrant/bm25' loaded once via a lazy module-level singleton (CPU ONNX; no torch/GPU) (D-01, D-02)"
    - "embed_sparse_doc uses SparseTextEmbedding.embed (document side); embed_sparse_query uses .query_embed (query side) — the two are distinct (Pitfall 6, D-03)"
    - "pytest tests/unit/test_sparse_embedder.py -q passes with fastembed mocked (no live model download in the unit run)"
  artifacts:
    - src/knowledge_lake/plugins/builtin/sparse_embedder.py (NEW)
    - tests/unit/test_sparse_embedder.py (NEW)
  key_links:
    - "sparse construction lives inside the plugins/builtin/ boundary (D-01 plugin ethos); consumed by index.py (embed_sparse_doc) and search.py (embed_sparse_query) in Plan 10-07"
    - "Returns models.SparseVector(indices=e.indices.tolist(), values=e.values.tolist()) per RESEARCH Code Example (b)"
    - "Lazy-singleton import of fastembed avoids import-time cost and keeps the ONNX model warm across calls"
---

<objective>
Wave 2 — Create the BM25 sparse encoder wrapper that turns text into a Qdrant `SparseVector`, living inside the `plugins/builtin/` seam per the D-01 plugin ethos. Exposes distinct document-side and query-side functions (Pitfall 6) so index-time and query-time embeddings are correct. Depends on the verified fastembed install from Plan 10-03.

Purpose: RETR-01 needs BM25 sparse vectors for every indexed point (document side) and for hybrid/sparse queries (query side), produced by `fastembed` `Qdrant/bm25` (D-01, D-02, D-03).

Output: sparse_embedder.py with embed_sparse_doc / embed_sparse_query, plus a mocked unit test proving the SparseVector wrapping and the doc-vs-query method split.
</objective>

<artifacts_produced>
- `src/knowledge_lake/plugins/builtin/sparse_embedder.py` — `embed_sparse_doc(text) -> SparseVector`, `embed_sparse_query(text) -> SparseVector`, lazy `Qdrant/bm25` singleton
- `tests/unit/test_sparse_embedder.py` — mocked-fastembed unit coverage
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-retrieval/10-CONTEXT.md
@.planning/phases/10-hybrid-retrieval/10-RESEARCH.md
@.planning/phases/10-hybrid-retrieval/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sparse_embedder.py — fastembed Qdrant/bm25 wrapper (RETR-01, D-01, D-02, D-03, Pitfall 6)</name>
  <files>src/knowledge_lake/plugins/builtin/sparse_embedder.py</files>
  <read_first>
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (b) — verified sparse_doc/sparse_query snippet against qdrant-client 1.18
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Common Pitfalls (Pitfall 6) — embed vs query_embed distinction
    - src/knowledge_lake/plugins/builtin/qdrant_store.py (lines 53-61) — the lazy-import-and-cache convention used across plugins/builtin
  </read_first>
  <action>
    Create src/knowledge_lake/plugins/builtin/sparse_embedder.py. Define a module-level lazy singleton accessor (e.g. a private _get_model() that imports `from fastembed import SparseTextEmbedding` on first call and caches `SparseTextEmbedding(model_name="Qdrant/bm25")` in a module global) so the ONNX model loads once and stays warm. Use structlog for a debug log on first load, mirroring qdrant_store's logging style.

    Expose two public functions returning `qdrant_client.models.SparseVector`:
    - embed_sparse_doc(text: str) -> SparseVector: uses the model's document-side embedding (`.embed([text])`), takes the first result, and wraps it as SparseVector(indices=e.indices.tolist(), values=e.values.tolist()). Used at index time (D-05) and by the re-embedding migration (Plan 10-07).
    - embed_sparse_query(text: str) -> SparseVector: uses `.query_embed(text)` (the query-side method, NOT the document method — Pitfall 6, D-03), same SparseVector wrapping. Used by pipeline.search (Plan 10-07).

    Guard the empty-text edge (RESEARCH Example (h) note): if text is empty/whitespace, return a SparseVector with empty indices/values rather than raising. Do NOT create a collection, call the server, or import qdrant_store here — this module only produces SparseVector objects. Do NOT use fastembed-gpu.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_sparse_embedder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Module exposes `embed_sparse_doc` and `embed_sparse_query`, both returning `SparseVector` with `.indices` and `.values`
    - `embed_sparse_query` calls the model's `query_embed`; `embed_sparse_doc` calls `embed` (distinct methods — Pitfall 6)
    - Empty/whitespace text returns an empty SparseVector (no exception)
    - The model is loaded via a single cached singleton (not re-instantiated per call)
  </acceptance_criteria>
  <done>sparse_embedder.py produces correct document-side and query-side BM25 SparseVectors from within the plugin boundary.</done>
</task>

<task type="auto">
  <name>Task 2: test_sparse_embedder.py — mocked-fastembed unit coverage (RETR-01, Pitfall 6)</name>
  <files>tests/unit/test_sparse_embedder.py</files>
  <read_first>
    - tests/unit/test_search_filters.py (lines 1-32) — monkeypatch style for module-level dependencies
    - src/knowledge_lake/plugins/builtin/sparse_embedder.py (the module created in Task 1)
  </read_first>
  <action>
    Create tests/unit/test_sparse_embedder.py. Monkeypatch the lazy model singleton (or the SparseTextEmbedding import) with a MagicMock whose embed/query_embed return an object exposing .indices and .values arrays (use simple objects with .tolist()), so no real fastembed model is loaded in the unit run. Assert:
    - embed_sparse_doc("administrative safeguards") returns a SparseVector with the expected indices/values and that the mock's embed (document method) was called, not query_embed.
    - embed_sparse_query("administrative safeguards") returns a SparseVector and that the mock's query_embed was called, not embed (Pitfall 6).
    - embed_sparse_doc("") returns a SparseVector with empty indices/values and does not raise.
    Reset/clear the cached singleton between tests if the module memoizes it (e.g. monkeypatch the module global).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_sparse_embedder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_sparse_embedder.py -q` exits 0 with no network / model download
    - Tests distinguish the document (`embed`) vs query (`query_embed`) code paths
    - Empty-text edge asserted
  </acceptance_criteria>
  <done>Unit coverage proves the SparseVector wrapping and the doc/query method split without a live model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→fastembed model | Local ONNX inference over the cached Qdrant/bm25 model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering (supply chain) | Qdrant/bm25 model load | medium | mitigate | Model pinned to `Qdrant/bm25` (Qdrant HF org) via the fastembed pin verified in Plan 10-03; CPU-only, no GPU/torch path; loaded from the verified local cache |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_sparse_embedder.py -q` → green (mocked, no network)
- `uv run pytest tests/unit -q` → whole unit suite green
</verification>

<success_criteria>
sparse_embedder.py produces document-side and query-side BM25 SparseVectors inside the plugin boundary, unit-verified with a mocked model — ready for consumption by index.py and search.py in Plan 10-07.
</success_criteria>

<output>
Create `.planning/phases/10-hybrid-retrieval/10-05-SUMMARY.md` when done.
</output>
