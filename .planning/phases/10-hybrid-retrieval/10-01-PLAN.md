---
phase: 10-hybrid-retrieval
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_qdrant_hybrid.py
  - tests/integration/test_qdrant_hybrid_migration.py
autonomous: true
requirements:
  - RETR-01
must_haves:
  truths:
    - "tests/unit/test_qdrant_hybrid.py collects with zero collection errors (RED via xfail(strict=False))"
    - "test_named_create_config asserts create_collection is called with vectors_config={'dense': VectorParams} AND sparse_vectors_config={'sparse': SparseVectorParams(modifier=Modifier.IDF)} (RETR-01, D-05, D-13)"
    - "test_get_dim_named asserts get_collection_dim returns dense dim (no AttributeError) when params.vectors is a dict (Pitfall 2)"
    - "test_hybrid_prefetch_limits asserts query_points receives two Prefetch branches (using='dense'/'sparse') each with limit == top_k+offset and query=FusionQuery(fusion=Fusion.RRF) (D-11, D-12)"
    - "test_server_preflight asserts a RuntimeError is raised when client.info().version < 1.10 (D-07)"
    - "test_upsert_named_shape / test_upsert_legacy_shape assert upsert builds named {'dense',...} vs bare vector per collection shape (Pitfall 1, D-09)"
    - "tests/integration/test_qdrant_hybrid_migration.py collects under pytest.mark.integration and covers reembed parity, all-points-have-sparse, IDF-set, payload-index-survival, dense-on-both-shapes (RETR-01)"
  artifacts:
    - tests/unit/test_qdrant_hybrid.py (NEW — mock-client unit RED scaffold)
    - tests/integration/test_qdrant_hybrid_migration.py (NEW — live-Qdrant RED scaffold)
  key_links:
    - "Unit fixture reuses the QdrantVectorStore.__new__ + MagicMock _client pattern from tests/unit/test_qdrant_payload_indexes.py:28-41"
    - "Integration fixtures reuse store/alias fixtures from tests/integration/test_qdrant_alias_reindex.py:28-45 (pytestmark = pytest.mark.integration)"
    - "RED markers (xfail strict=False + module-scope ImportError guards) keep the suite green until Plans 10-04/10-06/10-07 land the symbols"
---

<objective>
Wave 0 — RED test scaffold for the RETR-01 store + migration surface. Creates the two test files the store/migration implementation plans (10-06, 10-07) will turn green: a mock-client unit file for named-vector creation, hybrid prefetch/RRF assembly, `get_collection_dim` branch, server preflight, and upsert shape branching; and a live-Qdrant integration file for the re-embedding migration (count parity, all-points-have-sparse, IDF applied, payload-index survival, dense-on-both-shapes).

Purpose: Establishes the Nyquist feedback contract from 10-VALIDATION.md for RETR-01 before any implementation exists, so every downstream store/migration task has a concrete automated target.

Output: tests/unit/test_qdrant_hybrid.py and tests/integration/test_qdrant_hybrid_migration.py in RED state (xfail / integration-gated), collecting cleanly with no import or collection errors.
</objective>

<artifacts_produced>
- `tests/unit/test_qdrant_hybrid.py` — test functions: `test_named_create_config`, `test_get_dim_named`, `test_hybrid_prefetch_limits`, `test_server_preflight`, `test_upsert_named_shape`, `test_upsert_legacy_shape`
- `tests/integration/test_qdrant_hybrid_migration.py` — test functions: `test_reembed_parity`, `test_all_points_have_sparse`, `test_idf_modifier_set`, `test_payload_indexes_survive`, `test_dense_both_shapes`
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-retrieval/10-CONTEXT.md
@.planning/phases/10-hybrid-retrieval/10-RESEARCH.md
@.planning/phases/10-hybrid-retrieval/10-PATTERNS.md
@.planning/phases/10-hybrid-retrieval/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit RED scaffold — tests/unit/test_qdrant_hybrid.py (RETR-01, D-05, D-07, D-11, D-12, Pitfall 1/2)</name>
  <files>tests/unit/test_qdrant_hybrid.py</files>
  <read_first>
    - tests/unit/test_qdrant_payload_indexes.py (lines 1-55) — the mock_store fixture: QdrantVectorStore.__new__ bypass + MagicMock _client/_Distance/_PointStruct/_VectorParams; copy this fixture verbatim and extend it
    - src/knowledge_lake/plugins/builtin/qdrant_store.py (lines 77-100, 103-141, 235-296, 298-365) — current create-paths, reindex, get_collection_dim, upsert, search shapes these tests will drive
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (a),(c),(d),(f) — verified qdrant-client 1.18 call shapes the assertions must match
    - .planning/phases/10-hybrid-retrieval/10-VALIDATION.md — Per-Task Verification Map rows for RETR-01/D-07 (exact test names)
  </read_first>
  <action>
    Create tests/unit/test_qdrant_hybrid.py mirroring test_qdrant_payload_indexes.py. Reuse the mock_store fixture (QdrantVectorStore.__new__ + MagicMock _client). Add MagicMock attributes for the new cached qdrant models the implementation will reference (SparseVectorParams, Modifier, SparseVector, Prefetch, FusionQuery, Fusion) so the fixture stays self-contained. Guard any import of not-yet-existing helper symbols at module scope with try/except ImportError, and mark each RED test with pytest.mark.xfail(reason pointing at Plan 10-06, strict=False) so the file collects clean and the unit suite stays green.

    Write these test functions (assert on MagicMock call kwargs, no live server):
    - test_named_create_config: after ensure_aliased_collection(alias, dim=384), assert _client.create_collection was called with a vectors_config mapping carrying a "dense" key and a sparse_vectors_config mapping carrying a "sparse" key whose modifier is Modifier.IDF (per D-05, D-13; RESEARCH Example (a)).
    - test_get_dim_named: stub _client.get_collection(...).config.params.vectors to a dict {"dense": obj(size=384)}; assert get_collection_dim returns 384 with no AttributeError (Pitfall 2).
    - test_hybrid_prefetch_limits: call search(..., top_k=5, mode="hybrid", sparse_query=<mock>, offset=0); assert query_points received prefetch as a two-element list, each a Prefetch with using="dense" and using="sparse" respectively, each limit == top_k+offset, and query is a FusionQuery with fusion == Fusion.RRF (D-11, D-12).
    - test_server_preflight: stub _client.info() to return an object with version="1.9.0"; assert the server-preflight helper raises RuntimeError naming server version and the >= 1.10 requirement (D-07; RESEARCH Example (f)).
    - test_upsert_named_shape: with _is_named stubbed True, upsert a VectorPoint carrying a sparse value; assert the built point vector is a dict with "dense" and "sparse" keys (Pitfall 1, D-09).
    - test_upsert_legacy_shape: with _is_named stubbed False, upsert a VectorPoint; assert the built point vector is the bare list (legacy unnamed collection).

    Do NOT implement any production code here. These are RED targets for Plan 10-06.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_qdrant_hybrid.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_qdrant_hybrid.py -q` exits 0 with all six tests reported xfailed (no errors, no collection failure)
    - File defines exactly the six named test functions listed in <artifacts_produced>
    - Fixture never constructs a real QdrantClient (uses `__new__` + MagicMock)
  </acceptance_criteria>
  <done>test_qdrant_hybrid.py exists, collects clean, and all RED tests are xfail — ready for Plan 10-06 to remove xfail as each turns green.</done>
</task>

<task type="auto">
  <name>Task 2: Integration RED scaffold — tests/integration/test_qdrant_hybrid_migration.py (RETR-01, D-05, D-06, D-13, D-14)</name>
  <files>tests/integration/test_qdrant_hybrid_migration.py</files>
  <read_first>
    - tests/integration/test_qdrant_alias_reindex.py (lines 1-55) — store + alias fixtures (settings.qdrant_url, teardown cleanup, pytestmark = pytest.mark.integration); reuse wholesale
    - src/knowledge_lake/pipeline/index.py (lines 189-238) — reindex_collection driver the migration extends
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (h) and §Validation Architecture (live-Qdrant rows)
  </read_first>
  <action>
    Create tests/integration/test_qdrant_hybrid_migration.py with pytestmark = pytest.mark.integration, reusing the store and alias fixtures from test_qdrant_alias_reindex.py. Write these RED tests (they exercise a live Qdrant >= 1.10 and only run under -m integration, so they will NOT run in the unit suite; keep imports to symbols that already exist — QdrantVectorStore, VectorPoint — so module collection never fails):
    - test_reembed_parity: seed a legacy unnamed collection behind an alias, run the hybrid re-embedding reindex, assert client.count(old) == client.count(new) before the alias swap is honored (D-06 parity gate).
    - test_all_points_have_sparse: after migration, scroll the new physical with with_vectors=True and assert every point carries a non-empty "sparse" named vector (D-05).
    - test_idf_modifier_set: assert get_collection(new).config.params.sparse_vectors["sparse"].modifier == Modifier.IDF (D-13).
    - test_payload_indexes_survive: assert the 7 keyword payload indexes exist on the migrated collection so filtered hybrid search does not full-scan (D-14).
    - test_dense_both_shapes: assert dense-mode search returns hits against BOTH a legacy unnamed collection and a migrated named collection (D-09).

    Mark each test body with pytest.mark.xfail(strict=False) referencing Plan 10-07 so a live run before implementation reports xfail rather than hard failure.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_qdrant_hybrid_migration.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_qdrant_hybrid_migration.py --collect-only -q` collects all five tests with zero collection errors
    - Module carries `pytestmark = pytest.mark.integration` so the tests are excluded from the default unit run
    - Reuses the store/alias fixtures from test_qdrant_alias_reindex.py (no new live-connection fixture invented)
  </acceptance_criteria>
  <done>test_qdrant_hybrid_migration.py exists, collects under the integration marker, and is the RED target for Plan 10-07's live migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→Qdrant server | Collection lifecycle + hybrid query ops (validated by these tests) |
| operator→CLI | Migration is operator-triggered; parity gate + rollback are the safety contract these tests assert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Denial of Service | qdrant_store.search prefetch | high | mitigate | test_hybrid_prefetch_limits asserts prefetch limit == top_k+offset (D-12), not 10× |
| T-10-04 | Denial of Service / Integrity | reindex migration / alias swap | high | mitigate | test_reembed_parity asserts count(old)==count(new) before swap (D-06); keep_old_collections rollback |
| T-10-03 | Repudiation / Integrity | dense-on-both-shapes back-compat | high | mitigate | test_dense_both_shapes proves dense never silently breaks on legacy collections (D-09) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_qdrant_hybrid.py -q` → all xfail, no errors
- `uv run pytest tests/integration/test_qdrant_hybrid_migration.py --collect-only -q` → clean collection
- `uv run pytest tests/unit -q` → whole unit suite stays green (no new failures introduced)
</verification>

<success_criteria>
Both RED test files exist, collect without error, and encode the RETR-01 store + migration acceptance behaviors as concrete automated targets for Plans 10-06 and 10-07.
</success_criteria>

<output>
Create `.planning/phases/10-hybrid-retrieval/10-01-SUMMARY.md` when done.
</output>
