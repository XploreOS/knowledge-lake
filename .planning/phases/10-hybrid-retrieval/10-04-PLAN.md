---
phase: 10-hybrid-retrieval
plan: "04"
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/knowledge_lake/config/settings.py
  - src/knowledge_lake/plugins/protocols.py
autonomous: true
requirements:
  - RETR-01
  - RETR-03
must_haves:
  truths:
    - "settings.py defines SearchSettings(BaseModel) with mode: Literal['hybrid','dense','sparse'] = 'hybrid' (D-08)"
    - "Settings gains a `search: SearchSettings = Field(default_factory=SearchSettings)` field so settings.search.mode resolves (D-08)"
    - "KLAKE_SEARCH__MODE=dense resolves to settings.search.mode == 'dense' via the existing env_nested_delimiter='__' (no custom parsing)"
    - "An out-of-range value for KLAKE_SEARCH__MODE fails validation at the pydantic boundary (fail-closed, T-10-02)"
    - "VectorPoint gains `sparse: Optional[Any] = None` (additive default → back-compat, RETR-01, D-09)"
    - "VectorStorePlugin.search Protocol signature gains keyword-only mode='dense', sparse_query=None, offset=0 (D-09)"
    - "pytest tests/unit/test_settings_search.py -q passes (RED tests from Plan 10-02 turn green)"
  artifacts:
    - src/knowledge_lake/config/settings.py (updated — SearchSettings + Settings.search field)
    - src/knowledge_lake/plugins/protocols.py (updated — VectorPoint.sparse + VectorStorePlugin.search signature)
  key_links:
    - "SearchSettings copies the IndexSettings nested-BaseModel pattern (settings.py:282-295) exactly; Settings.search attaches alongside settings.index (settings.py:397)"
    - "VectorPoint.sparse mirrors the additive-default precedent CrawlPageResult.http_status_code=None (protocols.py:451)"
    - "Protocol search signature must match the concrete QdrantVectorStore.search extended in Plan 10-06"
---

<objective>
Wave 2 — Land the two data/config contracts the rest of the phase threads through: the `SearchSettings` nested config model (RETR-03 mode surface, fail-closed validation) and the additive protocol changes (`VectorPoint.sparse`, `VectorStorePlugin.search` keyword-only `mode`/`sparse_query`/`offset`). Pure contract work with no behavior wiring, so the store (10-06) and pipeline (10-07) plans have stable types to build against.

Purpose: RETR-03 requires `KLAKE_SEARCH__MODE=hybrid|dense|sparse` (default hybrid) resolvable via the established nested-settings convention; RETR-01/D-09 require an optional sparse field on VectorPoint and a back-compat store search signature.

Output: SearchSettings + settings.search on Settings; VectorPoint.sparse; extended VectorStorePlugin.search Protocol. Plan 10-02's test_settings_search.py turns green.
</objective>

<artifacts_produced>
- `SearchSettings` (config/settings.py) — new nested BaseModel
- `settings.search` field on `Settings`
- `KLAKE_SEARCH__MODE` env var (resolved via env_nested_delimiter)
- `VectorPoint.sparse: Optional[Any] = None` (protocols.py)
- `VectorStorePlugin.search(*, mode='dense', sparse_query=None, offset=0)` Protocol signature
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-retrieval/10-CONTEXT.md
@.planning/phases/10-hybrid-retrieval/10-RESEARCH.md
@.planning/phases/10-hybrid-retrieval/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: settings.py — add SearchSettings + Settings.search field (RETR-03, D-08, T-10-02)</name>
  <files>src/knowledge_lake/config/settings.py</files>
  <read_first>
    - src/knowledge_lake/config/settings.py (lines 282-320) — IndexSettings nested model to copy + model_config with env_nested_delimiter="__"
    - src/knowledge_lake/config/settings.py (lines 372-404) — where nested fields (storage, crawl, index, domain, ...) attach to Settings
    - tests/unit/test_settings_search.py — the RED target this task turns green
  </read_first>
  <behavior>
    - Settings(_env_file=None).search.mode == "hybrid" by default
    - patch.dict env KLAKE_SEARCH__MODE=dense → Settings(_env_file=None).search.mode == "dense"
    - KLAKE_SEARCH__MODE=bogus → pydantic ValidationError (fail-closed)
  </behavior>
  <action>
    Add a new nested config model SearchSettings(BaseModel) mirroring IndexSettings (settings.py:282-295): a single field `mode: Literal["hybrid", "dense", "sparse"] = "hybrid"` with a docstring documenting the env pattern KLAKE_SEARCH__MODE and the default of "hybrid" per D-08/RETR-03. Import Literal from typing if not already imported. Attach the field on Settings alongside the other nested models (near settings.py:397, next to `index`): `search: SearchSettings = Field(default_factory=SearchSettings)`. Do NOT add any loose top-level mode setting — the nested BaseModel + existing env_nested_delimiter="__" (settings.py:316) resolves KLAKE_SEARCH__MODE with no custom parsing. The Literal type is the fail-closed input-validation boundary (T-10-02).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings_search.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `Settings(_env_file=None).search.mode == "hybrid"`
    - `KLAKE_SEARCH__MODE=dense` resolves to `settings.search.mode == "dense"`
    - `KLAKE_SEARCH__MODE=bogus` raises a pydantic ValidationError
    - `uv run pytest tests/unit/test_settings_search.py -q` exits 0 (RED tests now green; remove their xfail markers)
  </acceptance_criteria>
  <done>SearchSettings is defined, attached as settings.search, and env-resolvable with fail-closed validation.</done>
</task>

<task type="auto">
  <name>Task 2: protocols.py — VectorPoint.sparse + VectorStorePlugin.search signature (RETR-01, D-09)</name>
  <files>src/knowledge_lake/plugins/protocols.py</files>
  <read_first>
    - src/knowledge_lake/plugins/protocols.py (lines 82-101) — VectorPoint dataclass
    - src/knowledge_lake/plugins/protocols.py (lines 314-336) — VectorStorePlugin.search Protocol signature + docstring
    - src/knowledge_lake/plugins/protocols.py (around line 451) — CrawlPageResult.http_status_code additive-default precedent (defaults to None so existing constructions remain valid)
  </read_first>
  <action>
    On the VectorPoint dataclass, add a new optional field `sparse: Optional[Any] = None` after `payload` (default None → back-compat: existing VectorPoint constructions and pre-migration points are unaffected until they opt in). Follow the additive-default precedent documented on CrawlPageResult.http_status_code (protocols.py:451). Update the VectorPoint docstring to note sparse carries the optional BM25 sparse vector for named collections.

    Extend the VectorStorePlugin.search Protocol signature (protocols.py:314) to keyword-only additive defaults matching the concrete implementation Plan 10-06 will land: `search(self, collection, query, top_k, query_filter=None, *, mode="dense", sparse_query=None, offset=0) -> list[Hit]`. Update the docstring to document the three new keyword-only params (mode selects dense|sparse|hybrid; sparse_query is the query-side SparseVector for sparse/hybrid; offset for paging, with prefetch limit == top_k+offset). Ensure `Optional`/`Any` are imported (they already are). This is contract-only — do NOT implement query logic here.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_protocols.py -q 2>/dev/null || uv run python -c "from knowledge_lake.plugins.protocols import VectorPoint, VectorStorePlugin; import inspect; p=VectorPoint(id='x', vector=[0.1]); assert p.sparse is None; s=inspect.signature(VectorStorePlugin.search); assert {'mode','sparse_query','offset'} <= set(s.parameters); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `VectorPoint(id='x', vector=[0.1]).sparse is None` (additive default holds; existing constructions unchanged)
    - `inspect.signature(VectorStorePlugin.search)` contains keyword-only params `mode`, `sparse_query`, `offset`
    - Import of protocols module succeeds with no error; whole unit suite (`uv run pytest tests/unit -q`) stays green
  </acceptance_criteria>
  <done>VectorPoint carries an optional sparse field and VectorStorePlugin.search declares the back-compat mode/sparse_query/offset contract for Plan 10-06 to implement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API/CLI/env | `KLAKE_SEARCH__MODE` and request `mode` cross the config/validation boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | SearchSettings.mode | medium | mitigate | `Literal["hybrid","dense","sparse"]` validates KLAKE_SEARCH__MODE at the pydantic boundary, fail-closed; unknown values raise ValidationError before reaching the store |
| T-10-03 | Repudiation / Integrity | VectorStorePlugin.search contract | high | mitigate | Explicit `mode` param in the contract makes the attempted retrieval mode unambiguous end-to-end (enables the D-10 fail-loud enforcement in Plan 10-06) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_settings_search.py -q` → green (RED tests turned)
- `uv run python -c "from knowledge_lake.plugins.protocols import VectorPoint; assert VectorPoint(id='x', vector=[0.1]).sparse is None"` → exits 0
- `uv run pytest tests/unit -q` → whole unit suite green (no regressions from the additive changes)
</verification>

<success_criteria>
SearchSettings resolves KLAKE_SEARCH__MODE (default hybrid) with fail-closed validation, and the protocol layer carries the additive VectorPoint.sparse + VectorStorePlugin.search(mode/sparse_query/offset) contract — stable types for Plans 10-06 and 10-07.
</success_criteria>

<output>
Create `.planning/phases/10-hybrid-retrieval/10-04-SUMMARY.md` when done.
</output>