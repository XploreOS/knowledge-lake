---
phase: 10-hybrid-retrieval
plan: "07"
type: execute
wave: 4
depends_on:
  - 10-02
  - 10-04
  - 10-05
  - 10-06
files_modified:
  - src/knowledge_lake/pipeline/index.py
  - src/knowledge_lake/pipeline/search.py
autonomous: true
requirements:
  - RETR-01
  - RETR-03
must_haves:
  truths:
    - "index() attaches sparse=embed_sparse_doc(chunk text) to each VectorPoint so upsert writes named dense+sparse vectors for new chunks (RETR-01, D-05)"
    - "reindex_collection() gains hybrid: bool = False; when True it asserts the server >= 1.10 preflight, then reindexes via a re_embed_fn that calls vstore.reembed_all_points(collection, new_physical, embed_sparse_doc) (RETR-01, D-05, D-07)"
    - "The re-embedding migration reads chunk text from payload['text'] (index.py:158 since Phase 1) — NO registry/silver-zone join, NO new get_session block for text (research simplification)"
    - "reindex_collection(hybrid=False) keeps the existing copy_all_points path unchanged (back-compat)"
    - "pipeline.search() gains mode: Optional[str] = None defaulting to settings.search.mode; when mode in ('hybrid','sparse') it builds sparse_query=embed_sparse_query(query) and passes mode + sparse_query into vstore.search (RETR-03, D-03, D-09)"
    - "The search.py filter builder (lines 104-122) is reused verbatim across all modes (D-14)"
    - "A hybrid/sparse request against a sparse-less collection surfaces the store's fail-loud error unchanged (no dense fallback) (D-10)"
    - "pytest tests/unit/test_search_mode.py tests/unit/test_search_filters.py -q passes (RED tests from Plan 10-02 turn green)"
  artifacts:
    - src/knowledge_lake/pipeline/index.py (updated — index() sparse build + reindex_collection hybrid re-embed migration + preflight)
    - src/knowledge_lake/pipeline/search.py (updated — mode threading + sparse_query build)
    - tests/unit/test_index_sparse.py (NEW — unit coverage for index() sparse attach + hybrid re_embed wiring)
  key_links:
    - "index.py imports embed_sparse_doc from plugins.builtin.sparse_embedder; search.py imports embed_sparse_query — the D-01 plugin seam"
    - "reindex_collection registry re-registration block (index.py:219-225) is unchanged; only the upsert_fn and a preflight are added"
    - "The parity gate itself lives in vstore.reindex (Plan 10-06); reindex_collection just supplies the re_embed_fn and the preflight"
    - "search.py already imports the qdrant filter models (line 25) and resolves settings at line 81 — mode defaults to s.search.mode there"
---

<objective>
Wave 4 — Wire the sparse encoder into the pipeline: build sparse vectors at index time, thread search `mode` + query-side sparse embedding through `pipeline.search`, and turn `reindex_collection` into the operator-triggered live re-embedding migration (server preflight + re_embed upsert_fn) that carries the parity + rollback contract.

Purpose: Connects Plan 10-05 (sparse encoder) and Plan 10-06 (store) into the request/index/migration flows — the RETR-01 re-embedding migration and the RETR-03 mode threading.

Output: index.py (sparse build + hybrid migration) and search.py (mode + sparse_query); Plan 10-02's test_search_mode.py and the D-14 filter assertion turn green.
</objective>

<artifacts_produced>
- `pipeline.index.index()` — attaches VectorPoint.sparse via embed_sparse_doc
- `pipeline.index.reindex_collection(..., hybrid: bool = False)` — re-embedding migration + D-07 preflight
- `pipeline.search.search(..., mode=None)` — mode threading + embed_sparse_query build
- `tests/unit/test_index_sparse.py` (NEW)
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-retrieval/10-CONTEXT.md
@.planning/phases/10-hybrid-retrieval/10-RESEARCH.md
@.planning/phases/10-hybrid-retrieval/10-PATTERNS.md
@src/knowledge_lake/pipeline/index.py
@src/knowledge_lake/pipeline/search.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: index.py index() — attach sparse to each VectorPoint (RETR-01, D-05)</name>
  <files>src/knowledge_lake/pipeline/index.py, tests/unit/test_index_sparse.py</files>
  <read_first>
    - src/knowledge_lake/pipeline/index.py (lines 143-186) — the VectorPoint build loop and upsert call
    - src/knowledge_lake/plugins/builtin/sparse_embedder.py — embed_sparse_doc (from Plan 10-05)
    - tests/unit/test_search_filters.py (lines 19-32) — monkeypatch style for the vectorstore/embedder seam
  </read_first>
  <behavior>
    - Each VectorPoint built by index() carries a non-None sparse produced from the chunk's text
    - index() with an empty chunk text still builds a VectorPoint (empty SparseVector, no raise)
    - vstore.upsert receives points whose sparse field is set (asserted via a captured MagicMock)
  </behavior>
  <action>
    Import embed_sparse_doc from knowledge_lake.plugins.builtin.sparse_embedder at the top of index.py. In the VectorPoint build loop (148-179), set `sparse=embed_sparse_doc(chunk.get("text", ""))` on each VectorPoint so the store's named upsert (Plan 10-06) writes dense+sparse for new chunks (D-05). The chunk text is already available as `chunk.get("text", "")` (also the payload['text'] value at line 158) — no extra lookup. Do not change any other payload field.

    Create tests/unit/test_index_sparse.py: monkeypatch get_vectorstore and embed_sparse_doc on the index module; call index(...) with a couple of stub chunks + vectors and assert the VectorPoints passed to vstore.upsert each carry a non-None sparse, and that embed_sparse_doc was called with the chunk text. Mock the registry session helpers as the existing index tests do (or pass a settings/vstore stub) so no DB/Qdrant is touched.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_index_sparse.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Every VectorPoint from index() has sparse set from the chunk text (D-05)
    - Empty chunk text yields an empty SparseVector, no exception
    - test_index_sparse.py passes with no DB/Qdrant/live-model contact
  </acceptance_criteria>
  <done>New chunks are indexed with both dense and sparse named vectors.</done>
</task>

<task type="auto">
  <name>Task 2: index.py reindex_collection — hybrid re-embedding migration + preflight (RETR-01, D-04, D-05, D-06, D-07)</name>
  <files>src/knowledge_lake/pipeline/index.py</files>
  <read_first>
    - src/knowledge_lake/pipeline/index.py (lines 189-238) — reindex_collection driver, _copy_fn, registry re-registration block
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (h) + §Architecture Patterns (LIVE MIGRATION diagram)
    - src/knowledge_lake/plugins/builtin/qdrant_store.py — reembed_all_points + assert_server_supports_hybrid (from Plan 10-06)
  </read_first>
  <action>
    Add a keyword-only `hybrid: bool = False` parameter to reindex_collection (189). When hybrid is True: (1) call vstore.assert_server_supports_hybrid() BEFORE creating any collection (D-07) so an old server aborts loudly before the migration touches data; (2) define `re_embed_fn(new_physical)` that calls `vstore.reembed_all_points(collection, new_physical, embed_sparse_doc)` — scrolls the alias's current physical (reads payload['text'], reuses the scrolled dense, synthesizes sparse) and upserts named points; (3) pass `upsert_fn=re_embed_fn` into vstore.reindex(...). When hybrid is False, keep the existing `_copy_fn`/copy_all_points path unchanged (back-compat).

    The count-parity gate and the alias swap live inside vstore.reindex (Plan 10-06) — do not duplicate them here. Leave the registry re-registration get_session block (219-225) unchanged. Because the re-embed reads payload['text'] from the Qdrant scroll, NO get_session block is needed for chunk text (research simplification — the session-boundary rule does not apply to the text read).

    Import embed_sparse_doc from plugins.builtin.sparse_embedder (already imported in Task 1). Update the reindex_collection docstring to document hybrid=True as the operator-triggered live migration with the parity-gate + keep_old_collections rollback contract.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_index_sparse.py tests/unit -q -k "reindex or sparse or index"</automated>
  </verify>
  <acceptance_criteria>
    - reindex_collection(hybrid=True) calls assert_server_supports_hybrid() before creating the new collection (D-07)
    - reindex_collection(hybrid=True) reindexes via vstore.reembed_all_points (re-embed, not copy) reading payload['text'] (D-05)
    - reindex_collection(hybrid=False) is byte-for-byte behavior-equivalent to today (copy_all_points path)
    - The parity gate + rollback are enforced by vstore.reindex (Plan 10-06), not duplicated here
    - No new get_session block is added for chunk text
  </acceptance_criteria>
  <done>reindex_collection performs the operator-triggered live re-embedding migration with a server preflight, delegating parity + rollback to the store.</done>
</task>

<task type="auto">
  <name>Task 3: search.py — thread mode + build sparse_query (RETR-03, D-03, D-09, D-14)</name>
  <files>src/knowledge_lake/pipeline/search.py</files>
  <read_first>
    - src/knowledge_lake/pipeline/search.py (lines 34-47, 81-127) — signature, settings resolution, filter builder (verbatim reuse), vstore.search call
    - src/knowledge_lake/plugins/builtin/sparse_embedder.py — embed_sparse_query (from Plan 10-05)
    - tests/unit/test_search_mode.py, tests/unit/test_search_filters.py — RED targets from Plan 10-02
  </read_first>
  <action>
    Add a keyword-only `mode: Optional[str] = None` parameter to search() (mirror how the filter kwargs were added at 39-46). After `s = settings or get_settings()` (line 81), resolve `mode = mode or s.search.mode` (default hybrid via SearchSettings, D-08/D-09). Import embed_sparse_query from knowledge_lake.plugins.builtin.sparse_embedder. When mode in ("hybrid", "sparse"), build `sparse_query = embed_sparse_query(query)`; otherwise sparse_query stays None (D-03). Leave the filter-builder block (104-122) untouched and reused verbatim across every mode (D-14). Thread the new args into the store call (125-127): pass `mode=mode, sparse_query=sparse_query` (keyword-only) alongside the existing collection/query_vector/top_k/query_filter. Do NOT catch or translate the store's fail-loud error — let it propagate unchanged so the caller sees the missing-vector + remediation message (D-10). Update the search() docstring to document the mode param and the fail-loud behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_search_mode.py tests/unit/test_search_filters.py -q</automated>
  </verify>
  <acceptance_criteria>
    - search() defaults mode to settings.search.mode (hybrid) and accepts a per-request override (D-09)
    - sparse_query is built via embed_sparse_query only for hybrid/sparse modes; None for dense (D-03)
    - The Phase-7 filter builder is unchanged and the same query_filter is forwarded in every mode (D-14)
    - The store's fail-loud error propagates unchanged (no dense fallback) (D-10)
    - `uv run pytest tests/unit/test_search_mode.py tests/unit/test_search_filters.py -q` exits 0 (RED tests green)
  </acceptance_criteria>
  <done>pipeline.search threads mode + query-side sparse embedding to the store, defaulting to hybrid and failing loud on missing sparse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→CLI/pipeline | reindex_collection(hybrid=True) is the operator-triggered live migration entry |
| caller→pipeline.search | `mode` selects retrieval strategy; propagates fail-loud errors |
| app→Qdrant server | Migration scroll/upsert + hybrid query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Denial of Service / Integrity | reindex_collection re-embed migration | high | mitigate | D-07 server preflight before touching data; parity gate + keep_old_collections rollback enforced by vstore.reindex; re-embed reads immutable payload['text'] |
| T-10-03 | Repudiation / Integrity | pipeline.search mode threading | high | mitigate | D-10: store fail-loud error propagates unchanged; search never substitutes dense for a hybrid/sparse request |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_index_sparse.py tests/unit/test_search_mode.py tests/unit/test_search_filters.py -q` → green
- `uv run pytest tests/unit -q` → whole unit suite green
- Phase gate (live Qdrant >= 1.10): `uv run pytest tests/integration/test_qdrant_hybrid_migration.py -m integration -q` → parity, all-sparse, IDF, dense-on-both-shapes green
</verification>

<success_criteria>
New chunks index with dense+sparse vectors, pipeline.search threads mode + query-side BM25 (defaulting hybrid, failing loud), and reindex_collection performs the operator-triggered live re-embedding migration with a server preflight — turning Plan 10-02's mode + filter RED tests green.
</success_criteria>

<output>
Create `.planning/phases/10-hybrid-retrieval/10-07-SUMMARY.md` when done.
</output>
