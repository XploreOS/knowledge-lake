---
phase: 10-hybrid-retrieval
plan: "06"
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-04
files_modified:
  - src/knowledge_lake/plugins/builtin/qdrant_store.py
autonomous: true
requirements:
  - RETR-01
  - RETR-03
must_haves:
  truths:
    - "All three create-paths (ensure_collection, ensure_aliased_collection, reindex) build vectors_config={'dense': VectorParams(size=dim, distance=dist)} + sparse_vectors_config={'sparse': SparseVectorParams(modifier=Modifier.IDF)} (D-05, D-13; born-named Pattern 1)"
    - "get_collection_dim branches: dict params.vectors → vectors['dense'].size, else vectors.size (no AttributeError on named collections) (Pitfall 2)"
    - "A cached _is_named(collection) helper reads get_collection(...).config.params.vectors and returns True when it is a dict (Pitfall 1)"
    - "upsert builds named vector={'dense': v, 'sparse': SparseVector} for named collections (sparse attached only when VectorPoint.sparse is not None) and bare vector=v for legacy unnamed collections (Pitfall 1, D-09)"
    - "search gains keyword-only mode='dense', sparse_query=None, offset=0; hybrid uses two Prefetch branches (using='dense'/'sparse', filter=query_filter, limit=top_k+offset) + FusionQuery(fusion=Fusion.RRF) + top-level query_filter (D-11, D-12, D-14)"
    - "search fails loud: mode in ('hybrid','sparse') against a collection with no sparse vectors raises a clear error naming the missing 'sparse' vector + `klake reindex --hybrid` remediation; NEVER falls back to dense (D-10)"
    - "A memoized server-preflight helper raises RuntimeError when client.info().version < 1.10 (packaging.version.Version), gating hybrid/sparse queries (D-07)"
    - "reindex inserts a count-parity gate between ensure_payload_indexes and update_collection_aliases: if old_physical is not None and count(old)!=count(new) → raise BEFORE the alias swap (D-06, Pitfall 5)"
    - "reembed_all_points(source, dest, sparse_doc_fn) scrolls source with_vectors+with_payload, reuses the scrolled dense vector, synthesizes sparse via sparse_doc_fn(payload['text']), and upserts named points (D-05)"
    - "pytest tests/unit/test_qdrant_hybrid.py -q passes (RED tests from Plan 10-01 turn green)"
  artifacts:
    - src/knowledge_lake/plugins/builtin/qdrant_store.py (updated — 3 create-paths, get_collection_dim, _is_named, server-preflight, upsert shape branch, reembed_all_points, search hybrid/dense/sparse + fail-loud, reindex parity gate)
  key_links:
    - "ensure_payload_indexes calls after each create are preserved so keyword indexes survive the named recreate (D-14 free)"
    - "Prefetch's payload-filter field is named `filter`; the top-level query_points uses `query_filter` — the SAME reused Filter object attaches to both branches and the top level (D-14)"
    - "Parity gate uses self._client.count(name, exact=True); skipped when old_physical is None (first reindex)"
    - "Server preflight and the sparse-presence probe run BEFORE query_points when mode in ('hybrid','sparse')"
---

<objective>
Wave 3 — The highest-risk change surface: extend QdrantVectorStore to born-named dense+sparse collections, tolerate both vector shapes for back-compat, run server-side RRF hybrid queries with fail-loud enforcement, gate on a server-version preflight, and insert the migration count-parity safety gate. Single-file, sequenced deliberately across three tasks.

Purpose: Delivers the store-layer core of RETR-01 (named create-paths, hybrid RRF, re-embed helper, parity gate) and RETR-03 (fail-loud probe), plus the D-07 preflight — all against the verified qdrant-client 1.18 API.

Output: A fully hybrid-capable QdrantVectorStore whose unit RED tests (Plan 10-01) turn green; the live migration behaviors (parity, all-sparse, IDF) are proven by Plan 10-07's integration run.
</objective>

<artifacts_produced>
- `QdrantVectorStore._is_named(collection) -> bool` (cached shape helper)
- `QdrantVectorStore.assert_server_supports_hybrid()` (memoized server >= 1.10 preflight)
- `QdrantVectorStore.reembed_all_points(source, dest, sparse_doc_fn, batch_size=256)` (re-embedding scroll+upsert)
- Named create-paths + `get_collection_dim` branch + upsert shape branch + hybrid/dense/sparse `search` + reindex parity gate
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-retrieval/10-CONTEXT.md
@.planning/phases/10-hybrid-retrieval/10-RESEARCH.md
@.planning/phases/10-hybrid-retrieval/10-PATTERNS.md
@src/knowledge_lake/plugins/builtin/qdrant_store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Named create-paths + _is_named + get_collection_dim branch + server preflight (RETR-01, D-05, D-07, D-13, Pitfall 1/2)</name>
  <files>src/knowledge_lake/plugins/builtin/qdrant_store.py</files>
  <read_first>
    - src/knowledge_lake/plugins/builtin/qdrant_store.py (lines 53-61, 77-141, 235-296) — __init__ model cache, three create-paths, reindex, get_collection_dim
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (a),(f) + §Common Pitfalls (Pitfall 2)
    - .planning/phases/10-hybrid-retrieval/10-PATTERNS.md §"plugins/builtin/qdrant_store.py" — the three create sites (97-100, 126-129, 255-258) and get_collection_dim (293-296)
    - tests/unit/test_qdrant_hybrid.py — test_named_create_config, test_get_dim_named, test_server_preflight are the RED targets
  </read_first>
  <action>
    In __init__, extend the lazy model cache to also import and store the new qdrant models needed: SparseVectorParams, SparseIndexParams (if needed), Modifier, SparseVector, Prefetch, FusionQuery, Fusion (mirror the existing self._Distance/_PointStruct/_VectorParams caching at lines 57-59), or import them inline per method as the file already does for alias ops — pick one convention and apply consistently.

    Convert ALL THREE create-paths to born-named (Pattern 1, D-05, D-13). At ensure_collection (97-100), ensure_aliased_collection (126-129), and reindex (255-258), replace the unnamed `vectors_config=self._VectorParams(size=dim, distance=dist)` with `vectors_config={"dense": self._VectorParams(size=dim, distance=dist)}` and add `sparse_vectors_config={"sparse": SparseVectorParams(modifier=Modifier.IDF)}`. Keep the existing `self.ensure_payload_indexes(physical)` call that follows each create (D-14 preserved for free).

    Fix get_collection_dim (293-296) to branch on shape (Pitfall 2): read `vectors = self._client.get_collection(alias).config.params.vectors`; if isinstance(vectors, dict) return `vectors["dense"].size`, else return `vectors.size`.

    Add a cached `_is_named(self, collection) -> bool` helper reading `self._client.get_collection(collection).config.params.vectors` and returning True when it is a dict (Pitfall 1). Cache per collection name (e.g. a dict on self) to avoid repeated get_collection round-trips.

    Add a memoized server preflight `assert_server_supports_hybrid(self) -> None` (RESEARCH Example (f), D-07): read `self._client.info().version`, parse with `packaging.version.Version`, and raise RuntimeError naming the server version and the ">= 1.10" requirement if too old. Memoize the success on self so it runs at most once per process.
  </action>
  <verify>
    <automated>uv run pytest "tests/unit/test_qdrant_hybrid.py::test_named_create_config" "tests/unit/test_qdrant_hybrid.py::test_get_dim_named" "tests/unit/test_qdrant_hybrid.py::test_server_preflight" -q</automated>
  </verify>
  <acceptance_criteria>
    - create_collection is invoked with a named `{"dense": ...}` vectors_config AND `sparse_vectors_config={"sparse": SparseVectorParams(modifier=Modifier.IDF)}` at all three create-paths
    - get_collection_dim returns the dense dim for a dict-shaped params.vectors with no AttributeError
    - assert_server_supports_hybrid raises RuntimeError for version 1.9.x and is a no-op (memoized) for >= 1.10
    - The three listed RED tests pass (remove their xfail markers)
  </acceptance_criteria>
  <done>Collections are born named+sparse with IDF, dim reads on both shapes, and the server-version preflight is in place.</done>
</task>

<task type="auto">
  <name>Task 2: upsert shape branch + reembed_all_points re-embedding helper (RETR-01, D-05, D-09, Pitfall 1)</name>
  <files>src/knowledge_lake/plugins/builtin/qdrant_store.py</files>
  <read_first>
    - src/knowledge_lake/plugins/builtin/qdrant_store.py (lines 198-233, 298-322) — copy_all_points (scroll pattern to mirror) and upsert
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (c),(h) — named upsert shape + re-embed scroll snippet
    - tests/unit/test_qdrant_hybrid.py — test_upsert_named_shape, test_upsert_legacy_shape RED targets
  </read_first>
  <action>
    Rewrite upsert (298-322) to branch on collection shape via `_is_named(collection)`. For a named collection, build each PointStruct with `vector={"dense": p.vector}` and, when `p.sparse is not None`, add the `"sparse"` key set to p.sparse (a SparseVector). For a legacy unnamed collection, keep the current bare `vector=p.vector` (no sparse). This preserves D-09 (dense-mode indexing still works on un-migrated collections) and Pitfall 1.

    Add `reembed_all_points(self, source: str, dest: str, sparse_doc_fn, batch_size: int = 256) -> int` mirroring copy_all_points (198-233): scroll `source` with with_vectors=True + with_payload=True; for each record reuse the scrolled dense vector (`r.vector if isinstance(r.vector, list) else r.vector.get("dense")`), synthesize the sparse vector via `sparse_doc_fn((r.payload or {}).get("text", ""))`, and upsert a named PointStruct `vector={"dense": dense, "sparse": sparse}` with the original payload into `dest`. Return the total count. Use the same explicit `if next_offset is None: break` end-of-scroll sentinel the existing copy_all_points uses (do not use a falsy check). sparse_doc_fn is injected by the caller (Plan 10-07 passes embed_sparse_doc) so this method imports no encoder — the store stays generic (D-01 seam).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_qdrant_hybrid.py -k upsert -q</automated>
  </verify>
  <acceptance_criteria>
    - upsert builds a named `{"dense": v, "sparse": ...}` point for named collections (sparse only when VectorPoint.sparse is not None)
    - upsert builds a bare `vector=v` point for legacy unnamed collections
    - reembed_all_points reuses the scrolled dense vector and synthesizes sparse via the injected sparse_doc_fn; end-of-scroll uses explicit `next_offset is None`
    - The upsert-shape RED tests pass (remove their xfail markers)
  </acceptance_criteria>
  <done>upsert tolerates both collection shapes and the re-embedding scroll helper is available to the migration driver.</done>
</task>

<task type="auto">
  <name>Task 3: search hybrid/dense/sparse + fail-loud probe + reindex parity gate (RETR-01, RETR-03, D-06, D-10, D-11, D-12, D-14)</name>
  <files>src/knowledge_lake/plugins/builtin/qdrant_store.py</files>
  <read_first>
    - src/knowledge_lake/plugins/builtin/qdrant_store.py (lines 235-291, 324-365) — reindex swap region + search
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (d),(e),(g) + §Common Pitfalls (Pitfall 3, Pitfall 5)
    - .planning/phases/10-hybrid-retrieval/10-PATTERNS.md §"search" and §"reindex — insert the parity gate"
    - tests/unit/test_qdrant_hybrid.py — test_hybrid_prefetch_limits (and the fail-loud path also asserted via tests/unit/test_search_mode.py)
  </read_first>
  <action>
    Extend search (324-365) signature to `search(self, collection, query, top_k, query_filter=None, *, mode="dense", sparse_query=None, offset=0)`. Before querying, when mode in ("hybrid","sparse"): (1) call self.assert_server_supports_hybrid() (D-07); (2) probe sparse presence via a `_collection_has_sparse(collection)` check reading get_collection(collection).config.params.sparse_vectors and confirming a "sparse" key (RESEARCH Example (g)); if absent, RAISE a clear error (a dedicated exception or ValueError) naming the missing "sparse" vector on `collection` and the remediation `run the hybrid reindex (klake reindex --hybrid)`, and stating that dense mode still works — NEVER fall back to dense (D-10).

    Branch the query:
    - mode == "hybrid": call query_points with prefetch=[Prefetch(query=query, using="dense", filter=query_filter, limit=top_k+offset), Prefetch(query=sparse_query, using="sparse", filter=query_filter, limit=top_k+offset)], query=FusionQuery(fusion=Fusion.RRF), query_filter=query_filter, limit=top_k, offset=offset, with_payload=True (D-11, D-12, D-14). Note Prefetch's payload-filter field is `filter`; the top-level uses `query_filter`.
    - mode == "sparse": query_points(query=sparse_query, using="sparse", query_filter=query_filter, limit=top_k, offset=offset, with_payload=True).
    - mode == "dense" (default): query_points(query=query, using="dense" if _is_named(collection) else omitted, query_filter=query_filter, limit=top_k, offset=offset, with_payload=True) — bare (no using) for legacy unnamed collections (Pitfall 1, D-09).
    Reuse the existing Hit-construction loop (355-362) verbatim for all branches.

    In reindex (between ensure_payload_indexes at 261 and building change_aliases_operations at 270), insert the count-parity gate (D-06, Pitfall 5): if old_physical is not None and self._client.count(old_physical, exact=True).count != self._client.count(next_physical, exact=True).count, raise a clear error and return WITHOUT applying update_collection_aliases (the alias keeps pointing at old_physical — reversible). Skip the gate when old_physical is None (first reindex).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_qdrant_hybrid.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Hybrid search passes two Prefetch branches (using dense + sparse), each limit == top_k+offset, with FusionQuery(fusion=Fusion.RRF) and the reused query_filter on both branches and the top level (D-11, D-12, D-14)
    - Dense mode omits `using` for legacy unnamed collections and sets `using="dense"` for named ones (Pitfall 1)
    - A hybrid/sparse request against a sparse-less collection raises a clear error naming the missing vector + `klake reindex --hybrid` remediation, with no dense fallback (D-10)
    - reindex raises and does NOT swap the alias when count(old) != count(new); gate skipped on first reindex (D-06)
    - `uv run pytest tests/unit/test_qdrant_hybrid.py -q` exits 0 (all RED tests green)
  </acceptance_criteria>
  <done>QdrantVectorStore performs server-side RRF hybrid queries, enforces the fail-loud contract, and guards the migration alias swap with count parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→Qdrant server | Collection lifecycle, hybrid query, and migration alias swap cross here |
| caller→store.search | `mode`/`sparse_query`/`top_k`/`offset` shape the query and prefetch budget |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Denial of Service | search prefetch branches | high | mitigate | D-12: each Prefetch limit == top_k+offset (tight, not 10×); top_k bounded [1,100] upstream at the API |
| T-10-03 | Repudiation / Integrity | search mode enforcement | high | mitigate | D-10 fail-loud probe raises when sparse absent for hybrid/sparse; never substitutes dense |
| T-10-04 | Denial of Service / Integrity | reindex alias swap | high | mitigate | D-06 count-parity gate before update_collection_aliases; alias stays on old_physical on mismatch (reversible); keep_old_collections rollback |
| T-10-SC | Tampering | server capability assumption | high | mitigate | D-07 memoized server >= 1.10 preflight before any hybrid/sparse query and before migration; abort loud on old server |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_qdrant_hybrid.py -q` → all RED tests green
- `uv run pytest tests/unit -q` → whole unit suite green (dense-mode back-compat intact)
- Live migration behaviors (parity, all-sparse, IDF, dense-on-both-shapes) validated by Plan 10-07's integration run against Qdrant >= 1.10
</verification>

<success_criteria>
QdrantVectorStore creates born-named dense+sparse collections, tolerates legacy shapes for dense back-compat, runs server-side RRF hybrid queries with fail-loud enforcement and a server-version preflight, and guards the migration swap with a count-parity gate — turning all Plan 10-01 unit RED tests green.
</success_criteria>

<output>
Create `.planning/phases/10-hybrid-retrieval/10-06-SUMMARY.md` when done.
</output>