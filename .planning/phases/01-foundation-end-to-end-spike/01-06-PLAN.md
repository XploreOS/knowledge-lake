---
phase: 01-foundation-end-to-end-spike
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - src/knowledge_lake/api/app.py
  - src/knowledge_lake/api/schemas.py
  - src/knowledge_lake/dagster_defs/definitions.py
  - src/knowledge_lake/dagster_defs/assets.py
  - src/knowledge_lake/dagster_defs/resources.py
  - tests/integration/test_api_lineage.py
  - tests/integration/test_dagster_assets.py
autonomous: true
requirements: [FOUND-07, FOUND-01]
must_haves:
  truths:
    - "GET /search returns chunks with scores and citation fields; GET /lineage/{artifact_id} returns the machine-readable lineage graph (JSON)"
    - "The FastAPI service exposes OpenAPI docs and the same pipeline is driven in-process by the API (D-02)"
    - "The pipeline stages are wrapped as Dagster software-defined assets that call the same plain functions and materialize from the Dagster UI (D-01)"
    - "Wrapping in Dagster does not change the CLI/API command surface or outputs"
  artifacts:
    - "src/knowledge_lake/api/app.py search + lineage endpoints with pydantic schemas"
    - "src/knowledge_lake/dagster_defs/assets.py wrapping ingest/parse/chunk/embed/index and resources.py for MinIO/Postgres/Qdrant/LiteLLM"
    - "tests/integration/test_api_lineage.py and tests/integration/test_dagster_assets.py"
  key_links:
    - "API endpoints call the same pipeline.run/search/lineage functions as the CLI (single source of behavior)"
    - "Dagster assets use deps for ordering and call StorageBackend/registry explicitly — not IO managers (Pitfall 7)"
    - "Dagster resources read connection config via EnvVar from the first connection (Pitfall 14)"
  prohibitions:
    - "No change to CLI/API command names or outputs when the Dagster wrap is added (D-02)"
    - "No Dagster IO managers for object bytes — use deps + explicit storage calls (Pitfall 7)"
    - "No re-implementation of pipeline logic inside assets — assets call the existing plain functions"
---

<objective>
Complete the walking skeleton's user surface and close the phase's orchestration decision: expose the lineage/search over FastAPI (FOUND-07 API side, with OpenAPI) and wrap the already-proven pipeline functions as Dagster software-defined assets that materialize from the Dagster UI (D-01) — without changing the CLI/API surface (D-02).

Purpose: Finish FOUND-07 (lineage via API) and satisfy the "Dagster from day 1, assets before phase close" decision while honoring Pitfall #1 (functions proven first, orchestration second).
Output: search + lineage API endpoints, Dagster assets + resources, and integration tests for both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-end-to-end-spike/01-CONTEXT.md
@.planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md
@.planning/phases/01-foundation-end-to-end-spike/SKELETON.md
@.planning/phases/01-foundation-end-to-end-spike/01-01-SUMMARY.md
@.planning/phases/01-foundation-end-to-end-spike/01-05-SUMMARY.md
</context>

## Phase Goal

**As an** operator of the Knowledge Lake, **I want to** bring up the full stack with one command and flow one real document from ingest to a cited, lineage-traceable search result, **so that** the tool-agnostic foundation every later phase depends on is proven working end-to-end.

This plan delivers the final operator-visible capabilities: query search/lineage over the API, and watch the same pipeline materialize as Dagster assets — with the surface unchanged.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI search + lineage endpoints (FOUND-07 API, D-02)</name>
  <files>src/knowledge_lake/api/app.py, src/knowledge_lake/api/schemas.py, tests/integration/test_api_lineage.py</files>
  <read_first>src/knowledge_lake/api/app.py, src/knowledge_lake/pipeline/search.py, src/knowledge_lake/lineage.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Architecture Responsibility Map: User surface; Security V5 input validation)</read_first>
  <behavior>
    - GET /search?q=...&top_k=... returns 200 with a list of hits, each carrying score and citation (document, section_path, page)
    - GET /lineage/{artifact_id} returns 200 with the machine-readable lineage graph (chunk→parsed→raw→source, six fields per node)
    - GET /lineage/{unknown_id} returns 404 with a clear error body
    - OpenAPI docs are served and describe both endpoints
    - Query params are validated by pydantic models (V5)
  </behavior>
  <action>Extend the existing FastAPI app (health added in 01-01) with search and lineage endpoints that call the SAME pipeline.search and lineage.resolve_ancestry functions the CLI uses — no behavior re-implementation (D-02). Define request/response pydantic schemas in schemas.py (SearchHit with score + citation fields; LineageNode with the six FOUND-06 fields; LineageGraph). Validate inputs (top_k bounds, artifact_id format) per ASVS V5. Return 404 for unknown artifacts. Keep /health. Ensure OpenAPI docs render. Write tests/integration/test_api_lineage.py using FastAPI's test client against a seeded registry: assert /search hit shape, /lineage graph shape + six fields, 404 on unknown, and that /openapi.json lists the endpoints.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_api_lineage.py -x -q</automated>
  </verify>
  <done>/search and /lineage/{id} return correctly shaped, validated JSON reusing the CLI's pipeline functions; unknown id 404s; OpenAPI docs present; test green.</done>
  <acceptance_criteria>
    - /search returns hits with score + citation; /lineage/{id} returns the six-field lineage graph (FOUND-07 API)
    - Endpoints call the same pipeline/lineage functions as the CLI (D-02, no surface change)
    - Inputs validated via pydantic; unknown artifact 404s; OpenAPI docs served
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wrap pipeline as Dagster assets + resources (D-01 phase close)</name>
  <files>src/knowledge_lake/dagster_defs/definitions.py, src/knowledge_lake/dagster_defs/assets.py, src/knowledge_lake/dagster_defs/resources.py, tests/integration/test_dagster_assets.py</files>
  <read_first>src/knowledge_lake/pipeline/run.py, src/knowledge_lake/pipeline/ingest.py, src/knowledge_lake/dagster_defs/definitions.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Pattern 4: Prove-Then-Orchestrate; Pitfall 7 IO managers; Pitfall 14 resource config)</read_first>
  <action>Now that the document demonstrably flows (01-05), wrap the pipeline as Dagster software-defined assets that CALL the existing plain functions — do not re-implement logic. In assets.py define assets for raw_document → parsed_document → chunks → embeddings → index, ordered with `deps` (NOT IO managers for the object bytes — Pitfall 7; assets call StorageBackend/registry explicitly). In resources.py define Dagster resources for MinIO/Postgres/Qdrant/LiteLLM reading connection config via EnvVar from the first connection (Pitfall 14). Update definitions.py (minimal in 01-01) to register the assets + resources. The CLI/API surface must be unchanged (D-02) — assets are an additional execution path, not a replacement. Write tests/integration/test_dagster_assets.py that materializes the asset graph over the fixture document (or asserts the Definitions load and a materialization run succeeds) and confirms the resulting artifacts/lineage match what the in-process path produces.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_dagster_assets.py -x -q</automated>
  </verify>
  <done>Pipeline stages are Dagster assets calling the same functions; Definitions load and materialize; resources use EnvVar config; CLI/API surface unchanged; test green.</done>
  <acceptance_criteria>
    - Assets wrap (call) the existing pipeline functions; no logic duplicated (D-01)
    - Ordering via deps; no IO managers for object bytes (Pitfall 7)
    - Resources read connection config via EnvVar (Pitfall 14)
    - Materializing the asset graph yields the same artifacts/lineage as the in-process path
    - CLI/API command names and outputs unchanged (D-02)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → FastAPI | Query params / path IDs are untrusted input |
| Dagster resources → services | Connection config crosses to Postgres/MinIO/Qdrant/LiteLLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Tampering / Info disclosure | API input validation | medium | mitigate | pydantic-validated params, top_k bounds, artifact_id format check; parameterized registry queries (ASVS V5) |
| T-01-15 | Config drift | Dagster resource connection config | medium | mitigate | EnvVar-based resources from the first connection, no hardcoded URLs (Pitfall 14) |
| T-01-16 | Repudiation | Dagster path vs in-process path divergence | low | mitigate | Assets call the same functions; test asserts identical artifacts/lineage across both paths |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_api_lineage.py tests/integration/test_dagster_assets.py` green
- `curl -s localhost:<api-port>/lineage/<chunk-id>` returns the six-field graph; /docs renders
- Dagster UI shows the asset graph materializing over the spike document
- Grep confirms assets call pipeline functions (no duplicated logic) and no IO managers for bytes
</verification>

<success_criteria>
- FOUND-07: lineage queryable via API (completing CLI+API coverage)
- FOUND-01 fully satisfied: Dagster service is not just healthy but materializes the pipeline (D-01), CLI/API surface unchanged (D-02)
</success_criteria>

## Artifacts This Phase Produces (Plan 06)

- Files: `src/knowledge_lake/api/schemas.py`, `src/knowledge_lake/dagster_defs/assets.py`, `src/knowledge_lake/dagster_defs/resources.py`
- API routes: `GET /search`, `GET /lineage/{artifact_id}`
- Schemas: `SearchHit`, `LineageNode`, `LineageGraph`
- Dagster: `raw_document`/`parsed_document`/`chunks`/`embeddings`/`index` assets; MinIO/Postgres/Qdrant/LiteLLM resources; updated `Definitions`
- Test files: `tests/integration/test_api_lineage.py`, `tests/integration/test_dagster_assets.py`

<output>
Create `.planning/phases/01-foundation-end-to-end-spike/01-06-SUMMARY.md` when done.
</output>
