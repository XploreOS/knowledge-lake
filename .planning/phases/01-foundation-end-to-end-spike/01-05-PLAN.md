---
phase: 01-foundation-end-to-end-spike
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - src/knowledge_lake/pipeline/__init__.py
  - src/knowledge_lake/pipeline/ingest.py
  - src/knowledge_lake/pipeline/parse.py
  - src/knowledge_lake/pipeline/chunk.py
  - src/knowledge_lake/pipeline/embed.py
  - src/knowledge_lake/pipeline/index.py
  - src/knowledge_lake/pipeline/search.py
  - src/knowledge_lake/pipeline/run.py
  - src/knowledge_lake/lineage.py
  - src/knowledge_lake/cli/app.py
  - Makefile
  - tests/fixtures/hhs_security_rule.pdf
  - tests/fixtures/__init__.py
  - tests/integration/test_lineage.py
  - tests/integration/test_demo_spike.py
autonomous: true
requirements: [FOUND-07]
must_haves:
  truths:
    - "One real document flows ingest → parse → chunk → embed → index → search in-process and returns chunks for the fixed demo query"
    - "Each returned chunk carries a score and citation fields (document, section path, page)"
    - "`klake lineage <chunk-id>` prints a human-readable ancestry tree chunk → parsed doc → raw doc → source, each node showing id, type, content hash, timestamp, pipeline version, storage uri; `--json` returns the machine graph"
    - "`klake demo` runs the whole path end-to-end and prints cited results plus the resolved lineage tree"
    - "Lineage resolves via a recursive CTE over parent_artifact_id"
  artifacts:
    - "src/knowledge_lake/pipeline/*.py plain-function stages and run.py orchestrating them in-process"
    - "src/knowledge_lake/lineage.py recursive-CTE ancestry resolver"
    - "klake CLI commands ingest-url, search, lineage, demo"
    - "tests/integration/test_lineage.py and tests/integration/test_demo_spike.py (the smoke test)"
    - "a committed cached spike PDF fixture for hermetic smoke testing"
  key_links:
    - "each stage writes a registry artifact node with parent_artifact_id linking it to the prior stage — the lineage chain"
    - "chunk embedding dim must match the Qdrant collection created by the vector store plugin"
    - "the Qdrant point payload carries the citation fields the search result and CLI render"
  prohibitions:
    - "No Dagster asset graph yet — pipeline is plain functions called in-process by the CLI (D-01/D-02, Pitfall 1); assets are wrapped in 01-06"
    - "ingest-url must reject non-https schemes (SSRF seam) and cap download size"
    - "No provider model IDs / no direct provider SDK calls — embeddings go through the embedder plugin only"
---

<objective>
Wire the plain-function pipeline that makes the walking skeleton walk: download the spike PDF into the immutable raw zone, parse it with the Docling plugin, chunk it section-aware, embed with the local plugin, index into Qdrant with citation payload, and search — then resolve full lineage via recursive CTE and expose it through the `klake` CLI, capped by the `klake demo` smoke test.

Purpose: Satisfy FOUND-07 (query full lineage via CLI) and prove Phase 1 success criteria 2, 3, 4 end-to-end (the spike). Functions-first, no Dagster yet (D-01/Pitfall 1).
Output: pipeline stages + lineage resolver + CLI commands + a green end-to-end smoke test that prints cited results and a resolved lineage tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-end-to-end-spike/01-CONTEXT.md
@.planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md
@.planning/phases/01-foundation-end-to-end-spike/SKELETON.md
@.planning/phases/01-foundation-end-to-end-spike/01-02-SUMMARY.md
@.planning/phases/01-foundation-end-to-end-spike/01-03-SUMMARY.md
@.planning/phases/01-foundation-end-to-end-spike/01-04-SUMMARY.md
</context>

## Phase Goal

**As an** operator of the Knowledge Lake, **I want to** bring up the full stack with one command and flow one real document from ingest to a cited, lineage-traceable search result, **so that** the tool-agnostic foundation every later phase depends on is proven working end-to-end.

This plan delivers the core of that story: the operator runs `klake demo` and one real document flows the full pipe and comes back cited and lineage-traceable.

<tasks>

<task type="auto">
  <name>Task 1: Failing end-to-end smoke test + cached spike fixture (D-03, D-05)</name>
  <files>tests/fixtures/__init__.py, tests/fixtures/hhs_security_rule.pdf, tests/integration/test_demo_spike.py</files>
  <read_first>.planning/phases/01-foundation-end-to-end-spike/01-CONTEXT.md (D-03, D-05, D-06, D-07), .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Assumption A1; Environment Availability hhs.gov fallback; Validation Architecture test map)</read_first>
  <action>Commit a cached copy of a real HHS/OCR HIPAA guidance PDF (the "Summary of the HIPAA Security Rule" or an equivalent stable hhs.gov public-domain PDF — verify the live URL at build time; if it 404s, choose another HHS/OCR public-domain PDF and record the URL in a fixture manifest) under tests/fixtures/ so the smoke test is hermetic and needs no network egress. Write tests/integration/test_demo_spike.py FIRST, expressing the full acceptance: given the compose stack up, running the demo path over the fixture PDF produces search hits for the fixed query "what are administrative safeguards", each hit exposing a score and citation fields (document, section_path, page), and the lineage of a returned chunk resolves chunk → parsed doc → raw doc → source. This test is expected to FAIL until Tasks 2-3 land — that is the MVP red step.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_demo_spike.py -x -q || echo "expected RED until tasks 2-3"</automated>
  </verify>
  <done>A cached spike PDF fixture is committed; the end-to-end smoke test exists and encodes the full acceptance (cited results + resolved lineage); it fails cleanly (red) pending implementation.</done>
  <acceptance_criteria>
    - Real HHS/OCR public-domain PDF cached under tests/fixtures/ (live URL verified or documented fallback)
    - test_demo_spike.py asserts hits with score + citation fields for the fixed query and a resolved chunk→…→source lineage
    - Test is red before Tasks 2-3, by construction
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Plain-function pipeline stages, wired in-process (spike core)</name>
  <files>src/knowledge_lake/pipeline/__init__.py, src/knowledge_lake/pipeline/ingest.py, src/knowledge_lake/pipeline/parse.py, src/knowledge_lake/pipeline/chunk.py, src/knowledge_lake/pipeline/embed.py, src/knowledge_lake/pipeline/index.py, src/knowledge_lake/pipeline/search.py, src/knowledge_lake/pipeline/run.py</files>
  <read_first>src/knowledge_lake/storage/s3.py, src/knowledge_lake/registry/repo.py, src/knowledge_lake/plugins/resolver.py, src/knowledge_lake/plugins/protocols.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Architecture Patterns: data flow to trace; Pattern 4: Prove-Then-Orchestrate; Pitfall (SSRF) C)</read_first>
  <action>Implement each stage as a plain function that does its work through the plugins + storage + registry and writes a registry artifact node linking to its parent (the lineage chain). ingest.py: `ingest_url(url, source)` fetches via httpx with tenacity retry — validate the URL scheme is https (reject others; SSRF seam per Pitfall C) and cap the download size — then calls storage.put_raw (SHA256, content-addressed, no-op on repeat) returning the raw_document artifact; also expose `ingest_file(path, source)` for the hermetic test fixture. parse.py: `parse(raw_artifact)` loads bytes, resolves the parser plugin, produces a ParsedDoc, stores the parsed markdown/JSON to silver, and creates a parsed_document artifact with parent=raw. chunk.py: `chunk(parsed_artifact)` produces section-aware chunks (respect headings; keep tables atomic; carry section_path + page_ref) and creates chunk artifacts with parent=parsed. embed.py: `embed(chunks)` resolves the embedder plugin and returns vectors (dim from plugin). index.py: `index(chunks, vectors)` resolves the vector store, ensures the collection (size=embedder.dim, cosine), and upserts points whose payload carries document/section_path/page/chunk_id. search.py: `search(query, top_k)` embeds the query and returns Hits with score + citation payload. run.py: `run_document(source_or_url)` orchestrates ingest→parse→chunk→embed→index in-process (D-02) and returns identifiers for the demo. No Dagster here.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_demo_spike.py -x -q</automated>
  </verify>
  <done>The full pipeline runs in-process over the fixture PDF and produces indexed, citation-carrying chunks searchable by the fixed query; the smoke test's search assertions pass.</done>
  <acceptance_criteria>
    - ingest_url rejects non-https and caps download size; ingest_file works for the fixture
    - Each stage creates a registry artifact node with parent_artifact_id linking to the prior stage
    - Chunks carry section_path + page_ref; Qdrant payload carries the citation fields
    - search returns hits with score + citation for the fixed demo query
    - No Dagster definitions introduced
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Recursive-CTE lineage + klake CLI (ingest-url, search, lineage, demo) — FOUND-07, D-14</name>
  <files>src/knowledge_lake/lineage.py, src/knowledge_lake/cli/app.py, Makefile, tests/integration/test_lineage.py</files>
  <read_first>src/knowledge_lake/registry/models.py, src/knowledge_lake/registry/repo.py, src/knowledge_lake/pipeline/run.py, src/knowledge_lake/cli/app.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Pattern 3 lineage query; D-14)</read_first>
  <action>Implement lineage.py `resolve_ancestry(artifact_id)` as a recursive CTE walking parent_artifact_id from the given node up to its source, returning ordered nodes each with id, artifact_type, content_hash, created_at, pipeline_version, storage_uri (the six FOUND-06 fields) plus section_path/page for citation. Provide a tree renderer (default) and a JSON serializer (D-14). Expand the Typer CLI (built minimal in 01-01) with: `ingest-url <url>` (calls pipeline.ingest then run), `search "<query>" [--top-k]` (prints hits with score + citation), `lineage <artifact-id> [--json]` (prints the ancestry tree by default, JSON with the flag; accept unambiguous ID prefixes per D-15), and `demo` (D-03: ingest the spike doc — live URL download, falling back to the cached fixture if egress is blocked — run the fixed query, print cited results and the resolved lineage tree; doubles as the later-phase smoke). Point the Makefile `spike` target at `klake demo`. Write tests/integration/test_lineage.py asserting resolve_ancestry returns the full chunk→parsed→raw→source chain with all six fields on each node.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_lineage.py tests/integration/test_demo_spike.py -x -q</automated>
  </verify>
  <done>`klake lineage` prints the ancestry tree (and --json graph); `klake demo` runs the full path printing cited results + resolved lineage; lineage and demo smoke tests green.</done>
  <acceptance_criteria>
    - resolve_ancestry walks parent_artifact_id via recursive CTE to the source, all six FOUND-06 fields per node (FOUND-07)
    - klake lineage renders a human tree by default and JSON with --json; accepts unambiguous ID prefixes
    - klake demo prints cited results and a resolved lineage tree; make spike invokes it
    - test_lineage.py and test_demo_spike.py green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input → ingest-url | An arbitrary URL is fetched — SSRF surface |
| downloaded bytes → parser | Untrusted document bytes reach Docling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Information disclosure / Elevation | klake ingest-url SSRF | high | mitigate | Validate scheme is https, cap download size + timeout; Phase 1 demo uses one fixed public hhs.gov URL; private-IP/allow-list blocking hardens in Phase 2 (INGEST-02) |
| T-01-12 | DoS | oversized/malicious PDF | medium | mitigate | Download size cap on ingest; note Docling memory bounds for Phase 3; Phase 1 doc is trusted |
| T-01-13 | Tampering | lineage query SQL | medium | mitigate | Recursive CTE via SQLAlchemy parameterized query — no string-built SQL |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_lineage.py tests/integration/test_demo_spike.py` green (stack up)
- `klake demo` prints cited results (score + document/section/page) and a resolved lineage tree
- Grep confirms no Dagster asset/def introduced in this plan and no provider model IDs in pipeline/
</verification>

<success_criteria>
- FOUND-07: full lineage of any artifact resolves back to raw source via CLI
- Phase success criteria 2-4 proven: one document round-trips to a cited search result and lineage resolves
</success_criteria>

## Artifacts This Phase Produces (Plan 05)

- Files: `src/knowledge_lake/pipeline/ingest.py`, `parse.py`, `chunk.py`, `embed.py`, `index.py`, `search.py`, `run.py`, `src/knowledge_lake/lineage.py`, `tests/fixtures/hhs_security_rule.pdf`
- Functions: `ingest_url`, `ingest_file`, `parse`, `chunk`, `embed`, `index`, `search`, `run_document`, `resolve_ancestry`
- CLI commands: `klake ingest-url`, `klake search`, `klake lineage`, `klake demo`; Makefile `spike` target
- Test files: `tests/integration/test_lineage.py`, `tests/integration/test_demo_spike.py`

<output>
Create `.planning/phases/01-foundation-end-to-end-spike/01-05-SUMMARY.md` when done.
</output>
