---
phase: 01-foundation-end-to-end-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .env.example
  - Makefile
  - docker-compose.yml
  - infra/litellm/config.yaml
  - infra/postgres/init.sql
  - infra/minio/README.md
  - infra/qdrant/config.yaml
  - infra/dagster/dagster.yaml
  - src/knowledge_lake/__init__.py
  - src/knowledge_lake/config/__init__.py
  - src/knowledge_lake/config/settings.py
  - src/knowledge_lake/cli/__init__.py
  - src/knowledge_lake/cli/app.py
  - src/knowledge_lake/api/__init__.py
  - src/knowledge_lake/api/app.py
  - src/knowledge_lake/dagster_defs/__init__.py
  - src/knowledge_lake/dagster_defs/definitions.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/unit/test_settings.py
  - tests/integration/test_compose_health.py
autonomous: true
requirements: [FOUND-01, FOUND-02]
user_setup:
  - service: docker
    why: "Compose stack (PostgreSQL, MinIO, Qdrant, LiteLLM, Dagster, API) must run locally for FOUND-01"
    env_vars:
      - name: KLAKE_STORAGE__ACCESS_KEY_ID
        source: ".env — MinIO dev credentials (any value; MinIO root user), placeholder in .env.example"
      - name: KLAKE_STORAGE__SECRET_ACCESS_KEY
        source: ".env — MinIO dev credentials (any value; MinIO root password), placeholder in .env.example"

must_haves:
  truths:
    - "Running `docker compose up` brings postgres, minio, qdrant, litellm, dagster (webserver+daemon), and api services to a healthy state"
    - "The api service responds 200 on GET /health once the stack is up"
    - "Settings load from environment/.env through one typed pydantic-settings source with validated defaults"
    - "LiteLLM proxy reports healthy without any AWS/Bedrock credentials present"
    - "`klake --help` runs and exits 0 from the installed console script"
  artifacts:
    - "pyproject.toml with pinned dependencies and [project.scripts] klake entry point"
    - "docker-compose.yml defining all six services with healthchecks"
    - "src/knowledge_lake/config/settings.py exposing a typed Settings model"
    - "tests/conftest.py with shared fixtures; tests/integration/test_compose_health.py"
  key_links:
    - "api service command imports knowledge_lake.api.app:app — must resolve at container start"
    - "pydantic-settings env_prefix KLAKE_ + env_nested_delimiter __ maps env vars onto nested Settings"
    - "compose healthchecks gate readiness so the integration test can poll deterministically"
  prohibitions:
    - "No env var read anywhere except knowledge_lake.config.settings (no os.getenv sprinkles)"
    - "No real secrets committed — .env is gitignored; .env.example holds placeholders only; infra/litellm/config.yaml holds no real keys"
    - "No local filesystem used as an object store — data/ is gitignored dev scratch only"
---

<objective>
Stand up the walking-skeleton foundation: a uv-managed Python package, the typed pydantic-settings config source, the full six-service docker-compose stack that comes up healthy on one command, and the pytest test infrastructure (Wave 0) every later plan writes tests against.

Purpose: Satisfy FOUND-01 (one-command healthy stack) and FOUND-02 (typed config) and establish the test harness so no later task lacks an automated verify.
Output: Installable `knowledge-lake` package with `klake` CLI stub + FastAPI `/health` + minimal Dagster defs, a healthy compose stack, and a green settings unit test plus a compose-health integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-end-to-end-spike/01-CONTEXT.md
@.planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md
@.planning/phases/01-foundation-end-to-end-spike/SKELETON.md
</context>

## Phase Goal

**As an** operator of the Knowledge Lake, **I want to** bring up the full stack with one command and flow one real document from ingest to a cited, lineage-traceable search result, **so that** the tool-agnostic foundation every later phase depends on is proven working end-to-end.

This plan delivers the first operator-visible capability of that story: `docker compose up` yields a healthy stack with validated config loaded.

<tasks>

<task type="auto">
  <name>Task 1: Scaffold uv package and Wave 0 test infrastructure</name>
  <files>pyproject.toml, uv.lock, .gitignore, .env.example, Makefile, src/knowledge_lake/__init__.py, src/knowledge_lake/cli/__init__.py, src/knowledge_lake/cli/app.py, tests/__init__.py, tests/conftest.py</files>
  <read_first>.planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Standard Stack table, Installation subset, Validation Architecture section), .planning/phases/01-foundation-end-to-end-spike/01-CONTEXT.md (D-08, D-09, D-10, D-11)</read_first>
  <action>Initialize the package with `uv init --package --name knowledge-lake` so the import package is `knowledge_lake` and the distribution is `knowledge-lake` (D-09). Remove the pre-existing empty `configs/`, `services/`, and `workspace/` directories; keep `data/` and add it to `.gitignore` as local dev scratch (D-10). Add pinned runtime dependencies via `uv add` at the versions in RESEARCH Standard Stack EXCEPT `uuid-utils` (that install is gated by a legitimacy checkpoint in plan 01-02): pydantic 2.13.4, pydantic-settings 2.14.2, sqlalchemy 2.0.51, alembic 1.18.5, psycopg[binary] 3.3.4, boto3 1.43.39, docling 2.108.0, sentence-transformers 5.6.0, qdrant-client 1.18.0, litellm 1.90.2, dagster 1.13.11, dagster-webserver 1.13.11, fastapi 0.139.0, uvicorn 0.49.0, typer 0.26.8, structlog (>=26,<27), tenacity 9.1.4, httpx 0.28.1, xxhash 3.8.0, orjson 3.11.9. Add dev deps: pytest, pytest-asyncio, pytest-cov, ruff, mypy. Configure `[tool.pytest.ini_options]` (testpaths=tests, asyncio_mode=auto), a `[project.scripts]` entry `klake = "knowledge_lake.cli.app:app"`, and minimal ruff+mypy config in pyproject. Create a minimal real Typer app in cli/app.py exposing a `version` command that prints the package version (no pipeline commands yet — those arrive in 01-05). Create `.env.example` with placeholder KLAKE_ variables (KLAKE_DATABASE_URL, KLAKE_QDRANT_URL, KLAKE_LITELLM_URL, KLAKE_STORAGE__ENDPOINT_URL, KLAKE_STORAGE__BUCKET, KLAKE_STORAGE__ACCESS_KEY_ID, KLAKE_STORAGE__SECRET_ACCESS_KEY, KLAKE_EMBEDDER, KLAKE_PARSER, KLAKE_VECTORSTORE) and NO real secrets. Add a `Makefile` with a `spike` target placeholder that will call `klake demo` (wired fully in 01-05). Create tests/conftest.py with a `settings` fixture that constructs Settings with test overrides and an env-isolation autouse fixture. Configure structlog once at package import.</action>
  <verify>
    <automated>uv run klake version && uv run ruff check src && uv run pytest -q tests/ --co</automated>
  </verify>
  <done>`uv run klake version` prints a version and exits 0; ruff passes on src; pytest collects tests without import errors; configs/, services/, workspace/ are gone and data/ is gitignored.</done>
  <acceptance_criteria>
    - Package installs under uv; `klake` console script resolves to knowledge_lake.cli.app:app
    - All listed runtime deps present in pyproject at pinned versions; uuid-utils intentionally absent
    - pytest configured (asyncio auto), collects with zero errors
    - .env is gitignored; .env.example contains only placeholders
    - configs/, services/, workspace/ removed; data/ present and gitignored
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Typed pydantic-settings config source (FOUND-02)</name>
  <files>src/knowledge_lake/config/__init__.py, src/knowledge_lake/config/settings.py, tests/unit/test_settings.py</files>
  <read_first>src/knowledge_lake/__init__.py, .env.example, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Code Examples: pydantic-settings nested config)</read_first>
  <behavior>
    - Defaults load with no env set: database_url, qdrant_url, litellm_url have sensible dev defaults; embedder defaults to "local", parser to "docling", vectorstore to "qdrant"
    - A nested StorageSettings loads from KLAKE_STORAGE__ENDPOINT_URL / KLAKE_STORAGE__BUCKET etc. via env_nested_delimiter "__"
    - Env var KLAKE_EMBEDDER=litellm overrides the default (env precedence proven)
    - Invalid types (e.g., non-URL where a URL is required, if validated) raise a validation error at construction
  </behavior>
  <action>Implement a `Settings(BaseSettings)` model with `SettingsConfigDict(env_prefix="KLAKE_", env_nested_delimiter="__", env_file=".env", extra="ignore")` and a nested `StorageSettings` (endpoint_url: str | None, bucket, region, access_key_id: str | None, secret_access_key: str | None) mounted via `Field(default_factory=StorageSettings)`. Include top-level fields: database_url (postgresql+psycopg:// default), qdrant_url, litellm_url, and the three plugin swap keys embedder/parser/vectorstore with defaults local/docling/qdrant. Expose a cached `get_settings()` accessor so the whole app reads settings from this one place. Write tests asserting default construction, nested env mapping, env-precedence override, and that this is the only module reading env.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings.py -x -q</automated>
  </verify>
  <done>Settings constructs from defaults and from KLAKE_-prefixed env (including nested storage); env overrides defaults; test file green.</done>
  <acceptance_criteria>
    - Settings loads defaults with empty environment
    - KLAKE_STORAGE__ENDPOINT_URL maps onto Settings.storage.endpoint_url
    - KLAKE_EMBEDDER=litellm overrides the default via env precedence
    - get_settings() returns a validated singleton-style Settings instance
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Six-service compose stack, healthy on one command (FOUND-01)</name>
  <files>docker-compose.yml, infra/litellm/config.yaml, infra/postgres/init.sql, infra/minio/README.md, infra/qdrant/config.yaml, infra/dagster/dagster.yaml, src/knowledge_lake/api/__init__.py, src/knowledge_lake/api/app.py, src/knowledge_lake/dagster_defs/__init__.py, src/knowledge_lake/dagster_defs/definitions.py, tests/integration/test_compose_health.py</files>
  <read_first>src/knowledge_lake/config/settings.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Architecture Patterns: System Architecture Diagram + Open Questions 4 and 5 on api container and dagster DB; Assumptions A5 on LiteLLM boot without creds)</read_first>
  <action>Author docker-compose.yml with six services: postgres (16, with init.sql creating a separate `dagster` database/schema so Dagster's tables never touch the registry schema — Open Question 5), minio (with a bootstrap step or documented one-time bucket creation enabling versioning + object lock — full policy wiring lands in 01-03), qdrant (1.18 image), litellm (proxy pointed at infra/litellm/config.yaml, configured so it boots healthy with NO AWS creds per Assumption A5 — use a minimal/deferred-validation config; alias→Bedrock map is present but not exercised, D-12), dagster (webserver+daemon running knowledge_lake.dagster_defs), and api (uvicorn running knowledge_lake.api.app:app). Give every service a healthcheck (postgres pg_isready, minio /minio/health/live, qdrant /healthz, litellm /health/liveliness, api GET /health, dagster webserver root). Wire service config via environment (KLAKE_* / EnvVar) — no hardcoded URLs (Pitfall 14). Create a minimal real FastAPI app in api/app.py with a GET /health returning {"status":"ok"} and the settings-derived service wiring (endpoints for search/lineage are added in 01-06). Create dagster_defs/definitions.py with a real but minimal `Definitions()` (assets wrapping the pipeline are added in 01-06). Write tests/integration/test_compose_health.py that (given the stack is up, or bringing it up via a compose fixture) polls each service healthcheck/endpoint until healthy within a timeout and asserts all six are healthy and GET /health returns 200; mark it so it is skipped cleanly when Docker is unavailable.</action>
  <verify>
    <automated>docker compose config -q && uv run pytest tests/integration/test_compose_health.py -x -q</automated>
  </verify>
  <done>`docker compose config` validates; with the stack up, all six services report healthy and api /health returns 200; LiteLLM is healthy with no AWS creds present.</done>
  <acceptance_criteria>
    - docker-compose.yml defines postgres, minio, qdrant, litellm, dagster(web+daemon), api each with a healthcheck
    - `docker compose up` reaches all-healthy from a clean state
    - GET /health returns 200 from the api service
    - LiteLLM proxy is healthy with zero AWS/Bedrock credentials (Assumption A5 verified; if it refuses to boot, minimal/dummy-model config is used)
    - Dagster uses a separate database/schema from the registry
    - No hardcoded service URLs — all via env
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env/.env → process | Secrets (MinIO keys, future LiteLLM key) cross here; must stay out of git and logs |
| PyPI → build | Third-party packages enter the supply chain via uv |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information disclosure | .env / infra/litellm/config.yaml secrets | high | mitigate | .env gitignored; .env.example placeholders only; no real keys in committed infra configs; structlog configured to avoid logging secret fields |
| T-01-02 | Config drift / tampering | compose service URLs | medium | mitigate | All service wiring via EnvVar/KLAKE_ settings, no hardcoded URLs (Pitfall 14) |
| T-01-SC | Tampering | uv/PyPI package installs | high | mitigate | uv.lock pins all versions; versions PyPI-verified in RESEARCH Package Legitimacy Audit; the one [ASSUMED] package (uuid-utils) is gated by a blocking-human legitimacy checkpoint in plan 01-02 before install |
</threat_model>

<verification>
- `docker compose config -q` validates the compose file
- Full stack reaches all-healthy on `docker compose up`; `curl -s localhost:<api-port>/health` returns 200
- `uv run pytest tests/unit/test_settings.py tests/integration/test_compose_health.py` green
- Grep confirms env vars are read only inside `knowledge_lake/config/settings.py`
</verification>

<success_criteria>
- FOUND-01: single `docker compose up` yields all six services healthy
- FOUND-02: typed pydantic-settings loads config from env/.env with validated defaults
- Wave 0 test infrastructure exists (pytest + conftest fixtures) for all later plans
</success_criteria>

## Artifacts This Phase Produces (Plan 01)

New symbols/paths created here (exclude from source-grounding drift checks):
- Files: `pyproject.toml`, `uv.lock`, `.env.example`, `Makefile`, `docker-compose.yml`, `infra/litellm/config.yaml`, `infra/postgres/init.sql`, `infra/minio/README.md`, `infra/qdrant/config.yaml`, `infra/dagster/dagster.yaml`
- Package modules: `knowledge_lake`, `knowledge_lake.config`, `knowledge_lake.config.settings`, `knowledge_lake.cli.app`, `knowledge_lake.api.app`, `knowledge_lake.dagster_defs.definitions`
- Classes: `Settings`, `StorageSettings`
- Functions: `get_settings`; Typer `version` command; FastAPI `health` route handler
- CLI: `klake version`
- Test files: `tests/conftest.py`, `tests/unit/test_settings.py`, `tests/integration/test_compose_health.py`

<output>
Create `.planning/phases/01-foundation-end-to-end-spike/01-01-SUMMARY.md` when done.
</output>
