---
phase: 01-foundation-end-to-end-spike
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pyproject.toml
  - src/knowledge_lake/plugins/__init__.py
  - src/knowledge_lake/plugins/protocols.py
  - src/knowledge_lake/plugins/resolver.py
  - src/knowledge_lake/plugins/builtin/__init__.py
  - src/knowledge_lake/plugins/builtin/docling_parser.py
  - src/knowledge_lake/plugins/builtin/st_embedder.py
  - src/knowledge_lake/plugins/builtin/qdrant_store.py
  - tests/unit/test_plugin_resolver.py
  - tests/unit/test_builtin_plugins.py
autonomous: true
requirements: [FOUND-08]
must_haves:
  truths:
    - "Parser, embedder, and vector store are each defined by a typing.Protocol contract"
    - "The resolver returns the implementation named by a config value, over entry points, with no core edits needed to swap"
    - "Setting the embedder config key to a different registered name selects a different implementation"
    - "The default embedder is local sentence-transformers, requiring zero AWS credentials"
  artifacts:
    - "src/knowledge_lake/plugins/protocols.py with ParserPlugin, EmbedderPlugin, VectorStorePlugin"
    - "src/knowledge_lake/plugins/resolver.py resolving by group+name over entry points"
    - "Built-in implementations: docling_parser, st_embedder, qdrant_store registered as entry points in pyproject"
  key_links:
    - "pyproject entry-point groups knowledge_lake.parsers/.embedders/.vectorstores expose the built-ins under stable names"
    - "resolver.resolve(group, name) is the single seam the pipeline (01-05) uses to obtain plugins"
    - "st_embedder.dim must match the Qdrant collection vector size the store creates"
  prohibitions:
    - "No hardcoded provider model IDs anywhere — the litellm embedder path routes through the LiteLLM gateway using task aliases only"
    - "No direct provider SDK calls in business logic (LiteLLM-only constraint)"
    - "No pluggy dependency in Phase 1 — a plain config-keyed resolver only (pluggy is the Phase 3 fallback-chain evolution)"
---

<objective>
Establish the tool-agnostic seam: Protocol contracts for parser, embedder, and vector store, a config-keyed resolver over entry points, and in-package built-in implementations (Docling, local sentence-transformers, Qdrant) registered exactly as third-party plugins would be.

Purpose: Satisfy FOUND-08 — swap any of the three by changing a settings value, no core code change (D-11). This is the plug the pipeline (01-05) drives.
Output: Three Protocols, a resolver, three built-in plugins, and unit tests proving config-driven swap and the default local-embedder path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-end-to-end-spike/01-CONTEXT.md
@.planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md
@.planning/phases/01-foundation-end-to-end-spike/SKELETON.md
@.planning/phases/01-foundation-end-to-end-spike/01-01-SUMMARY.md
</context>

## Phase Goal

**As an** operator of the Knowledge Lake, **I want to** bring up the full stack with one command and flow one real document from ingest to a cited, lineage-traceable search result, **so that** the tool-agnostic foundation every later phase depends on is proven working end-to-end.

This plan delivers the operator-visible capability: the operator can swap the parser/embedder/vector store by editing one config value.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plugin Protocols + config-keyed resolver (FOUND-08)</name>
  <files>src/knowledge_lake/plugins/__init__.py, src/knowledge_lake/plugins/protocols.py, src/knowledge_lake/plugins/resolver.py, tests/unit/test_plugin_resolver.py</files>
  <read_first>src/knowledge_lake/config/settings.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Pattern 2: Plugin Protocols + Config-Driven Resolution)</read_first>
  <behavior>
    - resolve("knowledge_lake.embedders", "local") returns an object satisfying EmbedderPlugin
    - resolve for a name not registered in the group raises a clear LookupError naming the group and name
    - A dummy embedder registered in a group is returned when its name is requested — proving swap-by-name without editing resolver code
    - EmbedderPlugin/ParserPlugin/VectorStorePlugin are runtime_checkable and match the built-ins structurally
  </behavior>
  <action>Define runtime_checkable Protocols in protocols.py: EmbedderPlugin (name: str, dim: int, embed(texts: list[str]) -> list[list[float]]), ParserPlugin (can_parse(mime_type) -> bool, parse(raw: bytes, mime_type: str) -> ParsedDoc where ParsedDoc is a small dataclass carrying markdown/text plus per-section heading + page_ref metadata), and VectorStorePlugin (ensure_collection(name, dim, distance="Cosine"), upsert(collection, points), search(collection, query, top_k) -> list[Hit]; define VectorPoint and Hit dataclasses with citation payload fields document/section_path/page/chunk_id). Implement resolver.py `resolve(group, name)` iterating importlib.metadata.entry_points(group=group), matching name, loading and instantiating; raise LookupError otherwise. Add a convenience `get_parser(settings)/get_embedder(settings)/get_vectorstore(settings)` reading the swap keys from Settings. Write test_plugin_resolver.py using an in-test registered dummy plugin (or monkeypatched entry points) to prove swap-by-config and the not-found error.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_plugin_resolver.py -x -q</automated>
  </verify>
  <done>Protocols defined and runtime_checkable; resolver returns the named implementation or raises a clear LookupError; swap-by-name proven; test green.</done>
  <acceptance_criteria>
    - Three Protocols exist with the documented method signatures
    - resolve(group, name) loads over entry points and raises a clear error when absent
    - get_embedder(settings) selects the implementation named by Settings.embedder
    - Swapping the config name changes the returned implementation with no resolver edits
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Built-in Docling parser, local embedder, Qdrant store (D-11, D-13)</name>
  <files>pyproject.toml, src/knowledge_lake/plugins/builtin/__init__.py, src/knowledge_lake/plugins/builtin/docling_parser.py, src/knowledge_lake/plugins/builtin/st_embedder.py, src/knowledge_lake/plugins/builtin/qdrant_store.py, tests/unit/test_builtin_plugins.py</files>
  <read_first>src/knowledge_lake/plugins/protocols.py, src/knowledge_lake/config/settings.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Code Examples: Qdrant collection + citation payload; Don't Hand-Roll table)</read_first>
  <action>Implement three built-ins satisfying the Protocols. docling_parser.py: wraps Docling 2.108 to parse PDF bytes into a ParsedDoc preserving headings, section paths, and page references for citations (D-07); can_parse returns True for application/pdf in Phase 1. st_embedder.py: default local sentence-transformers embedder (384-dim MiniLM/bge-small — pick a 384-dim model; expose name and dim) requiring zero AWS creds (D-13), plus a sibling `litellm` embedder implementation that routes through the LiteLLM gateway using the embedding_model task alias (never a hardcoded provider model ID) so KLAKE_EMBEDDER=litellm is a pure config switch (ENRICH-06 seam). qdrant_store.py: wraps qdrant-client 1.18 — ensure_collection (cosine, size=dim), upsert points whose payload carries document/section_path/page/chunk_id, and search returning Hits with score + payload. Register all built-ins in pyproject under entry-point groups knowledge_lake.parsers (docling), knowledge_lake.embedders (local, litellm), knowledge_lake.vectorstores (qdrant). Write test_builtin_plugins.py: unit-test the local embedder end-to-end (embed short strings, assert dim and vector shape) and structurally assert each built-in satisfies its Protocol; Docling and Qdrant network/model-heavy paths may be lightly exercised or marked integration where a live service is needed.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_builtin_plugins.py -x -q</automated>
  </verify>
  <done>Docling parser, local ST embedder (+ litellm switch), and Qdrant store implement their Protocols and are registered as entry points; local embedder produces dim-correct vectors with zero AWS creds; test green.</done>
  <acceptance_criteria>
    - Each built-in structurally satisfies its Protocol
    - Local sentence-transformers embedder is the default and needs no AWS credentials
    - The litellm embedder routes through the gateway via the embedding_model alias — no provider model IDs in code
    - Built-ins are registered under the three entry-point groups in pyproject
    - Qdrant point payload carries document/section_path/page/chunk_id citation fields
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config value → plugin load | An entry-point name from settings selects code to import/instantiate |
| app → LiteLLM gateway | The litellm embedder path crosses to the model gateway |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering / Elevation | plugin resolver loads by name | low | accept | Names resolve only against entry points declared in this project's own pyproject in Phase 1 (no third-party plugin packages installed); revisit when external plugin packages are allowed |
| T-01-06 | Information disclosure | litellm embedder alias mapping | medium | mitigate | Task aliases only (embedding_model); provider IDs and creds live in infra/litellm/config.yaml (uncommitted), never in code |
| T-01-07 | DoS | Docling parsing oversized/malicious PDF | medium | mitigate | Phase 1 uses one trusted HHS PDF; download size cap is set on the ingest path in 01-05; note Docling memory bounds for Phase 3 |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_plugin_resolver.py tests/unit/test_builtin_plugins.py` green
- Grep confirms no hardcoded provider model IDs in plugins/ and no pluggy import
- pyproject lists the three entry-point groups with the built-in names
</verification>

<success_criteria>
- FOUND-08: parser, embedder, and vector store are pluggable behind Protocols and swappable via a settings value with no core edits
</success_criteria>

## Artifacts This Phase Produces (Plan 04)

- Files: `src/knowledge_lake/plugins/protocols.py`, `resolver.py`, `builtin/docling_parser.py`, `builtin/st_embedder.py`, `builtin/qdrant_store.py`
- Protocols: `ParserPlugin`, `EmbedderPlugin`, `VectorStorePlugin`
- Dataclasses: `ParsedDoc`, `VectorPoint`, `Hit`
- Functions: `resolve`, `get_parser`, `get_embedder`, `get_vectorstore`
- Built-in classes: `DoclingParser`, `SentenceTransformerEmbedder`, `LiteLLMEmbedder`, `QdrantVectorStore`
- Entry-point groups: `knowledge_lake.parsers`, `knowledge_lake.embedders`, `knowledge_lake.vectorstores`
- Test files: `tests/unit/test_plugin_resolver.py`, `tests/unit/test_builtin_plugins.py`

<output>
Create `.planning/phases/01-foundation-end-to-end-spike/01-04-SUMMARY.md` when done.
</output>
