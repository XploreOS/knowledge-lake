---
phase: 21-index-time-dedup
plan: 08
type: execute
wave: 5
depends_on: ["21-06", "21-07"]
files_modified:
  - tests/integration/test_dedup_cli_dagster_parity.py
  - tests/integration/test_dedup_reindex_survival.py
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03]

must_haves:
  truths:
    - "Processing identical (post-NFKC-normalized) text through the CLI/API/MCP path (process_crawled, Plan 21-06) and through the Dagster path (dedup_chunks/embed_chunks/index_chunks assets, Plan 21-07) — each into its own collection to avoid cross-run interference — produces the SAME Qdrant point_id for that text in both collections, since point_id_for_text() is a pure function of normalized text alone (D-06), independent of which call site computed it (D-18 parity guarantee)"
    - "In each path, a document containing the SAME boilerplate text as an earlier document in that SAME collection produces exactly one Qdrant point for that text (not two) — real success criterion 1 from ROADMAP.md, verified against the live dev Postgres + Qdrant stack, not mocks"
    - "reindex_collection() (both the default copy_all_points() path and the refresh_payload=True path) preserves a deduplicated point's contributors payload field intact in the new physical collection — the ledger row's point_id continues to resolve correctly after the alias repoints, with zero re-keying (D-08 dual-ID-scheme note from Plan 21-05, now proven, not just documented)"
  artifacts:
    - path: "tests/integration/test_dedup_cli_dagster_parity.py"
      provides: "D-18 parity proof across the CLI and Dagster call sites"
    - path: "tests/integration/test_dedup_reindex_survival.py"
      provides: "Proof that reindex_collection() does not disturb a deduplicated point's contributors or point_id"
  key_links:
    - "This plan is the final integration proof for DEDUP-01/02/03 across both wired call sites (Plans 21-06/21-07) and the pre-existing reindex machinery (pipeline/index.py)"
  prohibitions: []
---

<objective>
Prove, against the live dev Postgres + Qdrant stack (no mocks), that the two wired call sites — CLI/API/MCP (`process_crawled`, Plan 21-06) and Dagster (`dedup_chunks`/`embed_chunks`/`index_chunks` assets, Plan 21-07) — produce identical, deterministic point IDs for identical text (D-18), and that the existing `reindex_collection()` machinery does not disturb a deduplicated point's `contributors` payload or point ID (D-08, RESEARCH.md Open Question 1).

Purpose: Every prior plan in this phase unit-tests its own piece in isolation (mocked vector stores, SQLite ledgers). This is the phase's closing integration proof — the guardrail D-18 explicitly calls "parity is enforced by test, not by shared function," and the reindex-survival check RESEARCH.md's Open Questions section flagged as worth a planner-level test rather than a code change.

Output: Two new integration test files exercising the real dev stack (Postgres `chunk_dedup_ledger` table + live Qdrant), completing this phase's must_haves.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-index-time-dedup/21-CONTEXT.md
@.planning/phases/21-index-time-dedup/21-RESEARCH.md
@.planning/phases/21-index-time-dedup/21-06-SUMMARY.md
@.planning/phases/21-index-time-dedup/21-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-18 CLI/Dagster point-ID and ledger-state parity test</name>
  <files>tests/integration/test_dedup_cli_dagster_parity.py</files>
  <read_first>
    tests/integration/test_dagster_assets.py (lines 160-414 — the `materialize()` call pattern with real `PostgresResource`/`MinIOResource`/`QdrantResource`/`LiteLLMResource`, reading URLs from `get_settings()`; this is the exact pattern to reuse for exercising the Dagster path in this new test file)
    tests/integration/conftest.py (full file — confirms `alembic upgrade head` runs automatically via the session-scoped `_apply_migrations` fixture before any integration test executes, so `chunk_dedup_ledger` already exists when this test runs)
    src/knowledge_lake/pipeline/process.py (as wired by Plan 21-06 — `process_crawled()`'s exact call chain, to exercise the CLI path directly)
    src/knowledge_lake/pipeline/dedup.py (as extended by Plans 21-02/21-04 — `text_sha256_for`/`point_id_for_text`, used by this test to independently compute the EXPECTED point_id for its fixture text, rather than trusting either path's own output as ground truth)
  </read_first>
  <action>
    Create `tests/integration/test_dedup_cli_dagster_parity.py` with `pytestmark = pytest.mark.integration` (matching `test_qdrant_alias_reindex.py`'s convention). Use two distinct Qdrant collection aliases (e.g. `f"test_dedup_cli_{uuid4().hex[:8]}"` and `f"test_dedup_dagster_{uuid4().hex[:8]}"`) so the CLI-path run and the Dagster-path run never interfere with each other's ledger rows (D-12 scopes the ledger by collection). Define one fixed piece of boilerplate text (e.g. a realistic cookie-consent or page-footer string, at least 30 characters, distinct enough not to collide with any other fixture in the suite) used identically by BOTH paths' seed documents.

    CLI path: seed two `raw_document` artifacts (mirroring `test_process_crawled_clean.py`'s `_seed_raw_document` helper, or an equivalent local helper) whose parsed/cleaned/chunked text both include the fixed boilerplate string as one of their chunks (the simplest reliable way to get this deterministically is to call `pipeline.chunk.chunk()` directly on two small synthetic `ParsedDoc` fixtures — one section each, containing the shared boilerplate text — rather than depending on a full parse() of a real file, since this test's focus is the DEDUP layer, not parsing). Run `process_crawled(source_id=<source_1>, collection=<cli_collection>)` — or, if driving the full `process_crawled()` raw-document-discovery path is awkward for synthetic fixtures, call the equivalent `dedup_chunks() -> embed() -> index()` chain directly with the SAME call shape `process_crawled()` uses (matching Plan 21-06's wiring exactly) for each of the two documents in sequence within the SAME collection.

    Dagster path: use `dagster.materialize()` with real `PostgresResource`/`QdrantResource` (mirroring `test_dagster_assets.py`'s exact resource-construction pattern, reading `get_settings()` for URLs) over `[dedup_chunks, embed_chunks, index_chunks]` (or the fuller `ingest_raw_document, parsed_document, clean_document, chunk_document, dedup_chunks, embed_chunks, index_chunks` chain if a synthetic `chunk_document`-shaped input dict is simpler to construct directly than materializing from raw ingest — either is acceptable; prefer whichever needs less fixture-authoring code while still exercising the REAL `dedup_chunks`/`embed_chunks`/`index_chunks` assets, not mocks of them), run config targeting `<dagster_collection>`, twice (two separate materializations, simulating two separate documents) with the SAME shared boilerplate text.

    Assertions: (1) `point_id_for_text(<shared boilerplate text, after applying the SAME processing the pipeline would have applied before chunking — e.g. if the fixture text is used verbatim as chunk text, this is just point_id_for_text(text) directly>)` computed independently by the test equals the actual point_id present in BOTH collections' Qdrant points for that text (via a `scroll`/`search` against each collection, or via reading the `chunk_dedup_ledger` row's `point_id` column directly through `registry_repo.get_dedup_ledger_entry`) — proving determinism holds across both call sites, not just "the two paths agree with each other" (agreeing with each other while both being wrong would pass a weaker test). (2) Each collection has exactly ONE point for the shared text (query the ledger: `contributor_count == 2` for that collection's row, proving the SECOND document's chunk was recognized as a duplicate on both paths). (3) The `text_sha256` column value in the CLI-path's ledger row equals the `text_sha256` column value in the Dagster-path's ledger row (same text, same hash, regardless of collection) even though the two rows' `point_id`/`collection`/primary_* fields differ by ROW (different collections, D-12) — confirming the underlying hash/normalization logic is identical across both invocation paths.

    Clean up both test collections' physical Qdrant collections in a fixture teardown (mirroring `test_qdrant_alias_reindex.py`'s `alias` fixture's best-effort cleanup pattern).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_dedup_cli_dagster_parity.py -v -m integration` passes against the live dev stack
    - The test independently computes the expected point_id via `point_id_for_text()` rather than only comparing the two paths' outputs to each other
    - Both the CLI-path and Dagster-path collections show `contributor_count == 2` for the shared boilerplate text's ledger row after both documents are processed
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_dedup_cli_dagster_parity.py -v -m integration</automated>
  </verify>
  <done>Identical text processed via the CLI path and the Dagster path produces the same deterministic point_id and equivalent ledger state (contributor_count==2, matching text_sha256) in each path's own collection.</done>
</task>

<task type="auto">
  <name>Task 2: Reindex-survives-dedup integration test</name>
  <files>tests/integration/test_dedup_reindex_survival.py</files>
  <read_first>
    tests/integration/test_qdrant_alias_reindex.py (full file — the `store`/`alias` fixture pattern with best-effort teardown cleanup, and the `ensure_aliased_collection` -> `upsert` -> `reindex(upsert_fn=copy_all_points)` -> `search()` sequence to mirror)
    src/knowledge_lake/pipeline/index.py (as extended by Plan 21-05 — `reindex_collection()`'s exact signature and its new D-08 docstring note)
  </read_first>
  <action>
    Create `tests/integration/test_dedup_reindex_survival.py` with `pytestmark = pytest.mark.integration`. Use a fresh test collection alias (mirroring `test_qdrant_alias_reindex.py`'s `alias` fixture, with the same best-effort physical-collection cleanup in teardown). Seed two documents' worth of chunk dicts with SAME boilerplate text (via direct `dedup_chunks()` + `embed()` + `index()` calls, matching Plan 21-06's exact call shape, against the fresh test collection) so the collection ends up with one deduplicated point whose ledger row has `contributor_count == 2` and whose Qdrant payload carries a `contributors` list of length 2.

    Call `reindex_collection(collection=<test_alias>, hybrid=False, refresh_payload=False)` (the default copy-verbatim path) — capture the returned `new_physical`. Assert: the ledger row for this text's `(collection, text_sha256)` is UNCHANGED (same `point_id`, same `contributors`, same `contributor_count` — reindex never touches Postgres, only Qdrant); a `search()`/scroll against the NEW physical collection (or through the alias, now repointed) for this point's ID returns the SAME `contributors`/`contributor_count` payload the pre-reindex point had — proving `copy_all_points()` preserved the mirrored payload byte-for-byte, with zero re-keying (D-08).

    Additionally (a second scenario in the same test class or a second test method), repeat with `refresh_payload=True` instead of the default `hybrid=False, refresh_payload=False` — assert the `contributors`/`contributor_count` fields SURVIVE `refresh_all_points_payload()`'s payload-re-derivation path too (this path re-derives `domain`/`document_type`/`keywords`/`quality_score`/source-metadata fields from the registry via `_resolve_document_payload_fields`, but must NOT drop the `contributors`/`contributor_count` fields that are not part of that helper's output — verify this explicitly, since `_build_payload_refresh_fn`'s `_resolve()` function does `new_payload = dict(old_payload); new_payload.update(cache[parsed_artifact_id])` per `index.py`'s existing implementation, which preserves any payload key not present in the update dict — this test proves that existing merge behavior does in fact keep `contributors`/`contributor_count` intact, it does not require a code change).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_dedup_reindex_survival.py -v -m integration` passes against the live dev stack
    - The ledger row (Postgres) is byte-for-byte unchanged by either reindex mode (reindex never writes to `chunk_dedup_ledger`)
    - The deduplicated point's `contributors`/`contributor_count` payload fields survive BOTH `refresh_payload=False` (verbatim copy) and `refresh_payload=True` (payload re-derivation) reindex modes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_dedup_reindex_survival.py -v -m integration</automated>
  </verify>
  <done>reindex_collection() (both its default and refresh_payload=True modes) preserves a deduplicated point's contributors payload and the ledger's point_id reference with zero re-keying, proving D-08's accepted dual-ID-scheme design does not silently corrupt dedup state across a reindex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Integration test suite -> live dev Postgres + Qdrant | Test-only; exercises the real stack this phase's other plans built against mocks/SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-21-15 | Repudiation | D-18's "parity enforced by test, not shared function" guarantee silently regressing if either call site's wiring drifts | high | mitigate | This plan's Task 1 is exactly that test — it independently recomputes the expected point_id rather than only cross-checking the two paths against each other, so a bug shared by both paths (not just a divergence between them) is also caught. |
| T-21-16 | Denial of Service | reindex_collection() silently dropping contributors/contributor_count during a payload refresh | medium | mitigate | Task 2 explicitly asserts these two fields survive both `refresh_payload=False` and `refresh_payload=True` reindex modes against the live stack, not just unit-level dict-merge reasoning. |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this plan (RESEARCH.md Package Legitimacy Audit: not applicable). |

</threat_model>

## Artifacts This Phase Produces (this plan)

- `tests/integration/test_dedup_cli_dagster_parity.py` — new integration test file
- `tests/integration/test_dedup_reindex_survival.py` — new integration test file

<verification>
Run `uv run pytest tests/integration/test_dedup_cli_dagster_parity.py tests/integration/test_dedup_reindex_survival.py -v -m integration` against the live dev stack — all green. Run the full suite (`uv run pytest`) to confirm no regression across the project (971+ baseline plus this phase's new tests).
</verification>

<success_criteria>
DEDUP-01/02/03 are proven end-to-end against the real Postgres + Qdrant dev stack: identical text dedupes to one point regardless of which of the two wired call sites processed it, point IDs are deterministic across both paths, and reindexing does not disturb deduplicated points' contributor state.
</success_criteria>

<output>
Create `.planning/phases/21-index-time-dedup/21-08-SUMMARY.md` when done
</output>
</content>
