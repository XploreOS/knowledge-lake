---
phase: 09-storage-segmentation
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_parse_silver_key.py
  - tests/unit/test_clean_silver_key.py
  - tests/unit/test_export.py
autonomous: true
requirements:
  - STORE-01
  - STORE-03
must_haves:
  truths:
    - "test_parse_silver_key.py exists with TestParseSilverKeyDomain (2 methods) all xfail(strict=False)"
    - "test_clean_silver_key.py exists with TestCleanSilverKeyDomain (2 methods) all xfail(strict=False)"
    - "test_export.py contains TestGoldZoneDomainKey, TestGoldZoneUnclassified, TestGoldZonePretrain, TestGoldZoneFinetune — 5 new xfail methods total"
    - "pytest --collect-only on all three files exits 0 with no collection errors"
    - "Full unit suite (tests/unit/) passes with 0 failures after this plan"
  artifacts:
    - tests/unit/test_parse_silver_key.py
    - tests/unit/test_clean_silver_key.py
  key_links:
    - "test_parse_silver_key.py and test_clean_silver_key.py use the _patch_engine autouse pattern from test_export.py to route get_session() to the in-memory SQLite engine"
    - "TestGoldZone* classes in test_export.py patch knowledge_lake.pipeline.export._make_storage and assert the Key arg on put_object contains the domain segment"
    - "All new test classes in test_export.py are additive — no existing classes modified"
---

<objective>
Wave 0 test scaffold — pipeline stage (STORE-01 silver keys, STORE-03 gold keys). Creates RED-state test files for the silver-zone domain-scoping changes in parse.py/clean.py and the gold-zone domain-scoping changes in export.py. Plan 04 and Plan 06 make these tests green.

Purpose: Verify that domain segments appear correctly in silver and gold S3 keys before modifying parse.py, clean.py, and export.py.

Output: 2 new test files + 4 new test classes in test_export.py. All new tests are xfail(strict=False).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-storage-segmentation/09-CONTEXT.md
@.planning/phases/09-storage-segmentation/09-RESEARCH.md
@.planning/phases/09-storage-segmentation/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/unit/test_parse_silver_key.py — STORE-01 silver key domain scope in parse stage RED tests</name>
  <files>tests/unit/test_parse_silver_key.py</files>
  <read_first>
    - tests/unit/test_export.py (lines 1-60) — copy the engine/session/_patch_engine/source fixtures and the _make_storage patch pattern used to intercept storage.put_object calls
    - src/knowledge_lake/pipeline/parse.py (lines 1-130) — current parse() function signature, silver_key construction at line 100, session block starting at line 113, put_object call at line 129
    - .planning/phases/09-storage-segmentation/09-PATTERNS.md (section "parse.py") — exact expected key format silver/{domain}/{source_id}/{hash}.md
  </read_first>
  <action>
Create tests/unit/test_parse_silver_key.py.

Module docstring: "RED-state tests for domain-scoped silver key construction in pipeline/parse.py (STORE-01). Tests are xfail(strict=False) until Plan 09-04 moves silver_key inside the session block and adds domain resolution."

Imports: MagicMock, patch from unittest.mock; pytest; create_engine, Session from sqlalchemy; StaticPool from sqlalchemy.pool; knowledge_lake.registry.db as registry_db.

Fixtures: copy the engine, _patch_engine (autouse=True monkeypatch), session, and source fixtures from test_export.py lines 33-70. The source fixture should create a source with config={"domain": "healthcare"} so get_domain_for_source returns "healthcare" during parse(). Also add a parsed_artifact_for_parse fixture that creates a raw artifact and a parsed_document artifact seeded in the session for the parse() input — or simply let the tests call parse() with a mock for the docling parser.

The cleanest approach: patch the docling-based parser to avoid heavy ML imports. Patch knowledge_lake.pipeline.parse._make_parser (or whatever the parser factory is) to return a MagicMock that returns a fake parsed result. Also patch knowledge_lake.pipeline.parse._make_storage so put_object is captured without a real S3 client.

Class TestParseSilverKeyDomain: two methods, each decorated with @pytest.mark.xfail(strict=False, reason="STORE-01: silver key domain segment pending Plan 09-04").
- test_domain_segment_in_silver_key: patch _make_storage returning a mock; call parse(artifact_id=raw_artifact.id) where raw_artifact is seeded in the session with the source that has domain="healthcare"; assert the first positional argument to mock_storage.put_object contains "silver/healthcare/" as a key prefix.
- test_none_domain_uses_unclassified_segment: use a source with no domain in config (config={}); assert the put_object key contains "silver/_unclassified/".

If the parse() function signature or internal parser mocking is complex to figure out from reading the file, add a comment in the test that explains which patch targets need to be confirmed when implementing. The critical assertion is the S3 key passed to put_object.

Do not test the full parse pipeline — only the key format. Use the simplest possible mock setup that captures the key argument.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_parse_silver_key.py --collect-only -q 2>&1 | head -20 && python -m pytest tests/unit/test_parse_silver_key.py -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_parse_silver_key.py exists
    - pytest --collect-only exits 0 (no collection errors)
    - grep -c "class TestParseSilverKeyDomain" tests/unit/test_parse_silver_key.py == 1
    - Both test methods show as XFAIL in pytest -v output (not ERROR)
  </acceptance_criteria>
  <done>test_parse_silver_key.py created, 2 xfail tests collected, suite passes</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/unit/test_clean_silver_key.py — STORE-01 silver key domain scope in clean stage RED tests</name>
  <files>tests/unit/test_clean_silver_key.py</files>
  <read_first>
    - tests/unit/test_parse_silver_key.py (just created in Task 1) — mirror the same fixture and patch pattern
    - src/knowledge_lake/pipeline/clean.py (lines 280-330) — current cleaned_key construction at line 300, session block at line 301, put_object at line 321
    - .planning/phases/09-storage-segmentation/09-PATTERNS.md (section "clean.py") — expected key format silver/{domain}/{source_id}/cleaned/{hash}.md
  </read_first>
  <action>
Create tests/unit/test_clean_silver_key.py. Mirror the structure of test_parse_silver_key.py exactly, but:

1. Module docstring references clean.py and pipeline/clean.py, xfail reason references Plan 09-04.
2. Patch targets reference clean.py module path (knowledge_lake.pipeline.clean._make_storage or equivalent), not parse.py.
3. Key format assertion checks for "silver/{domain}/{source_id}/cleaned/" (note the /cleaned/ sub-path, not just silver/{domain}/{source_id}/).
4. Class name is TestCleanSilverKeyDomain.
5. Methods: test_domain_segment_in_cleaned_key and test_none_domain_uses_unclassified_segment.

The clean stage expects a parsed artifact as input (artifact_type="parsed_document"). Seed the session with a source (config={"domain": "healthcare"} for the positive test, config={} for the unclassified test) and a parsed_document artifact linked to that source.

Patch the text-cleaning logic (the function that produces cleaned text from parsed markdown) to avoid importing heavy ML models. The critical codepath is: clean() call → within session block → cleaned_key construction → put_object(cleaned_key, ...). Only the key argument to put_object needs to be captured and asserted.

All methods xfail(strict=False, reason="STORE-01: cleaned key domain segment pending Plan 09-04").
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_clean_silver_key.py --collect-only -q 2>&1 | head -20 && python -m pytest tests/unit/test_clean_silver_key.py -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_clean_silver_key.py exists
    - pytest --collect-only exits 0 (no collection errors)
    - grep -c "class TestCleanSilverKeyDomain" tests/unit/test_clean_silver_key.py == 1
    - Both test methods show as XFAIL (not ERROR)
  </acceptance_criteria>
  <done>test_clean_silver_key.py created, 2 xfail tests collected, suite passes</done>
</task>

<task type="auto">
  <name>Task 3: Add TestGoldZone* classes to tests/unit/test_export.py — STORE-03 gold key domain scope RED tests</name>
  <files>tests/unit/test_export.py</files>
  <read_first>
    - tests/unit/test_export.py (full file) — existing fixture pattern at lines 33-70, existing _make_storage patch pattern, end of file to find append point
    - src/knowledge_lake/pipeline/export.py (lines 236-240, 323, 343-348, 409, 429-432, 523) — current export function signatures (no domain kwarg) and gold key construction lines
    - .planning/phases/09-storage-segmentation/09-RESEARCH.md (Pitfall 2) — note that domain kwarg does NOT exist yet on export functions; tests must xfail because passing domain= will raise TypeError
  </read_first>
  <action>
Read the full test_export.py file. Find the last test class or function. After it, append four new test classes. Each class has a single method decorated with @pytest.mark.xfail(strict=False, reason="STORE-03: export domain kwarg pending Plan 09-06 (Pitfall 2: domain kwarg not yet on export signatures)").

All four classes reuse the module-level engine, _patch_engine, session, and source fixtures already defined in the file. Do not redefine fixtures.

The test pattern for each class: patch knowledge_lake.pipeline.export._make_storage to return a MagicMock; call the export function with domain="healthcare"; inspect mock_storage_instance.put_object.call_args[0][0] (the key positional arg); assert the expected domain segment appears in the key.

Class TestGoldZoneDomainKey — method test_rag_corpus_key_contains_domain_segment:
- Also seed a minimal rag corpus (at least one chunk + QAPair in the session — look at existing export tests to understand seeding)
- Call export_rag_corpus(domain="healthcare", settings=s) where s is a minimal Settings object
- Assert put_object key contains "gold/healthcare/rag_corpus/"

Class TestGoldZoneUnclassified — method test_rag_corpus_none_domain_uses_unclassified:
- Same as above but domain=None
- Assert key contains "gold/_unclassified/rag_corpus/"

Class TestGoldZonePretrain — method test_pretrain_key_contains_domain_segment:
- Seed a cleaned document or artifact; call export_pretrain_corpus(domain="healthcare", settings=s)
- Assert key contains "gold/healthcare/pretrain/"

Class TestGoldZoneFinetune — method test_finetune_key_contains_domain_segment:
- Seed a QA dataset; call export_finetune_dataset(dataset_id=..., domain="healthcare", settings=s)
- Assert key contains "gold/healthcare/finetune/"

Import the three export functions at the top of each class or at the top of the new test block: from knowledge_lake.pipeline.export import export_rag_corpus, export_pretrain_corpus, export_finetune_dataset.

Important: If the existing test_export.py already imports these functions, do not re-import. Check the existing imports first.

For minimal Settings: look at how existing export tests construct settings (likely using Settings() with defaults or a custom pydantic model). Mirror that pattern.

Do not modify any existing test classes or fixtures.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_export.py --collect-only -q 2>&1 | tail -10 && python -m pytest tests/unit/test_export.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "class TestGoldZone" tests/unit/test_export.py == 4
    - pytest --collect-only on test_export.py exits 0 (no collection errors)
    - The 4 new methods all show as XFAIL in pytest -v output
    - All previously passing tests in test_export.py remain passing
  </acceptance_criteria>
  <done>4 TestGoldZone* classes added to test_export.py, all xfail, existing tests unaffected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production imports | Tests import export functions and pipeline storage helpers; xfail guards prevent suite breakage if signatures change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-SC | Tampering | test xfail scaffolding | low | accept | xfail(strict=False) is correct for Wave 0 scaffold; strict=False means an unexpected pass is reported as XPASS, not silently ignored |
</threat_model>

<verification>
After all three tasks complete:

```
python -m pytest tests/unit/ -v -x 2>&1 | tail -20
```

Expected: 0 FAILED. New tests from test_parse_silver_key.py, test_clean_silver_key.py, and test_export.py (gold zone classes) appear as XFAIL.
</verification>

<success_criteria>
- test_parse_silver_key.py: 2 xfail tests (TestParseSilverKeyDomain)
- test_clean_silver_key.py: 2 xfail tests (TestCleanSilverKeyDomain)
- test_export.py: 4 new xfail tests (TestGoldZoneDomainKey, TestGoldZoneUnclassified, TestGoldZonePretrain, TestGoldZoneFinetune)
- Full unit test suite passes with 0 failures
</success_criteria>

<output>
Create .planning/phases/09-storage-segmentation/09-02-SUMMARY.md when done.
</output>

## Artifacts this phase produces

New files:
- `tests/unit/test_parse_silver_key.py` — class: `TestParseSilverKeyDomain`
- `tests/unit/test_clean_silver_key.py` — class: `TestCleanSilverKeyDomain`

Modified files:
- `tests/unit/test_export.py` — new classes: `TestGoldZoneDomainKey`, `TestGoldZoneUnclassified`, `TestGoldZonePretrain`, `TestGoldZoneFinetune`
