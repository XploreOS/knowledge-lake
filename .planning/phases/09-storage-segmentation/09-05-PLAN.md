---
phase: 09-storage-segmentation
plan: "05"
type: execute
wave: 2
depends_on:
  - 09-03
files_modified:
  - src/knowledge_lake/pipeline/ingest.py
  - src/knowledge_lake/pipeline/crawl.py
autonomous: true
requirements:
  - STORE-01
  - STORE-02
must_haves:
  truths:
    - "ingest_url() passes domain= and tags= to storage.put_raw at both call sites (lines 430 and 533 region)"
    - "crawl.py _write_artifacts passes domain= and tags= to both put_raw and put_bronze"
    - "domain is resolved via registry_repo.get_domain_for_source(session, source_id) or '_unclassified' inside the existing with get_session() block — not outside it"
    - "source_name for tags in crawl.py is resolved via registry_repo.get_source(session, source_id).name (ingest.py already has source_name as a function param)"
    - "ingest.py does NOT call get_source for source_name — it uses the source_name parameter already on ingest_url() and ingest_file()"
    - "python -m pytest tests/unit/ -v -x exits 0 after this plan"
  artifacts:
    - src/knowledge_lake/pipeline/ingest.py (updated — 2 put_raw call sites)
    - src/knowledge_lake/pipeline/crawl.py (updated — _write_artifacts domain + source_name resolution + tags on put_raw/put_bronze)
  key_links:
    - "domain resolved inside with get_session() block at each put_raw/put_bronze call site — never outside the session"
    - "source_name in ingest.py: already available as function parameter — no extra registry lookup needed"
    - "source_name in crawl.py: requires registry_repo.get_source(session, source_id).name lookup inside session block (mirrors parse.py/clean.py pattern)"
    - "Existing tests for ingest_url and crawl functions continue to pass (additive domain/tags kwargs have None defaults — backward-compatible)"
---

<objective>
Wave 2 — Raw/bronze zone callers (STORE-01 + STORE-02). Updates ingest.py and crawl.py to resolve domain inside their existing session blocks and pass domain + tags to storage.put_raw and storage.put_bronze. These files run in parallel with Plan 04 (parse.py/clean.py) and Plan 06 (export.py) since they touch different files.

Purpose: Ensures that raw and bronze artifacts created during URL ingestion and web crawling receive domain-scoped S3 keys and object tags.

Output: Updated ingest.py (2 put_raw call sites) and crawl.py (_write_artifacts function). The broader unit suite remains green; no new test files are needed — the storage layer tests from Plan 03 cover the key format contract.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-storage-segmentation/09-CONTEXT.md
@.planning/phases/09-storage-segmentation/09-RESEARCH.md
@.planning/phases/09-storage-segmentation/09-PATTERNS.md
@.planning/phases/09-storage-segmentation/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ingest.py — add domain resolution + pass domain + tags to put_raw at both call sites (STORE-01/02, D-02, D-11)</name>
  <files>src/knowledge_lake/pipeline/ingest.py</files>
  <read_first>
    - src/knowledge_lake/pipeline/ingest.py (full file) — ingest_url() signature (line 337 region: parameters include source_name as a string param), ingest_file() function; identify the two put_raw call sites (lines 430 and 533 region) and the surrounding with get_session() blocks
    - src/knowledge_lake/registry/repo.py (line 822) — get_domain_for_source(session, source_id) signature; session is FIRST argument (Pitfall 1)
    - src/knowledge_lake/storage/s3.py (updated in Plan 03) — new put_raw signature including domain= and tags= kwargs
    - .planning/phases/09-storage-segmentation/09-PATTERNS.md (section "ingest.py") — exact call patterns for both put_raw call sites
  </read_first>
  <action>
Read ingest.py fully before editing. Find both put_raw call sites.

Call site 1 (line 430 region, inside ingest_url's "with get_session() as session:" block):
The current call is: artifact = storage.put_raw(source.id, data, ext, session, mime_type=effective_mime)

Inside the same session block, BEFORE the put_raw call, add:
- domain = registry_repo.get_domain_for_source(session, source.id) or "_unclassified"

Then update the put_raw call to:
artifact = storage.put_raw(source.id, data, ext, session, mime_type=effective_mime, domain=domain, tags={"domain": domain, "source_name": source_name, "format": ext, "artifact_type": "raw_document"})

where source_name is already a parameter of ingest_url() — do not add a registry lookup for it.

Call site 2 (line 533 region, if ingest_file() has a second put_raw call):
Apply the same pattern. The ingest_file() function may also have source_name as a parameter — verify before assuming. If source_name is not a parameter on ingest_file(), add a registry_repo.get_source(session, source.id).name lookup inside the session block (with "unknown" fallback).

Verify that registry_repo is already imported in ingest.py. If not, add: from knowledge_lake.registry import repo as registry_repo.

CRITICAL (Pitfall 1): Always call get_domain_for_source(session, source.id) with session as the FIRST argument.

CRITICAL: All domain resolution calls must be INSIDE the existing with get_session() as session: block — do not add a new session block.

CRITICAL: Do not change the WORM layer logic in storage.put_raw — the callers only pass domain and tags; the WORM contract lives entirely inside s3.py.

Do not change the put_raw call in any code paths for ingest_url's existing tests (additive defaults: domain=None, tags=None keep old callers working).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/ -v -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "get_domain_for_source(session" src/knowledge_lake/pipeline/ingest.py >= 1 (session first arg confirmed)
    - grep -c "domain=domain" src/knowledge_lake/pipeline/ingest.py >= 2 (both put_raw call sites updated)
    - grep -c "artifact_type.*raw_document" src/knowledge_lake/pipeline/ingest.py >= 2 (tags dict present at both sites)
    - python -m pytest tests/unit/ -v -x: 0 failures (existing ingest tests still pass)
  </acceptance_criteria>
  <done>ingest.py updated at both put_raw call sites with domain resolution + tags; unit suite passes</done>
</task>

<task type="auto">
  <name>Task 2: crawl.py — add domain + source_name resolution in _write_artifacts, pass to put_raw and put_bronze (STORE-01/02, D-03, D-11)</name>
  <files>src/knowledge_lake/pipeline/crawl.py</files>
  <read_first>
    - src/knowledge_lake/pipeline/crawl.py (lines 662-700) — _write_artifacts function; current put_raw call (line 679) and put_bronze call (lines 687-692); the with get_session() as session: block that wraps them
    - src/knowledge_lake/registry/repo.py (lines 822-845) — get_domain_for_source(session, source_id) and get_source(session, source_id) signatures
    - src/knowledge_lake/storage/s3.py (updated in Plan 03) — new put_raw and put_bronze signatures
    - .planning/phases/09-storage-segmentation/09-PATTERNS.md (section "crawl.py") — exact new _write_artifacts code with domain + source_name resolution and both call updates
  </read_first>
  <action>
Read crawl.py lines 662-700 (_write_artifacts function) before editing. The function uses "with get_session() as session:" and inside that block calls:
- storage.put_raw(source_id, html, "html", session, mime_type="text/html")
- storage.put_bronze(source_id, md_bytes, "md", session, parent_artifact_id=raw_id)

Make the following targeted edit to _write_artifacts, adding three lines immediately AFTER "with get_session() as session:" opens (before any existing code in the block):
- domain = registry_repo.get_domain_for_source(session, source_id) or "_unclassified"
- source_obj = registry_repo.get_source(session, source_id)
- source_name = source_obj.name if source_obj else "unknown"

Then update the put_raw call to:
raw_artifact = storage.put_raw(source_id, html, "html", session, mime_type="text/html", domain=domain, tags={"domain": domain, "source_name": source_name, "format": "html", "artifact_type": "raw_document"})

Then update the put_bronze call (inside the "if markdown:" branch) to:
bronze_artifact = storage.put_bronze(source_id, md_bytes, "md", session, parent_artifact_id=raw_id, domain=domain, tags={"domain": domain, "source_name": source_name, "format": "md", "artifact_type": "bronze_document"})

CRITICAL: domain and source_name must be resolved inside the with get_session() block before the put_raw call (Pitfall 3 and D-04 session boundary rules).

CRITICAL (Pitfall 1): get_domain_for_source(session, source_id) — session is FIRST argument.

CRITICAL: registry_repo is already imported in crawl.py (it was added in Phase 8 for list_sources_for_crawl_all). Confirm before adding a duplicate import.

CRITICAL: get_source is already in repo.py at line 835 — no new function needed. The import path is the same registry_repo module.

Do not change the crawl logic, retry logic, or any other parts of crawl.py outside _write_artifacts.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/ -v -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "get_domain_for_source(session, source_id)" src/knowledge_lake/pipeline/crawl.py >= 1 (session first confirmed)
    - grep -c "get_source(session, source_id)" src/knowledge_lake/pipeline/crawl.py >= 1 (source_name lookup present)
    - grep -c "artifact_type.*raw_document" src/knowledge_lake/pipeline/crawl.py >= 1 (tags on put_raw)
    - grep -c "artifact_type.*bronze_document" src/knowledge_lake/pipeline/crawl.py >= 1 (tags on put_bronze)
    - python -m pytest tests/unit/ -v -x: 0 failures
  </acceptance_criteria>
  <done>crawl.py _write_artifacts updated: domain + source_name resolved inside session block, put_raw + put_bronze receive domain + tags; unit suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest_url()/ingest_file() → StorageBackend | domain resolved from DB registry inside session; passed as kwarg; storage layer never calls registry |
| _write_artifacts → StorageBackend | domain + source_name resolved from DB inside active session; WORM contract in s3.py unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Denial of Service | tag source_name values in ingest/crawl | medium | mitigate | _format_tags truncation (Plan 03) applies automatically; source_name from registry is typically short |
| T-09-03 | Tampering | domain segment in ingest/crawl raw keys | medium | accept | domain from Source.config (validated at register_source time); "_unclassified" fallback for None |
| T-09-05 | Information Disclosure | source_name fallback "unknown" in crawl.py | low | accept | Only fires if get_source returns None for a registered source_id (registry inconsistency); acceptable degradation |
</threat_model>

<verification>
After both tasks complete:

```
python -m pytest tests/unit/ -v -x 2>&1 | tail -10
```

Expected: 0 failures. All unit tests pass including the storage layer tests from Plan 03.

```
grep -n "get_domain_for_source\|domain=domain\|artifact_type" src/knowledge_lake/pipeline/ingest.py | head -20
grep -n "get_domain_for_source\|domain=domain\|artifact_type" src/knowledge_lake/pipeline/crawl.py | head -20
```

Expected: Both files show domain resolution and tags at each put_raw/put_bronze call site.
</verification>

<success_criteria>
- ingest.py: domain resolved via get_domain_for_source(session, source.id) inside session block at each put_raw call site; tags dict with domain/source_name/format/artifact_type passed to put_raw
- crawl.py _write_artifacts: domain + source_name resolved inside session block; tags on both put_raw (raw_document) and put_bronze (bronze_document)
- All existing unit tests continue to pass (backward-compatible changes — domain/tags default to None)
</success_criteria>

<output>
Create .planning/phases/09-storage-segmentation/09-05-SUMMARY.md when done.
</output>

## Artifacts this phase produces

Modified files:
- `src/knowledge_lake/pipeline/ingest.py` — 2 updated `put_raw` call sites: `domain=domain`, `tags={domain, source_name, format, artifact_type}`
- `src/knowledge_lake/pipeline/crawl.py` — updated `_write_artifacts`: `get_domain_for_source` + `get_source` added; `put_raw` and `put_bronze` updated with `domain=` and `tags=`
