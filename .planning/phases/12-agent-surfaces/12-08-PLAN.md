---
phase: 12-agent-surfaces
plan: 08
type: execute
wave: 6
depends_on: [12-05, 12-07]
files_modified:
  - tests/unit/test_surface_parity.py
  - skills/build-corpus.md
  - skills/search-knowledge.md
  - skills/add-source.md
  - skills/export-dataset.md
  - tests/unit/test_skills_present.py
autonomous: true
requirements: [SKILL-01, SKILL-03]
must_haves:
  truths:
    - "For every tool, normalized MCP list_tools inputSchema == OpenAI parameters == input_model.model_json_schema() (SKILL-03 no-drift, D-04)"
    - "The tool set is identical across stdio and HTTP transports (same Server object) — parity asserts this"
    - "Each tool's shared Pydantic model appears identically (normalized) in docs/openapi.json components where that model backs a FastAPI endpoint"
    - "Four skills exist (build-corpus, search-knowledge, add-source, export-dataset) with name/description frontmatter, each driving an end-to-end journey by MCP tool name (D-16)"
  artifacts:
    - tests/unit/test_surface_parity.py (real assertions)
    - skills/build-corpus.md
    - skills/search-knowledge.md
    - skills/add-source.md
    - skills/export-dataset.md
  key_links:
    - "the normalize() helper (from Plan 01) reconciles Pydantic v2 $defs/$ref/title noise so parity compares structure, not cosmetics (Pitfall 2)"
    - "skills reference tools by the exact registry names so they track the single tool registry"
  prohibitions:
    - "Do NOT compare raw model_json_schema to OpenAPI schema without normalization (false failures on $defs/$ref/title, Pitfall 2)"
    - "Do NOT hardcode tool schemas in the parity test — derive all three surfaces from the live registry/exports"
    - "Do NOT reference a tool name in a skill that is not in the registry (skills must track TOOLS)"
    - "Do NOT add a fifth skill — SKILL-01 scopes exactly four (extras are a Deferred Idea)"
---

<objective>
Prove no drift across surfaces (SKILL-03) and ship the four Claude Code skills that drive the lake by MCP tool name (SKILL-01) — the phase's correctness gate and its user-facing journeys.

Purpose: SKILL-03's guarantee is a test, not a runtime check (D-04): stdio == http == openapi == openai. The four skills are repo-visible Markdown that reference the exact registry tool names so they can never drift from the single source of truth.

Output: `tests/unit/test_surface_parity.py` with real assertions using the `normalize()` helper, four `skills/*.md` with frontmatter + tool-name workflows, and `tests/unit/test_skills_present.py` passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-surfaces/12-RESEARCH.md
@.planning/phases/12-agent-surfaces/12-CONTEXT.md
@src/knowledge_lake/agent/registry.py
@src/knowledge_lake/agent/server.py
@src/knowledge_lake/agent/openai_defs.py
</context>

<artifacts_this_phase_produces>
This plan creates: real `tests/unit/test_surface_parity.py` assertions, `skills/build-corpus.md`, `skills/search-knowledge.md`, `skills/add-source.md`, `skills/export-dataset.md`. (Full phase artifact list is in 12-01-PLAN.md.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface parity test (stdio == http == openapi == openai)</name>
  <files>tests/unit/test_surface_parity.py</files>
  <read_first>
    - tests/unit/test_surface_parity.py (the Plan-01 normalize/canonical helper to reuse)
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Common Pitfalls" Pitfall 2 (normalization spec) and §"Architecture Diagram" (four surfaces, one source)
    - src/knowledge_lake/agent/server.py (build_server list_tools), agent/openai_defs.py (openai_tool_defs), agent/registry.py (TOOLS)
    - docs/openapi.json (committed export from Plan 07)
  </read_first>
  <behavior>
    - For each of the 11 tools, canonical(list_tools inputSchema) == canonical(openai parameters) == canonical(model_json_schema())
    - The tool-name set from a stdio-built server equals the set from an http-built server (same registered_tools output)
    - For each tool whose input_model also backs a FastAPI endpoint, the model's schema appears (normalized) in docs/openapi.json components/schemas
    - The test fails loudly if any surface diverges (drift guard)
  </behavior>
  <action>
    Replace the Plan-01 scaffold with real assertions, reusing the `normalize`/`canonical` helper. Derive the three registry surfaces live: the MCP `inputSchema` for each tool (call the list_tools handler on `build_server(TOOLS)`, or read `t.input_model.model_json_schema()` the same way the server does), the OpenAI `parameters` (from `openai_tool_defs(TOOLS)`), and the shared `model_json_schema()`. Assert `canonical(a) == canonical(b) == canonical(c)` per tool. Assert the stdio-vs-http tool-name equality by comparing `{t.name for t in registered_tools(False)}` used to build each transport's server (same object → same set). For the OpenAPI leg, load `docs/openapi.json`, and for each tool whose input_model is referenced in `components/schemas`, assert its normalized schema matches the registry `model_json_schema()`. Use a consistent `ref_template` so `$ref`/`$defs` normalize identically (align with Plan 07). This is the SKILL-03 / D-04 gate.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_surface_parity.py -x` passes (no longer xfail)
    - the test iterates all 11 tools and asserts canonical equality across MCP inputSchema, OpenAI parameters, and model_json_schema
    - the test asserts the stdio/http tool-name sets are identical
    - deliberately mutating one tool's input_model (local sanity check, not committed) would turn the test red — parity is a real gate
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_surface_parity.py -x</automated>
  </verify>
  <done>The parity test proves stdio == http == openapi == openai for all 11 tools using normalized schemas — no surface drift.</done>
</task>

<task type="auto">
  <name>Task 2: Four Claude Code skills + presence test</name>
  <files>skills/build-corpus.md, skills/search-knowledge.md, skills/add-source.md, skills/export-dataset.md, tests/unit/test_skills_present.py</files>
  <read_first>
    - .planning/phases/12-agent-surfaces/12-CONTEXT.md D-16 (the four journeys by tool name)
    - src/knowledge_lake/agent/registry.py (the exact tool names the skills must reference)
    - tests/unit/test_skills_present.py (Plan-01 scaffold to replace)
  </read_first>
  <action>
    Create four repo-root `skills/*.md` files, each with YAML frontmatter (`name`, `description`) plus a workflow body that drives an end-to-end journey by MCP tool name (D-16): `build-corpus.md` — add_source → crawl / crawl_all → process_crawled → search (verify); `search-knowledge.md` — search with metadata filters (source_name, format, tags, domain) + lineage for provenance; `add-source.md` — add_source (register) and/or ingest_url (one-shot fetch+ingest); `export-dataset.md` — export (rag_corpus / pretrain / finetune) → stats. Reference only tool names present in the registry. Then implement `tests/unit/test_skills_present.py`: assert all four files exist, each parses valid `name`/`description` frontmatter, and each references at least the tool names in its journey — and that every tool name a skill mentions exists in `TOOLS` (skills track the registry). Assert exactly four skill files (no fifth — SKILL-01 scope).
  </action>
  <acceptance_criteria>
    - `ls skills/build-corpus.md skills/search-knowledge.md skills/add-source.md skills/export-dataset.md` all exist
    - each skill file has `name:` and `description:` frontmatter keys
    - every tool name referenced in any skill is a member of `{t.name for t in TOOLS}`
    - `uv run pytest tests/unit/test_skills_present.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_skills_present.py -x</automated>
  </verify>
  <done>Four skills drive real journeys by registry tool name with valid frontmatter; the presence test enforces existence, frontmatter, and registry-tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry schema → agent-facing surfaces | drift between surfaces could mislead agents into malformed/unsafe calls |
| skill doc → agent behavior | skills instruct agents which tools to invoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-DRIFT | Tampering | surface schema drift | medium | mitigate | test_surface_parity.py asserts normalized equality across MCP/OpenAI/OpenAPI + identical stdio/http tool sets, failing on any divergence (D-04) |
| T-12-SKILL | Spoofing | skill references stale tool | low | mitigate | test_skills_present.py asserts every tool name a skill mentions exists in TOOLS, so skills cannot reference a non-existent/renamed tool |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_surface_parity.py tests/unit/test_skills_present.py -x` passes
- full gate: `uv run pytest -q` green (all Wave-0 RED tests now green)
- four skills exist and reference only registry tool names
</verification>

<success_criteria>
The parity gate proves no drift across all four surfaces; four skills drive end-to-end journeys by registry tool name — SKILL-01 and SKILL-03's no-drift guarantee are satisfied and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/12-agent-surfaces/12-08-SUMMARY.md` when done
</output>
