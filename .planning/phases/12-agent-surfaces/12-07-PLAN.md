---
phase: 12-agent-surfaces
plan: 07
type: execute
wave: 5
depends_on: [12-04, 12-05, 12-06]
files_modified:
  - src/knowledge_lake/agent/openai_defs.py
  - src/knowledge_lake/cli/app.py
  - docs/openapi.json
  - docs/openai_tools.json
  - tests/unit/test_openapi_export.py
autonomous: true
requirements: [MCP-02, SKILL-02, SKILL-03]
must_haves:
  truths:
    - "`klake mcp` runs the stdio server (fd-locked) and `klake mcp --sse --port 3001` runs the Streamable-HTTP server, both from registered_tools(settings.mcp.readonly) built into build_server()"
    - "`klake openapi` writes docs/openapi.json as a deterministic json.dumps(app.openapi(), indent=2, sort_keys=True)+newline (re-runs are no-op diffs)"
    - "openai_tool_defs(TOOLS) emits one {type:function, function:{name, description, parameters: input_model.model_json_schema()}} per tool from the SAME schema source as MCP inputSchema"
    - "docs/openai_tools.json is a committed deterministic dump of openai_tool_defs(TOOLS)"
  artifacts:
    - src/knowledge_lake/agent/openai_defs.py (openai_tool_defs)
    - docs/openapi.json
    - docs/openai_tools.json
    - klake mcp / klake openapi CLI verbs
  key_links:
    - "the --sse flag NAME backs Streamable HTTP (build_http_app), not the deprecated HTTP+SSE transport"
    - "openai parameters and MCP inputSchema both derive from input_model.model_json_schema() — one schema source"
  prohibitions:
    - "Do NOT bind the HTTP server anywhere but McpSettings.host (127.0.0.1 default) — the CLI passes settings.mcp.host/port"
    - "Do NOT dump docs/openapi.json without sort_keys=True (noisy git diffs, Pitfall 3)"
    - "Do NOT hand-write OpenAI parameters — generate from the registry's model_json_schema() (D-15)"
    - "Do NOT apply the stdio lockdown in --sse mode (D-08)"
---

<objective>
Expose the surfaces to users and commit the static exports: `klake mcp` (stdio / `--sse` Streamable HTTP), `klake openapi` → committed `docs/openapi.json`, and OpenAI tool-defs generated from the registry → committed `docs/openai_tools.json`.

Purpose: MCP-02 requires the two CLI entrypoints; SKILL-02 requires the committed OpenAPI export; SKILL-03 requires OpenAI-format tool defs from the same Pydantic source. All three are deterministic so re-runs produce no-op diffs.

Output: `agent/openai_defs.py`, `klake mcp` + `klake openapi` verbs in `cli/app.py`, committed `docs/openapi.json` and `docs/openai_tools.json`, and `tests/unit/test_openapi_export.py` passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-surfaces/12-RESEARCH.md
@.planning/phases/12-agent-surfaces/12-PATTERNS.md
@src/knowledge_lake/cli/app.py
@src/knowledge_lake/agent/registry.py
@src/knowledge_lake/agent/stdio.py
@src/knowledge_lake/agent/http.py
</context>

<artifacts_this_phase_produces>
This plan creates: `agent/openai_defs.py:openai_tool_defs()`, CLI verbs `klake mcp` / `klake openapi`, `docs/openapi.json`, `docs/openai_tools.json`. (Full phase artifact list is in 12-01-PLAN.md.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenAI tool-defs generator + committed docs/openai_tools.json</name>
  <files>src/knowledge_lake/agent/openai_defs.py, docs/openai_tools.json</files>
  <read_first>
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Code Examples" (openai_tool_defs snippet) and Pitfall 3 (deterministic committed artifact)
    - src/knowledge_lake/agent/registry.py (TOOLS, ToolDef)
  </read_first>
  <behavior>
    - openai_tool_defs(tools) returns a list of {"type":"function","function":{"name","description","parameters": input_model.model_json_schema()}} — one per tool
    - the parameters block is byte-identical to the tool's MCP inputSchema source (same model_json_schema call)
    - docs/openai_tools.json is a deterministic sort_keys=True dump ending in a trailing newline
  </behavior>
  <action>
    Create `agent/openai_defs.py` with `openai_tool_defs(tools) -> list[dict]` per RESEARCH §Code Examples: one function-tool object per tool using `t.input_model.model_json_schema()` as `parameters` (D-15 — same schema call as the MCP inputSchema so there is one source). Add a small helper (or inline in the Task-2 CLI verb) that writes `docs/openai_tools.json` as `json.dumps(openai_tool_defs(TOOLS), indent=2, sort_keys=True) + "\n"` and commit the file. Use a consistent `model_json_schema(ref_template=...)` if the parity test (Plan 08) requires a canonical `$ref` prefix — align the ref_template here with the one the parity test normalizes.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from knowledge_lake.agent.openai_defs import openai_tool_defs; from knowledge_lake.agent.registry import TOOLS; d=openai_tool_defs(TOOLS); assert len(d)==11 and all(x['type']=='function' and 'parameters' in x['function'] for x in d); print('ok')"` prints ok
    - `docs/openai_tools.json` exists and re-generating it produces an identical file (deterministic)
    - each function.parameters equals the tool's input_model.model_json_schema()
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from knowledge_lake.agent.openai_defs import openai_tool_defs; from knowledge_lake.agent.registry import TOOLS; d=openai_tool_defs(TOOLS); assert len(d)==11 and all(x['function']['parameters']==t.input_model.model_json_schema() for x,t in zip(d,TOOLS)); print('ok')"</automated>
  </verify>
  <done>openai_tool_defs generates OpenAI function-tool defs from the registry's model_json_schema(); docs/openai_tools.json is committed and deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: klake mcp (stdio + --sse) and klake openapi verbs</name>
  <files>src/knowledge_lake/cli/app.py, docs/openapi.json, tests/unit/test_openapi_export.py</files>
  <read_first>
    - src/knowledge_lake/cli/app.py:672-705 (cmd_search option-plumbing style), :971-1031 (cmd_export echo/try-except + typer.Exit(code=1))
    - .planning/phases/12-agent-surfaces/12-PATTERNS.md §"cli/app.py — klake mcp / klake openapi verbs" (verb structure + deterministic dump)
    - src/knowledge_lake/agent/stdio.py (run_stdio), agent/http.py (build_http_app), agent/server.py (build_server), agent/registry.py (registered_tools), config/settings.py (McpSettings)
    - src/knowledge_lake/api/app.py:145 (the FastAPI `app` whose app.openapi() is dumped, D-14)
  </read_first>
  <action>
    Add two Typer verbs to `cli/app.py`. `cmd_mcp` (`@app.command(name="mcp")`) with options `--sse` (bool, default False) and `--port` (int, default from settings.mcp.port=3001): resolve `settings = Settings()`, build `server = build_server(registered_tools(settings.mcp.readonly))`, then if `--sse` run the Streamable-HTTP app via `uvicorn.run(build_http_app(server, host=settings.mcp.host, port=port, token=settings.mcp.token), host=settings.mcp.host, port=port)` (Streamable HTTP — the `--sse` name only; NO stdout lockdown in this mode), else run stdio via `anyio.run(run_stdio, server, init_opts)` (the fd lockdown applies here, D-08). `cmd_openapi` (`@app.command(name="openapi")`): import the FastAPI `app` (api/app.py:145) and write `docs/openapi.json` as `json.dumps(app.openapi(), indent=2, sort_keys=True) + "\n"` (Pitfall 3). Commit `docs/openapi.json`. Then implement `tests/unit/test_openapi_export.py`: assert the committed file equals a fresh deterministic dump (re-export is a no-op diff) and is valid JSON with an `openapi` version key.
  </action>
  <acceptance_criteria>
    - `uv run klake --help` lists `mcp` and `openapi`; `uv run klake mcp --help` shows `--sse` and `--port`
    - `uv run klake openapi` writes docs/openapi.json; running it twice leaves the file byte-identical (`git diff --quiet docs/openapi.json` after a second run)
    - `grep -c 'sort_keys=True' src/knowledge_lake/cli/app.py` >= 1 (deterministic openapi dump)
    - `uv run pytest tests/unit/test_openapi_export.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run klake openapi && uv run pytest tests/unit/test_openapi_export.py -x && uv run klake --help 2>&1 | grep -Eq 'mcp' && uv run klake --help 2>&1 | grep -Eq 'openapi'</automated>
  </verify>
  <done>klake mcp starts stdio (fd-locked) or --sse Streamable HTTP; klake openapi writes a deterministic committed docs/openapi.json; the export test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invocation → server bind | user runs klake mcp; bind host/port come from McpSettings |
| generated artifact → repo | committed docs/openapi.json and docs/openai_tools.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02 | Elevation of Privilege | klake mcp --sse bind | high | mitigate | CLI passes settings.mcp.host (127.0.0.1 default) and settings.mcp.token to build_http_app; never binds 0.0.0.0; readonly posture flows via registered_tools |
| T-12-05 | Tampering | klake mcp stdio | high | mitigate | stdio path runs run_stdio (fd-level lockdown); --sse path intentionally skips lockdown (its stdout logs are fine, D-08) |
| T-12-INFO | Information Disclosure | committed openapi/openai json | low | accept | Exports contain only public schema shapes (no secrets); deterministic dump avoids accidental data churn |
</threat_model>

<verification>
- `uv run klake mcp --help` shows --sse/--port; `uv run klake openapi` is idempotent
- `uv run pytest tests/unit/test_openapi_export.py -x` passes
- `uv run python -c "from knowledge_lake.agent.openai_defs import openai_tool_defs"` imports cleanly
- docs/openapi.json and docs/openai_tools.json committed
</verification>

<success_criteria>
Both CLI entrypoints exist (stdio + Streamable-HTTP), the OpenAPI export is deterministic and committed, and OpenAI tool defs are generated from the registry and committed — MCP-02, SKILL-02, and the SKILL-03 generator are satisfied.
</success_criteria>

<output>
Create `.planning/phases/12-agent-surfaces/12-07-SUMMARY.md` when done
</output>
