---
phase: "13"
plan: "02"
type: execute
wave: 1
depends_on:
  - "13-01"
files_modified:
  - src/knowledge_lake/ids.py
  - src/knowledge_lake/plugins/protocols.py
  - src/knowledge_lake/config/settings.py
autonomous: true
requirements:
  - TREE-01
  - TREE-02
  - TREE-03
  - TREE-04
  - TREE-05

must_haves:
  truths:
    - "new_id('tree_index') returns a string starting with 'idx_' without raising ValueError"
    - "TreeNode and TreeIndex dataclasses are importable from knowledge_lake.plugins.protocols"
    - "IndexerPlugin Protocol is importable from knowledge_lake.plugins.protocols and runtime_checkable"
    - "TreeSettings is importable from knowledge_lake.config.settings with deterministic default mode"
    - "Settings instance has a .tree attribute of type TreeSettings and an .indexer str attribute"
    - "settings.indexer passes _validate_swap_key regex validation (no ValueError on 'pageindex')"
  artifacts:
    - src/knowledge_lake/ids.py
    - src/knowledge_lake/plugins/protocols.py
    - src/knowledge_lake/config/settings.py
  key_links:
    - "ids.py _PREFIX['tree_index'] = 'idx' — mandatory for create_tree_index_artifact (repo.py) to call new_id('tree_index') without ValueError"
    - "protocols.py TreeNode/TreeIndex dataclasses are the schema contract frozen per D-01/D-02 before any builder or plugin implementation"
    - "settings.py indexer field added to _validate_swap_key field_validator list (ASVS V5)"
  prohibitions:
    - "Do NOT add Alembic migration for tree_index — artifact_type is free-form String"
    - "Do NOT hardcode any provider model ID in settings or protocols"
    - "Do NOT add any tree retrieval logic (Phase 14 scope)"
---

<objective>
Enabling edits (Wave 1): Three small, focused changes to existing files that unblock all Wave 2 implementation tasks. Each change is a direct clone/extension of an existing pattern in the same file.

1. ids.py: add "tree_index": "idx" to _PREFIX — mandatory or new_id("tree_index") raises ValueError (per D-02, Critical Finding 3).
2. protocols.py: add TreeNode + TreeIndex dataclasses and IndexerPlugin Protocol — shared schema contract defined BEFORE the builder (per D-01/D-05, Pitfall 4).
3. settings.py: add TreeSettings submodel + wire into Settings + add "indexer" to _validate_swap_key — config surface per D-08, security per D-05/ASVS V5.

Purpose: These are enabling edits with no business logic. All three files have exact analogs for each change. Tests in Plan 13-01 immediately go from ImportError to assertion failures after this plan.

Output: Three modified files. No new files created.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-tree-index-foundation/13-CONTEXT.md
@.planning/phases/13-tree-index-foundation/13-PATTERNS.md
@src/knowledge_lake/ids.py
@src/knowledge_lake/plugins/protocols.py
@src/knowledge_lake/config/settings.py
</context>

<tasks>

<task id="T1" type="auto" tdd="true">
  <name>Task 1: ids.py: add "tree_index" to _PREFIX</name>
  <files>src/knowledge_lake/ids.py</files>
  <read_first>
    - src/knowledge_lake/ids.py — full file; _PREFIX dict is at lines ~32-46; new_id() is at ~58-80; read the exact current last entry to append after it
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §ids.py — exact entry to add
  </read_first>
  <behavior>
    - new_id("tree_index") returns a string matching r"^idx_[0-9a-f-]{36,}$" without raising ValueError
    - new_id("tree_index") distinct on repeated calls (UUIDv7 is unique)
    - All existing new_id() calls for other kinds still return correct prefixes (regression: "chunk" → "chk_", "source" → "src_")
  </behavior>
  <action>
    Read ids.py fully. Find the _PREFIX dict (lines ~32-46). Add one entry after the last existing entry "dataset_example": "dex":

    "tree_index": "idx"

    No other changes to the file. Do not modify new_id() or any docstring. This is a single-line dict addition.

    Per D-02 and ARCHITECTURE.md §2, the prefix for tree_index artifacts is "idx" (short for "index"). This is also consistent with the RESEARCH.md Assumption A4.
  </action>
  <acceptance_criteria>
    - `python -c "from knowledge_lake.ids import new_id; v = new_id('tree_index'); assert v.startswith('idx_'), v"` exits 0
    - `grep -c '"tree_index": "idx"' src/knowledge_lake/ids.py` returns 1
    - `python -c "from knowledge_lake.ids import new_id; new_id('chunk')"` still exits 0 (regression check)
  </acceptance_criteria>
  <verify>
    <automated>cd /root/healthlake && python -c "from knowledge_lake.ids import new_id; v = new_id('tree_index'); assert v.startswith('idx_'), v; print('OK:', v)"</automated>
  </verify>
  <done>"tree_index": "idx" is present in _PREFIX and new_id("tree_index") returns an idx_-prefixed string.</done>
</task>

<task id="T2" type="auto" tdd="true">
  <name>Task 2: protocols.py: add TreeNode, TreeIndex dataclasses and IndexerPlugin Protocol (D-01, D-02, D-05)</name>
  <files>src/knowledge_lake/plugins/protocols.py</files>
  <read_first>
    - src/knowledge_lake/plugins/protocols.py — full file; note the exact last dataclass and Protocol defined (lines ~467-540 area) to append after; note the imports at the top (dataclass, field, Protocol, runtime_checkable, Any)
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §protocols.py — exact field lists for TreeNode/TreeIndex and IndexerPlugin Protocol shape
    - .planning/phases/13-tree-index-foundation/13-CONTEXT.md D-02 — locked field list for both dataclasses
    - .planning/phases/13-tree-index-foundation/13-RESEARCH.md §Finding 1 — Section has NO level and NO page_end; document this in the TreeNode docstring
  </read_first>
  <behavior>
    - TreeNode is a dataclass with fields: node_id:str, title:str, summary:str, page_start:int, page_end:int, level:int, section_path:str, children:list[TreeNode] with field(default_factory=list)
    - TreeIndex is a dataclass with fields: parsed_artifact_id:str, source_id:str, roots:list[TreeNode] with field(default_factory=list), mode:str="deterministic", schema_version:str="1", content_hash:str=""
    - IndexerPlugin is a @runtime_checkable Protocol with class-level name:str and build_index(self, parsed_doc:ParsedDoc, *, mode:str, metadata:dict[str,Any]) -> TreeIndex method
    - isinstance(obj, IndexerPlugin) does not raise for arbitrary objects (runtime_checkable)
    - from knowledge_lake.plugins.protocols import TreeNode, TreeIndex, IndexerPlugin succeeds
  </behavior>
  <action>
    Read protocols.py in full. Append the three new definitions at the END of the file, after the last existing Protocol definition (CrawlerPlugin).

    Do not modify any existing dataclass or Protocol. Do not change imports unless Any or field is not already imported (verify first — they are already used by existing definitions in the file).

    TreeNode dataclass: add a docstring "Tree index node per D-02. level and page_end are DERIVED by the builder — Section has no level or page_end fields (Finding 1 in 13-RESEARCH.md)." Use @dataclass decorator. The children field uses dataclasses.field(default_factory=list). Note: forward reference in children's type annotation requires either postponed evaluation (from __future__ import annotations, already present via the existing dataclasses pattern) or a string annotation "list[TreeNode]".

    TreeIndex dataclass: add a docstring "Tree index artifact wrapper per D-02. mode is 'deterministic' or 'llm'. schema_version anchors TREE-06 migration (deferred to v2.6+)." Use @dataclass decorator. roots field uses field(default_factory=list).

    IndexerPlugin Protocol: add a docstring "Swap-capable tree indexer plugin (D-05, FOUND-08). Swap via settings.indexer entry-point group 'knowledge_lake.indexers'." Use @runtime_checkable decorator and Protocol base. name class attribute is a str. build_index method signature: (self, parsed_doc: ParsedDoc, *, mode: str, metadata: dict[str, Any]) -> TreeIndex. Method body is ... (ellipsis, same as all other Protocols in the file).

    Export all three from the module — if the file has an __all__ list, add "TreeNode", "TreeIndex", "IndexerPlugin" to it.
  </action>
  <acceptance_criteria>
    - `python -c "from knowledge_lake.plugins.protocols import TreeNode, TreeIndex, IndexerPlugin; from dataclasses import fields; assert {f.name for f in fields(TreeNode)} == {'node_id','title','summary','page_start','page_end','level','section_path','children'}"` exits 0
    - `python -c "from knowledge_lake.plugins.protocols import TreeIndex; from dataclasses import fields; assert {f.name for f in fields(TreeIndex)} == {'parsed_artifact_id','source_id','roots','mode','schema_version','content_hash'}"` exits 0
    - `python -c "from knowledge_lake.plugins.protocols import IndexerPlugin; from typing import runtime_checkable; assert hasattr(IndexerPlugin, '__protocol_attrs__') or True; print('OK')"` exits 0
    - `pytest tests/unit/test_builtin_plugins.py -x -q 2>&1 | grep -v 'ImportError.*pageindex_indexer'` should not show new failures in existing tests
  </acceptance_criteria>
  <verify>
    <automated>cd /root/healthlake && python -c "from knowledge_lake.plugins.protocols import TreeNode, TreeIndex, IndexerPlugin; from dataclasses import fields; assert len(fields(TreeNode))==8; assert len(fields(TreeIndex))==6; print('TreeNode fields:', [f.name for f in fields(TreeNode)]); print('TreeIndex fields:', [f.name for f in fields(TreeIndex)]); print('IndexerPlugin OK')"</automated>
  </verify>
  <done>TreeNode (8 fields), TreeIndex (6 fields), and IndexerPlugin Protocol are importable from protocols.py. All existing protocol imports still work.</done>
</task>

<task id="T3" type="auto" tdd="true">
  <name>Task 3: settings.py: add TreeSettings submodel, tree + indexer fields, extend _validate_swap_key (D-08, ASVS V5)</name>
  <files>src/knowledge_lake/config/settings.py</files>
  <read_first>
    - src/knowledge_lake/config/settings.py — read lines 139-170 (EnrichSettings) as the exact template for TreeSettings; read lines 447-455 (Settings enrich field declaration) for where to add tree + indexer; read lines 483-495 (_validate_swap_key validator) for where to add "indexer"
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §settings.py — exact TreeSettings field list including prompt_version and max_tokens
    - .planning/phases/13-tree-index-foundation/13-CONTEXT.md D-08 — locked: mode Literal, budget_usd=5.0, model_alias="cheap_model", env KLAKE_TREE__MODE
  </read_first>
  <behavior>
    - TreeSettings() default instance has mode="deterministic", budget_usd=5.0, model_alias="cheap_model", schema_version="1", prompt_version="v1", max_tokens=1024
    - Settings().tree is a TreeSettings instance with those defaults
    - Settings().indexer == "pageindex"
    - Settings(indexer="bad value!") raises pydantic ValidationError (swap key validator fires)
    - Settings(indexer="pageindex") succeeds
    - KLAKE_TREE__MODE env var overrides tree.mode (via env_nested_delimiter="__") — no code change needed, just verify the field name matches
  </behavior>
  <action>
    Read settings.py in full. Make three targeted additions using Edit (never Write the full file):

    Addition 1 — TreeSettings class: Insert after the EnrichSettings class definition (line ~163 area, after its closing line). TreeSettings is a Pydantic BaseModel with exactly these fields (mirror EnrichSettings style, per D-08 and PATTERNS.md):
    - mode: Literal["deterministic", "llm"] = "deterministic"  — deterministic-first (D-08)
    - budget_usd: float = 5.0  — mirrors EnrichSettings
    - model_alias: str = "cheap_model"  — never a hardcoded provider ID (CLAUDE.md)
    - schema_version: str = "1"  — D-02 anchor; TREE-06 deferred to v2.6+
    - prompt_version: str = "v1"  — bump to invalidate LLM-mode cache
    - max_tokens: int = 1024  — LLM summary output cap

    Add class docstring: "Tree-index generation config (TREE-01..05). Env override prefix: KLAKE_TREE__ (e.g. KLAKE_TREE__MODE=llm). model_alias routes through cheap_model task alias — never a hardcoded provider ID (D-08)."

    Addition 2 — Wire into Settings: In the Settings class body, next to the existing `enrich: EnrichSettings = Field(default_factory=EnrichSettings)` line, add two new fields:
    - tree: TreeSettings = Field(default_factory=TreeSettings)
    - indexer: str = "pageindex"  — with docstring "Indexer plugin swap key. Entry-point group: knowledge_lake.indexers (D-05)."

    Addition 3 — Extend _validate_swap_key: The @field_validator decorator at line ~483 currently validates "crawler", "discovery", "embedder", "parser", "vectorstore". Add "indexer" to this tuple so the ASVS-V5 regex also validates settings.indexer. The line should become: @field_validator("crawler", "discovery", "embedder", "indexer", "parser", "vectorstore", mode="after")

    Make sure Literal is already imported (it is used by SearchSettings); if not, add it to the typing imports.
  </action>
  <acceptance_criteria>
    - `python -c "from knowledge_lake.config.settings import TreeSettings, Settings; t = TreeSettings(); assert t.mode == 'deterministic'; assert t.budget_usd == 5.0; assert t.model_alias == 'cheap_model'; assert t.schema_version == '1'; print('OK')"` exits 0
    - `python -c "from knowledge_lake.config.settings import Settings; s = Settings(_env_file=None); assert hasattr(s, 'tree'); assert s.tree.mode == 'deterministic'; assert s.indexer == 'pageindex'; print('OK')"` exits 0
    - `python -c "from knowledge_lake.config.settings import Settings; from pydantic import ValidationError; Settings(_env_file=None, indexer='bad value!')" 2>&1 | grep -i 'validat'` shows validation error output
    - `grep -c '"indexer"' src/knowledge_lake/config/settings.py` returns at least 1 (field_validator tuple updated)
  </acceptance_criteria>
  <verify>
    <automated>cd /root/healthlake && python -c "from knowledge_lake.config.settings import TreeSettings, Settings; t = TreeSettings(); assert t.mode=='deterministic' and t.budget_usd==5.0 and t.model_alias=='cheap_model'; s = Settings(_env_file=None); assert s.tree.mode=='deterministic' and s.indexer=='pageindex'; print('TreeSettings and Settings.tree OK')"</automated>
  </verify>
  <done>TreeSettings class added to settings.py; Settings has tree:TreeSettings and indexer:str fields; "indexer" is in the _validate_swap_key field_validator tuple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → Settings | KLAKE_TREE__* env overrides are validated by Pydantic field validators before use |
| settings.indexer → entry-point loader | swap key validated by _validate_swap_key ASVS V5 regex before any entry-point resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Tampering | settings.indexer swap key (path to entry-point load) | high | mitigate | Add "indexer" to _validate_swap_key @field_validator (ASVS V5 regex ^[a-zA-Z][a-zA-Z0-9_-]{0,63}$); prevents arbitrary entry-point names from reaching importlib.metadata.entry_points |
| T-13-04 | Tampering | TreeSettings.mode Literal validation | low | mitigate | Pydantic Literal["deterministic","llm"] fails closed at config load — invalid mode raises ValidationError before any pipeline code runs |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs NO new packages (all edits are to existing files only); no slopcheck required |
</threat_model>

<verification>
After all three tasks complete:
- `cd /root/healthlake && python -c "from knowledge_lake.ids import new_id; assert new_id('tree_index').startswith('idx_')"` exits 0
- `cd /root/healthlake && python -c "from knowledge_lake.plugins.protocols import TreeNode, TreeIndex, IndexerPlugin; print('contract OK')"` exits 0
- `cd /root/healthlake && python -c "from knowledge_lake.config.settings import Settings; s = Settings(_env_file=None); assert s.tree.mode=='deterministic' and s.indexer=='pageindex'"` exits 0
- `cd /root/healthlake && pytest tests/unit/ -q --ignore=tests/unit/test_tree_index.py --ignore=tests/unit/test_tree_index_asset.py 2>&1 | tail -5` — all pre-existing tests still pass
</verification>

<success_criteria>
Three enabling edits are in place:
1. new_id("tree_index") works without ValueError
2. TreeNode/TreeIndex/IndexerPlugin are importable and structurally correct per D-01/D-02/D-05
3. Settings has tree:TreeSettings and indexer with swap-key validation

All pre-existing unit tests still pass (no regressions).
</success_criteria>

## Artifacts this phase produces (Wave 1 enabling edits)

Modified files:
- `src/knowledge_lake/ids.py` — adds `"tree_index": "idx"` to `_PREFIX` dict
- `src/knowledge_lake/plugins/protocols.py` — adds `TreeNode` dataclass (8 fields), `TreeIndex` dataclass (6 fields), `IndexerPlugin` @runtime_checkable Protocol
- `src/knowledge_lake/config/settings.py` — adds `TreeSettings` BaseModel (6 fields), `tree: TreeSettings` field on `Settings`, `indexer: str = "pageindex"` field on `Settings`, extends `_validate_swap_key` to include `"indexer"`

New importable symbols:
- `knowledge_lake.ids._PREFIX["tree_index"]` = `"idx"`
- `knowledge_lake.plugins.protocols.TreeNode` (dataclass)
- `knowledge_lake.plugins.protocols.TreeIndex` (dataclass)
- `knowledge_lake.plugins.protocols.IndexerPlugin` (Protocol)
- `knowledge_lake.config.settings.TreeSettings` (BaseModel)
- `Settings.tree` (TreeSettings)
- `Settings.indexer` (str, default "pageindex")

<output>
Create `.planning/phases/13-tree-index-foundation/13-02-SUMMARY.md` when done
</output>
