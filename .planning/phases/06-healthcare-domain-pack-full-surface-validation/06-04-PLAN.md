---
phase: 06-healthcare-domain-pack-full-surface-validation
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - src/knowledge_lake/dagster_defs/assets.py
  - src/knowledge_lake/dagster_defs/definitions.py
  - tests/unit/test_dagster_retry_policies.py
  - tests/unit/test_dagster_e2e_job.py
  - tests/e2e/__init__.py
  - tests/e2e/test_e2e_healthcare.py
autonomous: false
requirements:
  - IFACE-03
  - DOMAIN-04

must_haves:
  truths:
    - "D-05: tests/e2e/test_e2e_healthcare.py uses dagster.materialize() for healthcare_e2e_job with 5 sources (2 HTML, 2 PDF, 1 CSV) against live MinIO+Postgres+Qdrant stack and verifies lineage integrity, klake search returns ≥1 result, and Parquet file exists in gold zone"
    - "D-08: dagster-webserver is exposed in docker-compose on port 3000 — no compose changes needed per RESEARCH.md §Dagster Observability"
    - All 12 Dagster assets in dagster_defs/assets.py have a retry_policy argument set on their @asset decorator
    - Pipeline group assets (9) use RetryPolicy(max_retries=2, delay=1, backoff=Backoff.EXPONENTIAL)
    - Export group assets (3: export_rag_corpus, export_pretrain_corpus, export_finetune_dataset) use RetryPolicy(max_retries=1, delay=2) — TrainEvalContaminationError is not transient
    - healthcare_e2e_job is defined and registered in Definitions.jobs
    - healthcare_e2e_job selects exactly the core pipeline assets: ingest_raw_document, parsed_document, clean_document, chunk_document, enrich_document, embed_chunks, index_chunks (NOT curate/generate_dataset/export — per Pitfall 6)
    - tests/unit/test_dagster_retry_policies.py: all 12 assets inspected for retry_policy via object introspection
    - tests/unit/test_dagster_e2e_job.py: healthcare_e2e_job is importable from definitions and isinstance(healthcare_e2e_job, JobDefinition)
    - tests/e2e/test_e2e_healthcare.py materializes the 7-asset core pipeline against live docker-compose stack and verifies lineage + search + Parquet export
  artifacts:
    - src/knowledge_lake/dagster_defs/assets.py (modified — RetryPolicy on all 12 assets + healthcare_e2e_job definition)
    - src/knowledge_lake/dagster_defs/definitions.py (modified — healthcare_e2e_job in Definitions)
    - tests/unit/test_dagster_retry_policies.py
    - tests/unit/test_dagster_e2e_job.py
    - tests/e2e/__init__.py
    - tests/e2e/test_e2e_healthcare.py
  key_links:
    - _PIPELINE_RETRY shared constant used by 9 pipeline assets (DRY)
    - _EXPORT_RETRY separate constant with max_retries=1 for the 3 export assets
    - healthcare_e2e_job uses AssetSelection.assets() with direct Python object references (not string names) to avoid breakage on rename — per RESEARCH.md Assumption A6
    - E2E test uses dagster.materialize() pattern from tests/integration/test_dagster_assets.py
    - E2E test fixture uses 5 sources: 2 HTML fixtures, 2 PDF fixtures, 1 CSV fixture (or URLs to real public sources if docker-compose network available)
---

<objective>
Add RetryPolicy to all 12 existing Dagster assets, define healthcare_e2e_job, register it in Definitions, and implement the 5-source end-to-end healthcare validation test.

Purpose: Satisfies IFACE-03 (Dagster assets with retries observable from UI) and DOMAIN-04 (5-source E2E pipeline validation). This is the final integration gate proving the entire Phase 6 surface works together.

Output: All 12 assets with RetryPolicy; healthcare_e2e_job defined and registered; E2E test proving ingest→parse→clean→chunk→enrich→embed→index→lineage→search→export for 5 healthcare-format sources.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-healthcare-domain-pack-full-surface-validation/06-CONTEXT.md
@.planning/phases/06-healthcare-domain-pack-full-surface-validation/06-RESEARCH.md

# Files being modified — read before touching
@src/knowledge_lake/dagster_defs/assets.py
@src/knowledge_lake/dagster_defs/definitions.py
@tests/integration/test_dagster_assets.py

# Prior plan SUMMARYs for context
@.planning/phases/06-healthcare-domain-pack-full-surface-validation/06-01-SUMMARY.md
@.planning/phases/06-healthcare-domain-pack-full-surface-validation/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test stubs for Dagster retry policies and e2e job</name>
  <files>
    tests/unit/test_dagster_retry_policies.py,
    tests/unit/test_dagster_e2e_job.py,
    tests/e2e/__init__.py,
    tests/e2e/test_e2e_healthcare.py
  </files>
  <read_first>
    tests/integration/test_dagster_assets.py — full file; extract the materialize() pattern, resource setup, conftest fixture usage; find SPIKE_PDF fixture path
    src/knowledge_lake/dagster_defs/definitions.py — verify current Definitions structure, jobs list presence
    src/knowledge_lake/dagster_defs/assets.py — list all @asset names and group_names
  </read_first>
  <behavior>
    test_dagster_retry_policies.py stubs:
      - test_all_pipeline_assets_have_retry_policy: iterates [ingest_raw_document, parsed_document, clean_document, chunk_document, enrich_document, curate_document_asset, generate_dataset, embed_chunks, index_chunks] and asserts each asset.retry_policy is not None
      - test_all_export_assets_have_retry_policy: iterates [export_rag_corpus, export_pretrain_corpus, export_finetune_dataset] and asserts each has retry_policy
      - test_pipeline_retry_policy_max_retries: pipeline assets retry_policy.max_retries == 2
      - test_export_retry_policy_max_retries: export assets retry_policy.max_retries == 1
    test_dagster_e2e_job.py stubs:
      - test_healthcare_e2e_job_importable: healthcare_e2e_job can be imported from knowledge_lake.dagster_defs.assets
      - test_healthcare_e2e_job_in_definitions: defs.jobs contains healthcare_e2e_job (check defs.jobs list or by name)
      - test_healthcare_e2e_job_is_job_definition: isinstance(healthcare_e2e_job, dagster.JobDefinition)
    test_e2e_healthcare.py stubs (all marked @pytest.mark.integration and @pytest.mark.xfail):
      - test_e2e_pipeline_materializes: materialize() result.success == True for the core 7 assets
      - test_e2e_lineage_intact: for the ingested raw_artifact_id, lineage chain has ≥ 3 nodes (raw→parsed→chunk)
      - test_e2e_search_returns_result: pipeline.search.search(query="healthcare", collection="klake_healthcare_e2e", top_k=1) returns non-empty list
      - test_e2e_parquet_exported: gold zone S3 key matching "gold/*parquet" exists in MinIO bucket
  </behavior>
  <action>
    Create tests/e2e/__init__.py as empty file.

    Create test stub files following the pattern from test_dagster_assets.py. For retry policy tests, directly import the asset functions from dagster_defs.assets and inspect .retry_policy attribute. For job tests, import from dagster_defs.definitions.

    For test_e2e_healthcare.py, use a module-scoped fixture (scope="module") with dagster.materialize() — same pattern as test_dagster_assets.py. The fixture should accept 5 source documents via IngestConfig. Use existing test fixtures directory (tests/fixtures/) for PDF/HTML/CSV fixture files. If fixtures don't exist for all formats, note which ones the implementation task must create.

    Mark all E2E tests with @pytest.mark.integration so they are excluded from fast unit-test runs.
  </action>
  <verify>
    <automated>cd /root/healthlake && python -m pytest tests/unit/test_dagster_retry_policies.py tests/unit/test_dagster_e2e_job.py tests/e2e/test_e2e_healthcare.py -x -q --co 2>&1 | tail -10</automated>
  </verify>
  <done>pytest --collect-only shows all stub tests collected without ImportError; tests/e2e/__init__.py exists; test_e2e_healthcare.py has @pytest.mark.integration markers on all tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RetryPolicy on all 12 Dagster assets + healthcare_e2e_job</name>
  <files>
    src/knowledge_lake/dagster_defs/assets.py,
    src/knowledge_lake/dagster_defs/definitions.py
  </files>
  <read_first>
    src/knowledge_lake/dagster_defs/assets.py — full file; list all 12 @asset decorator calls; note current imports from dagster
    src/knowledge_lake/dagster_defs/definitions.py — full file; check current jobs=[] in Definitions or absence of jobs argument
    .planning/phases/06-healthcare-domain-pack-full-surface-validation/06-RESEARCH.md — Pattern 3 (RetryPolicy), Pattern 4 (define_asset_job), Pitfall 6 (asset selection for E2E job must not include curate/generate_dataset)
  </read_first>
  <behavior>
    - All 9 pipeline group assets (ingest_raw_document, parsed_document, clean_document, chunk_document, enrich_document, curate_document_asset, generate_dataset, embed_chunks, index_chunks) have retry_policy=_PIPELINE_RETRY where _PIPELINE_RETRY = RetryPolicy(max_retries=2, delay=1, backoff=Backoff.EXPONENTIAL)
    - All 3 export assets (export_rag_corpus, export_pretrain_corpus, export_finetune_dataset) have retry_policy=_EXPORT_RETRY where _EXPORT_RETRY = RetryPolicy(max_retries=1, delay=2)
    - healthcare_e2e_job defined as define_asset_job("healthcare_e2e_job", selection=AssetSelection.assets(ingest_raw_document, parsed_document, clean_document, chunk_document, enrich_document, embed_chunks, index_chunks), description="Full pipeline E2E validation job for healthcare domain (DOMAIN-04)")
    - Definitions in definitions.py has jobs=[healthcare_e2e_job]
    - All existing asset definitions and Definitions structure unchanged (additive only)
  </behavior>
  <action>
    In assets.py, add the following imports to the dagster import line:
      from dagster import Config, asset, RetryPolicy, Backoff, define_asset_job, AssetSelection

    Define two retry policy constants immediately after the imports section:
      _PIPELINE_RETRY = RetryPolicy(max_retries=2, delay=1, backoff=Backoff.EXPONENTIAL)
      _EXPORT_RETRY = RetryPolicy(max_retries=1, delay=2)

    For each of the 9 pipeline @asset decorators, add retry_policy=_PIPELINE_RETRY to the decorator keyword arguments. Search for all @asset( blocks with group_name="pipeline" and add the kwarg. Be careful to add it inside the @asset() call, not as a separate argument to the decorated function.

    For each of the 3 export @asset decorators, add retry_policy=_EXPORT_RETRY.

    Define healthcare_e2e_job after all asset definitions (before any if __name__ == main block):
      healthcare_e2e_job = define_asset_job(
          name="healthcare_e2e_job",
          selection=AssetSelection.assets(
              ingest_raw_document,
              parsed_document,
              clean_document,
              chunk_document,
              enrich_document,
              embed_chunks,
              index_chunks,
          ),
          description=(
              "Full pipeline job for healthcare E2E validation (DOMAIN-04). "
              "Materializes the core ingest-to-index chain over 5 healthcare sources."
          ),
      )

    In definitions.py, add to the Definitions() call:
      - Import healthcare_e2e_job from assets.py alongside existing asset imports
      - Add jobs=[healthcare_e2e_job] argument to Definitions()
  </action>
  <verify>
    <automated>cd /root/healthlake && python -m pytest tests/unit/test_dagster_retry_policies.py tests/unit/test_dagster_e2e_job.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    All retry policy and job tests pass;
    `python -c "from knowledge_lake.dagster_defs.assets import ingest_raw_document; print(ingest_raw_document.retry_policy)"` shows RetryPolicy(max_retries=2);
    `python -c "from knowledge_lake.dagster_defs.definitions import defs; print([j.name for j in defs.jobs])"` includes "healthcare_e2e_job"
  </done>
</task>

<task type="auto">
  <name>Task 3: 5-source E2E healthcare test implementation + human verification checkpoint</name>
  <files>
    tests/e2e/test_e2e_healthcare.py,
    tests/fixtures/cms_cop_sample.html,
    tests/fixtures/cdc_icd_overview.html,
    tests/fixtures/nppes_npi_sample.csv
  </files>
  <read_first>
    tests/integration/test_dagster_assets.py — full file; extract the conftest fixture setup, resources dict construction, materialize() invocation with run_config, and assertion patterns
    tests/integration/conftest.py — check if there is a conftest with shared fixtures (postgres_url, minio, qdrant etc.)
    tests/fixtures/ — list existing fixtures (ls) to know which PDF/HTML/CSV files already exist vs must be created
    src/knowledge_lake/dagster_defs/assets.py — IngestConfig class fields (fixture_path, url, source_name, collection, mime_type)
    src/knowledge_lake/lineage.py — lineage function signatures to verify lineage chain assertion
    src/knowledge_lake/pipeline/search.py — search() function signature
  </read_first>
  <action>
    Read tests/integration/test_dagster_assets.py to extract the exact resource setup pattern (PostgresResource, MinIOResource, QdrantResource, LiteLLMResource from env vars).

    Determine which fixture files already exist. Two PDF fixtures should already exist (hhs_security_rule.pdf is confirmed). If cms_cop_sample.html, cdc_icd_overview.html, and nppes_npi_sample.csv don't exist, create them as minimal valid samples:
      - cms_cop_sample.html: minimal valid HTML with a heading "CMS Conditions of Participation" and a paragraph with regulatory text
      - cdc_icd_overview.html: minimal valid HTML with "ICD-10-CM Classification System" heading
      - nppes_npi_sample.csv: 3-row CSV with headers NPI,Provider_First_Name,Provider_Last_Name and 2 data rows with fake 10-digit NPIs

    Implement tests/e2e/test_e2e_healthcare.py as a pytest module with @pytest.mark.integration on all tests.

    E2E structure: one module-scoped fixture `e2e_results` that materializes the 7-asset pipeline 5 times (once per source document — OR in a single materialize() run if IngestConfig supports a batch pattern — check what test_dagster_assets.py does):

    Actually, dagster.materialize() is called once per document because IngestConfig is per-run config. Model the fixture as materializing the pipeline once using an HTML fixture, then run 4 additional materializations for the other 4 sources. Alternatively, call materialize() once per source and collect all artifact_ids.

    Use collection name "klake_healthcare_e2e" to avoid collision with other test collections.

    After materializing the 5 sources, the test suite asserts:
      - test_e2e_pipeline_materializes: all 5 materialize() calls result.success == True
      - test_e2e_lineage_intact: for at least one raw_artifact_id, calling lineage.resolve_ancestry() returns ≥3 nodes
      - test_e2e_search_returns_result: search(query="medical record", collection="klake_healthcare_e2e", top_k=1) returns non-empty list
      - test_e2e_parquet_exported: calls export_rag_corpus pipeline function (or pipeline.export) to produce Parquet in gold zone; checks S3 key exists

    Mark the entire test module with @pytest.mark.integration (use pytestmark at module level).

    Include a module-level docstring: "E2E healthcare validation test (DOMAIN-04). Requires: docker-compose up (MinIO + Postgres + Qdrant + LiteLLM). Run with: pytest tests/e2e/ -m integration -v"
  </action>
  <verify>
    <automated>cd /root/healthlake && python -m pytest tests/e2e/test_e2e_healthcare.py --co -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest --collect-only shows 4 E2E tests collected; all marked @pytest.mark.integration; fixture files cms_cop_sample.html, cdc_icd_overview.html, nppes_npi_sample.csv exist in tests/fixtures/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 6 full-surface human verification</name>
  <what-built>
    All four plans of Phase 6 have been implemented:
    - Plan 01: DomainLoader + healthcare domain pack (domains/healthcare/)
    - Plan 02: Domain prompt override in pipeline/enrich.py + DomainSettings
    - Plan 03: klake init + klake index CLI, 8 new API endpoints
    - Plan 04: RetryPolicy on all 12 Dagster assets + healthcare_e2e_job + E2E test stubs

    Unit and integration tests are in place. The E2E test requires the full docker-compose stack.
  </what-built>
  <how-to-verify>
    1. Run the full unit test suite (no docker required):
       cd /root/healthlake
       pytest tests/unit/ -x -q
       Expected: all tests pass (green)

    2. Verify the domain loader works end-to-end:
       python -c "
       from knowledge_lake.domains.loader import DomainLoader
       l = DomainLoader.from_name('healthcare')
       print('sources:', len(l.sources))
       print('manifest:', l.manifest.name, l.manifest.version)
       r = l.validator.validate_document({'text': 'ICD-10 code E11.9'})
       print('validation:', r)
       "
       Expected: "sources: 28", "manifest: healthcare 1.0.0", ValidationResult printed

    3. Verify CLI commands are registered:
       klake init --help 2>&1 | grep -E "\-\-domain|Load a domain"
       klake index --help 2>&1 | grep -E "\-\-collection|Reindex"

    4. Verify API routes are registered:
       python -c "
       from knowledge_lake.api.app import app
       routes = [r.path for r in app.routes]
       for r in ['/sources', '/documents', '/datasets']:
           matches = [p for p in routes if r in p]
           print(r, ':', matches)
       "

    5. Verify Dagster retry policies:
       python -c "
       from knowledge_lake.dagster_defs.assets import ingest_raw_document, export_rag_corpus, healthcare_e2e_job
       print('pipeline retry:', ingest_raw_document.retry_policy)
       print('export retry:', export_rag_corpus.retry_policy)
       print('job:', healthcare_e2e_job.name)
       "
       Expected: ingest_raw_document has max_retries=2, export_rag_corpus has max_retries=1

    6. If docker-compose stack is running, run the E2E test:
       pytest tests/e2e/test_e2e_healthcare.py -v -m integration
       (Skip if stack not running — mark results accordingly)

    7. Check Dagster UI: if dagster-webserver is running (docker compose up), open
       http://localhost:3000 and verify "healthcare_e2e_job" appears under Jobs.
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any issues found.</resume-signal>
  <action>Human verification — no automated action. Follow the how-to-verify steps above.</action>
  <verify>
    <automated>MISSING — human verification required; see how-to-verify steps</automated>
  </verify>
  <done>Human types "approved" after confirming unit tests pass and spot-check verifications succeed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dagster executor → @asset functions | retry policies are transparent to business logic; executor triggers retries |
| test runner → docker-compose services | E2E test sends real data to MinIO/Postgres/Qdrant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Tampering | Dagster export assets retrying on TrainEvalContaminationError | medium | mitigate | export assets use RetryPolicy(max_retries=1, delay=2) not max_retries=2; TrainEvalContaminationError is a business-logic failure not retried (RESEARCH.md Anti-Pattern) |
| T-06-13 | Information Disclosure | E2E test fixtures containing fake PHI patterns | low | mitigate | nppes_npi_sample.csv uses completely fabricated NPI numbers (not real patient data); cms_cop_sample.html contains only regulatory text, no patient records |
| T-06-14 | Tampering | healthcare_e2e_job asset selection including curate/export assets prematurely | medium | mitigate | AssetSelection.assets() uses direct Python object references for exactly 7 core pipeline assets (Pitfall 6 — curate/generate_dataset not included) |
</threat_model>

<verification>
After all tasks complete (pre-checkpoint):

```bash
cd /root/healthlake

# Unit tests
python -m pytest tests/unit/test_dagster_retry_policies.py tests/unit/test_dagster_e2e_job.py -v

# Retry policies verified
python -c "
from knowledge_lake.dagster_defs import assets
pipeline_assets = [
    assets.ingest_raw_document, assets.parsed_document, assets.clean_document,
    assets.chunk_document, assets.enrich_document, assets.curate_document_asset,
    assets.generate_dataset, assets.embed_chunks, assets.index_chunks,
]
export_assets = [assets.export_rag_corpus, assets.export_pretrain_corpus, assets.export_finetune_dataset]
for a in pipeline_assets:
    assert a.retry_policy is not None and a.retry_policy.max_retries == 2, f'{a.key} missing/wrong retry'
for a in export_assets:
    assert a.retry_policy is not None and a.retry_policy.max_retries == 1, f'{a.key} wrong export retry'
print('All 12 assets have correct RetryPolicy')
"

# E2E job registered
python -c "
from knowledge_lake.dagster_defs.definitions import defs
job_names = [j.name for j in defs.jobs]
assert 'healthcare_e2e_job' in job_names, f'Missing job: {job_names}'
print('healthcare_e2e_job registered in Definitions')
"

# E2E test collected
python -m pytest tests/e2e/test_e2e_healthcare.py --co -q
```
</verification>

<success_criteria>
- All 12 Dagster assets have RetryPolicy configured (9 pipeline with max_retries=2, 3 export with max_retries=1)
- healthcare_e2e_job defined with AssetSelection.assets() of exactly 7 core pipeline assets
- healthcare_e2e_job registered in Definitions.jobs
- tests/unit/test_dagster_retry_policies.py passes (4 tests)
- tests/unit/test_dagster_e2e_job.py passes (3 tests)
- tests/e2e/test_e2e_healthcare.py collected without import errors (4 E2E tests, all @pytest.mark.integration)
- E2E fixture files exist: cms_cop_sample.html, cdc_icd_overview.html, nppes_npi_sample.csv
- Human checkpoint approved after verifying unit tests green and spot-checking domain loader, CLI, API, and Dagster
</success_criteria>

## Artifacts this phase produces

**New symbols created by this plan:**

Dagster constants (assets.py):
- `_PIPELINE_RETRY = RetryPolicy(max_retries=2, delay=1, backoff=Backoff.EXPONENTIAL)`
- `_EXPORT_RETRY = RetryPolicy(max_retries=1, delay=2)`
- `healthcare_e2e_job` (define_asset_job — JobDefinition)

Modified @asset decorators (additive):
- `ingest_raw_document` — added retry_policy=_PIPELINE_RETRY
- `parsed_document` — added retry_policy=_PIPELINE_RETRY
- `clean_document` — added retry_policy=_PIPELINE_RETRY
- `chunk_document` — added retry_policy=_PIPELINE_RETRY
- `enrich_document` — added retry_policy=_PIPELINE_RETRY
- `curate_document_asset` — added retry_policy=_PIPELINE_RETRY
- `generate_dataset` — added retry_policy=_PIPELINE_RETRY
- `embed_chunks` — added retry_policy=_PIPELINE_RETRY
- `index_chunks` — added retry_policy=_PIPELINE_RETRY
- `export_rag_corpus` — added retry_policy=_EXPORT_RETRY
- `export_pretrain_corpus` — added retry_policy=_EXPORT_RETRY
- `export_finetune_dataset` — added retry_policy=_EXPORT_RETRY

Modified Definitions (definitions.py):
- `jobs=[healthcare_e2e_job]` added to Definitions() call

New test files:
- `tests/unit/test_dagster_retry_policies.py`
- `tests/unit/test_dagster_e2e_job.py`
- `tests/e2e/__init__.py`
- `tests/e2e/test_e2e_healthcare.py`

New fixture files:
- `tests/fixtures/cms_cop_sample.html`
- `tests/fixtures/cdc_icd_overview.html`
- `tests/fixtures/nppes_npi_sample.csv`

<output>
Create `.planning/phases/06-healthcare-domain-pack-full-surface-validation/06-04-SUMMARY.md` when done.
</output>
