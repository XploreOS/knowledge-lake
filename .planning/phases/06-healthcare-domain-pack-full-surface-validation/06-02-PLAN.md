---
phase: 06-healthcare-domain-pack-full-surface-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge_lake/config/settings.py
  - src/knowledge_lake/pipeline/enrich.py
  - tests/unit/test_enrich_domain_override.py
autonomous: true
requirements:
  - DOMAIN-01
  - DOMAIN-03

must_haves:
  truths:
    - Settings has a domain_name Optional[str] field (default None) readable via KLAKE_DOMAIN_NAME env var
    - Settings has a domains_root str field (default "domains") readable via KLAKE_DOMAINS_ROOT env var
    - _build_enrichment_prompt accepts an optional domain_system_prompt parameter; when provided it replaces _ENRICHMENT_SYSTEM_PROMPT as the system prompt
    - enrich_document() accepts an optional domain_system_prompt kwarg that flows through to _build_enrichment_prompt
    - When domain_system_prompt is None, existing behavior is 100% unchanged — generic _ENRICHMENT_SYSTEM_PROMPT is used
    - Existing enrich tests (tests/unit/test_enrich.py) continue to pass without modification
  artifacts:
    - src/knowledge_lake/config/settings.py (modified — DomainSettings added, domain_name + domains_root on Settings)
    - src/knowledge_lake/pipeline/enrich.py (modified — _build_enrichment_prompt optional kwarg, enrich_document optional kwarg)
  key_links:
    - domain_system_prompt is an Optional[str] kwarg with default None — not a Settings read inside enrich.py (keeps enrichment side-effect-free)
    - DomainSettings nested model follows existing StorageSettings/CrawlSettings pattern under KLAKE_DOMAIN__ prefix
---

<objective>
Add DomainSettings to config/settings.py and inject an optional domain_system_prompt override into pipeline/enrich.py. This is the single core-code touch point for domain packs (D-02). Entirely additive — no existing behavior changes.

Purpose: Enables klake init --domain healthcare (Plan 3) to pass a rendered enrich.j2 prompt into the enrichment pipeline without any pipeline re-design.

Output: Two modified files (settings.py + enrich.py) and a new test file. All existing enrich tests continue to pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-healthcare-domain-pack-full-surface-validation/06-CONTEXT.md
@.planning/phases/06-healthcare-domain-pack-full-surface-validation/06-RESEARCH.md

# Files being modified — read before touching
@src/knowledge_lake/config/settings.py
@src/knowledge_lake/pipeline/enrich.py
@tests/unit/test_enrich.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DomainSettings in config/settings.py</name>
  <files>
    src/knowledge_lake/config/settings.py
  </files>
  <read_first>
    src/knowledge_lake/config/settings.py — full file; note CrawlSettings, StorageSettings pattern; find where nested fields are declared on Settings; find _SWAP_KEY_RE
  </read_first>
  <behavior>
    - DomainSettings(BaseModel) class exists with two fields: domain_name: Optional[str] = None, domains_root: str = "domains"
    - Settings has field: domain: DomainSettings = Field(default_factory=DomainSettings)
    - KLAKE_DOMAIN__DOMAIN_NAME env var sets settings.domain.domain_name
    - KLAKE_DOMAIN__DOMAINS_ROOT env var sets settings.domain.domains_root
    - Settings() with no env vars has settings.domain.domain_name == None, settings.domain.domains_root == "domains"
    - All existing settings tests still pass
  </behavior>
  <action>
    Read settings.py fully. Add DomainSettings class between ExportSettings and Settings class definition:

      class DomainSettings(BaseModel):
          """Domain pack configuration (DOMAIN-01).

          Nested under Settings as settings.domain. Environment variable pattern:
          KLAKE_DOMAIN__DOMAIN_NAME, KLAKE_DOMAIN__DOMAINS_ROOT.
          """

          domain_name: Optional[str] = None
          """Active domain pack name (e.g. 'healthcare'). None = no domain pack loaded.
          Set via KLAKE_DOMAIN__DOMAIN_NAME env var. (D-01, D-02)"""

          domains_root: str = "domains"
          """Path to the domains/ directory relative to the working directory,
          or an absolute path. Override via KLAKE_DOMAIN__DOMAINS_ROOT env var
          for containerised deployments (Pitfall 1)."""

    Add field to Settings (after existing nested settings, before validators):
          domain: DomainSettings = Field(default_factory=DomainSettings)
          """Domain pack configuration (DOMAIN-01)."""

    Also add Optional import to the from typing import line if not already present (it is — check the existing imports).
  </action>
  <verify>
    <automated>cd /root/healthlake && python -c "from knowledge_lake.config.settings import Settings, DomainSettings; s = Settings(_env_file=None); print(s.domain.domain_name, s.domain.domains_root)"</automated>
  </verify>
  <done>`python -c "from knowledge_lake.config.settings import Settings; s=Settings(_env_file=None); assert s.domain.domain_name is None; assert s.domain.domains_root == 'domains'; print('ok')"` prints "ok"; existing settings tests pass via `pytest tests/unit/test_settings.py -x -q`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Domain prompt override in pipeline/enrich.py + new test file</name>
  <files>
    src/knowledge_lake/pipeline/enrich.py,
    tests/unit/test_enrich_domain_override.py
  </files>
  <read_first>
    src/knowledge_lake/pipeline/enrich.py — full file; find _build_enrichment_prompt() signature and _ENRICHMENT_SYSTEM_PROMPT; find enrich_document() function signature and where it calls _build_enrichment_prompt
    tests/unit/test_enrich.py — fixture patterns (engine, session, monkeypatched db, mocked litellm.completion)
  </read_first>
  <behavior>
    test_enrich_domain_override.py:
      - test_domain_system_prompt_replaces_generic: when enrich_document is called with domain_system_prompt="CUSTOM SYSTEM", the litellm.completion call receives messages[0]["content"] == "CUSTOM SYSTEM"
      - test_no_domain_prompt_uses_generic: when enrich_document is called without domain_system_prompt, messages[0]["content"] == _ENRICHMENT_SYSTEM_PROMPT (generic unchanged)
      - test_existing_enrich_tests_pass: placeholder test that runs pytest tests/unit/test_enrich.py internally (or just marks a comment that existing tests must still pass)
    Existing tests/unit/test_enrich.py: all tests continue to pass without any modification to that file
  </behavior>
  <action>
    In pipeline/enrich.py, make two additive changes only:

    Change 1: Modify _build_enrichment_prompt signature to add optional domain_system_prompt parameter:
      Old: def _build_enrichment_prompt(excerpt: str, deterministic: dict) -> tuple[str, str]:
      New: def _build_enrichment_prompt(excerpt: str, deterministic: dict, domain_system_prompt: Optional[str] = None) -> tuple[str, str]:
    Inside the function, change the first line of the body:
      Old: user_prompt = (
      New (first add): system = domain_system_prompt or _ENRICHMENT_SYSTEM_PROMPT
    Then at the end replace `return _ENRICHMENT_SYSTEM_PROMPT, user_prompt` with `return system, user_prompt`

    Change 2: Modify enrich_document() function signature to accept and thread through the kwarg:
      Find the def enrich_document(...) signature and add: domain_system_prompt: Optional[str] = None
      Inside enrich_document, find the call to _build_enrichment_prompt and update it to:
        system_prompt, user_prompt = _build_enrichment_prompt(excerpt, deterministic, domain_system_prompt=domain_system_prompt)

    Ensure Optional is imported from typing (already is — check).

    Create tests/unit/test_enrich_domain_override.py using the same fixture/mock pattern from tests/unit/test_enrich.py. Mock litellm.completion to capture the messages argument. Test that domain_system_prompt="CUSTOM SYSTEM" causes messages[0]["content"] == "CUSTOM SYSTEM" in the call. Test that default (no kwarg) causes messages[0]["content"] == _ENRICHMENT_SYSTEM_PROMPT.
  </action>
  <verify>
    <automated>cd /root/healthlake && python -m pytest tests/unit/test_enrich.py tests/unit/test_enrich_domain_override.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All existing test_enrich.py tests pass unchanged; test_enrich_domain_override.py has at least 2 passing tests; `grep -n "domain_system_prompt" src/knowledge_lake/pipeline/enrich.py` shows the parameter in _build_enrichment_prompt and enrich_document signatures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/API caller → enrich_document() | domain_system_prompt is operator-supplied rendered template content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | domain_system_prompt as LLM system prompt injection vector | medium | accept | The domain_system_prompt is rendered from operator-authored Jinja2 templates in domains/; it is not user-submitted at runtime. Document that domain packs are operator-controlled artifacts. The existing user-content prompt-injection guard in the user prompt remains in effect. |
| T-06-07 | Information Disclosure | DomainSettings exposing internal path (domains_root) | low | accept | domains_root is a path config, not a secret; it appears only in Settings, not in any API response |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /root/healthlake

# Existing enrich tests unaffected
python -m pytest tests/unit/test_enrich.py -x -q

# New override tests pass
python -m pytest tests/unit/test_enrich_domain_override.py -v

# Settings DomainSettings accessible
python -c "
from knowledge_lake.config.settings import Settings
s = Settings(_env_file=None)
print('domain_name:', s.domain.domain_name)
print('domains_root:', s.domain.domains_root)
assert s.domain.domain_name is None
assert s.domain.domains_root == 'domains'
print('ok')
"

# enrich.py has domain_system_prompt kwarg
grep -n "domain_system_prompt" src/knowledge_lake/pipeline/enrich.py
```
</verification>

<success_criteria>
- All tests/unit/test_enrich.py tests continue to pass without modification
- tests/unit/test_enrich_domain_override.py has 2+ passing tests
- DomainSettings exists in settings.py with domain_name: Optional[str] = None, domains_root: str = "domains"
- Settings.domain field exists and is accessible
- enrich_document() accepts domain_system_prompt kwarg that overrides system prompt when provided
- No existing caller of enrich_document() requires changes (kwarg defaults to None)
</success_criteria>

## Artifacts this phase produces

**New symbols created by this plan:**

Classes:
- `knowledge_lake.config.settings.DomainSettings` (Pydantic BaseModel with domain_name, domains_root)

Modified functions (additive-only):
- `knowledge_lake.pipeline.enrich._build_enrichment_prompt(excerpt, deterministic, domain_system_prompt=None)` — new optional kwarg
- `knowledge_lake.pipeline.enrich.enrich_document(..., domain_system_prompt=None)` — new optional kwarg threads through to _build_enrichment_prompt

Modified Settings fields:
- `knowledge_lake.config.settings.Settings.domain: DomainSettings` — new nested field

New test files:
- `tests/unit/test_enrich_domain_override.py`

<output>
Create `.planning/phases/06-healthcare-domain-pack-full-surface-validation/06-02-SUMMARY.md` when done.
</output>
