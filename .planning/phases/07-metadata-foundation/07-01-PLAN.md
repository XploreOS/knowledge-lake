---
phase: 07-metadata-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_qdrant_payload_indexes.py
autonomous: true
requirements:
  - PAYLOAD-02
must_haves:
  truths:
    - "tests/unit/test_qdrant_payload_indexes.py exists and is collected by pytest"
    - "three test classes are present: TestEnsurePayloadIndexes, TestEnsureAliasedCollectionCallsIndexes, TestReindexCallsIndexes"
    - "running pytest on this file before Plan 03 produces FAILURES (RED state — method does not exist yet)"
    - "test harness mocks QdrantVectorStore._client and does not require a live Qdrant server"
  artifacts:
    - tests/unit/test_qdrant_payload_indexes.py
  key_links:
    - "Plan 03 extends this file to GREEN; Plan 01 writes only the test contract"
    - "fixture pattern mirrors tests/unit/test_builtin_plugins.py monkeypatch style"
---

<objective>
Create the Wave 0 unit-test scaffold for the `ensure_payload_indexes()` feature before any
implementation begins. This establishes the RED (failing) test contract for PAYLOAD-02's
payload-index creation requirement, per the gap identified in VALIDATION.md.

Purpose: guarantee test-first discipline for the new QdrantVectorStore method and its
integration with `ensure_aliased_collection()` and `reindex()` before those methods are
touched in Plan 03.

Output: `tests/unit/test_qdrant_payload_indexes.py` — importable, collected, all tests failing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-metadata-foundation/07-CONTEXT.md
@.planning/phases/07-metadata-foundation/07-RESEARCH.md
@.planning/phases/07-metadata-foundation/07-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_qdrant_payload_indexes.py scaffold (RED state)</name>
  <files>tests/unit/test_qdrant_payload_indexes.py</files>

  <read_first>
    - tests/unit/test_index_payload.py — monkeypatch engine + MagicMock vstore pattern to mirror
    - tests/unit/test_search_filters.py — module-level monkeypatch pattern to mirror
    - src/knowledge_lake/plugins/builtin/qdrant_store.py — QdrantVectorStore class, __init__ args, ensure_aliased_collection signature, reindex signature
    - .planning/phases/07-metadata-foundation/07-VALIDATION.md — Wave 0 gap requirements
    - .planning/phases/07-metadata-foundation/07-RESEARCH.md — Pattern 1 (ensure_payload_indexes API contract), Pitfall 1, Pitfall 4
  </read_first>

  <behavior>
    - TestEnsurePayloadIndexes: instantiate QdrantVectorStore with mocked _client, call store.ensure_payload_indexes("test_col"), verify _client.create_payload_index was called for field names "domain", "document_type", "source_name", "format", "source_id", "tags", "keywords". FAILS with AttributeError until method is added.
    - TestEnsureAliasedCollectionCallsIndexes: instantiate store with mocked _client (_client.collection_exists returns False), monkeypatch store.ensure_payload_indexes with a MagicMock, call store.ensure_aliased_collection("alias", dim=4), assert store.ensure_payload_indexes was called with the physical collection name "alias_v1". FAILS assertion until call site is wired.
    - TestReindexCallsIndexes: instantiate store with mocked _client (_client.get_collections returns empty list, _client.get_aliases returns empty list), monkeypatch store.ensure_payload_indexes with a MagicMock, call store.reindex("alias", dim=4, upsert_fn=lambda _: None), assert store.ensure_payload_indexes was called with the next physical collection name. FAILS assertion until call site is wired.
  </behavior>

  <action>
    Create `tests/unit/test_qdrant_payload_indexes.py` with the following structure (no fenced
    code blocks in this action — the structure is described as prose with exact identifiers):

    Module docstring: note this file covers D-07/D-08/D-09 for ensure_payload_indexes.

    Import `from unittest.mock import MagicMock, patch, call` and `import pytest` and
    `from knowledge_lake.plugins.builtin.qdrant_store import QdrantVectorStore`.

    Shared helper `_mock_store()`: instantiates QdrantVectorStore with qdrant_url="http://localhost:6333"
    BUT patches `qdrant_client.QdrantClient.__init__` with a MagicMock that returns None, so no
    real connection is made. Store the mock client on `store._client` for assertion access.
    Use `unittest.mock.patch("qdrant_client.QdrantClient", autospec=True)` as a context manager
    inside a pytest fixture named `mock_store` that yields the configured QdrantVectorStore
    instance with `store._client` replaced by a MagicMock().

    Class `TestEnsurePayloadIndexes`: one test method
    `test_calls_create_payload_index_for_all_expected_fields(mock_store)`. Call
    `mock_store.ensure_payload_indexes("klake_chunks_v1")`. Assert
    `mock_store._client.create_payload_index.call_count` equals 7 (the number of fields in
    `_KEYWORD_FIELDS`). Assert each expected field name appears in the `field_name` kwarg of one
    of the calls: "domain", "document_type", "source_name", "format", "source_id", "tags",
    "keywords". Use `{c.kwargs["field_name"] for c in mock_store._client.create_payload_index.call_args_list}`
    to collect called field names and assert the expected set is a subset.

    Class `TestEnsureAliasedCollectionCallsIndexes`: one test method
    `test_calls_ensure_payload_indexes_on_new_physical(mock_store, monkeypatch)`. Set
    `mock_store._client.collection_exists.return_value = False`. Monkeypatch
    `mock_store.ensure_payload_indexes` to a MagicMock. Call
    `mock_store.ensure_aliased_collection("klake_chunks", dim=384)`. Assert
    `mock_store.ensure_payload_indexes.assert_called_once_with("klake_chunks_v1")`.

    Class `TestReindexCallsIndexes`: one test method
    `test_calls_ensure_payload_indexes_on_next_physical(mock_store, monkeypatch)`. Configure
    `mock_store._client.get_collections.return_value` to have an empty `.collections` list.
    Configure `mock_store._client.get_aliases.return_value` to have an empty `.aliases` list.
    Monkeypatch `mock_store.ensure_payload_indexes` to a MagicMock. Call
    `mock_store.reindex("klake_chunks", dim=384, upsert_fn=lambda col: None)`. Assert
    `mock_store.ensure_payload_indexes.assert_called_once_with("klake_chunks_v1")`.

    The `mock_store` fixture: use `@pytest.fixture()` decorator. Inside, create a bare
    `QdrantVectorStore.__new__(QdrantVectorStore)` to bypass `__init__`, then set
    `store._client = MagicMock()`, `store._Distance = MagicMock()`,
    `store._PointStruct = MagicMock()`, `store._VectorParams = MagicMock()`. Yield store.
    This avoids needing a real QdrantClient connection.

    Note (per D-13): do NOT add a `keywords` index assertion to PAYLOAD-02 acceptance criteria
    — it is an opportunistic improvement noted in RESEARCH.md Open Question 2, not a
    gating requirement.
  </action>

  <verify>
    <automated>uv run pytest tests/unit/test_qdrant_payload_indexes.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/unit/test_qdrant_payload_indexes.py --collect-only` collects 3 test items without import errors
    - Running the tests produces FAILURES (not errors), confirming RED state; specifically:
      TestEnsurePayloadIndexes fails with AttributeError or similar (method does not exist)
      TestEnsureAliasedCollectionCallsIndexes fails with AssertionError (call not wired yet)
      TestReindexCallsIndexes fails with AssertionError (call not wired yet)
    - No SyntaxError, no IndentationError, no missing-import errors on collection
  </acceptance_criteria>

  <done>
    tests/unit/test_qdrant_payload_indexes.py exists, is collected by pytest, all 3 tests fail
    in RED state. No import errors. Plan 03 will turn these GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → QdrantVectorStore | Tests instantiate QdrantVectorStore; _client is mocked — no real Qdrant connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-SC | Tampering | test file — no new packages | low | accept | No new pip/cargo installs in this plan; only stdlib unittest.mock + existing qdrant-client |
</threat_model>

<verification>
`uv run pytest tests/unit/test_qdrant_payload_indexes.py -v` — collects 3 tests, all FAIL (RED).
`uv run pytest tests/unit/ -v -q` — existing 11 tests still pass (Plan 01 adds only new failures).
</verification>

<success_criteria>
- tests/unit/test_qdrant_payload_indexes.py exists with 3 test classes
- pytest collects 3 test items; all fail in RED state
- No regressions in existing unit test suite
</success_criteria>

## Artifacts this phase produces

New files:
- `tests/unit/test_qdrant_payload_indexes.py` — stub test file with 3 test classes: `TestEnsurePayloadIndexes`, `TestEnsureAliasedCollectionCallsIndexes`, `TestReindexCallsIndexes`

New symbols:
- `mock_store` pytest fixture (local to this file)
- `TestEnsurePayloadIndexes.test_calls_create_payload_index_for_all_expected_fields`
- `TestEnsureAliasedCollectionCallsIndexes.test_calls_ensure_payload_indexes_on_new_physical`
- `TestReindexCallsIndexes.test_calls_ensure_payload_indexes_on_next_physical`

<output>
Create `.planning/phases/07-metadata-foundation/07-01-SUMMARY.md` when done.
</output>
