---
phase: 21-index-time-dedup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge_lake/config/settings.py
  - src/knowledge_lake/pipeline/dedup.py
  - tests/unit/test_index_dedup.py
autonomous: true
requirements: [DEDUP-01, DEDUP-02]

must_haves:
  truths:
    - "normalize_for_dedup(text) applies exactly three transformations in order — Unicode NFKC normalization, whitespace-run collapse (including newlines/tabs) to a single space, then strip — and nothing else (D-01): no casefolding, no punctuation stripping, no stopword removal (D-02), so 'WBC' and 'wbc' produce DIFFERENT normalized strings and therefore different text_sha256 values"
    - "normalize_for_dedup('') == '' and normalize_for_dedup('   \\n\\t  ') == '' — empty and whitespace-only input normalize to the empty string without raising (DEDUP-01 empty-input edge case)"
    - "text_sha256_for(text) == hashlib.sha256(normalize_for_dedup(text).encode('utf-8')).hexdigest() — UTF-8 encoded bytes of the NORMALIZED (not raw) string is the exact encoding/equality contract (DEDUP-01 encoding edge case, D-04)"
    - "point_id_for_text(text) is deterministic: calling it twice on identical text yields the identical bare-UUID string both times, and calling it on two byte-different-but-NFKC-equivalent strings (e.g. a precomposed vs decomposed accented character) also yields the identical point_id (DEDUP-02 idempotent re-index, D-06)"
    - "KLAKE_DEDUP_NAMESPACE is a module-level uuid.UUID constant, hardcoded at import time — never derived from Settings, an env var, or the collection name (D-05); two separate Python processes importing pipeline.dedup always see the same namespace value"
    - "point_id_for_text always returns a bare UUID string (str(uuid.UUID), no prefix, no underscore) — the exact form Qdrant's point-ID validation requires, needing no _strip_prefix()-style transformation"
  artifacts:
    - path: "src/knowledge_lake/config/settings.py"
      provides: "DedupSettings class + Settings.dedup field"
      contains: "class DedupSettings(BaseModel)"
    - path: "src/knowledge_lake/pipeline/dedup.py"
      provides: "KLAKE_DEDUP_NAMESPACE, normalize_for_dedup, text_sha256_for, point_id_for_text"
    - path: "tests/unit/test_index_dedup.py"
      provides: "Pure-function proof for normalize/hash/point-id determinism"
  key_links:
    - "point_id_for_text()/text_sha256_for() -> consumed by Plan 21-04's dedup_chunks() to annotate every chunk before ledger claim"
    - "DedupSettings.contributor_cap -> consumed by Plan 21-05's index() when capping the Qdrant contributors payload mirror"
  prohibitions:
    - statement: "MUST NOT reuse clean.py's _normalize_whitespace() for the dedup key (D-03) — that function is line-oriented (preserves single newlines, collapses 3+ blank lines to 2) and serves cleaned-text READABILITY, not exact-dedup-key equality. Coupling the two would let a future cosmetic cleaning tweak silently repartition the dedup space and desync ledger rows from what chunk() actually persisted."
      status: flagged-unverified
---

<objective>
Add `DedupSettings` (the one operator-tunable knob this phase needs: the Qdrant contributors-payload cap) and the three pure, zero-I/O functions that define DEDUP-01's exact-dedup key and DEDUP-02's deterministic point ID: `normalize_for_dedup`, `text_sha256_for`, `point_id_for_text`, plus the frozen `KLAKE_DEDUP_NAMESPACE` constant.

Purpose: These are the foundational, dependency-free primitives every other plan in this phase calls — the dedup router (21-04), index()'s duplicate routing (21-05), and both call-site wirings (21-06/21-07) all import from `pipeline/dedup.py`. Deliberately kept pure (no DB, no S3, no Dagster) so they are independently unit-testable with `expect(fn(input)).toBe(output)`-style assertions, mirroring Phase 19's `pipeline/quality/` zero-I/O convention.

Output: `pipeline/dedup.py` (new module, pure-function section only — the ledger-consuming `dedup_chunks()` router is Plan 21-04's job, added to this same file in Wave 2), `DedupSettings` wired into `Settings`, and `tests/unit/test_index_dedup.py` (new file — this is the SAME file RESEARCH.md's Validation Architecture designates for every DEDUP-01/02/03 unit test in this phase; later plans extend it, they do not replace it).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-index-time-dedup/21-CONTEXT.md
@.planning/phases/21-index-time-dedup/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DedupSettings model</name>
  <files>src/knowledge_lake/config/settings.py</files>
  <read_first>
    src/knowledge_lake/config/settings.py (lines 313-355 `ChunkQualitySettings` — the exact docstring/field shape to mirror for a small, single-purpose nested settings model; lines 706-745 for where nested-settings fields are registered on the `Settings` class, specifically the `mcp` field's position as the last nested-settings registration before the `# ── Validators ──` section)
  </read_first>
  <action>
    Add a new `DedupSettings(BaseModel)` class immediately after `ChunkQualitySettings` (after line 355) and before `class DatasetSettings(BaseModel):`. Docstring: "Index-time exact-dedup configuration (DEDUP-01..03). Nested under Settings as settings.dedup. Environment variable pattern: KLAKE_DEDUP__CONTRIBUTOR_CAP (resolved via the existing env_nested_delimiter='__')." One field: `contributor_cap: int = 50` with docstring "Maximum contributors mirrored into the Qdrant payload per point (D-23) — an unresearched round number, not a tuned threshold. The ledger (Postgres) always retains the FULL, unbounded contributor list (D-13); only the Qdrant payload mirror is capped, to bound payload growth on hot boilerplate text shared across many source documents. contributor_count in both the ledger and the Qdrant payload always reports the exact total regardless of this cap." Register it on the `Settings` class: add `dedup: DedupSettings = Field(default_factory=DedupSettings)` immediately after the existing `mcp: McpSettings = Field(default_factory=McpSettings)` field (before the `# ── Validators ──` comment), with a one-line docstring noting the env var pattern.
  </action>
  <acceptance_criteria>
    - `DedupSettings` class exists with exactly one field, `contributor_cap: int = 50`
    - `Settings(_env_file=None).dedup.contributor_cap == 50`
    - Setting `KLAKE_DEDUP__CONTRIBUTOR_CAP=10` as an environment variable and constructing `Settings()` yields `dedup.contributor_cap == 10`
    - No existing settings field is modified
  </acceptance_criteria>
  <verify>
    <automated>python -c "from knowledge_lake.config.settings import Settings; s=Settings(_env_file=None); assert s.dedup.contributor_cap==50; print('OK')"</automated>
  </verify>
  <done>DedupSettings exists with contributor_cap=50 default; Settings.dedup resolves correctly; env var override works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure dedup primitives — normalize_for_dedup, text_sha256_for, point_id_for_text</name>
  <files>src/knowledge_lake/pipeline/dedup.py, tests/unit/test_index_dedup.py</files>
  <read_first>
    src/knowledge_lake/pipeline/clean.py (lines 115-127 `_normalize_whitespace()` — read to understand exactly why D-03 forbids reusing it: it is line-oriented, preserving single newlines and collapsing 3+ blank lines to 2, a cosmetic-readability contract, not an exact-key contract)
    src/knowledge_lake/pipeline/quality/predicates.py (module docstring and any one `check_*` function — the zero-I/O pure-function style this new module's normalize/hash/id functions should match)
    src/knowledge_lake/ids.py (full file — confirms this codebase's convention of module-level constants and a single-purpose pure function per concern, the style `pipeline/dedup.py` should follow for its own constant/functions)
  </read_first>
  <behavior>
    - normalize_for_dedup("Hello   World\n\n\t") == "Hello World" (whitespace-run collapse + strip).
    - normalize_for_dedup("") == "" and normalize_for_dedup("   ") == "" (empty/whitespace-only -> empty string, no exception).
    - normalize_for_dedup("WBC") != normalize_for_dedup("wbc") (no casefolding, D-02).
    - A precomposed character (e.g. "é", é) and its NFKC-equivalent decomposed form (e.g. "é") normalize to the IDENTICAL string via NFKC (D-01 encoding contract).
    - text_sha256_for(text) exactly equals `hashlib.sha256(normalize_for_dedup(text).encode("utf-8")).hexdigest()` for several distinct inputs, confirmed by direct recomputation in the test (not just structural mirroring of the implementation).
    - point_id_for_text(text) is a valid `str(uuid.UUID(...))` (parseable back into a UUID, exactly 36 characters with hyphens, no prefix).
    - point_id_for_text(text_a) == point_id_for_text(text_b) whenever text_sha256_for(text_a) == text_sha256_for(text_b), and only then (two texts differing only in a way normalize_for_dedup neutralizes produce the SAME point_id; two texts differing in a way it does NOT neutralize, e.g. casing, produce DIFFERENT point_ids).
    - Calling point_id_for_text on the same text twice across two separate calls (simulating two separate process_crawled runs) yields the identical value (DEDUP-02 idempotent re-index).
  </behavior>
  <action>
    Create `src/knowledge_lake/pipeline/dedup.py` as a new module. Module docstring: explain this is the index-time EXACT dedup module (DEDUP-01/02/03), that it is deliberately NOT a near-duplicate/MinHash module (that already exists corpus-wide in `pipeline/curate.py`'s pretrain path — CLEAN-03 — and is a wholly separate concern; do not conflate this module's tests with `tests/unit/test_dedup.py`, which already covers MinHash/`remove_boilerplate` and is unrelated to this phase). Imports: `from __future__ import annotations`, `import hashlib`, `import unicodedata`, `import uuid`. Define `KLAKE_DEDUP_NAMESPACE = uuid.UUID("94eca03b-54f1-4438-a007-2f835b9d2c07")` (this exact literal — generated once for this plan via Python's `uuid.uuid4()`, never regenerate it) with a docstring/comment: "Frozen namespace for uuid5 point-ID derivation (D-05). NEVER derive this from settings, an env var, or the collection name — changing it would silently orphan every previously-indexed point (a different namespace produces entirely different uuid5 outputs for the same text_sha256 input)."

    Define `normalize_for_dedup(text: str) -> str`: docstring citing D-01/D-02/D-03 (the exact contract: NFKC normalize, collapse all whitespace runs to a single space, strip; deliberately does NOT casefold/strip punctuation/remove stopwords — this is EXACT dedup, not near-dup; deliberately does NOT reuse `clean.py`'s `_normalize_whitespace()`, which is line-oriented and serves cleaned-text readability, not an exact-key contract — coupling the two would let a cosmetic cleaning tweak silently repartition the dedup key space). Implementation: `normalized = unicodedata.normalize("NFKC", text)`, `collapsed = " ".join(normalized.split())`, `return collapsed.strip()`.

    Define `text_sha256_for(text: str) -> str`: docstring citing D-04 (computed from the chunk's text field only — never section_path/page/any per-document field, since those would defeat cross-document dedup). Implementation: `return hashlib.sha256(normalize_for_dedup(text).encode("utf-8")).hexdigest()`.

    Define `point_id_for_text(text: str) -> str`: docstring citing D-06 (uuid5 name input is the 64-char hex digest STRING, matching DEDUP-02's literal formulation; returns the bare-UUID string form Qdrant requires — no `_strip_prefix()`-style transformation needed). Implementation: `return str(uuid.uuid5(KLAKE_DEDUP_NAMESPACE, text_sha256_for(text)))`.

    Create `tests/unit/test_index_dedup.py` as a new file (this file will be extended by later plans in this phase — Wave 2's `dedup_chunks()` tests, Wave 3's `index()` duplicate-routing tests — so structure it with clear class-per-concern sections, e.g. `class TestNormalizeForDedup`, `class TestTextSha256For`, `class TestPointIdForText`, leaving room for later classes to be appended, not interleaved). Cover every item in `<behavior>` above. No fixtures, no mocking, no DB — these are pure function calls against literal string inputs.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_index_dedup.py -x` passes
    - `pipeline/dedup.py` has zero imports of `knowledge_lake.registry`, `knowledge_lake.config.settings`, or any Dagster/S3/DB module at this point in the file (the pure-function section only — settings/DB imports are added by Plan 21-04 in Wave 2 for the `dedup_chunks()` router)
    - `KLAKE_DEDUP_NAMESPACE == uuid.UUID("94eca03b-54f1-4438-a007-2f835b9d2c07")`, a literal constant, not derived from any function call or import
    - `normalize_for_dedup`, `text_sha256_for`, `point_id_for_text` are all present with the exact signatures given above
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_index_dedup.py -x</automated>
  </verify>
  <done>pipeline/dedup.py exists with a frozen KLAKE_DEDUP_NAMESPACE and three pure functions (normalize_for_dedup, text_sha256_for, point_id_for_text), each proven deterministic and edge-case-safe by tests/unit/test_index_dedup.py with zero DB/I/O dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Chunk text (already substance-gated by Phase 20) -> normalize_for_dedup/hashing | Internal pipeline data; no new externally-controlled input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-21-04 | Tampering | KLAKE_DEDUP_NAMESPACE accidentally changed in a future edit | high | mitigate | Hardcoded literal constant with an explicit comment warning against deriving it from settings/env/collection; a namespace change silently orphans every prior indexed point — the docstring makes this consequence explicit at the exact point a future editor would be tempted to "parameterize" it. |
| T-21-05 | Information Disclosure | Cryptographic hash used as content-addressing, not secrecy | low | accept | `hashlib.sha256`/`uuid.uuid5` here are content-addressing primitives (V6 applies as "cryptography," not confidentiality) — no key material, no secret hashing; SHA-256 collision resistance is far beyond what corpus-wide exact-text dedup needs. |
| T-21-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs — `hashlib`/`unicodedata`/`uuid` are Python stdlib (RESEARCH.md Package Legitimacy Audit: not applicable). |

</threat_model>

## Artifacts This Phase Produces (this plan)

- `src/knowledge_lake/config/settings.py` — new `DedupSettings` class + `Settings.dedup` field
- `src/knowledge_lake/pipeline/dedup.py` — new module: `KLAKE_DEDUP_NAMESPACE`, `normalize_for_dedup()`, `text_sha256_for()`, `point_id_for_text()` (pure-function section; `dedup_chunks()` router added by Plan 21-04)
- `tests/unit/test_index_dedup.py` — new test file (pure-function tests only; extended by Plans 21-04 and 21-05)

<verification>
Run `uv run pytest tests/unit/test_index_dedup.py -x` — all green. Run `python -c "from knowledge_lake.config.settings import Settings; print(Settings(_env_file=None).dedup.contributor_cap)"` — prints `50`.
</verification>

<success_criteria>
`pipeline/dedup.py` exists with a frozen dedup namespace and three deterministic pure functions covering DEDUP-01's exact-hash key derivation and DEDUP-02's point-ID determinism; `DedupSettings.contributor_cap` is wired into `Settings.dedup`.
</success_criteria>

<output>
Create `.planning/phases/21-index-time-dedup/21-02-SUMMARY.md` when done
</output>
</content>
