---
phase: 22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified: []
autonomous: true
requirements: [MEAS-01, EXPORT-01]

must_haves:
  truths:
    - "A real klake quality-audit --domain healthcare --full run against the live 34-source healthcare corpus produces actual kept/rejected chunk counts and an export-junk count, reported against the 28%/33% baselines from MILESTONE-CONTEXT.md"
    - "If chunk_garbage_rate and export_junk_rate do not converge as expected (export_junk_rate should be <= chunk_garbage_rate per RESEARCH.md Pitfall 2/Assumption A1), the anomaly is explicitly flagged and investigated, not silently reported"
    - "Nyquist reconciliation for phases 17-21 is explicitly logged as an operator follow-up (/gsd-validate-phase), never implemented as phase-22 code"
  artifacts:
    - path: ".planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-03-SUMMARY.md"
      provides: "Narrative + raw JSON capture of the real measurement run's numbers, the before/after comparison, and the Nyquist operator follow-up note"
  key_links:
    - from: "operator/executor terminal"
      to: "uv run klake quality-audit --domain healthcare --full --json"
      via: "direct CLI invocation against the live docker-compose stack (postgres, minio) built in Plans 22-01/22-02"
      pattern: "quality-audit.*--full"
---

<objective>
Produce the actual, real-data deliverable of this phase: run the chunk-level/export-level measurement (Plans 22-01/22-02) against the live dev stack's 34 real healthcare sources, and record the genuine before/after comparison against the milestone's 28%-garbage / 33%-junk baselines. This is the manual-only verification called out in `22-VALIDATION.md` — "the actual deliverable of the phase, not incidental verification." Also closes out D-08/D-09 by explicitly documenting Nyquist reconciliation as an operator follow-up rather than phase code.

Purpose: Close tech-debt item #1 from `.planning/v2.6-MILESTONE-AUDIT.md` with a real, reproducible number — not just working code.
Output: A real `klake quality-audit --full` run's captured JSON output, a computed before/after comparison, and this plan's `22-03-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/MILESTONE-CONTEXT.md
@.planning/v2.6-MILESTONE-AUDIT.md
@.planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-CONTEXT.md
@.planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-RESEARCH.md
@.planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-01-SUMMARY.md
@.planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real measurement against the live 34-source healthcare corpus and record results</name>
  <files></files>
  <read_first>
    .planning/MILESTONE-CONTEXT.md (§D-2 — the original 34-source/4,499-chunk/28%-garbage/33%-junk baseline evidence table this run compares against)
    .planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-CONTEXT.md (D-02, D-04, D-05, D-06 — full 34/34 sources, side-by-side baseline reporting)
    .planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-RESEARCH.md (Pitfall 2 / Assumption A1 — the expected export_junk_rate <= chunk_garbage_rate convergence, and what an anomaly would mean)
    .planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-01-SUMMARY.md, 22-02-SUMMARY.md (confirm the actual shipped CLI invocation and return-shape before running it)
  </read_first>
  <action>
    Confirm the live dev stack is reachable: `docker compose ps` should show `postgres` and `minio` (and ideally all services) in a healthy/running state — this was confirmed live during this phase's `/gsd-discuss-phase` session and independently confirmed live during planning. If either service is down, run `docker compose up -d postgres minio` (or the full stack) and re-check before proceeding; do not attempt a `docker compose down -v` wipe (explicitly out of scope, D-2/deferred). Run `uv run klake quality-audit --domain healthcare --full --json` from the project root — no `--limit`/subset flag, the full 34/34 healthcare sources per D-05 (matching Phase 17's D-06-established `quality-audit` scope for a consistent before/after baseline) — (confirmed working invocation form — `uv run klake quality-audit --help` succeeds against this environment) and capture its full stdout. This single run reprocesses all 34 healthcare sources through `parse` (reusing existing parsed children where present) `→ clean → chunk → export_rag_corpus`, per D-02/D-03; expect it to take longer than a typical unit test (real S3/MinIO and Postgres I/O across ~34 sources) — allow it to run to completion rather than treating a multi-minute runtime as a hang. Parse the captured JSON's `summary` object and compute: `chunk_garbage_rate` vs `baseline_chunk_garbage_rate` (0.28) and `export_junk_rate` vs `baseline_export_junk_rate` (0.33), both as absolute and relative deltas. Verify the Pitfall-2/Assumption-A1 expectation: `export_junk_rate` should be <= `chunk_garbage_rate` (enforce-mode `chunk()` already filters garbage before any chunk artifact is created, so export-time rejection of THIS run's own chunks should be at or near zero) — if `export_junk_rate` is instead HIGHER than `chunk_garbage_rate`, this is a genuine anomaly (per RESEARCH.md's named risk: a `chunk.storage_uri` read failure falling back to empty text at export time) and must be called out explicitly in the summary, not silently reported as a normal result. Also run `uv run klake quality-audit --domain healthcare --full` (table mode, no `--json`) once for a human-readable capture. Write a narrative into this plan's own `22-03-SUMMARY.md` (per the standard execute-plan summary template) covering: the raw captured JSON (or the key summary fields verbatim), the before/after comparison for both milestone success criteria (chunk garbage rate 28% → X%, export junk rate 33% → Y%), whether the milestone's <5%/<2% targets were met by this run, and the Pitfall-2 convergence check's result. If the live stack is unreachable and cannot be brought up within this task, do NOT fail the phase — instead record in `22-03-SUMMARY.md`, explicitly and prominently, that the live run did not execute, the exact command an operator must run later (`uv run klake quality-audit --domain healthcare --full --json`), and that the milestone's quantitative criteria remain unmeasured until that command is run.
  </action>
  <verify>
    <automated>pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - Either (a) `uv run klake quality-audit --domain healthcare --full --json` was executed against the live stack and its captured `summary` JSON (chunk_garbage_rate, export_junk_rate, and both baselines) is reproduced verbatim in `22-03-SUMMARY.md`, or (b) the live stack was unavailable and `22-03-SUMMARY.md` explicitly documents this as an unmet operator follow-up with the exact re-run command — one of these two outcomes MUST be true, never silence.
    - If (a): the summary explicitly states whether `export_junk_rate <= chunk_garbage_rate` held (Pitfall 2/Assumption A1) — if it did not hold, the anomaly is flagged with a plausible cause, not glossed over.
    - `pytest -x` (full existing suite, 1176+ baseline per PROJECT.md) passes — this phase introduced no regression across Plans 22-01/22-02's code changes.
  </acceptance_criteria>
  <done>The real chunk-level garbage rate and export-level junk rate for the 34 healthcare sources are captured and compared against the 28%/33% baselines in 22-03-SUMMARY.md (or the live-stack-unavailable fallback is explicitly documented); full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Executor terminal → live dev stack | This task only invokes CLI surfaces already threat-modeled and mitigated in Plans 22-01 (T-22-01 through T-22-05-SC) and 22-02 (T-22-06 through T-22-08-SC) — no new code, no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Repudiation | Real `export_rag_corpus()` write during this run (not a dry-run — writes a new gold Parquet + dataset row every invocation, Pitfall 3) | low | accept | Expected/documented behavior, unchanged from the pre-existing `export_rag_corpus()` contract — this task does not introduce a new write path, it invokes the existing one once as designed (D-03). |
| T-22-10-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — this task only runs the already-shipped `klake` CLI. |
</threat_model>

<operational_followup>
**D-08/D-09 — Nyquist reconciliation is NOT part of this phase's coded work.** After this plan executes, the operator (not this plan) should run `/gsd-validate-phase 17`, `/gsd-validate-phase 18`, `/gsd-validate-phase 19`, `/gsd-validate-phase 20`, and `/gsd-validate-phase 21` to reconcile each phase's `VALIDATION.md` `status`/`nyquist_compliant` fields from their pre-execution seed state to their true post-execution state (per `.planning/v2.6-MILESTONE-AUDIT.md`'s Nyquist Coverage finding). Per D-09, if any of those five invocations surfaces genuine missing test coverage (as opposed to a merely stale `status:` field), the decision to fold the fix into a new quick task or a phase-22 follow-up plan is deliberately deferred to that moment — do not pre-decide it here.
</operational_followup>

<verification>
`pytest -x` (full existing suite) must be green after this task. The primary verification is the real captured measurement itself — see acceptance_criteria above.
</verification>

<success_criteria>
- A real, reproducible chunk-level garbage rate and export-level junk rate for the 34 healthcare sources exist, reported against the 28%/33% baselines (or the unavailable-stack fallback is explicitly documented).
- Nyquist reconciliation is logged as an operator follow-up, not coded.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-03-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

- No new source symbols — this plan produces `.planning/phases/22-address-tech-debt-measure-garbage-junk-rates-end-to-end-reco/22-03-SUMMARY.md` (the real measurement run's narrative and captured numbers) as its sole artifact.
