---
phase: "13"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_tree_index.py
  - tests/unit/test_tree_index_asset.py
  - tests/unit/test_builtin_plugins.py
autonomous: true
requirements:
  - TREE-01
  - TREE-02
  - TREE-03
  - TREE-04
  - TREE-05

must_haves:
  truths:
    - "tests/unit/test_tree_index.py exists and is collected by pytest with all Wave-0 test functions present"
    - "tests/unit/test_tree_index_asset.py exists and is collected by pytest"
    - "test_builtin_plugins.py contains IndexerPlugin and PageIndexIndexer conformance stubs that fail with ImportError until implementation ships"
    - "A multi-section ParsedDoc fixture (sections §1, §1.1, §2, plus is_table=True) is defined and available to both test files"
    - "In-memory SQLite fixture mirrors test_enrich.py:45-68 (StaticPool, _patch_engine autouse, Base.metadata.create_all)"
  artifacts:
    - tests/unit/test_tree_index.py
    - tests/unit/test_tree_index_asset.py
  key_links:
    - "test_tree_index.py imports from knowledge_lake.pipeline.tree_index (module does not exist yet — tests will fail with ImportError, which is the correct RED state)"
    - "test_builtin_plugins.py stub imports IndexerPlugin from protocols.py and PageIndexIndexer from pageindex_indexer.py (both absent until Wave 1/2)"
---

<objective>
Create the Wave 0 test scaffold for Phase 13. All Wave 1 and 2 implementation tasks reference specific test functions in these files as their automated verify commands. Tests must be collectible by pytest but fail with ImportError or assertion failures (RED state) until the corresponding implementation tasks complete.

Purpose: Nyquist compliance — every implementation task has an automated verify target before code is written. Unblocks parallel Wave 1 plans 13-02 and 13-03.

Output: tests/unit/test_tree_index.py (covers TREE-01..04), tests/unit/test_tree_index_asset.py (covers TREE-05), and an IndexerPlugin conformance stub appended to tests/unit/test_builtin_plugins.py.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-tree-index-foundation/13-CONTEXT.md
@.planning/phases/13-tree-index-foundation/13-RESEARCH.md
@.planning/phases/13-tree-index-foundation/13-VALIDATION.md
@.planning/phases/13-tree-index-foundation/13-PATTERNS.md
</context>

<tasks>

<task id="T1" type="auto" tdd="true">
  <name>Task 1: Create tests/unit/test_tree_index.py — TREE-01..04 scaffold</name>
  <files>tests/unit/test_tree_index.py</files>
  <read_first>
    - tests/unit/test_enrich.py — verbatim fixture patterns: in-memory SQLite (lines 45-60), _patch_engine autouse (63-68), seeded Source→raw→parsed chain (77-104), fake_storage (107-113), parsed_doc fixture (116-122), mock LLM (130-134,163-164), no-hardcoded-model test (140-152)
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §tests/unit/test_tree_index.py for exact fixture shapes
    - .planning/phases/13-tree-index-foundation/13-VALIDATION.md — Per-Task Verification Map table (exact test function names and expected behaviors)
  </read_first>
  <behavior>
    - test_deterministic_tree_from_sections: given a multi-section ParsedDoc (§1 p1, §1.1 p2, §2 p4, §2 is_table=True p5), calling tree_index() produces a TreeIndex whose root node for §1 has level=1, page_start=1, page_end=3, summary==heading, and child node §1.1 has level=2, page_start=2, page_end=3; §2 is a top-level leaf with is_table metadata; artifact row registered with artifact_type="tree_index" and storage_uri set
    - test_tree_storage_key: put_object called with key matching pattern `tree_index/{domain}/{source_id}/{hash}.json`; object tags include domain, source_name, format="json", artifact_type="tree_index"
    - test_content_hash_noop: second call with identical parsed_artifact_id + mode returns result with cached=True; put_object call count does not increase on second call; litellm.completion is not called on second call in LLM mode
    - test_node_fields_and_fallback: deterministic mode node.summary == node.title == section.heading; ParsedDoc with empty sections list produces a single root node (title from metadata or "§1"); every node has page_start, page_end, level, section_path populated
    - test_llm_mode_budget_cap: after seeding record_llm_spend(session, "global", budget_usd) to fill the budget, tree_index(mode="llm") returns {"status": "skipped_budget_exceeded"} with no artifact written and litellm.completion call count == 0; happy-path LLM mode with mock litellm.completion returns populated summaries, record_llm_spend is called once, result contains cost_usd
    - test_no_hardcoded_provider_model_ids: inspect.getsource(tree_index_module) confirms the string "openai/cheap_model" pattern is present (or model kwarg == "openai/cheap_model" on mock call); no hardcoded provider model IDs (no patterns like gpt-4, claude-3, anthropic/, bedrock/) present in module source
  </behavior>
  <action>
    Create `tests/unit/test_tree_index.py` as a NEW file. Do not modify any existing file in this task.

    Structure the file in four sections, mirroring test_enrich.py exactly:

    Section 1 — Module import and aliases. Import tree_index pipeline module with an alias (e.g., `import knowledge_lake.pipeline.tree_index as tree_index_module`). This will raise ImportError during collection until Plan 13-04 ships; that ImportError is the correct RED state for Wave 0.

    Section 2 — Fixtures (reuse test_enrich.py verbatim where possible):
    - `engine` fixture (session-scoped, StaticPool in-memory SQLite, Base.metadata.create_all) — verbatim from test_enrich.py:45-60
    - `_patch_engine` autouse fixture (monkeypatch registry_db.get_engine to return the in-memory engine) — verbatim from test_enrich.py:63-68
    - `seeded` fixture: create a Source row, a RawDocument artifact row, and a ParsedDocument artifact row with content_hash="abc123"; use orm helpers from registry.repo; yield the fixture's parsed_artifact_id
    - `fake_storage` fixture: creates a FakeStorage object with put_object(key, data, tags=None) recording calls and get_object returning empty bytes; patches StorageBackend at module level via `monkeypatch.setattr(tree_index_module, "StorageBackend", lambda *_a, **_k: fake)`
    - `multi_section_doc` fixture: construct a ParsedDoc with metadata={"title": "Test Doc", "page_count": 6} and four Section objects in this order: Section(heading="Introduction", section_path="§1", page=1), Section(heading="Background", section_path="§1.1", page=2), Section(heading="Results", section_path="§2", page=4), Section(heading="Table 1", section_path="§2.1", page=5, is_table=True). This fixture exercises nesting (§1.1 is child of §1), page_end derivation, and table leaf handling.

    Section 3 — Test class for deterministic mode: TestDeterministicTree. Implement six test methods matching the behavior block above. Each test calls tree_index(parsed_artifact_id, source_id, multi_section_doc, settings=Settings(..., _env_file=None)) with mode="deterministic" (via TreeSettings). For model assertions use the TreeIndex and TreeNode dataclasses imported from knowledge_lake.plugins.protocols.

    Section 4 — Test class for LLM mode: TestLlmMode. Implement test_llm_mode_budget_cap and test_no_hardcoded_provider_model_ids. For LLM mock, patch litellm.completion to return a MagicMock with usage.total_cost=0.001 and choices[0].message.content as a JSON string with a "summary" key. For budget-cap test, seed the LLM spend to the configured budget_usd before calling tree_index with mode="llm".

    Do not add fenced code blocks inside action text. Do not implement logic yet — the import at the top will cause collection failure in RED state, which is the intended Wave 0 behavior.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_tree_index.py --collect-only 2>&1 | grep -c 'test_deterministic_tree_from_sections\|test_tree_storage_key\|test_content_hash_noop\|test_node_fields_and_fallback\|test_llm_mode_budget_cap\|test_no_hardcoded_provider_model_ids'` returns 6 (all six test functions visible in collection output even if import fails at module level — use `--collect-only` on the file which should enumerate names from AST even with import errors in some pytest versions; alternatively, grep the file: `grep -c 'def test_' tests/unit/test_tree_index.py` returns 6)
    - File exists: `test -f tests/unit/test_tree_index.py` exits 0
    - `grep -c 'multi_section_doc\|is_table=True\|page_count' tests/unit/test_tree_index.py` returns 3 (fixture and its fields present)
    - `grep -c 'StaticPool\|_patch_engine\|fake_storage' tests/unit/test_tree_index.py` returns 3
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/unit/test_tree_index.py && grep -c 'def test_' tests/unit/test_tree_index.py | grep -E '^[6-9]|^[1-9][0-9]'</automated>
  </verify>
  <done>tests/unit/test_tree_index.py exists with at least 6 test function stubs covering TREE-01..04, the multi-section ParsedDoc fixture, in-memory SQLite engine fixture, and fake_storage fixture.</done>
</task>

<task id="T2" type="auto">
  <name>Task 2: Create tests/unit/test_tree_index_asset.py and extend test_builtin_plugins.py</name>
  <files>tests/unit/test_tree_index_asset.py, tests/unit/test_builtin_plugins.py</files>
  <read_first>
    - tests/unit/test_dagster_retry_policies.py — asset unit test pattern (mock resources, call asset function directly with dict input)
    - tests/unit/test_dagster_e2e_job.py — fan-out asset structure and clean_document dict shape
    - tests/unit/test_builtin_plugins.py — existing structure: entry-point registration tests, isinstance conformance tests (lines 1-50 for imports and TestEntryPointRegistrations shape)
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §dagster_defs/assets.py for the tree_index_document function signature and clean_document dict shape
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §tests/unit/test_tree_index.py for asset test pattern notes
  </read_first>
  <action>
    Task A — Create `tests/unit/test_tree_index_asset.py` as a NEW file.

    The asset test must verify TREE-05: tree_index_document asset is a thin shell over pipeline.tree_index.tree_index(), takes clean_document dict input, returns the pipeline result dict, and runs in parallel to chunk_document (same clean_document input shape).

    Import `tree_index_document` from knowledge_lake.dagster_defs.assets (ImportError is the RED state until Plan 13-06). Import `tree_index` from knowledge_lake.pipeline.tree_index (also RED until Plan 13-04). Structure one test class TestTreeIndexDocumentAsset with two test methods:
    - test_asset_calls_pipeline: constructs a fake clean_document dict with keys parsed_artifact_id, source_id, parsed_doc (a minimal ParsedDoc), and calls the asset function directly with mock Postgres/MinIO/LiteLLM resources. Patches pipeline.tree_index.tree_index to return a known result dict. Asserts the asset returns the same dict (thin shell — no added logic).
    - test_asset_input_shape_matches_chunk_document: asserts that tree_index_document's function signature accepts clean_document as its first parameter, by importing the function and inspecting its parameter names via inspect.signature. This verifies the fan-out shape matches chunk_document.

    Task B — Extend `tests/unit/test_builtin_plugins.py` (MODIFY existing file).

    Read the full existing file first. Append at the end of the file:
    1. Add `IndexerPlugin` to the import block from knowledge_lake.plugins.protocols (add it to the existing import tuple — it does not exist yet, so this triggers ImportError in RED state).
    2. Add a new test class TestIndexerPluginBuiltin with two test methods:
       - test_pageindex_indexer_entry_point_registered: calls self._get_ep_names("knowledge_lake.indexers") and asserts "pageindex" is in the result (mirrors test_vectorstores_group_has_qdrant pattern).
       - test_pageindex_indexer_satisfies_protocol: imports PageIndexIndexer from knowledge_lake.plugins.builtin.pageindex_indexer, constructs an instance, and asserts isinstance(instance, IndexerPlugin) is True.

    Do not modify any other test in the file. Preserve all existing test classes and imports.
  </action>
  <acceptance_criteria>
    - `test -f tests/unit/test_tree_index_asset.py` exits 0
    - `grep -c 'def test_' tests/unit/test_tree_index_asset.py` returns 2
    - `grep -c 'test_pageindex_indexer_entry_point_registered\|test_pageindex_indexer_satisfies_protocol\|IndexerPlugin' tests/unit/test_builtin_plugins.py` returns 3 (both new test function names and the IndexerPlugin import present)
    - Existing test count in test_builtin_plugins.py has not decreased: `grep -c 'def test_' tests/unit/test_builtin_plugins.py` returns the original count plus 2 new functions
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/unit/test_tree_index_asset.py && grep -c 'def test_' tests/unit/test_tree_index_asset.py | grep '^2$' && grep -c 'IndexerPlugin' tests/unit/test_builtin_plugins.py | grep -E '^[1-9]'</automated>
  </verify>
  <done>test_tree_index_asset.py created with 2 test stubs; test_builtin_plugins.py extended with IndexerPlugin import and 2 new conformance test stubs for TestIndexerPluginBuiltin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → in-memory SQLite | Test DB is a fake; no production data accessible |
| test harness → FakeStorage | Storage is mocked; no real S3 calls in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | test fixture DB setup | low | accept | Tests use StaticPool in-memory SQLite (verbatim from test_enrich.py); no connection to production Postgres; accept at ASVS L1 |
| T-13-02 | Information Disclosure | fake_storage in tests | low | accept | FakeStorage stores bytes in memory only, never writes to disk or S3; test isolation verified by StaticPool pattern |
</threat_model>

<verification>
After both tasks complete:
- `grep -c 'def test_' tests/unit/test_tree_index.py` returns 6 or more
- `grep -c 'def test_' tests/unit/test_tree_index_asset.py` returns 2
- `grep -c 'IndexerPlugin' tests/unit/test_builtin_plugins.py` returns at least 1
- `pytest tests/unit/test_tree_index.py --collect-only 2>&1 | grep ERROR` should show ImportError (correct RED state)
</verification>

<success_criteria>
Wave 0 is complete when all three test files exist and are parseable by pytest (collect-only works even if tests fail due to ImportError). Every subsequent Wave 1 and Wave 2 implementation task has a concrete pytest command it can run to verify RED→GREEN state transitions.
</success_criteria>

## Artifacts this phase produces (Wave 0 contribution)

New files:
- `tests/unit/test_tree_index.py` — test scaffold for TREE-01..04
- `tests/unit/test_tree_index_asset.py` — test scaffold for TREE-05

Modified files:
- `tests/unit/test_builtin_plugins.py` — adds `IndexerPlugin` import stub + `TestIndexerPluginBuiltin` class with 2 test methods

New test functions (stubs):
- `TestDeterministicTree::test_deterministic_tree_from_sections`
- `TestDeterministicTree::test_tree_storage_key`
- `TestDeterministicTree::test_content_hash_noop`
- `TestDeterministicTree::test_node_fields_and_fallback`
- `TestLlmMode::test_llm_mode_budget_cap`
- `TestLlmMode::test_no_hardcoded_provider_model_ids`
- `TestTreeIndexDocumentAsset::test_asset_calls_pipeline`
- `TestTreeIndexDocumentAsset::test_asset_input_shape_matches_chunk_document`
- `TestIndexerPluginBuiltin::test_pageindex_indexer_entry_point_registered`
- `TestIndexerPluginBuiltin::test_pageindex_indexer_satisfies_protocol`

<output>
Create `.planning/phases/13-tree-index-foundation/13-01-SUMMARY.md` when done
</output>
