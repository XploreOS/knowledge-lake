---
phase: "13"
plan: "03"
type: execute
wave: 1
depends_on:
  - "13-01"
files_modified:
  - src/knowledge_lake/registry/repo.py
autonomous: true
requirements:
  - TREE-01
  - TREE-02

must_haves:
  truths:
    - "create_tree_index_artifact(session, source_id=..., parent_artifact_id=..., content_hash=...) creates an Artifact row with artifact_type='tree_index' and the correct lineage parent"
    - "The artifact row produced has storage_uri set when passed"
    - "Calling new_id('tree_index') inside create_tree_index_artifact does not raise (requires Plan 13-02 T1 to run first, but can proceed in parallel since ids.py change is trivial and its own task)"
  artifacts:
    - src/knowledge_lake/registry/repo.py
  key_links:
    - "create_tree_index_artifact wraps _make_artifact — no direct Artifact() construction, no Alembic migration"
    - "parent_artifact_id = parsed_artifact_id (D-07 lineage: tree_index parents off parsed_document)"
  prohibitions:
    - "Do NOT add an Alembic migration — artifact_type is a free-form String column"
    - "Do NOT construct Artifact() directly — always use _make_artifact"
    - "Do NOT modify any existing function in repo.py"
---

<objective>
Wave 1 registry helper: add create_tree_index_artifact() to repo.py. This is a direct clone of create_chunk_artifact (repo.py:270-300) with tree_index substituted for chunk. Enables the tree builder (Plan 13-04) to register artifacts without duplicating code.

Purpose: Without this helper, the tree builder would have to call _make_artifact directly (breaking the established wrapper contract). This is a 15-line addition that mirrors an existing pattern exactly.

Output: One new function appended to repo.py after create_chunk_artifact.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-tree-index-foundation/13-CONTEXT.md
@.planning/phases/13-tree-index-foundation/13-PATTERNS.md
@src/knowledge_lake/registry/repo.py
</context>

<tasks>

<task id="T1" type="auto" tdd="true">
  <name>Task 1: repo.py: add create_tree_index_artifact() helper (mirrors create_chunk_artifact)</name>
  <files>src/knowledge_lake/registry/repo.py</files>
  <read_first>
    - src/knowledge_lake/registry/repo.py — read lines 270-305 (create_chunk_artifact) as the exact template to clone; also read _make_artifact (lines 119-145) to understand what it stamps (pipeline_version, created_at, new_id(kind)); read get_artifact_by_hash (lines 306-335) to confirm the "tree_index" artifact_type lookup pattern
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §repo.py — exact function signature and docstring
    - .planning/phases/13-tree-index-foundation/13-CONTEXT.md D-07 — lineage parent = parsed_document artifact
  </read_first>
  <behavior>
    - create_tree_index_artifact(session, source_id="src_x", parent_artifact_id="doc_y", content_hash="abc") adds an Artifact row to session with artifact_type="tree_index", source_id="src_x", parent_artifact_id="doc_y", content_hash="abc"
    - Returned artifact has an id starting with "idx_" (from new_id("tree_index") after Plan 13-02 T1)
    - storage_uri and metadata are passed through to _make_artifact when provided
    - mime_type defaults to "application/json"
    - Calling get_artifact_by_hash(session, "abc", "tree_index") after flush returns the same artifact (content_hash + artifact_type dedup works)
    - No existing repo.py functions are changed (regression: create_chunk_artifact still works)
  </behavior>
  <action>
    Read repo.py fully (or at minimum lines 119-145 and 270-305). After the closing line of create_chunk_artifact, insert the new function create_tree_index_artifact.

    The function signature is:
    create_tree_index_artifact(session: Session, *, source_id: str, parent_artifact_id: str, content_hash: str, storage_uri: str | None = None, mime_type: str | None = "application/json", metadata: Any | None = None) -> Artifact

    Docstring: "Persist a tree_index artifact node with full lineage. parent_artifact_id must be the parsed_document artifact ID (D-07 lineage). Zero Alembic migration required — artifact_type is a free-form String."

    Body: call _make_artifact with keyword arguments kind="tree_index", source_id=source_id, artifact_type="tree_index", content_hash=content_hash, storage_uri=storage_uri, parent_artifact_id=parent_artifact_id, mime_type=mime_type, metadata=metadata. Assign the result to art. Call session.add(art). Return art.

    This is an exact structural clone of create_chunk_artifact with "chunk" replaced by "tree_index" and mime_type default changed to "application/json" (chunks are plain text; tree indexes are JSON). No other deviation from the pattern.
  </action>
  <acceptance_criteria>
    - `grep -c 'def create_tree_index_artifact' src/knowledge_lake/registry/repo.py` returns 1
    - `python -c "from knowledge_lake.registry.repo import create_tree_index_artifact; print('OK')"` exits 0 (when run after Plan 13-02 completes)
    - `grep -c '_make_artifact' src/knowledge_lake/registry/repo.py` increases by 1 compared to before (new call inside create_tree_index_artifact)
    - `pytest tests/unit/test_tree_index.py::test_tree_storage_key -x 2>&1 | grep -v 'ImportError'` — after Plan 13-04 ships, this test passes
  </acceptance_criteria>
  <verify>
    <automated>cd /root/healthlake && grep -c 'def create_tree_index_artifact' src/knowledge_lake/registry/repo.py | grep '^1$'</automated>
  </verify>
  <done>create_tree_index_artifact() is present in repo.py, calls _make_artifact with artifact_type="tree_index", and session.add()s the result. No existing functions modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → registry | Artifact writes use ORM; no raw SQL injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | create_tree_index_artifact content_hash | low | accept | content_hash is a sha256 hex string generated internally by the pipeline; not derived from attacker-controlled input directly; accept at ASVS L1 |
| T-13-06 | Information Disclosure | parent_artifact_id lineage | low | accept | parent_artifact_id comes from the Dagster asset context (parsed_artifact_id); internal pipeline only; no user-supplied value |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs NO new packages — single function addition to existing file |
</threat_model>

<verification>
After task completes:
- `grep -c 'def create_tree_index_artifact' src/knowledge_lake/registry/repo.py` returns 1
- `python -c "from knowledge_lake.registry.repo import create_chunk_artifact, create_tree_index_artifact; print('both imported OK')"` exits 0
- `pytest tests/unit/ -q -k 'not test_tree_index and not test_tree_index_asset' 2>&1 | tail -3` — all pre-existing tests still pass
</verification>

<success_criteria>
create_tree_index_artifact() is importable from knowledge_lake.registry.repo, mirrors create_chunk_artifact structurally, and the pre-existing test suite passes without regressions.
</success_criteria>

## Artifacts this phase produces (Wave 1 registry helper)

Modified files:
- `src/knowledge_lake/registry/repo.py` — adds `create_tree_index_artifact()` function (mirrors `create_chunk_artifact`)

New importable symbols:
- `knowledge_lake.registry.repo.create_tree_index_artifact` (function)

<output>
Create `.planning/phases/13-tree-index-foundation/13-03-SUMMARY.md` when done
</output>
