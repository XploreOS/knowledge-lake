---
phase: "13"
plan: "05"
type: execute
wave: 2
depends_on:
  - "13-02"
files_modified:
  - src/knowledge_lake/plugins/builtin/pageindex_indexer.py
  - src/knowledge_lake/plugins/resolver.py
  - src/knowledge_lake/plugins/builtin/__init__.py
  - pyproject.toml
autonomous: true
requirements:
  - TREE-01
  - TREE-04
  - TREE-05

must_haves:
  truths:
    - "PageIndexIndexer class is importable from knowledge_lake.plugins.builtin.pageindex_indexer"
    - "isinstance(PageIndexIndexer(), IndexerPlugin) returns True (runtime_checkable conformance)"
    - "get_indexer(settings) resolves to a PageIndexIndexer instance via entry-point group 'knowledge_lake.indexers'"
    - "Entry-point 'pageindex' is registered in the 'knowledge_lake.indexers' group in pyproject.toml"
    - "settings.indexer='pageindex' is the default and resolves without error after uv pip install -e ."
    - "PageIndexIndexer does NOT import the pageindex pre-release library (deferred per RESEARCH.md Open Question 1)"
  artifacts:
    - src/knowledge_lake/plugins/builtin/pageindex_indexer.py
    - src/knowledge_lake/plugins/resolver.py
    - src/knowledge_lake/plugins/builtin/__init__.py
    - pyproject.toml
  key_links:
    - "GROUP_INDEXERS = 'knowledge_lake.indexers' in resolver.py — mirrors GROUP_VECTORSTORES"
    - "get_indexer(settings) uses _resolve_with_kwargs to inject litellm_url and litellm_api_key"
    - "pyproject.toml entry-point pageindex = 'knowledge_lake.plugins.builtin.pageindex_indexer:PageIndexIndexer' requires editable reinstall"
  prohibitions:
    - "Do NOT import pageindex==0.3.0.dev3 or PyPDF2 — both are [SUS] pre-release per RESEARCH.md Package Legitimacy Audit"
    - "Do NOT add pip install commands for pageindex/PyPDF2/pymupdf without a checkpoint:human-verify"
    - "PageIndexIndexer.build_index must call the same _build_deterministic_tree from pipeline/tree_index.py (or equivalent logic) — no re-parse of the document"
---

<objective>
Wave 2 plugin surface: create the IndexerPlugin builtin and register it via the entry-point mechanism. This enables settings.indexer swap key to work and closes the D-04/D-05 requirement (tool-agnostic swappability, FOUND-08).

Per RESEARCH.md Open Question 1 recommendation: PageIndexIndexer builds the deterministic skeleton from ParsedDoc.sections (D-03) and optionally calls litellm.completion per node for summaries (D-09). The actual pageindex==0.3.0.dev3 library is NOT imported — it is [SUS] pre-release and its build-from-PDF approach conflicts with D-03.

Purpose: Satisfies D-04/D-05 by implementing the builtin plugin seam. Enables runtime swap of the indexer (future phases can plug in a different implementation without changing the pipeline). Closes test_builtin_plugins.py conformance test.

Output: pageindex_indexer.py builtin, resolver.py GROUP_INDEXERS + get_indexer, builtin/__init__.py docstring update, pyproject.toml entry-point registration.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-tree-index-foundation/13-CONTEXT.md
@.planning/phases/13-tree-index-foundation/13-PATTERNS.md
@src/knowledge_lake/plugins/resolver.py
@src/knowledge_lake/plugins/builtin/__init__.py
@src/knowledge_lake/plugins/protocols.py
@pyproject.toml
</context>

<tasks>

<task id="T1" type="auto" tdd="true">
  <name>Task 1: Create pageindex_indexer.py builtin + extend resolver.py + update builtin/__init__.py + pyproject.toml</name>
  <files>
    src/knowledge_lake/plugins/builtin/pageindex_indexer.py,
    src/knowledge_lake/plugins/resolver.py,
    src/knowledge_lake/plugins/builtin/__init__.py,
    pyproject.toml
  </files>
  <read_first>
    - src/knowledge_lake/plugins/builtin/st_embedder.py — full file; this is the exact structural analog: class with name:str class attr, __init__ that injects litellm_url/litellm_api_key, lazy import pattern; mirrors the shape for PageIndexIndexer
    - src/knowledge_lake/plugins/resolver.py — lines 39-45 (GROUP_* constants), lines 214-290 (resolve, _resolve_with_kwargs, get_embedder, get_vectorstore) — GROUP_INDEXERS and get_indexer mirror get_vectorstore exactly
    - src/knowledge_lake/plugins/builtin/__init__.py — existing docstring block (lines 1-15) to see where to add the indexers line
    - pyproject.toml — lines around 95-105 ([project.entry-points."knowledge_lake.vectorstores"]) as the exact template for the new indexers group
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §pageindex_indexer.py, §resolver.py, §pyproject.toml — exact class/function shapes
    - src/knowledge_lake/plugins/protocols.py — IndexerPlugin Protocol fields (added in Plan 13-02): name:str, build_index(self, parsed_doc, *, mode, metadata) -> TreeIndex
    - src/knowledge_lake/pipeline/tree_index.py — _build_deterministic_tree (added in Plan 13-04) — PageIndexIndexer.build_index can import and reuse this function
  </read_first>
  <behavior>
    - PageIndexIndexer() is instantiable with no required args (defaults: litellm_url="http://localhost:4000", litellm_api_key="sk-local-noauth")
    - PageIndexIndexer.name == "pageindex"
    - isinstance(PageIndexIndexer(), IndexerPlugin) is True
    - PageIndexIndexer().build_index(parsed_doc, mode="deterministic", metadata={}) returns a TreeIndex with roots derived from parsed_doc.sections via _build_deterministic_tree (or equivalent inline logic)
    - get_indexer(settings) returns a PageIndexIndexer instance when settings.indexer == "pageindex"
    - importlib.metadata.entry_points(group="knowledge_lake.indexers") contains an entry with name "pageindex" (after `uv pip install -e .` editable reinstall)
    - No import of pageindex, PyPDF2, or pymupdf package anywhere in pageindex_indexer.py
  </behavior>
  <action>
    File A — Create `src/knowledge_lake/plugins/builtin/pageindex_indexer.py` as a NEW file.

    Class shape: class PageIndexIndexer with class attribute name: str = "pageindex". __init__(self, litellm_url: str = "http://localhost:4000", litellm_api_key: str = "sk-local-noauth") stores the url and key as instance attributes. build_index(self, parsed_doc: ParsedDoc, *, mode: str = "deterministic", metadata: dict[str, Any] | None = None) -> TreeIndex: imports _build_deterministic_tree from knowledge_lake.pipeline.tree_index (deferred import to avoid circular imports); derives page_count from parsed_doc.metadata.get("page_count", 1); calls _build_deterministic_tree(parsed_doc.sections, page_count); constructs and returns TreeIndex(parsed_artifact_id=metadata.get("parsed_artifact_id","") if metadata else "", source_id=metadata.get("source_id","") if metadata else "", roots=roots, mode=mode, schema_version="1", content_hash="").

    Class docstring: "Built-in IndexerPlugin using deterministic section-tree construction (D-04/D-05). Builds a tree directly from ParsedDoc.sections — no re-parse, no pageindex==0.3.0.dev3 import this phase (RESEARCH.md Open Question 1). LiteLLM URL injected via constructor (CR-03). Satisfies IndexerPlugin Protocol (runtime_checkable)."

    Imports: from __future__ import annotations; from typing import Any; from knowledge_lake.plugins.protocols import IndexerPlugin, ParsedDoc, TreeIndex (TreeNode imported inside build_index if needed).

    Do NOT add `import pageindex` — it is [SUS] pre-release and not installed.

    File B — Modify `src/knowledge_lake/plugins/resolver.py`.

    Read the full file. After the GROUP_VECTORSTORES = "knowledge_lake.vectorstores" line (around line 40), add:
    GROUP_INDEXERS = "knowledge_lake.indexers"

    After the get_vectorstore function definition (around line 288), add the get_indexer function:
    def get_indexer(settings: Settings) -> Any: with docstring "Return the IndexerPlugin named by settings.indexer (default: 'pageindex'). Injected via entry-point group 'knowledge_lake.indexers'."; local variable name = settings.indexer; construct kwargs = {"litellm_url": settings.litellm_url, "litellm_api_key": settings.litellm_api_key} if name == "pageindex" else {}; return _resolve_with_kwargs(GROUP_INDEXERS, name, **kwargs).

    Do not modify any existing function.

    File C — Modify `src/knowledge_lake/plugins/builtin/__init__.py`.

    Read the full file. Find the docstring block listing existing groups (knowledge_lake.embedders, knowledge_lake.vectorstores, etc.). Add a line for the new group: "  knowledge_lake.indexers  — pageindex (PageIndexIndexer: deterministic section tree)". Keep all existing lines intact.

    File D — Modify `pyproject.toml`.

    Read the full file. Find the [project.entry-points."knowledge_lake.vectorstores"] block (around line 99). After it (or after the last existing entry-points block), add a new TOML table:
    [project.entry-points."knowledge_lake.indexers"]
    pageindex = "knowledge_lake.plugins.builtin.pageindex_indexer:PageIndexIndexer"

    IMPORTANT: After adding the entry-point, the executor must run `uv pip install -e .` (or equivalent editable reinstall) so importlib.metadata.entry_points picks up the new group. Include this as a post-task shell step in verify.
  </action>
  <acceptance_criteria>
    - `python -c "from knowledge_lake.plugins.builtin.pageindex_indexer import PageIndexIndexer; from knowledge_lake.plugins.protocols import IndexerPlugin; p = PageIndexIndexer(); assert isinstance(p, IndexerPlugin); assert p.name == 'pageindex'; print('OK')"` exits 0 (after Plan 13-02 and Plan 13-04 complete)
    - `grep -c 'GROUP_INDEXERS\|get_indexer' src/knowledge_lake/plugins/resolver.py` returns 2
    - `grep -c 'knowledge_lake.indexers' pyproject.toml` returns 2 (the section header line and the pageindex entry)
    - `pytest tests/unit/test_builtin_plugins.py::TestIndexerPluginBuiltin -x 2>&1 | grep PASSED` — conformance tests pass after editable reinstall
  </acceptance_criteria>
  <verify>
    <automated>cd /root/healthlake && uv pip install -e . -q && python -c "import importlib.metadata; eps = importlib.metadata.entry_points(group='knowledge_lake.indexers'); names = {e.name for e in eps}; assert 'pageindex' in names, f'pageindex not in {names}'; print('Entry-point registered:', names)"</automated>
  </verify>
  <done>PageIndexIndexer passes isinstance(_, IndexerPlugin); get_indexer(settings) resolves correctly; "knowledge_lake.indexers" entry-point group registered in pyproject.toml; test_builtin_plugins.py conformance tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.indexer → entry-point group resolution | swap key validated by _validate_swap_key (ASVS V5) before reaching _resolve_with_kwargs |
| entry-point group → importlib load | only named entry-point in pyproject.toml is PageIndexIndexer; no user-controlled import path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11 | Tampering | Entry-point swap-key injection | high | mitigate | settings.indexer passes _validate_swap_key @field_validator (added in Plan 13-02 T3) before reaching _resolve_with_kwargs; arbitrary entry-point names are rejected at config load |
| T-13-12 | Tampering | [SUS] pre-release pageindex package | high | mitigate | pageindex==0.3.0.dev3 is NOT imported in this plan (Open Question 1 recommendation); no pip install of pageindex, PyPDF2, or pymupdf; Package Legitimacy Audit in RESEARCH.md flags these as [SUS]; deferred to a future human-approved checkpoint if adopted |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed; pyproject.toml entry-point addition requires only editable reinstall of the existing package — no new dependency added |
</threat_model>

<verification>
After task completes:
- `uv pip install -e . -q && python -c "import importlib.metadata; eps = importlib.metadata.entry_points(group='knowledge_lake.indexers'); assert 'pageindex' in {e.name for e in eps}"` exits 0
- `pytest tests/unit/test_builtin_plugins.py -x -q` → all tests pass including new TestIndexerPluginBuiltin class
- `pytest tests/unit/ -q 2>&1 | tail -5` → no regressions
</verification>

<success_criteria>
1. PageIndexIndexer passes isinstance(_, IndexerPlugin) conformance check
2. Entry-point "pageindex" registered in "knowledge_lake.indexers" group (pyproject.toml + editable reinstall)
3. get_indexer(settings) resolves to a PageIndexIndexer instance
4. test_builtin_plugins.py conformance tests GREEN
5. No pageindex pre-release library imported or installed
</success_criteria>

## Artifacts this phase produces (Wave 2 plugin surface)

New files:
- `src/knowledge_lake/plugins/builtin/pageindex_indexer.py`

Modified files:
- `src/knowledge_lake/plugins/resolver.py` — adds `GROUP_INDEXERS = "knowledge_lake.indexers"` constant and `get_indexer(settings)` function
- `src/knowledge_lake/plugins/builtin/__init__.py` — adds knowledge_lake.indexers line to docstring
- `pyproject.toml` — adds `[project.entry-points."knowledge_lake.indexers"]` block with `pageindex` entry

New importable symbols:
- `knowledge_lake.plugins.builtin.pageindex_indexer.PageIndexIndexer` (class)
- `knowledge_lake.plugins.resolver.GROUP_INDEXERS` (str constant)
- `knowledge_lake.plugins.resolver.get_indexer` (function)

New entry-point group:
- `knowledge_lake.indexers` with member `pageindex = ...pageindex_indexer:PageIndexIndexer`

<output>
Create `.planning/phases/13-tree-index-foundation/13-05-SUMMARY.md` when done
</output>
