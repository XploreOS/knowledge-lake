---
phase: "13"
plan: "06"
type: execute
wave: 3
depends_on:
  - "13-04"
  - "13-05"
files_modified:
  - src/knowledge_lake/dagster_defs/assets.py
  - src/knowledge_lake/dagster_defs/definitions.py
autonomous: false
requirements:
  - TREE-05

must_haves:
  truths:
    - "tree_index_document asset is a thin shell over pipeline.tree_index.tree_index() with no duplicated logic"
    - "tree_index_document takes clean_document: dict[str, Any] as its first parameter (same fan-out input as chunk_document and enrich_document)"
    - "tree_index_document is registered in definitions.py assets list and is importable from dagster_defs.assets"
    - "test_tree_index_asset.py tests pass GREEN"
  artifacts:
    - src/knowledge_lake/dagster_defs/assets.py
    - src/knowledge_lake/dagster_defs/definitions.py
  key_links:
    - "tree_index_document fans out from clean_document in parallel to chunk_document and enrich_document (TREE-05)"
    - "Dagster code-location reload required after definitions.py change for asset to appear in live daemon (memory: dagster-code-location-reload)"
  prohibitions:
    - "Do NOT add tree_index_document to healthcare_e2e_job selection — Assumption A6 in RESEARCH.md says the 7-asset E2E job intentionally excludes non-core assets"
    - "Do NOT duplicate any logic from pipeline.tree_index.tree_index() in the asset body"
    - "Do NOT remove chunk_document or enrich_document from definitions.py"
---

<objective>
Wave 3 Dagster surface: wire tree_index_document as a third parallel fan-out asset from clean_document. The asset is a thin shell (no logic duplicated from pipeline/tree_index.py), mirroring the exact shape of chunk_document (assets.py:335) and enrich_document (assets.py:384).

Purpose: Delivers TREE-05 — tree index generation runs as a Dagster asset parallel to chunking. Both chunk_document and tree_index_document take clean_document as input; neither blocks the other.

Output: assets.py extended with tree_index_document; definitions.py updated to include it. After a Dagster code-location reload, the asset appears in the pipeline group.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-tree-index-foundation/13-CONTEXT.md
@.planning/phases/13-tree-index-foundation/13-PATTERNS.md
@src/knowledge_lake/dagster_defs/assets.py
@src/knowledge_lake/dagster_defs/definitions.py
</context>

<tasks>

<task id="T1" type="auto" tdd="true">
  <name>Task 1: assets.py: add tree_index_document asset (thin shell over pipeline.tree_index)</name>
  <files>src/knowledge_lake/dagster_defs/assets.py</files>
  <read_first>
    - src/knowledge_lake/dagster_defs/assets.py — read lines 327-430 (chunk_document lines 327-371 and enrich_document lines 374-429) as the two exact analogs; note _PIPELINE_RETRY usage, PostgresResource/MinIOResource/LiteLLMResource resource types, clean_document dict key access pattern, Settings construction, and how each is a thin shell with no logic
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §assets.py — exact function signature, resources, Settings construction, and return pattern
    - tests/unit/test_tree_index_asset.py — test_asset_calls_pipeline and test_asset_input_shape_matches_chunk_document exact assertions (created in Plan 13-01)
  </read_first>
  <behavior>
    - tree_index_document is decorated with @asset(group_name="pipeline", retry_policy=_PIPELINE_RETRY, description="...")
    - Function signature: tree_index_document(clean_document: dict[str, Any], postgres: PostgresResource, minio: MinIOResource, litellm: LiteLLMResource) -> dict[str, Any]
    - Body: extract parsed_artifact_id = clean_document["parsed_artifact_id"], source_id = clean_document["source_id"], doc = clean_document["parsed_doc"] (in-memory ParsedDoc — Pitfall 7: no IO managers)
    - Build Settings with database_url=postgres.database_url, storage=StorageSettings(endpoint_url=minio.endpoint_url, bucket=minio.bucket, access_key_id=minio.access_key_id, secret_access_key=minio.secret_access_key, region=minio.region), litellm_url=litellm.litellm_url, _env_file=None
    - Call pipeline.tree_index.tree_index(parsed_artifact_id, source_id, doc, settings=settings) and return the result dict
    - Zero logic beyond the thin shell — no branching, no processing, no field manipulation
    - test_asset_calls_pipeline passes: patching pipeline.tree_index.tree_index to a mock and calling tree_index_document with fake resources returns the mock return value
    - test_asset_input_shape_matches_chunk_document passes: inspect.signature(tree_index_document).parameters first key is "clean_document"
  </behavior>
  <action>
    Read assets.py in full (or at minimum lines 327-429 for chunk_document and enrich_document). After the closing line of enrich_document, insert the tree_index_document function.

    Use these as exact templates:
    - Decorator: @asset(group_name="pipeline", retry_policy=_PIPELINE_RETRY, description="Generate a hierarchical tree index from a cleaned document (TREE-01/TREE-05). Fan-out branch off clean_document parallel to chunk_document. Thin shell over pipeline.tree_index.tree_index(). Requires Dagster code-location reload to appear in live daemon.")
    - Function signature: def tree_index_document(clean_document: dict[str, Any], postgres: PostgresResource, minio: MinIOResource, litellm: LiteLLMResource) -> dict[str, Any]:
    - Body: 4 lines — extract the 3 fields from clean_document, construct Settings, call tree_index(), return result
    - Deferred imports inside the function body (same pattern as enrich_document): from knowledge_lake.config.settings import Settings, StorageSettings; from knowledge_lake.pipeline.tree_index import tree_index
    - Log lines: log.info("dagster.tree_index_document.start", parsed_artifact_id=parsed_artifact_id); log.info("dagster.tree_index_document.complete", status=result.get("status"), cached=result.get("cached"))

    Do not modify chunk_document or enrich_document. Do not add tree_index_document to any existing job definition here (that is a definitions.py concern).
  </action>
  <acceptance_criteria>
    - `grep -c 'def tree_index_document' src/knowledge_lake/dagster_defs/assets.py` returns 1
    - `python -c "from knowledge_lake.dagster_defs.assets import tree_index_document; import inspect; sig = inspect.signature(tree_index_document); assert list(sig.parameters.keys())[0] == 'clean_document'; print('OK')"` exits 0
    - `pytest tests/unit/test_tree_index_asset.py -x -q` → both asset tests pass GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd /root/healthlake && grep -c 'def tree_index_document' src/knowledge_lake/dagster_defs/assets.py | grep '^1$'</automated>
  </verify>
  <done>tree_index_document asset added to assets.py with correct decorator, resources, Settings construction, and thin-shell body. Asset tests GREEN.</done>
</task>

<task id="T2" type="auto">
  <name>Task 2: definitions.py: import and register tree_index_document in assets list</name>
  <files>src/knowledge_lake/dagster_defs/definitions.py</files>
  <read_first>
    - src/knowledge_lake/dagster_defs/definitions.py — full file; focus on: import block (lines 35-50 for asset imports), assets=[...] list (lines 69-82), healthcare_e2e_job definition and its asset selection (around line 867 area)
    - .planning/phases/13-tree-index-foundation/13-PATTERNS.md §definitions.py — exact import line and assets list addition
    - .planning/phases/13-tree-index-foundation/13-RESEARCH.md Assumption A6 — do NOT add to healthcare_e2e_job
  </read_first>
  <action>
    Read definitions.py in full. Make two targeted edits using Edit (never Write the full file):

    Edit 1 — Add import: In the asset import block (where chunk_document and enrich_document are imported), add tree_index_document to the import tuple from knowledge_lake.dagster_defs.assets. Mirror the exact import style already used.

    Edit 2 — Add to assets list: In the assets=[...] list passed to Definitions(...), add tree_index_document after enrich_document. Keep the existing list members and ordering intact.

    Do NOT add tree_index_document to the healthcare_e2e_job asset selection (Assumption A6 in RESEARCH.md: the 7-asset E2E job intentionally excludes non-core assets like tree indexing).

    Do not change any other part of definitions.py.
  </action>
  <acceptance_criteria>
    - `grep -c 'tree_index_document' src/knowledge_lake/dagster_defs/definitions.py` returns 2 (import + assets list)
    - `python -c "from knowledge_lake.dagster_defs.definitions import defs; print('Definitions loaded OK')"` exits 0 (no import errors)
    - `python -c "from knowledge_lake.dagster_defs.definitions import defs; names = {a.key.path[-1] for a in defs.get_all_asset_defs()}; assert 'tree_index_document' in names, f'missing from: {names}'"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /root/healthlake && python -c "from knowledge_lake.dagster_defs.definitions import defs; names = {a.key.path[-1] for a in defs.get_all_asset_defs()}; assert 'tree_index_document' in names, f'tree_index_document missing; got: {sorted(names)}'; print('tree_index_document in Definitions assets:', sorted(names))"</automated>
  </verify>
  <done>tree_index_document imported and included in Definitions assets=[...] list. defs.get_all_asset_defs() includes tree_index_document. healthcare_e2e_job is unchanged.</done>
</task>

<task id="T3" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — full Phase 13 automated checks + Dagster asset confirmation</name>
  <action>Run the verification steps below and confirm all pass before approving.</action>
  <what-built>
    Plans 13-01 through 13-06 implemented: TreeNode/TreeIndex/IndexerPlugin schema contract, ids.py _PREFIX, TreeSettings config, create_tree_index_artifact repo helper, pipeline/tree_index.py deterministic builder + LLM mode, PageIndexIndexer builtin, entry-point registration, tree_index_document Dagster asset wired into Definitions.
  </what-built>
  <how-to-verify>
    Step 1 — Unit tests: `cd /root/healthlake && pytest tests/unit/test_tree_index.py tests/unit/test_tree_index_asset.py tests/unit/test_builtin_plugins.py -v` → all tests pass GREEN (6 in test_tree_index.py, 2 in test_tree_index_asset.py, all pre-existing + 2 new in test_builtin_plugins.py).

    Step 2 — Entry-point: `cd /root/healthlake && python -c "import importlib.metadata; eps = importlib.metadata.entry_points(group='knowledge_lake.indexers'); print({e.name for e in eps})"` → shows {'pageindex'}.

    Step 3 — Dagster Definitions: `cd /root/healthlake && python -c "from knowledge_lake.dagster_defs.definitions import defs; names = {a.key.path[-1] for a in defs.get_all_asset_defs()}; assert 'tree_index_document' in names; print('Asset registered:', 'tree_index_document' in names)"` → prints True.

    Step 4 — Full test suite: `cd /root/healthlake && pytest -q 2>&1 | tail -10` → all tests pass (no regressions).

    Step 5 — Dagster code-location reload: If a Dagster daemon is running locally, reload the code location (Dagster UI → Code Locations → Reload, or restart the container). Confirm tree_index_document appears in the pipeline asset group alongside chunk_document. This step is optional if Dagster is not running locally — the unit tests cover the asset's correctness.
  </how-to-verify>
  <resume-signal>Type "approved" if all automated checks pass and the Dagster asset appears correctly, or describe any failures found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dagster resource → Settings constructor | Resource credentials (database_url, S3 keys) come from Dagster resource config, not user input |
| clean_document dict → asset body | Dict shape is set by the upstream clean_document asset; treated as trusted internal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-13 | Tampering | clean_document dict key access | low | accept | clean_document is produced by the upstream clean_document Dagster asset (internal pipeline); not user-controlled; key access raises KeyError if upstream shape changes — this is a design contract, not a security boundary |
| T-13-14 | Information Disclosure | tree_index_document logs | low | accept | Log lines include only parsed_artifact_id (internal ID) and status/cached (internal pipeline state); no user PII or document content logged |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages installed; assets.py and definitions.py edits only import from existing installed modules |
</threat_model>

<verification>
After all tasks complete:
- `pytest tests/unit/test_tree_index.py tests/unit/test_tree_index_asset.py tests/unit/test_builtin_plugins.py -v` → all GREEN
- `python -c "from knowledge_lake.dagster_defs.definitions import defs; names = {a.key.path[-1] for a in defs.get_all_asset_defs()}; assert 'tree_index_document' in names"` exits 0
- `pytest -q` → full suite GREEN (no regressions)
- Human checkpoint approved (T3)
</verification>

<success_criteria>
TREE-05 delivered:
1. tree_index_document asset exists in assets.py as a thin shell over pipeline.tree_index.tree_index()
2. Asset is registered in Definitions assets list
3. Asset input shape matches chunk_document and enrich_document fan-out pattern (clean_document dict)
4. Both asset tests GREEN
5. Full test suite passes
6. Human checkpoint confirms Dagster code location loads correctly
</success_criteria>

## Artifacts this phase produces (Wave 3 Dagster surface)

Modified files:
- `src/knowledge_lake/dagster_defs/assets.py` — adds `tree_index_document` @asset function
- `src/knowledge_lake/dagster_defs/definitions.py` — imports `tree_index_document` and adds it to `assets=[...]`

New Dagster assets:
- `tree_index_document` — group_name="pipeline", retry_policy=_PIPELINE_RETRY, fan-out from clean_document

New symbols:
- `knowledge_lake.dagster_defs.assets.tree_index_document` (Dagster asset function)

Post-execution requirement:
- Dagster code-location reload required for the new asset to appear in a running daemon (MEMORY: dagster-code-location-reload)

<output>
Create `.planning/phases/13-tree-index-foundation/13-06-SUMMARY.md` when done
</output>
