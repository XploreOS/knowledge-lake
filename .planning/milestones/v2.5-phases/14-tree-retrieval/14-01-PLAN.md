---
phase: "14"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_tree_search.py
  - tests/unit/test_builtin_plugins.py
autonomous: true
requirements:
  - RETR-04
  - RETR-05
  - RETR-06
  - RETR-07
  - RETR-08

must_haves:
  truths:
    - "tests/unit/test_tree_search.py exists and is collected by pytest with all Wave-0 test functions present (RED state until Waves 1-3 ship)"
    - "test_tree_search.py contains direct-retriever tests (heuristic no-LLM, citation_source=tree, _dict_to_tree round-trip, LLM-nav degrade) and orchestrator tests (two-stage shortlist, parallel-load-and-skip)"
    - "test_builtin_plugins.py contains a TestPageIndexRetriever class asserting entry-point registration in knowledge_lake.retrievers and isinstance(PageIndexRetriever(), RetrieverPlugin)"
    - "A hand-built TreeIndex fixture (2-3 levels) plus its serialized dict is defined for _dict_to_tree round-trip and heuristic scoring without S3"
    - "In-memory SQLite fixtures mirror test_tree_index.py (StaticPool, _patch_engine autouse, Base.metadata.create_all) so LLM-nav budget tests resolve get_session against the test DB"
  artifacts:
    - tests/unit/test_tree_search.py
  key_links:
    - "test_tree_search.py imports from knowledge_lake.pipeline.tree_search (module absent until Plan 14-04 — ImportError is the correct RED state)"
    - "test_tree_search.py and test_builtin_plugins.py import RetrieverPlugin from protocols.py and PageIndexRetriever from pageindex_retriever.py (both absent until Waves 1-2)"
---

<objective>
Create the Wave 0 test scaffold for Phase 14. Every Wave 1-3 implementation task references specific test functions in these files as its automated verify command. Tests must be collectible by pytest but fail with ImportError or assertion failures (RED state) until the corresponding implementation ships. Covers RETR-04..08 and the D-11 `_dict_to_tree` round-trip.

Purpose: Nyquist compliance — every implementation task has an automated verify target before code is written. This unblocks the sequential Waves 1-3 (14-02 protocols+settings, 14-03 retriever builtin, 14-04 orchestrator+CLI).

Output: tests/unit/test_tree_search.py (covers RETR-04..08 + D-11) and a TestPageIndexRetriever conformance class appended to tests/unit/test_builtin_plugins.py (D-04).
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-tree-retrieval/14-CONTEXT.md
@.planning/phases/14-tree-retrieval/14-RESEARCH.md
@.planning/phases/14-tree-retrieval/14-PATTERNS.md
@.planning/phases/14-tree-retrieval/14-VALIDATION.md
</context>

<tasks>

<task id="T1" type="auto" tdd="true">
  <name>Task 1: Create tests/unit/test_tree_search.py — RETR-04..08 + D-11 scaffold</name>
  <files>tests/unit/test_tree_search.py</files>
  <read_first>
    - tests/unit/test_tree_index.py — verbatim fixture patterns: engine (lines 34-49), _patch_engine autouse (52-57), session (60-63), seeded Source→raw→parsed chain (66-90), fake_storage (93-101), multi_section_doc ParsedDoc (104-122). Reuse the engine / _patch_engine / seeded fixtures unchanged.
    - .planning/phases/14-tree-retrieval/14-RESEARCH.md — "Validation Architecture" Phase Requirements → Test Map (exact test names and behaviors), "Code Examples" (Hit additive field, stage-1 grouping, node_path), Pattern 4 (_dict_to_tree shape), Common Pitfalls 2/5/6.
    - .planning/phases/14-tree-retrieval/14-PATTERNS.md — pipeline/tree_search.py section (stage-1 grouping, _dict_to_tree, async load) and pageindex_retriever.py section (heuristic DFS, LLM-nav) for the exact identifiers the tests exercise.
    - src/knowledge_lake/plugins/protocols.py — Hit (lines 114-129), TreeNode (598-630), TreeIndex (633-657) field names the fixtures construct and the tests assert against.
  </read_first>
  <behavior>
    - test_hit_citation_source_default (D-02): Hit(id="x", score=1.0) has attribute citation_source == "chunk" (additive default; regression guard so chunk search is unchanged).
    - test_dict_to_tree_roundtrip (D-11): a hand-built TreeIndex serialized to a dict (matching tree_index.py _tree_to_dict + the tree_dict wrapper keys parsed_artifact_id, source_id, mode, schema_version, content_hash, roots) and passed through tree_search._dict_to_tree_index reconstructs an equal TreeIndex (same node_id/title/summary/page_start/page_end/level/section_path and nested children).
    - test_heuristic_no_llm (RETR-05): PageIndexRetriever().search(tree_index, query, mode="heuristic") over the hand-built tree returns Hits whose order is identical across two calls (deterministic) AND litellm.completion is never invoked (patch litellm.completion and assert call_count == 0). Empty/whitespace query returns [] (Pitfall 6); a tree with no roots returns [] (Pitfall 6).
    - test_citation_source_tree (RETR-08): every Hit from the heuristic search has citation_source == "tree" and payload contains keys document, node_id, section_path, page_start, page_end, node_path (node_path is the root→node title chain).
    - test_llm_nav_degrades (RETR-06): with mode="llm" — (a) when get_llm_spend is seeded at/over budget_usd, search returns the heuristic result and litellm.completion call_count == 0 (budget degrade, D-06/D-07); (b) when litellm.completion raises, search catches it and returns the heuristic result (never raises, D-06). Both assert the returned Hits equal the heuristic-mode Hits.
    - test_no_hardcoded_provider_model_ids (CLAUDE.md): inspect.getsource of the retriever module shows the LLM model argument is the f-string form openai/ + settings alias, and contains no hardcoded provider IDs (no gpt-4, claude-3, anthropic/, bedrock/ substrings).
    - test_two_stage_shortlist (RETR-04): patch tree_search.search to return a list of chunk Hits whose payload["document"] spans several parsed_artifact_ids with varying scores; patch registry_repo.get_child_artifact_by_type to return a tree_index artifact (with storage_uri) for the top docs; patch StorageBackend so get_object returns serialized tree bytes. Assert tree_search() groups by payload["document"], keeps at most max_docs documents (max score per doc, D-08), and returns tree Hits only from the shortlisted documents.
    - test_parallel_load_and_skip (RETR-07/D-09): one shortlisted document resolves to no tree_index artifact (get_child_artifact_by_type returns None) and is skipped without error; the remaining documents still return Hits; the concurrency limit is read from settings.tree_search.concurrency (assert the code path honors the Semaphore bound — e.g. patch asyncio.Semaphore with a spy and assert it was constructed with the configured concurrency).
  </behavior>
  <action>
    Create tests/unit/test_tree_search.py as a NEW file. Do not modify any existing file in this task.

    Module import + aliases: import knowledge_lake.pipeline.tree_search as tree_search_module and, inside the retriever tests, import PageIndexRetriever from knowledge_lake.plugins.builtin.pageindex_retriever. These imports raise ImportError during collection until Plans 14-03 and 14-04 ship — that ImportError is the correct Wave 0 RED state.

    Fixtures: reuse test_tree_index.py verbatim for engine (StaticPool in-memory SQLite), _patch_engine (autouse monkeypatch of registry_db.get_engine), session, and seeded (Source→raw→parsed chain). Add a hand_tree fixture: build a TreeIndex (imported from knowledge_lake.plugins.protocols) with parsed_artifact_id, source_id, and 2-3 levels of TreeNode roots/children populating node_id, title, summary, page_start, page_end, level, section_path — choose titles/summaries so a fixed query (e.g. "budget cap") matches some nodes and not others, exercising keyword scoring. Add a hand_tree_dict fixture returning the serialized dict form (keys parsed_artifact_id, source_id, mode, schema_version, content_hash, roots; each node dict has node_id, title, summary, page_start, page_end, level, section_path, children).

    Structure the file in classes: TestHitContract (test_hit_citation_source_default), TestDictToTree (test_dict_to_tree_roundtrip), TestHeuristicRetriever (test_heuristic_no_llm, test_citation_source_tree, test_no_hardcoded_provider_model_ids), TestLlmNav (test_llm_nav_degrades), TestTwoStageSearch (test_two_stage_shortlist, test_parallel_load_and_skip). Implement each method's assertions per the behavior block. For orchestrator tests, patch tree_search_module.search, registry_repo.get_child_artifact_by_type, and tree_search_module.StorageBackend (module-level patch, mirroring test_tree_index.py fake_storage). For LLM-nav tests, seed spend with registry_repo.record_llm_spend(session, scope="tree_search", cost_usd=budget) and patch litellm.completion.

    Do not add fenced code blocks inside this action. Do not implement any production logic — the top-level imports failing at collection is the intended Wave 0 behavior.
  </action>
  <acceptance_criteria>
    - File exists: `test -f tests/unit/test_tree_search.py` exits 0.
    - `grep -c 'def test_' tests/unit/test_tree_search.py` returns 8 or more (all Wave-0 test functions present).
    - `grep -c 'citation_source\|_dict_to_tree_index\|scope="tree_search"\|concurrency' tests/unit/test_tree_search.py` returns 4 or more (the phase's load-bearing identifiers are exercised).
    - `grep -c 'StaticPool\|_patch_engine\|hand_tree' tests/unit/test_tree_search.py` returns 3 or more (fixtures wired).
    - `pytest tests/unit/test_tree_search.py --collect-only 2>&1 | grep -q ImportError` (import of the absent tree_search module is the correct RED state).
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/unit/test_tree_search.py && grep -v '^#' tests/unit/test_tree_search.py | grep -c 'def test_' | grep -E '^([8-9]|[1-9][0-9])$'</automated>
  </verify>
  <done>tests/unit/test_tree_search.py exists with at least 8 test functions covering RETR-04..08 and the D-11 round-trip, the hand_tree/hand_tree_dict fixtures, and the reused in-memory SQLite fixtures.</done>
</task>

<task id="T2" type="auto">
  <name>Task 2: Extend tests/unit/test_builtin_plugins.py with RetrieverPlugin conformance stub (D-04)</name>
  <files>tests/unit/test_builtin_plugins.py</files>
  <read_first>
    - tests/unit/test_builtin_plugins.py — full file; the protocols import block (lines 22-30), the TestEntryPointRegistrations class, and TestIndexerPluginBuiltin (lines 337-371) with its _get_ep_names helper are the exact template to mirror for the retriever.
    - src/knowledge_lake/plugins/protocols.py — RetrieverPlugin will be added by Plan 14-02 (name:str + search(...) -> list[Hit]); the isinstance check targets that Protocol.
  </read_first>
  <behavior>
    - test_pageindex_retriever_entry_point_registered: importlib.metadata.entry_points(group="knowledge_lake.retrievers") contains an entry named "pageindex".
    - test_pageindex_retriever_satisfies_protocol: isinstance(PageIndexRetriever(), RetrieverPlugin) is True.
  </behavior>
  <action>
    Modify tests/unit/test_builtin_plugins.py (read the full file first; preserve every existing test and import).

    Add RetrieverPlugin to the existing `from knowledge_lake.plugins.protocols import (...)` tuple (alphabetical placement near IndexerPlugin). RetrieverPlugin does not exist yet, so this triggers ImportError in the RED state — intended.

    Append a new class TestPageIndexRetriever mirroring TestIndexerPluginBuiltin (lines 341-371): reuse the same _get_ep_names(self, group) helper shape (importlib.metadata.entry_points(group=group) → set of ep.name). Add test_pageindex_retriever_entry_point_registered asserting "pageindex" is in _get_ep_names("knowledge_lake.retrievers"), and test_pageindex_retriever_satisfies_protocol importing PageIndexRetriever from knowledge_lake.plugins.builtin.pageindex_retriever, constructing an instance, and asserting isinstance(instance, RetrieverPlugin).

    Do not modify any other test class. Do not add fenced code blocks inside this action.
  </action>
  <acceptance_criteria>
    - `grep -c 'TestPageIndexRetriever\|test_pageindex_retriever_entry_point_registered\|test_pageindex_retriever_satisfies_protocol\|RetrieverPlugin' tests/unit/test_builtin_plugins.py` returns 4 or more (new class, both methods, and the import present).
    - `grep -c 'knowledge_lake.retrievers' tests/unit/test_builtin_plugins.py` returns 1 or more.
    - Existing test count did not decrease: `grep -c 'def test_' tests/unit/test_builtin_plugins.py` returns the original count plus 2.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'RetrieverPlugin' tests/unit/test_builtin_plugins.py | grep -E '^[1-9]' && grep -c 'knowledge_lake.retrievers' tests/unit/test_builtin_plugins.py | grep -E '^[1-9]'</automated>
  </verify>
  <done>test_builtin_plugins.py imports RetrieverPlugin and defines TestPageIndexRetriever with two conformance stubs (entry-point registration + isinstance), all failing with ImportError until Waves 1-2 ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → in-memory SQLite | Test DB is a fake (StaticPool); no connection to production Postgres |
| test harness → patched StorageBackend / litellm | Storage and LLM are mocked; no real S3 or LiteLLM calls in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | test fixture DB setup | low | accept | StaticPool in-memory SQLite verbatim from test_tree_index.py; no production DB reachable; accept at ASVS L1 |
| T-14-02 | Information Disclosure | patched storage/litellm in tests | low | accept | Storage and litellm.completion are mocked in-memory; no S3/LLM egress from the test suite |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this plan (pytest + pytest-asyncio already present per RESEARCH.md); Package Legitimacy Audit is a no-op |
</threat_model>

<verification>
After both tasks complete:
- `grep -v '^#' tests/unit/test_tree_search.py | grep -c 'def test_'` returns 8 or more.
- `grep -c 'RetrieverPlugin' tests/unit/test_builtin_plugins.py` returns 1 or more.
- `pytest tests/unit/test_tree_search.py --collect-only 2>&1 | grep ImportError` shows ImportError (correct RED state — tree_search / RetrieverPlugin / PageIndexRetriever absent until Waves 1-3).
</verification>

<success_criteria>
Wave 0 is complete when both test files exist, are parseable by pytest (collect-only enumerates or errors with ImportError), and every subsequent Wave 1-3 implementation task has a concrete pytest command to verify its RED→GREEN transition.
</success_criteria>

## Artifacts this phase produces (Wave 0 contribution)

New files:
- `tests/unit/test_tree_search.py` — scaffold for RETR-04..08 and the D-11 `_dict_to_tree` round-trip

Modified files:
- `tests/unit/test_builtin_plugins.py` — adds `RetrieverPlugin` import + `TestPageIndexRetriever` conformance class (2 methods)

New test functions (stubs):
- `TestHitContract::test_hit_citation_source_default`
- `TestDictToTree::test_dict_to_tree_roundtrip`
- `TestHeuristicRetriever::test_heuristic_no_llm`
- `TestHeuristicRetriever::test_citation_source_tree`
- `TestHeuristicRetriever::test_no_hardcoded_provider_model_ids`
- `TestLlmNav::test_llm_nav_degrades`
- `TestTwoStageSearch::test_two_stage_shortlist`
- `TestTwoStageSearch::test_parallel_load_and_skip`
- `TestPageIndexRetriever::test_pageindex_retriever_entry_point_registered`
- `TestPageIndexRetriever::test_pageindex_retriever_satisfies_protocol`

<output>
Create `.planning/phases/14-tree-retrieval/14-01-SUMMARY.md` when done
</output>
