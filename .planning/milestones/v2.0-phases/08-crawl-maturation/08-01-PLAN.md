---
phase: 08-crawl-maturation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_robots_ratelimit.py
  - tests/unit/test_crawl_all.py
  - tests/unit/test_enrich.py
  - tests/unit/test_linked_doc_ingest.py
autonomous: true
requirements:
  - CRAWL-01
  - CRAWL-02
  - CRAWL-03
  - ENRICH-07
  - INGEST-10

must_haves:
  truths:
    - All four Wave 0 test files exist with failing (xfail/skip) stubs covering every requirement
    - pytest tests/unit/ -v -x passes green (stubs do not error — they are xfail or skip)
    - TestAdaptiveRateLimiter class is present in test_robots_ratelimit.py
    - test_partial_enrichment, test_partial_cache_key, test_partial_not_returned_as_complete are present in test_enrich.py
  artifacts:
    - tests/unit/test_crawl_all.py
    - tests/unit/test_linked_doc_ingest.py
    - tests/unit/test_robots_ratelimit.py (extended)
    - tests/unit/test_enrich.py (extended)
  key_links:
    - Every stub imports the real symbol path so import errors surface immediately
    - xfail stubs use pytest.importorskip or pytest.mark.xfail so the suite stays green
---

<objective>
Wave 0 test scaffold: create all failing-but-importable test stubs for Phase 8 requirements before any implementation begins.

Purpose: Nyquist compliance — every automated verify command in subsequent plans must have a target file. RED stubs enforce the contract and catch import regressions immediately.
Output: Four test files with xfail/skip stubs; pytest tests/unit/ passes green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-crawl-maturation/08-CONTEXT.md
@.planning/phases/08-crawl-maturation/08-RESEARCH.md
@.planning/phases/08-crawl-maturation/08-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend test_robots_ratelimit.py and create test_crawl_all.py stubs (CRAWL-01/02/03)</name>
  <files>tests/unit/test_robots_ratelimit.py, tests/unit/test_crawl_all.py</files>
  <read_first>
    tests/unit/test_robots_ratelimit.py
    src/knowledge_lake/crawl/ratelimit.py
    src/knowledge_lake/pipeline/crawl.py
  </read_first>
  <behavior>
    - TestAdaptiveRateLimiter.test_record_error_increments_count: after record_error('http://example.com'), consecutive_errors for 'example.com' key is 1 — xfail (PerHostLimiter lacks record_error)
    - TestAdaptiveRateLimiter.test_reset_errors_clears_count: after record_error then reset_errors, consecutive_errors key is absent or 0 — xfail
    - TestAdaptiveRateLimiter.test_backoff_extra_exponential: record_error twice on 'http://example.com', backoff_extra returns min(base * 4, MAX_BACKOFF_SECONDS) — xfail
    - TestAdaptiveRateLimiter.test_backoff_extra_capped: record_error N times until base * 2**N exceeds MAX_BACKOFF_SECONDS; backoff_extra must not exceed MAX_BACKOFF_SECONDS — xfail
    - TestResolveDelay.test_rate_limit_rps_tier1: resolve_delay({'rate_limit_rps': 2.0}, None, 1.0) returns 0.5 — xfail (rps not yet supported)
    - TestResolveDelay.test_backoff_extra_raises_floor: resolve_delay({}, None, 1.0, backoff_extra=3.0) returns >= 1.0 + 3.0 — xfail (backoff_extra param not yet present)
    - test_crawl_all.py: test_crawl_all_sources_returns_summary: crawl_all_sources() returns dict with keys total, succeeded, failed, results — xfail (function not yet defined)
    - test_crawl_all.py: test_crawl_all_sources_failure_does_not_abort: one source raises, others still processed, failed count = 1 — xfail
    - test_crawl_all.py: test_crawl_all_sources_domain_filter: passing domain='healthcare' calls list_sources_for_crawl_all with domain='healthcare' — xfail
  </behavior>
  <action>
    Append a new TestAdaptiveRateLimiter class to the end of tests/unit/test_robots_ratelimit.py. Each test method in it uses pytest.mark.xfail(strict=False, reason="Phase 8 CRAWL-03 — not yet implemented") as a decorator. Each test imports from knowledge_lake.crawl.ratelimit. The backoff cap tests must import MAX_BACKOFF_SECONDS from knowledge_lake.crawl.ratelimit (will be defined in Plan 2).

    Also add two new test methods to the existing TestResolveDelay class: test_rate_limit_rps_tier1 and test_backoff_extra_raises_floor. Both decorated with pytest.mark.xfail(strict=False, reason="Phase 8 CRAWL-01 — not yet implemented").

    Create tests/unit/test_crawl_all.py from scratch. Import asyncio and pytest. Three async test functions decorated with pytest.mark.asyncio and pytest.mark.xfail(strict=False, reason="Phase 8 CRAWL-02 — not yet implemented"). Each function imports crawl_all_sources from knowledge_lake.pipeline.crawl (will exist after Plan 3).
  </action>
  <verify>
    <automated>pytest tests/unit/test_robots_ratelimit.py tests/unit/test_crawl_all.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    TestAdaptiveRateLimiter class with 4 methods present in test_robots_ratelimit.py; 2 new xfail methods in TestResolveDelay; test_crawl_all.py with 3 async xfail tests. All run without collection errors (only xfail/xpass outcomes).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend test_enrich.py and create test_linked_doc_ingest.py stubs (ENRICH-07/INGEST-10)</name>
  <files>tests/unit/test_enrich.py, tests/unit/test_linked_doc_ingest.py</files>
  <read_first>
    tests/unit/test_enrich.py
    src/knowledge_lake/pipeline/enrich.py
    src/knowledge_lake/pipeline/crawl.py
  </read_first>
  <behavior>
    - test_partial_enrichment: when LiteLLM response has finish_reason='length' with truncated JSON, enrich_document returns status='enriched' with is_partial=True in the result dict — xfail (is_partial not yet in return dict)
    - test_partial_cache_key: when finish_reason='length', the enriched artifact is stored under a content_hash starting with 'partial:' not the normal synthetic hash — xfail
    - test_partial_not_returned_as_complete: after a partial enrichment, calling enrich_document again for the same content returns status!='cached' (partial is not a cache hit for complete) — xfail
    - test_linked_doc_ingest.py: test_extract_linked_docs_pdf_only: _extract_linked_docs('html with href to .pdf and .html', 'http://example.com') returns only the .pdf link — xfail (_extract_linked_docs not yet defined)
    - test_linked_doc_ingest.py: test_extract_linked_docs_docx: href to .docx is also returned — xfail
    - test_linked_doc_ingest.py: test_max_linked_docs_cap: when HTML has more than MAX_LINKED_DOCS_PER_PAGE pdf links, only MAX_LINKED_DOCS_PER_PAGE are returned — xfail (MAX_LINKED_DOCS_PER_PAGE not yet defined)
    - test_linked_doc_ingest.py: test_ssrf_blocked_link_counted_as_failed: when validate_public_url raises ValueError for a linked URL, linked_docs_failed is incremented and the parent crawl continues — xfail (function not yet implemented)
  </behavior>
  <action>
    Append three new test functions (test_partial_enrichment, test_partial_cache_key, test_partial_not_returned_as_complete) to tests/unit/test_enrich.py. They reuse the existing engine/session fixtures from that file. Each is decorated with pytest.mark.xfail(strict=False, reason="Phase 8 ENRICH-07 — not yet implemented"). The test_partial_enrichment function sets up a mock LiteLLM response with finish_reason='length' and truncated JSON content, calls enrich_document, and asserts result.get('is_partial') is True.

    Create tests/unit/test_linked_doc_ingest.py from scratch. Four test functions with pytest.mark.xfail(strict=False, reason="Phase 8 INGEST-10 — not yet implemented"). Import _extract_linked_docs and MAX_LINKED_DOCS_PER_PAGE from knowledge_lake.pipeline.crawl (guarded by try/except ImportError to avoid collection failures before Plan 3 adds them). The SSRF test patches validate_public_url to raise ValueError and asserts that calling _crawl_loop or the linked-doc extraction path logs the failure and increments linked_docs_failed.
  </action>
  <verify>
    <automated>pytest tests/unit/test_enrich.py tests/unit/test_linked_doc_ingest.py -v 2>&1 | tail -25</automated>
  </verify>
  <done>
    Three new xfail stubs appended to test_enrich.py; test_linked_doc_ingest.py with 4 xfail tests. Full suite pytest tests/unit/ -v passes without collection errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | Tests import from production modules; import errors surface immediately |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering | test stubs | low | accept | xfail stubs are read-only assertions; no production code modified |
</threat_model>

<verification>
pytest tests/unit/ -v -x must pass green (all stubs are xfail — no ERROR or FAILED outcomes).
grep -c "TestAdaptiveRateLimiter" tests/unit/test_robots_ratelimit.py returns >= 1.
grep -c "test_partial_enrichment\|test_partial_cache_key\|test_partial_not_returned" tests/unit/test_enrich.py returns 3.
</verification>

<success_criteria>
All four Wave 0 test files exist with xfail stubs covering all 5 requirements (CRAWL-01/02/03, ENRICH-07, INGEST-10). pytest tests/unit/ -v -x exits 0.
</success_criteria>

<output>
Create .planning/phases/08-crawl-maturation/08-01-SUMMARY.md when done.
</output>

## Artifacts this phase produces

New files:
- `tests/unit/test_crawl_all.py` — xfail stubs for CRAWL-02 batch crawl tests
- `tests/unit/test_linked_doc_ingest.py` — xfail stubs for INGEST-10 linked-doc tests

Modified files:
- `tests/unit/test_robots_ratelimit.py` — adds `TestAdaptiveRateLimiter` class + 2 new `TestResolveDelay` xfail methods
- `tests/unit/test_enrich.py` — adds `test_partial_enrichment`, `test_partial_cache_key`, `test_partial_not_returned_as_complete`

Symbols added:
- Class `TestAdaptiveRateLimiter` (4 test methods)
- Functions `test_crawl_all_sources_returns_summary`, `test_crawl_all_sources_failure_does_not_abort`, `test_crawl_all_sources_domain_filter`
- Functions `test_partial_enrichment`, `test_partial_cache_key`, `test_partial_not_returned_as_complete`
- Functions `test_extract_linked_docs_pdf_only`, `test_extract_linked_docs_docx`, `test_max_linked_docs_cap`, `test_ssrf_blocked_link_counted_as_failed`
