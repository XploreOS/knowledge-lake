---
phase: 09-storage-segmentation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_put_raw_domain.py
  - tests/unit/test_format_tags.py
  - tests/unit/test_put_object_tags.py
  - tests/unit/test_put_bronze.py
autonomous: true
requirements:
  - STORE-01
  - STORE-02
must_haves:
  truths:
    - "pytest --collect-only on test_put_raw_domain.py, test_format_tags.py, test_put_object_tags.py exits 0 (no collection errors)"
    - "test_put_raw_domain.py contains TestPutRawDomainKey (2 methods) and TestDeduplicationOrderPreserved (1 method), all xfail(strict=False)"
    - "test_format_tags.py contains module-level ImportError guard for _format_tags and 2 pure-function test methods"
    - "test_put_object_tags.py contains TestPutObjectTagging (1 method) and TestTaggingBestEffortFallback (1 method), all xfail(strict=False)"
    - "test_put_bronze.py contains TestPutBronzeDomainKey class with 2 xfail(strict=False) methods"
    - "python -m pytest tests/unit/ -v -x exits 0 (xfailed tests do not fail the suite)"
  artifacts:
    - tests/unit/test_put_raw_domain.py
    - tests/unit/test_format_tags.py
    - tests/unit/test_put_object_tags.py
  key_links:
    - "test_put_raw_domain.py and test_put_object_tags.py copy the engine/session/mock_storage fixture pattern from tests/unit/test_put_bronze.py lines 22-64"
    - "test_format_tags.py imports _format_tags from knowledge_lake.storage.s3 under try/except ImportError"
    - "TestPutBronzeDomainKey in test_put_bronze.py reuses existing module-level fixtures without duplication"
---

<objective>
Wave 0 test scaffold — storage layer (STORE-01 and STORE-02). Creates the RED-state test files that Plans 03 through 05 will make green.

Purpose: Establish verifiable contracts for domain-scoped raw/bronze keys (STORE-01) and S3 object tag encoding (STORE-02) before any production code is changed, following the established Wave 0 TDD pattern from Phase 8 Plan 01.

Output: 3 new test files + 1 new class in existing test_put_bronze.py. All new tests are xfail(strict=False) so the unit suite continues to pass while implementation is pending.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-storage-segmentation/09-CONTEXT.md
@.planning/phases/09-storage-segmentation/09-RESEARCH.md
@.planning/phases/09-storage-segmentation/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/unit/test_put_raw_domain.py — STORE-01 raw key + dedup ordering RED tests</name>
  <files>tests/unit/test_put_raw_domain.py</files>
  <read_first>
    - tests/unit/test_put_bronze.py (lines 1-80) — copy engine/session/mock_storage fixtures verbatim; Source creation pattern at line 68-80
    - src/knowledge_lake/storage/s3.py (lines 153-260) — put_raw signature and WORM layer structure to understand what the tests exercise
    - src/knowledge_lake/registry/repo.py (lines 822-845) — get_domain_for_source and get_source signatures for context
  </read_first>
  <action>
Create a new file at tests/unit/test_put_raw_domain.py.

Module docstring: "RED-state tests for StorageBackend.put_raw domain-scoped key construction and dedup ordering (STORE-01). Tests are xfail(strict=False) until Plan 09-03 adds the domain kwarg to put_raw."

Imports: copy the same imports as test_put_bronze.py (MagicMock, patch, pytest, create_engine, Session). Also import hashlib to compute SAMPLE_HASH. Import ClientError from botocore.exceptions.

Constants at module top: SAMPLE_DATA = b"test raw content for domain key testing", SAMPLE_EXT = "pdf", SAMPLE_HASH = hashlib.sha256(SAMPLE_DATA).hexdigest().

Fixtures: copy the engine, session, and mock_storage fixtures exactly from test_put_bronze.py lines 22-64 (scope="module" engine, function-scoped session with rollback, mock_storage with patch("boto3.client") and head_object raising 404 ClientError). Also add a source fixture that calls registry_repo.create_source(session, name="test-domain-source", source_type="upload") and session.flush(), returning the source.

Class TestPutRawDomainKey: two methods, each decorated with @pytest.mark.xfail(strict=False, reason="STORE-01: put_raw domain kwarg pending Plan 09-03").
- test_domain_segment_in_raw_key(self, mock_storage, source, session): get backend and mock_client from mock_storage; call backend.put_raw(source.id, SAMPLE_DATA, SAMPLE_EXT, session, domain="healthcare"); assert mock_client.put_object.called; inspect the Key kwarg of the call and assert it starts with "raw/healthcare/" and ends with f"{SAMPLE_HASH}.{SAMPLE_EXT}".
- test_none_domain_uses_unclassified_segment(self, mock_storage, source, session): same but domain=None; assert Key starts with "raw/_unclassified/" and ends with the same hash suffix.

Class TestDeduplicationOrderPreserved: one method decorated with @pytest.mark.xfail(strict=False, reason="STORE-01: put_raw domain kwarg pending Plan 09-03").
- test_no_put_object_when_artifact_already_in_registry(self, mock_storage, source, session): import create_raw_artifact from knowledge_lake.registry.repo; seed an artifact with content_hash=SAMPLE_HASH and storage_uri="s3://test-bucket/raw/_unclassified/{source.id}/{SAMPLE_HASH}.pdf" via create_raw_artifact(session, source_id=source.id, content_hash=SAMPLE_HASH, storage_uri=...); session.flush(); call backend.put_raw(source.id, SAMPLE_DATA, SAMPLE_EXT, session, domain="healthcare"); assert mock_client.put_object.not_called() (the registry no-op fires before key construction and before any S3 write).

Do not add any tests for features not in STORE-01 (no tag checks here — those are in test_put_object_tags.py).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_put_raw_domain.py --collect-only -q 2>&1 | grep -E "^(tests/|ERROR)" | head -20 && python -m pytest tests/unit/test_put_raw_domain.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_put_raw_domain.py exists with non-zero byte size
    - pytest --collect-only reports 3 tests collected (no ERROR lines in collection output)
    - pytest -v shows all 3 tests as XFAIL (not ERROR, not FAILED)
    - Class names TestPutRawDomainKey and TestDeduplicationOrderPreserved present: grep -c "class TestPutRaw\|class TestDedup" tests/unit/test_put_raw_domain.py == 2
  </acceptance_criteria>
  <done>test_put_raw_domain.py exists, 3 tests collected, all XFAIL, suite still green</done>
</task>

<task type="auto">
  <name>Task 2: Create test_format_tags.py + test_put_object_tags.py — STORE-02 tag encoding + best-effort fallback RED tests</name>
  <files>tests/unit/test_format_tags.py, tests/unit/test_put_object_tags.py</files>
  <read_first>
    - src/knowledge_lake/storage/s3.py (lines 81-95) — current put_object signature to understand what changes are coming
    - tests/unit/test_put_bronze.py (lines 40-64) — mock_storage fixture to copy into test_put_object_tags.py
    - .planning/phases/09-storage-segmentation/09-RESEARCH.md (Pattern 1, Pattern 2) — exact expected behavior of _format_tags and best-effort fallback
  </read_first>
  <action>
Create tests/unit/test_format_tags.py. This file tests only the pure _format_tags helper function — no DB, no S3 mock needed.

Module docstring: "RED-state tests for _format_tags helper (STORE-02). Uses ImportError guard because _format_tags is not yet defined in s3.py."

At module level, import _format_tags under a try/except ImportError guard: attempt `from knowledge_lake.storage.s3 import _format_tags` and on ImportError set `_format_tags = None`. This prevents collection failures while the symbol is absent.

Two test functions (not in a class):
- test_format_tags_produces_urlencode_string: if _format_tags is None, call pytest.skip("_format_tags not yet defined in s3.py"); call _format_tags({"domain": "healthcare", "format": "html"}) and assert the result equals "domain=healthcare&format=html" (URL-encoded string, not JSON).
- test_tag_value_truncated_at_256_chars: if _format_tags is None, call pytest.skip; create a dict {"key": "x" * 300}; call _format_tags on it; assert the value portion of the result string has length 256 (the raw 300-char value is truncated to 256 before encoding).

Both functions decorated with @pytest.mark.xfail(strict=False, reason="STORE-02: _format_tags not yet defined in s3.py").

---

Create tests/unit/test_put_object_tags.py. This file tests the tags kwarg on put_object and the best-effort ClientError fallback.

Module docstring: "RED-state tests for StorageBackend.put_object tags support (STORE-02, D-07, D-08, D-10). Tests are xfail(strict=False) until Plan 09-03 adds the tags kwarg to put_object."

Copy engine, session, and mock_storage fixtures exactly from test_put_bronze.py (same pattern). Do not import or reuse fixtures from another test module — define them locally.

Class TestPutObjectTagging: method test_tags_passed_as_tagging_kwarg decorated with @pytest.mark.xfail(strict=False, reason="STORE-02: put_object tags kwarg pending Plan 09-03"). In the method: get backend and mock_client from mock_storage; call backend.put_object("raw/_unclassified/src1/hash.pdf", b"data", tags={"domain": "healthcare", "format": "pdf"}); assert mock_client.put_object.called; inspect the call kwargs and assert "Tagging" is present in the kwargs dict; assert the Tagging value contains "domain=healthcare" (URL-encoded format, not a dict).

Class TestTaggingBestEffortFallback: method test_clienterror_retries_without_tags decorated with @pytest.mark.xfail(strict=False, reason="STORE-02: put_object tags kwarg pending Plan 09-03"). In the method: configure mock_client.put_object to raise ClientError({"Error": {"Code": "AccessDenied"}}, "PutObject") on the first call, then succeed on the second; call backend.put_object("some/key", b"data", tags={"domain": "x"}); assert no exception was raised (the fallback succeeds); assert mock_client.put_object.call_count == 2 (first with Tagging, second without).
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_format_tags.py tests/unit/test_put_object_tags.py --collect-only -q 2>&1 | grep -E "^(tests/|ERROR)" | head -20 && python -m pytest tests/unit/test_format_tags.py tests/unit/test_put_object_tags.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_format_tags.py and tests/unit/test_put_object_tags.py both exist
    - pytest --collect-only reports 4 tests total (2 from each file), no collection ERROR lines
    - All 4 tests show as XFAIL (or SKIP if ImportError guard triggers)
    - grep -c "xfail" tests/unit/test_format_tags.py returns at least 2 (decorators present)
    - grep -c "class TestPutObjectTagging\|class TestTaggingBestEffort" tests/unit/test_put_object_tags.py == 2
  </acceptance_criteria>
  <done>test_format_tags.py and test_put_object_tags.py exist, 4 tests collected, all XFAIL/SKIP, suite passes</done>
</task>

<task type="auto">
  <name>Task 3: Add TestPutBronzeDomainKey class to tests/unit/test_put_bronze.py — STORE-01 bronze key RED tests</name>
  <files>tests/unit/test_put_bronze.py</files>
  <read_first>
    - tests/unit/test_put_bronze.py (full file) — existing fixtures, class structure, and end-of-file location to append the new class
    - src/knowledge_lake/storage/s3.py (lines 263-360) — put_bronze current signature to understand what the domain kwarg will add
  </read_first>
  <action>
Read the full test_put_bronze.py file to find the end of the last class/function. Append a new class TestPutBronzeDomainKey after all existing content.

The new class reuses the module-level engine, session, and mock_storage fixtures already defined in the file — do not redefine them.

Class TestPutBronzeDomainKey: two methods, each decorated with @pytest.mark.xfail(strict=False, reason="STORE-01: put_bronze domain kwarg pending Plan 09-03").

The class also needs a source_and_raw fixture reference. Either use the existing source_and_raw fixture from the module or define a similar pattern inline in each test method. Prefer the module-level source_and_raw fixture if it already provides a source and raw artifact.

- test_domain_segment_in_bronze_key(self, mock_storage, source_and_raw, session): get backend and mock_client from mock_storage; get source and raw from source_and_raw; call backend.put_bronze(source.id, b"bronze content data", "md", session, parent_artifact_id=raw.id, domain="healthcare"); assert mock_client.put_object.called; inspect Key kwarg; assert it starts with "bronze/healthcare/" and ends with the sha256 hash of the bronze bytes followed by ".md".
- test_none_domain_uses_unclassified_segment(self, mock_storage, source_and_raw, session): same but domain=None; assert Key starts with "bronze/_unclassified/".

The SAMPLE_HASH constant from the existing file is for raw content — use a different literal for bronze content (e.g., b"bronze content data") and compute its hash inline in the test as hashlib.sha256(b"bronze content data").hexdigest() if needed for the assertion. Import hashlib at the top of the class or inline.

Do not modify any existing test classes or fixtures.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_put_bronze.py --collect-only -q 2>&1 | tail -10 && python -m pytest tests/unit/test_put_bronze.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "class TestPutBronzeDomainKey" tests/unit/test_put_bronze.py == 1
    - pytest --collect-only on test_put_bronze.py shows 2 more tests than before this task (the new xfail methods)
    - All new TestPutBronzeDomainKey tests show as XFAIL in pytest -v output
    - Existing tests in test_put_bronze.py continue to pass (not modified)
  </acceptance_criteria>
  <done>TestPutBronzeDomainKey class added to test_put_bronze.py, 2 new xfail tests collected, existing tests unaffected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production imports | Test files import production symbols; ImportError guards prevent collection failures when symbols are absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-SC | Tampering | test xfail decorators | low | accept | xfail(strict=False) means the test fails loudly if production code accidentally makes it pass incorrectly; strict=False is intentional for scaffold plans |
</threat_model>

<verification>
After all three tasks complete:

```
python -m pytest tests/unit/ -v -x 2>&1 | tail -20
```

Expected: 0 FAILED, new tests appear as XFAIL. Existing suite unchanged.
</verification>

<success_criteria>
- 3 new test files created: test_put_raw_domain.py (3 tests), test_format_tags.py (2 tests), test_put_object_tags.py (2 tests)
- 1 new class added to test_put_bronze.py: TestPutBronzeDomainKey (2 tests)
- All 9 new tests collect without errors and show as XFAIL or SKIP
- Full unit test suite (tests/unit/) passes with 0 failures
</success_criteria>

<output>
Create .planning/phases/09-storage-segmentation/09-01-SUMMARY.md when done.
</output>

## Artifacts this phase produces

New files:
- `tests/unit/test_put_raw_domain.py` — classes: `TestPutRawDomainKey`, `TestDeduplicationOrderPreserved`
- `tests/unit/test_format_tags.py` — functions: `test_format_tags_produces_urlencode_string`, `test_tag_value_truncated_at_256_chars`
- `tests/unit/test_put_object_tags.py` — classes: `TestPutObjectTagging`, `TestTaggingBestEffortFallback`

Modified files:
- `tests/unit/test_put_bronze.py` — new class: `TestPutBronzeDomainKey`
