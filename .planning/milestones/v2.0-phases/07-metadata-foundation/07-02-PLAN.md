---
phase: 07-metadata-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge_lake/registry/repo.py
  - src/knowledge_lake/pipeline/index.py
  - src/knowledge_lake/pipeline/ingest.py
  - tests/unit/test_index_payload.py
autonomous: true
requirements:
  - PAYLOAD-01
must_haves:
  truths:
    - "Every newly indexed chunk payload contains source_id, source_name, source_url, format, tags, title, organization"
    - "All 7 new fields degrade gracefully to None/[] when source row is absent or config is empty (per D-03)"
    - "get_source() in repo.py returns Optional[Source] via session.get(Source, source_id) — same PK-lookup pattern as get_artifact()"
    - "register_source() in ingest.py persists tags and organization into Source.config alongside domain (per D-05)"
    - "The source join happens once per index() call inside the existing get_session() block, not per chunk (per D-01)"
  artifacts:
    - src/knowledge_lake/registry/repo.py
    - src/knowledge_lake/pipeline/index.py
    - src/knowledge_lake/pipeline/ingest.py
    - tests/unit/test_index_payload.py
  key_links:
    - "index.py payload dict extended: 7 new keys added per D-02 field mapping table"
    - "get_source() added to repo.py: used by index.py join block"
    - "register_source() in ingest.py: tags/organization kwargs added to function signature and config dict"
    - "test_index_payload.py extended: new test class TestPayloadSourceFields verifies all 7 new payload fields"
---

<objective>
Deliver the PAYLOAD-01 payload expansion: add `get_source()` to the registry, extend the
`index()` join block to assemble all 7 new payload fields, fix `register_source()` to persist
`tags`/`organization` into `Source.config`, and extend `test_index_payload.py` to assert
the new payload fields.

This plan is independent of Plan 01 and Plan 03 and runs in Wave 1 in parallel with Plan 01.

Purpose: satisfy PAYLOAD-01 — every newly indexed chunk carries provenance metadata traceable
to its source row without breaking backward compatibility for old points or existing callers.

Output: 4 modified files; all test_index_payload tests pass GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-metadata-foundation/07-CONTEXT.md
@.planning/phases/07-metadata-foundation/07-RESEARCH.md
@.planning/phases/07-metadata-foundation/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_source() to repo.py and extend index.py payload join (per D-01, D-02)</name>
  <files>src/knowledge_lake/registry/repo.py, src/knowledge_lake/pipeline/index.py</files>

  <read_first>
    - src/knowledge_lake/registry/repo.py lines 315-335 (get_artifact pattern), lines 820-831 (get_domain_for_source pattern — exact match for new getter style)
    - src/knowledge_lake/pipeline/index.py lines 87-134 (full join block and payload dict to extend)
    - src/knowledge_lake/registry/models.py lines 55-85 (Source model: name, url, source_type, config columns)
    - .planning/phases/07-metadata-foundation/07-RESEARCH.md Pattern 2 (get_source_metadata shape), Pattern 2 usage in index.py join block
    - .planning/phases/07-metadata-foundation/07-PATTERNS.md repo.py and index.py pattern assignments
    - .planning/phases/07-metadata-foundation/07-CONTEXT.md D-01, D-02, D-03, D-04
  </read_first>

  <behavior>
    - get_source() in repo.py: session.get(Source, source_id) returns Optional[Source]; None when missing.
    - index() join block: calls get_source(session, source_id) once inside the existing with get_session() block (not per chunk). Assigns source_name, source_url, fmt, tags, organization from the Source row. All degrade to None/[].
    - Payload dict: adds source_id, source_name, source_url, format (key name "format"), tags, title, organization alongside existing keys.
    - title comes from enrichment_metadata.get("title"), degrades to None.
    - format = Source.source_type (the short label: "html", "pdf", "csv") per D-04.
    - tags = (Source.config or {}).get("tags", []) — list, degrades to [].
  </behavior>

  <action>
    Step 1 — repo.py: add `get_source()` function after `get_domain_for_source()` (around line 831).
    Follow the exact same structure as `get_artifact` (line 323): single session.get() call, returns
    Optional[Source], docstring "Fetch a Source by its primary key. Returns None if not found (does not raise)."
    `Source` is already imported at line 46 — no new import needed.

    Signature: `def get_source(session: Session, source_id: str) -> Optional["Source"]:` — check the
    existing import style in repo.py for Source to use the correct import path (direct import, not
    string annotation if Source is already imported directly).

    Step 2 — index.py: inside the `with get_session() as session:` block that starts at line 90, add a
    third lookup after the existing `enriched = ...` block. The lookup must be:
      source = registry_repo.get_source(session, source_id) if source_id is not None else None
    where `source_id` is extracted from `parsed_artifact.source_id if parsed_artifact is not None else None`.

    After the session block closes, derive the 7 new payload variables:
      _sc = (source.config or {}) if source is not None else {}
      source_name = source.name if source is not None else None
      source_url = source.url if source is not None else None
      fmt = source.source_type if source is not None else None      # D-04: source_type IS the format label
      tags = _sc.get("tags", [])
      organization = _sc.get("organization")
      title = enrichment_metadata.get("title")
      source_id_val = parsed_artifact.source_id if parsed_artifact is not None else None

    Add these 7 keys to the payload dict (lines 115-126):
      "source_id": source_id_val,
      "source_name": source_name,
      "source_url": source_url,
      "format": fmt,
      "tags": tags,
      "title": title,
      "organization": organization,

    CRITICAL: all 7 new keys must be inside the payload dict literal (alongside the existing keys),
    not appended after vstore.upsert. The payload dict is assembled once before the for-loop that
    builds VectorPoints — wait, looking at the code: the payload dict IS inside the for-loop body
    (lines 115-126 inside `for chunk, vector in zip(chunks, vectors):`). The source_id_val, source_name,
    etc. variables are computed OUTSIDE the loop (after the session block). Only `full_chunk_id` and
    `qdrant_point_id` are chunk-specific. So add the 7 new key-value pairs to the payload dict at
    lines 115-126, but the variable assignments (`source_name = ...` etc.) happen outside the for-loop
    (after the `with get_session() as session:` block ends). This is correct and follows D-01.

    Also update the module-level docstring at lines 1-25 to list the 7 new payload fields by name.

    Do NOT change the for-loop structure, do NOT add per-chunk DB calls.
  </action>

  <verify>
    <automated>uv run pytest tests/unit/test_index_payload.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "def get_source" src/knowledge_lake/registry/repo.py` returns a hit
    - `grep -n "source_name" src/knowledge_lake/pipeline/index.py` returns a hit inside the payload dict
    - `grep -n "\"format\"" src/knowledge_lake/pipeline/index.py` returns a hit inside the payload dict
    - `grep -n "\"tags\"" src/knowledge_lake/pipeline/index.py` returns a hit inside the payload dict
    - `grep -n "\"title\"" src/knowledge_lake/pipeline/index.py` returns a hit inside the payload dict
    - `grep -n "\"organization\"" src/knowledge_lake/pipeline/index.py` returns a hit inside the payload dict
    - `uv run pytest tests/unit/test_index_payload.py -x -q` passes (existing tests still GREEN before new tests added in Task 2)
  </acceptance_criteria>

  <done>
    get_source() added to repo.py; index.py payload dict carries all 7 new fields; existing
    test_index_payload tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix register_source() tags gap and extend test_index_payload.py (per D-05)</name>
  <files>src/knowledge_lake/pipeline/ingest.py, tests/unit/test_index_payload.py</files>

  <read_first>
    - src/knowledge_lake/pipeline/ingest.py lines 230-310 (register_source() full function)
    - tests/unit/test_index_payload.py full file — understand all existing test classes, fixture shapes, _captured_payload helper
    - .planning/phases/07-metadata-foundation/07-PATTERNS.md ingest.py pattern assignment (current config dict, correct api/app.py config pattern)
    - .planning/phases/07-metadata-foundation/07-RESEARCH.md Pitfall 3 (register_source() gap vs CLI/API init paths), Pattern 2 usage in index.py
    - .planning/phases/07-metadata-foundation/07-CONTEXT.md D-05, D-06
  </read_first>

  <behavior>
    register_source() new behavior:
    - Accepts new optional kwargs: tags: Optional[list[str]] = None, organization: Optional[str] = None
    - Config dict construction: start with empty dict; add "domain" if domain; add "tags" if tags; add "organization" if organization; assign config = config_dict or None.
    - Existing callers with no tags/organization kwarg produce identical behavior to today (backward-compatible).

    test_index_payload.py new test class TestPayloadSourceFields:
    - test_payload_includes_all_7_new_fields_when_source_has_metadata: creates a Source with name="IFM", url="https://ifm.org", source_type="html", config={"domain": "healthcare", "tags": ["functional-medicine", "ifm"], "organization": "IFM"}. Creates enriched artifact with metadata_ containing "title": "Functional Medicine Basics". Calls index(). Asserts payload["source_id"] == source.id, payload["source_name"] == "IFM", payload["source_url"] == "https://ifm.org", payload["format"] == "html", payload["tags"] == ["functional-medicine", "ifm"], payload["title"] == "Functional Medicine Basics", payload["organization"] == "IFM".
    - test_payload_source_fields_degrade_gracefully_when_no_source: calls index() with a parsed_artifact_id that has no parent Source row (source_id is None or source not in registry). Asserts payload["source_name"] is None, payload["source_url"] is None, payload["format"] is None, payload["tags"] == [], payload["title"] is None, payload["organization"] is None.
    - test_payload_title_from_enriched_metadata: creates Source (no org/tags in config). Creates enriched artifact with metadata_ = {"title": "HIPAA Security Rule", "document_type": "regulation"}. Asserts payload["title"] == "HIPAA Security Rule".
    - test_register_source_persists_tags_into_config: calls register_source(url, name, domain="healthcare", tags=["fhir", "hl7"]). Queries the resulting Source row. Asserts source.config["tags"] == ["fhir", "hl7"].
  </behavior>

  <action>
    Step 1 — ingest.py: extend `register_source()` function at line 230.

    Add two new keyword-only parameters after `source_type_override`:
      tags: Optional[list[str]] = None,
      organization: Optional[str] = None,

    Replace the config dict construction at line 277 (current: `config = {"domain": domain} if domain else None`)
    with a multi-step construction that preserves all existing behavior:
      config_dict: dict = {}
      if domain:
          config_dict["domain"] = domain
      if tags:
          config_dict["tags"] = tags
      if organization:
          config_dict["organization"] = organization
      config = config_dict if config_dict else None

    Also update the Args docstring for register_source() to document the two new parameters.
    Do NOT change the dedup path (line 262-274) — existing sources without tags keep their config as-is.

    Step 2 — test_index_payload.py: add class TestPayloadSourceFields with the 4 test methods
    described in the behavior block. Reuse the existing engine, _patch_engine, session, and
    fake_vstore fixtures (already defined at module level in this file). Use the existing
    _captured_payload() helper. Use the same Source/Artifact creation pattern already present in
    the file's other test classes.

    For the degradation test (no source row), create a parsed artifact whose source_id points to a
    Source that exists in the test DB but has config=None (already covered by existing
    test_payload_domain_none_when_source_config_empty pattern), OR create a parsed artifact with
    source_id=None via a direct ORM insert. The simplest approach: use
    `registry_repo.create_source(session, name="NoMeta", source_type="web", config=None)` and verify
    that the absence of tags/organization in config produces degraded values.

    For test_register_source_persists_tags_into_config: this test cannot use the in-memory SQLite
    engine fixture because register_source() opens its own get_session(). Instead, monkeypatch
    `knowledge_lake.pipeline.ingest.get_session` to return a context manager that uses the test
    session — OR, simpler: call register_source() and immediately query the Source via the test
    session. Mirror the pattern from existing tests that also monkeypatch get_session in other test
    files (check tests/unit/ for the _patch_engine autouse fixture: it monkeypatches registry_db.get_engine,
    which is what get_session() uses internally — so ingest.register_source() will use the test
    engine automatically via the autouse _patch_engine fixture).
  </action>

  <verify>
    <automated>uv run pytest tests/unit/test_index_payload.py -x -v</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "tags: Optional\[list\[str\]\]" src/knowledge_lake/pipeline/ingest.py` returns a hit in register_source() signature
    - `grep -n "organization: Optional\[str\]" src/knowledge_lake/pipeline/ingest.py` returns a hit in register_source() signature
    - `grep -n "config_dict" src/knowledge_lake/pipeline/ingest.py` returns a hit (new dict-building pattern)
    - `grep -n "class TestPayloadSourceFields" tests/unit/test_index_payload.py` returns a hit
    - `uv run pytest tests/unit/test_index_payload.py -x -v` — all tests PASS (GREEN), including the 4 new test methods
    - `uv run pytest tests/unit/ -x -q` — all unit tests pass (no regressions)
  </acceptance_criteria>

  <done>
    register_source() accepts tags/organization and persists them into Source.config. All
    test_index_payload tests pass including 4 new TestPayloadSourceFields tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → register_source() | tags and organization are caller-supplied list[str]/str; stored into Source.config JSON column |
| index() session join → Qdrant payload | source metadata fields are read from trusted registry DB and written to Qdrant payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | index.py payload | low | accept | source_url and organization are already public registry fields; exposing them in Qdrant payload adds no new disclosure risk beyond what the registry already stores |
| T-07-02-02 | Tampering | register_source() tags | low | accept | tags is an Optional[list[str]] written to Source.config JSON column via SQLAlchemy ORM; no raw SQL. Input originates from sources.yaml (trusted operator-controlled file) or crawl.py (operator-controlled code path) |
| T-07-02-03 | Denial of Service | register_source() oversized tags list | low | accept | tags is sourced from sources.yaml (operator-controlled) and from crawl.py callers; no external user input path exists for this parameter in the current codebase. Acceptable per ASVS L1 |
</threat_model>

<verification>
`uv run pytest tests/unit/test_index_payload.py -v` — all tests GREEN, including 4 new source-field tests.
`uv run pytest tests/unit/ -q` — no regressions.
</verification>

<success_criteria>
- get_source() added to repo.py; returns Optional[Source]
- index() payload dict carries source_id, source_name, source_url, format, tags, title, organization
- register_source() persists tags and organization into Source.config
- test_index_payload.py: TestPayloadSourceFields with 4 passing tests
- All existing tests still pass
</success_criteria>

## Artifacts this phase produces

New symbols:
- `src/knowledge_lake/registry/repo.py`: `get_source(session, source_id) -> Optional[Source]`
- `src/knowledge_lake/pipeline/index.py`: payload keys "source_id", "source_name", "source_url", "format", "tags", "title", "organization" added to the payload dict in `index()`
- `src/knowledge_lake/pipeline/ingest.py`: `register_source()` new kwargs `tags: Optional[list[str]] = None`, `organization: Optional[str] = None`
- `tests/unit/test_index_payload.py`: `TestPayloadSourceFields` class with 4 test methods

<output>
Create `.planning/phases/07-metadata-foundation/07-02-SUMMARY.md` when done.
</output>
