---
phase: 10-hybrid-retrieval
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_settings_search.py
  - tests/unit/test_search_mode.py
  - tests/unit/test_cli_search_mode.py
  - tests/unit/test_api_search_mode.py
  - tests/unit/test_search_filters.py
autonomous: true
requirements:
  - RETR-01
  - RETR-03
must_haves:
  truths:
    - "test_settings_search.py asserts Settings(_env_file=None).search.mode == 'hybrid' (default) and KLAKE_SEARCH__MODE=dense resolves to settings.search.mode == 'dense' (RETR-03, D-08)"
    - "test_search_mode.py asserts pipeline.search threads mode + a sparse_query into vstore.search.call_args.kwargs when mode in ('hybrid','sparse') (RETR-03, D-09, D-03)"
    - "test_search_mode.py asserts a hybrid/sparse request against a sparse-less collection raises a clear error naming the missing 'sparse' vector + remediation (RETR-03, D-10)"
    - "test_cli_search_mode.py asserts `klake search --mode hybrid` forwards mode='hybrid' into pipeline.search (RETR-03)"
    - "test_api_search_mode.py asserts GET /search?mode=hybrid forwards mode into pipeline.search and that an invalid mode returns HTTP 422 (RETR-03, T-10-02)"
    - "test_search_filters.py gains an assertion that the reused filter object attaches to each prefetch branch AND the top level in hybrid mode (D-14)"
  artifacts:
    - tests/unit/test_settings_search.py (NEW)
    - tests/unit/test_search_mode.py (NEW)
    - tests/unit/test_cli_search_mode.py (NEW)
    - tests/unit/test_api_search_mode.py (NEW)
    - tests/unit/test_search_filters.py (extended — new D-14 prefetch-filter assertion)
  key_links:
    - "test_settings_search.py mirrors tests/unit/test_settings.py:21-58 (Settings(_env_file=None) + patch.dict(os.environ))"
    - "test_search_mode.py / test_cli_search_mode.py / test_api_search_mode.py mirror tests/unit/test_search_filters.py:19-32 fake_embedder/fake_vstore monkeypatch fixtures"
    - "RED markers (xfail strict=False + ImportError guards) keep the suite green until Plans 10-04/10-06/10-07/10-08 land the symbols"
---

<objective>
Wave 0 — RED test scaffold for the RETR-03 mode surface and the D-14 filter-parity guarantee. Creates four new unit files plus one extension: settings resolution of KLAKE_SEARCH__MODE, mode + sparse_query threading through pipeline.search, the fail-loud contract on a sparse-less collection, CLI `--mode` and API `?mode=` threading (with invalid-mode rejection), and an assertion that the Phase-7 filter attaches identically across dense/sparse/hybrid.

Purpose: Encodes the RETR-03 acceptance behaviors and the D-14 filter contract from 10-VALIDATION.md as concrete automated targets before implementation exists.

Output: Four new RED unit test files plus an extended test_search_filters.py, all collecting clean and green-or-xfail so the suite stays green.
</objective>

<artifacts_produced>
- `tests/unit/test_settings_search.py` — `test_search_mode_default_hybrid`, `test_search_mode_env`
- `tests/unit/test_search_mode.py` — `test_mode_threads_sparse_query`, `test_fail_loud_missing_sparse`, `test_dense_mode_no_sparse_query`
- `tests/unit/test_cli_search_mode.py` — `test_cli_mode_forwarded`
- `tests/unit/test_api_search_mode.py` — `test_api_mode_forwarded`, `test_api_invalid_mode_422`
- `tests/unit/test_search_filters.py` — extended with `test_filter_attaches_each_prefetch_branch` (D-14)
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-retrieval/10-CONTEXT.md
@.planning/phases/10-hybrid-retrieval/10-RESEARCH.md
@.planning/phases/10-hybrid-retrieval/10-PATTERNS.md
@.planning/phases/10-hybrid-retrieval/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings + pipeline mode RED scaffolds — test_settings_search.py, test_search_mode.py (RETR-03, D-08, D-09, D-10)</name>
  <files>tests/unit/test_settings_search.py, tests/unit/test_search_mode.py</files>
  <read_first>
    - tests/unit/test_settings.py (lines 21-58) — Settings(_env_file=None) + patch.dict(os.environ) pattern
    - tests/unit/test_search_filters.py (lines 19-32) — fake_embedder/fake_vstore monkeypatch fixtures on search_module
    - src/knowledge_lake/config/settings.py (lines 282-320) — IndexSettings + model_config env_nested_delimiter="__"
    - src/knowledge_lake/pipeline/search.py (lines 34-47, 81-127) — search() signature, settings resolution, vstore.search call site
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (g) — the sparse-presence probe / fail-loud error the test asserts
  </read_first>
  <action>
    Create tests/unit/test_settings_search.py mirroring test_settings.py: test_search_mode_default_hybrid asserts Settings(_env_file=None).search.mode == "hybrid"; test_search_mode_env uses patch.dict(os.environ, {"KLAKE_SEARCH__MODE": "dense"}) then asserts Settings(_env_file=None).search.mode == "dense" (D-08). Guard the settings.search access with try/except AttributeError at module scope or per-test so collection does not fail before Plan 10-04 adds SearchSettings; xfail(strict=False) each RED test pointing at Plan 10-04.

    Create tests/unit/test_search_mode.py mirroring test_search_filters.py's fake_embedder/fake_vstore monkeypatch fixtures on knowledge_lake.pipeline.search:
    - test_mode_threads_sparse_query: call search("q", mode="hybrid"); assert fake_vstore.search.call_args.kwargs carries mode == "hybrid" and a non-None sparse_query (D-09, D-03). Monkeypatch the sparse-embedder query function so no real fastembed model is needed.
    - test_dense_mode_no_sparse_query: call search("q", mode="dense"); assert sparse_query kwarg is None (back-compat, D-09).
    - test_fail_loud_missing_sparse: configure fake_vstore.search to raise the fail-loud error type the store will raise when sparse is absent; assert search propagates a clear error naming the missing "sparse" vector and the `klake reindex --hybrid` remediation, and does NOT fall back to dense (D-10).
    Mark RED tests xfail(strict=False) referencing Plans 10-06/10-07.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings_search.py tests/unit/test_search_mode.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both files collect with zero errors; RED tests report xfail
    - test_settings_search.py exercises BOTH the default (hybrid) and the KLAKE_SEARCH__MODE=dense override paths
    - test_search_mode.py asserts the fail-loud error names the missing vector and the remediation, with no silent dense fallback
  </acceptance_criteria>
  <done>Settings + pipeline mode RED scaffolds exist as concrete targets for Plans 10-04, 10-06, 10-07.</done>
</task>

<task type="auto">
  <name>Task 2: CLI + API mode RED scaffolds — test_cli_search_mode.py, test_api_search_mode.py (RETR-03, T-10-02)</name>
  <files>tests/unit/test_cli_search_mode.py, tests/unit/test_api_search_mode.py</files>
  <read_first>
    - tests/unit/test_search_filters.py (lines 1-60) — monkeypatch style for the pipeline search seam
    - src/knowledge_lake/cli/app.py (lines 671-726) — cmd_search + its search() call
    - src/knowledge_lake/api/app.py (lines 155-262) — search_endpoint + its search() delegation and 422 validation style (lines 235-242)
    - src/knowledge_lake/api/schemas.py (lines 30-52, 168-172) — SearchParams + the ExportRequest.kind bounded-pattern precedent
  </read_first>
  <action>
    Create tests/unit/test_cli_search_mode.py: using typer.testing.CliRunner (or by monkeypatching knowledge_lake.pipeline.search.search imported inside cmd_search), assert that invoking `search <query> --mode hybrid` forwards mode="hybrid" into pipeline.search. Capture the forwarded kwargs via a monkeypatched search stub.

    Create tests/unit/test_api_search_mode.py using fastapi.testclient.TestClient against the app (mirror the API test style used elsewhere): test_api_mode_forwarded asserts GET /search?q=x&mode=hybrid forwards mode="hybrid" into the delegated pipeline.search (patch the search symbol the endpoint imports); test_api_invalid_mode_422 asserts GET /search?q=x&mode=bogus returns HTTP 422 (Literal/pattern validation at the boundary, T-10-02).

    Mark RED tests xfail(strict=False) referencing Plan 10-08. Guard any import of not-yet-existing params so collection never fails.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_cli_search_mode.py tests/unit/test_api_search_mode.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both files collect with zero errors; RED tests report xfail
    - test_api_search_mode.py includes an invalid-mode → 422 assertion (fail-closed input validation)
    - No real embedder / Qdrant server is contacted (search seam is stubbed/patched)
  </acceptance_criteria>
  <done>CLI + API mode RED scaffolds exist as concrete targets for Plan 10-08.</done>
</task>

<task type="auto">
  <name>Task 3: Extend test_search_filters.py with the D-14 prefetch-filter assertion (RETR-01/03, D-14)</name>
  <files>tests/unit/test_search_filters.py</files>
  <read_first>
    - tests/unit/test_search_filters.py (full file) — existing filter-builder assertions and fake_vstore fixture
    - src/knowledge_lake/pipeline/search.py (lines 104-127) — the filter builder reused verbatim across modes
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Code Examples (d) note — Prefetch's filter field is named `filter`; top-level uses `query_filter`
  </read_first>
  <action>
    Add one RED test, test_filter_attaches_each_prefetch_branch, to the existing test_search_filters.py. Call search("q", mode="hybrid", domain="healthcare") with the existing fake_vstore fixture; assert the same Filter object the builder produces is forwarded such that (once the store implements hybrid) it attaches to each prefetch branch and the top-level query. At the pipeline layer this means asserting fake_vstore.search.call_args.kwargs still carries the built query_filter unchanged when mode="hybrid" (the per-branch attachment is asserted at the store layer in test_qdrant_hybrid.py). Mark xfail(strict=False) referencing Plan 10-06/10-07. Do NOT modify existing passing tests.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_search_filters.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Existing test_search_filters.py tests still pass; the new test reports xfail
    - The new test proves the Phase-7 filter is preserved unchanged when mode="hybrid" (D-14 continuity)
  </acceptance_criteria>
  <done>test_search_filters.py carries the D-14 continuity assertion; existing coverage untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted `mode`, `top_k`, `tags` query params cross here (validated by these tests) |
| operator→CLI | `--mode` flag input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | mode param (CLI/API/settings) | medium | mitigate | test_api_invalid_mode_422 asserts Literal/pattern validation, fail-closed at the boundary |
| T-10-03 | Repudiation / Integrity | pipeline.search mode substitution | high | mitigate | test_fail_loud_missing_sparse asserts no silent dense fallback (D-10) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_settings_search.py tests/unit/test_search_mode.py tests/unit/test_cli_search_mode.py tests/unit/test_api_search_mode.py tests/unit/test_search_filters.py -q` → all new RED tests xfail, existing tests green
- `uv run pytest tests/unit -q` → whole unit suite stays green
</verification>

<success_criteria>
Four new RED unit files plus the extended test_search_filters.py exist, collect clean, and encode the RETR-03 mode surface + D-14 filter continuity as concrete automated targets for Plans 10-04, 10-06, 10-07, and 10-08.
</success_criteria>

<output>
Create `.planning/phases/10-hybrid-retrieval/10-02-SUMMARY.md` when done.
</output>
