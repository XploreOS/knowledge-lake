---
phase: 10-hybrid-retrieval
plan: "08"
type: execute
wave: 5
depends_on:
  - 10-02
  - 10-07
files_modified:
  - src/knowledge_lake/cli/app.py
  - src/knowledge_lake/api/app.py
  - src/knowledge_lake/api/schemas.py
autonomous: true
requirements:
  - RETR-01
  - RETR-03
must_haves:
  truths:
    - "klake search --mode hybrid|dense|sparse forwards mode into pipeline.search (RETR-03, D-09)"
    - "klake reindex --hybrid forwards hybrid=True into reindex_collection, triggering the live re-embedding migration; the server-preflight RuntimeError is surfaced as a clean CLI error (typer.Exit code 1) (RETR-01, D-04, D-07)"
    - "GET /search?mode=hybrid|dense|sparse forwards mode into pipeline.search (RETR-03, D-09)"
    - "An invalid mode value at the API/CLI boundary is rejected (HTTP 422 for the API; fail-closed) via a bounded pattern/enum (T-10-02, ASVS V5)"
    - "SearchParams gains a bounded mode field mirroring the ExportRequest.kind pattern precedent"
    - "pytest tests/unit/test_cli_search_mode.py tests/unit/test_api_search_mode.py -q passes (RED tests from Plan 10-02 turn green)"
  artifacts:
    - src/knowledge_lake/cli/app.py (updated — cmd_search --mode, cmd_reindex --hybrid + RuntimeError handling)
    - src/knowledge_lake/api/app.py (updated — search_endpoint mode query param + delegation)
    - src/knowledge_lake/api/schemas.py (updated — SearchParams.mode bounded field)
  key_links:
    - "cmd_search --mode mirrors the existing typer.Option filter flags (cli/app.py:678-699); mode threads into the search() call (715-726)"
    - "cmd_reindex --hybrid threads hybrid into reindex_collection (769-778); add RuntimeError to the existing except tuple so the D-07 preflight abort surfaces cleanly"
    - "search_endpoint mode uses Query(default=None) mirroring the existing filter Query declarations (api/app.py:167-197); bounded by a Literal/pattern so an unknown mode returns 422 (T-10-02)"
    - "SearchParams.mode bounded-pattern precedent: ExportRequest.kind pattern=r'^(rag-corpus|pretrain|finetune)$' (schemas.py:168-172)"
---

<objective>
Wave 5 — Expose the hybrid + mode features on the two operator/user surfaces: `klake search --mode`, `klake reindex --hybrid` (the migration trigger), and `GET /search?mode=`, with fail-closed input validation on the mode value. Thin surface wiring over the pipeline functions from Plan 10-07.

Purpose: Completes RETR-03 (mode switchable from CLI + API, default hybrid) and the operator-facing half of RETR-01 (the `--hybrid` migration command), with the T-10-02 validation contract enforced at the boundary.

Output: CLI + API mode/hybrid surfaces; Plan 10-02's test_cli_search_mode.py and test_api_search_mode.py turn green.
</objective>

<artifacts_produced>
- `klake search --mode` CLI flag
- `klake reindex --hybrid` CLI flag (live migration trigger)
- `GET /search?mode=` API query param
- `SearchParams.mode` bounded field (api/schemas.py)
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-retrieval/10-CONTEXT.md
@.planning/phases/10-hybrid-retrieval/10-RESEARCH.md
@.planning/phases/10-hybrid-retrieval/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: cli/app.py — search --mode + reindex --hybrid (RETR-01, RETR-03, D-04, D-07, D-09)</name>
  <files>src/knowledge_lake/cli/app.py</files>
  <read_first>
    - src/knowledge_lake/cli/app.py (lines 671-726) — cmd_search options + search() call
    - src/knowledge_lake/cli/app.py (lines 757-778) — cmd_reindex + its try/except (ValueError, LookupError) → typer.Exit(1)
    - src/knowledge_lake/pipeline/search.py — search(mode=...) signature (from Plan 10-07)
    - src/knowledge_lake/pipeline/index.py — reindex_collection(hybrid=...) signature (from Plan 10-07)
  </read_first>
  <action>
    Add a `mode: Optional[str] = typer.Option(None, "--mode", help="Search mode: hybrid|dense|sparse (default from KLAKE_SEARCH__MODE, else hybrid).")` option to cmd_search (mirroring the existing filter Options at 678-699) and pass `mode=mode` into the search(...) call (715-726). Leaving mode as None lets pipeline.search fall back to settings.search.mode.

    Add a `hybrid: bool = typer.Option(False, "--hybrid", help="Migrate the alias to named dense+sparse vectors via a re-embedding reindex (RETR-01 live migration).")` option to cmd_reindex (757) and pass `hybrid=hybrid` into reindex_collection(collection, hybrid=hybrid). Add RuntimeError to the existing except tuple (currently (ValueError, LookupError) at 776) so the D-07 server-preflight abort and the D-06 parity-gate failure surface as a clean `Error: ...` message + typer.Exit(code=1) rather than a traceback. Update the cmd_reindex docstring to note --hybrid triggers the operator live migration and that the alias keeps the old collection on any preflight/parity abort (rollback).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_cli_search_mode.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `klake search --mode hybrid` forwards mode='hybrid' into pipeline.search; omitting --mode forwards None (settings default applies)
    - `klake reindex --hybrid` forwards hybrid=True into reindex_collection
    - A RuntimeError from the server preflight / parity gate is surfaced as a clean CLI error with exit code 1 (no traceback)
    - `uv run pytest tests/unit/test_cli_search_mode.py -q` exits 0 (RED tests green)
  </acceptance_criteria>
  <done>CLI exposes --mode search and the --hybrid migration trigger with clean preflight/parity error handling.</done>
</task>

<task type="auto">
  <name>Task 2: api/app.py + api/schemas.py — search ?mode= with fail-closed validation (RETR-03, T-10-02, D-09)</name>
  <files>src/knowledge_lake/api/app.py, src/knowledge_lake/api/schemas.py</files>
  <read_first>
    - src/knowledge_lake/api/app.py (lines 155-262) — search_endpoint params, 422 validation style (235-242), search() delegation (258)
    - src/knowledge_lake/api/schemas.py (lines 30-52) — SearchParams; and ExportRequest.kind bounded-pattern precedent (168-172)
    - src/knowledge_lake/pipeline/search.py — search(mode=...) signature (from Plan 10-07)
  </read_first>
  <action>
    In api/schemas.py, add a bounded `mode` field to SearchParams mirroring the ExportRequest.kind precedent (168-172): `mode: Optional[str] = Field(default=None, pattern=r"^(hybrid|dense|sparse)$", description="Search mode; default resolves from KLAKE_SEARCH__MODE (hybrid).")`. This is the fail-closed validation boundary (T-10-02, ASVS V5).

    In api/app.py search_endpoint (155), add a `mode: Optional[str] = Query(default=None, pattern=r"^(hybrid|dense|sparse)$", description="Search mode: hybrid|dense|sparse.")` param mirroring the existing filter Query declarations (167-197). Pass `mode=mode` into the search(...) delegation (258). The Query pattern rejects an unknown mode with FastAPI's automatic 422 before the handler runs; do not add a manual mode check at the store. Keep the existing collection-name and tag-length validations intact. Update the endpoint docstring to document ?mode= and the fail-loud behavior (a hybrid/sparse request against a sparse-less collection returns the store's clear error).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_api_search_mode.py -q</automated>
  </verify>
  <acceptance_criteria>
    - GET /search?mode=hybrid forwards mode='hybrid' into pipeline.search
    - GET /search?mode=bogus returns HTTP 422 (fail-closed, T-10-02) via the Query pattern
    - SearchParams.mode carries the bounded ^(hybrid|dense|sparse)$ pattern
    - `uv run pytest tests/unit/test_api_search_mode.py -q` exits 0 (RED tests green)
  </acceptance_criteria>
  <done>The REST API exposes ?mode= with fail-closed validation, delegating to the same pipeline.search the CLI uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted `mode` and `top_k` query params validated here |
| operator→CLI | `--mode` and `--hybrid` (migration trigger) inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | search_endpoint mode / SearchParams.mode | medium | mitigate | Bounded `^(hybrid\|dense\|sparse)$` pattern rejects unknown modes with 422 at the pydantic/Query boundary, fail-closed (ASVS V5) |
| T-10-01 | Denial of Service | search_endpoint top_k | high | mitigate | Existing top_k ge=1/le=100 bound unchanged; prefetch limit == top_k+offset downstream (D-12) |
| T-10-04 | Denial of Service / Integrity | reindex --hybrid trigger | high | mitigate | RuntimeError from the D-07 preflight / D-06 parity gate surfaces as a clean CLI abort; alias keeps the old collection (rollback) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_cli_search_mode.py tests/unit/test_api_search_mode.py -q` → green (RED tests turned)
- `uv run pytest tests/unit -q` → whole unit suite green
- Manual: `klake search "hipaa safeguards" --mode hybrid` returns hits; `klake reindex --hybrid` runs the migration or aborts cleanly on an old/parity-mismatched server
</verification>

<success_criteria>
The CLI and REST API expose mode-switchable search (default hybrid) and the `--hybrid` migration trigger, with fail-closed mode validation and clean preflight/parity error surfacing — completing RETR-03 and the operator-facing half of RETR-01.
</success_criteria>

<output>
Create `.planning/phases/10-hybrid-retrieval/10-08-SUMMARY.md` when done.
</output>
