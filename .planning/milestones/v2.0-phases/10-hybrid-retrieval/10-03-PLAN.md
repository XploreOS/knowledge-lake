---
phase: 10-hybrid-retrieval
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: false
requirements:
  - RETR-01
user_setup:
  - service: huggingface
    why: "fastembed downloads the Qdrant/bm25 ONNX model from the HuggingFace Hub on first use (RESEARCH Open Question 2)"
    dashboard_config:
      - task: "Ensure outbound HTTPS to huggingface.co is available on the droplet, or pre-warm the fastembed model cache (~/.cache/fastembed or the HF cache)"
        location: "Deployment host network / fastembed model cache"
must_haves:
  truths:
    - "pyproject.toml declares fastembed>=0.8,<0.9 in [project].dependencies, adjacent to qdrant-client==1.18.0 (D-01)"
    - "`uv run python -c 'import fastembed'` succeeds (CPU install, no torch/GPU pulled)"
    - "A human-verify checkpoint confirms SparseTextEmbedding('Qdrant/bm25') loads, embeds one string, caches the ONNX model, and pulls no GPU/torch deps (RESEARCH Open Question 2, A4)"
  artifacts:
    - pyproject.toml (updated — fastembed pin added)
    - uv.lock (updated by uv add / uv sync)
  key_links:
    - "fastembed is qdrant-client 1.18's own declared `fastembed` extra (>=0.8,<0.9) — Package Legitimacy Audit verdict OK (publisher == Qdrant)"
    - "Use the CPU `fastembed` package, NEVER `fastembed-gpu` (D-02 — CPU droplet constraint)"
    - "The Qdrant/bm25 model requires Modifier.IDF server-side (consumed by Plan 10-06) — install-time only concerns the encoder library here"
---

<objective>
Wave 1 — Add the `fastembed` dependency (qdrant-client's own CPU BM25 extra) and gate its first-use model download behind a human-verify checkpoint. This is a dedicated dependency + verification plan (isolated per the checkpoint-splitting rule) so no implementation is blocked mid-plan.

Purpose: RETR-01 requires BM25 sparse vectors produced by `fastembed` `Qdrant/bm25` (D-01, D-02). The library is CPU-only (onnxruntime) but downloads its ONNX model from HuggingFace on first use — a supply-chain + runtime-footprint concern that must be verified by a human on the target host (RESEARCH Open Question 2).

Output: fastembed pinned in pyproject.toml, installed CPU-only, and a passing checkpoint confirming the Qdrant/bm25 model loads and embeds.
</objective>

<artifacts_produced>
- `fastembed>=0.8,<0.9` dependency in `pyproject.toml`
- Verified CPU install + cached `Qdrant/bm25` ONNX model (consumed by Plans 10-05, 10-07)
</artifacts_produced>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hybrid-retrieval/10-CONTEXT.md
@.planning/phases/10-hybrid-retrieval/10-RESEARCH.md
@.planning/phases/10-hybrid-retrieval/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fastembed>=0.8,<0.9 to pyproject and install CPU-only (RETR-01, D-01, D-02)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (lines 10-45) — [project].dependencies block; add adjacent to qdrant-client==1.18.0 (line 19) and sentence-transformers==5.6.0 (line 18)
    - .planning/phases/10-hybrid-retrieval/10-RESEARCH.md §Standard Stack + §Package Legitimacy Audit — fastembed pin provenance (qdrant-client's own extra) and OK verdict
  </read_first>
  <action>
    Add the line `fastembed>=0.8,<0.9` to [project].dependencies in pyproject.toml adjacent to the qdrant-client pin, per D-01. Run `uv add 'fastembed>=0.8,<0.9'` (or edit pyproject then `uv sync`) to resolve and write uv.lock. Do NOT add `fastembed-gpu` — the CPU package is required for the droplet (D-02). After install, confirm `uv run python -c "import fastembed"` succeeds and that no torch/GPU wheel was pulled into the environment (a quick check of the resolved packages for torch/nvidia entries). This task installs the library only; it does not write any application code.
  </action>
  <verify>
    <automated>uv run python -c "import fastembed; print(fastembed.__version__)" && grep -n "fastembed" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `fastembed>=0.8,<0.9` appears in pyproject.toml [project].dependencies
    - `uv run python -c "import fastembed"` exits 0
    - No torch / nvidia / fastembed-gpu package present in the resolved environment
  </acceptance_criteria>
  <done>fastembed CPU library is pinned and importable in the project environment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: fastembed CPU install + Qdrant/bm25 model download (blocking)</name>
  <files>pyproject.toml</files>
  <what-built>Added `fastembed>=0.8,<0.9` (qdrant-client's official CPU BM25 extra) to pyproject.toml and installed it CPU-only via uv. The Qdrant/bm25 ONNX model is fetched from the HuggingFace Hub on first use.</what-built>
  <how-to-verify>
    1. Run: `uv run python -c "from fastembed import SparseTextEmbedding; m = SparseTextEmbedding(model_name='Qdrant/bm25'); e = next(iter(m.embed(['administrative safeguards for protected health information']))); print('indices', len(e.indices), 'values', len(e.values))"`
    2. Confirm it prints non-zero indices/values counts (a real sparse embedding was produced).
    3. Confirm the Qdrant/bm25 model downloaded and cached (subsequent runs are offline-fast) — inspect `~/.cache/fastembed` or the HF cache.
    4. Confirm NO torch / CUDA / GPU packages were pulled (`uv run python -c "import importlib.util; print('torch', importlib.util.find_spec('torch'))"` should print `torch None`).
    5. If the install conflicts or pulls GPU/torch deps, STOP — the D-01 fallback is `rank_bm25` (already installed); flag this before proceeding to Plan 10-05.
  </how-to-verify>
  <resume-signal>Type "approved" if the Qdrant/bm25 model loads and embeds CPU-only with no torch/GPU deps, or describe the failure (triggers the rank_bm25 fallback discussion).</resume-signal>
  <action>Do not mark this checkpoint resolved without an explicit human resume-signal. The first-use HuggingFace model download and the CPU/GPU footprint cannot be asserted in an automated unit test (RESEARCH Open Question 2). On approval, proceed to Plan 10-05 (sparse_embedder); on a reported failure, treat it as the signal to fall back to the already-installed `rank_bm25` (D-01 fallback) and revisit the sparse-encoder decision before coding.</action>
  <verify>
    <human-check>A human has installed fastembed on the target host, loaded SparseTextEmbedding('Qdrant/bm25'), embedded one string with non-zero indices/values, confirmed the model cached and that no torch/CUDA/GPU package was pulled, and typed the approval resume-signal.</human-check>
  </verify>
  <done>A human has explicitly confirmed the Qdrant/bm25 model loads and embeds CPU-only on the target host with no GPU/torch dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→PyPI | New package install (fastembed) |
| app→HuggingFace | First-use ONNX model download (Qdrant/bm25) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering (supply chain) | fastembed first-use model download | medium | mitigate | fastembed is Qdrant-official; pin >=0.8,<0.9; model Qdrant/bm25 from the Qdrant HF org; human-verify checkpoint confirms load + cache and no GPU/torch |
| T-10-SC | Tampering | pip/uv install (fastembed) | high | mitigate | Package Legitimacy Audit verdict OK (publisher == Qdrant, qdrant-client's own declared extra); blocking human-verify checkpoint gates first-use before any consumer plan |
</threat_model>

<verification>
- `grep -n "fastembed" pyproject.toml` shows the `>=0.8,<0.9` pin
- `uv run python -c "import fastembed"` exits 0
- Checkpoint approved: Qdrant/bm25 loads, embeds, caches, CPU-only
</verification>

<success_criteria>
fastembed is pinned CPU-only, importable, and its Qdrant/bm25 model is verified to load and embed on the target host — unblocking the sparse encoder (Plan 10-05) and the re-embedding migration (Plan 10-07).
</success_criteria>

<output>
Create `.planning/phases/10-hybrid-retrieval/10-03-SUMMARY.md` when done.
</output>
