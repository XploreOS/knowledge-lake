---
phase: 12-agent-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/unit/test_tool_registry.py
  - tests/unit/test_tool_handlers.py
  - tests/unit/test_readonly.py
  - tests/unit/test_openapi_export.py
  - tests/unit/test_surface_parity.py
  - tests/unit/test_skills_present.py
  - tests/unit/test_pipeline_extractions.py
  - tests/unit/test_input_models.py
  - tests/integration/test_stdio_lockdown.py
  - tests/integration/test_mcp_http.py
autonomous: true
requirements: [MCP-01, MCP-02, SKILL-01, SKILL-02, SKILL-03]
must_haves:
  truths:
    - "mcp==1.28.1 is a resolvable, locked dependency importable via `uv run python -c 'import mcp'`"
    - "Every phase-12 requirement has at least one RED test file present that fails or xfails before implementation (Nyquist Wave 0)"
    - "A shared normalize(schema) helper exists that strips title, canonicalizes $ref/$defs, and sorts keys (Pitfall 2)"
  artifacts:
    - pyproject.toml (mcp==1.28.1 pin)
    - uv.lock (updated)
    - tests/unit/test_surface_parity.py (with normalize helper)
    - tests/integration/test_stdio_lockdown.py
  key_links:
    - "mcp SDK import must succeed under uv before any agent/ module can be authored"
  prohibitions:
    - "Do NOT add mcp extras ([cli], [rich], [ws]) — pin bare `mcp==1.28.1` (RESEARCH State-of-the-art)"
    - "Do NOT write real assertions that pass before implementation exists — Wave 0 tests MUST be RED/xfail with import guards"
    - "Do NOT introduce any Alembic migration or ORM column change — this phase is pure surface (CONTEXT no_db_schema_changes)"
---

<objective>
Land the single new dependency (`mcp==1.28.1`) and the complete Wave 0 RED test scaffold for every phase-12 requirement, so all downstream implementation is test-first (Nyquist).

Purpose: `mcp` is not yet in `pyproject.toml`; no `agent/` code can import the SDK until it is locked. The Wave 0 scaffold creates one failing/xfail test per requirement (MCP-01/02, SKILL-01/02/03, D-05) plus the shared `normalize(schema)` parity helper, giving every later task a concrete red→green target.

Output: `mcp==1.28.1` in `pyproject.toml` + `uv.lock`; nine test files under `tests/unit/` and `tests/integration/` in a RED/xfail state; a `normalize(schema)` helper in `test_surface_parity.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-surfaces/12-RESEARCH.md
@.planning/phases/12-agent-surfaces/12-VALIDATION.md
@.claude/CLAUDE.md
</context>

<artifacts_this_phase_produces>
NEW symbols created across phase 12 (source-grounding excludes these from drift verification):
- Package `src/knowledge_lake/agent/`: `__init__.py`, `registry.py`, `server.py`, `stdio.py`, `http.py`, `openai_defs.py`
- Symbols: `ToolDef`, `TOOLS`, `registered_tools()`, `build_server()`, `run_stdio()`, `build_http_app()`, `StaticBearerMiddleware`, `openai_tool_defs()`
- ~11 tool names: `search`, `ingest_url`, `crawl`, `crawl_all`, `process_crawled`, `add_source`, `list_sources`, `lineage`, `export`, `init_domain`, `stats`
- CLI verbs: `klake mcp` (`--sse`, `--port`), `klake openapi`
- Settings: `McpSettings`; env `KLAKE_MCP__TOKEN`, `KLAKE_MCP__READONLY`, `KLAKE_MCP__HOST`, `KLAKE_MCP__PORT`
- Service fns: `pipeline/process.py:process_crawled()`, `pipeline/query.py:list_sources()`, `pipeline/query.py:stats()`, `pipeline/domains.py:load_domain()`
- `QdrantVectorStore.count_points()`
- api/schemas.py: extended `SearchParams` (+ filter fields) and new input models (`StatsInput`, `ProcessCrawledInput`, `ListSourcesInput`, `LineageInput`, `IngestUrlInput`, `CrawlAllInput`)
- Test files: test_tool_registry.py, test_tool_handlers.py, test_readonly.py, test_openapi_export.py, test_surface_parity.py (+ normalize helper), test_skills_present.py, test_pipeline_extractions.py, test_stdio_lockdown.py, test_mcp_http.py
- `skills/build-corpus.md`, `skills/search-knowledge.md`, `skills/add-source.md`, `skills/export-dataset.md`
- `docs/openapi.json`, `docs/openai_tools.json`
- Dependency `mcp==1.28.1`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add mcp==1.28.1 dependency and lock</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (the `dependencies = [` block at line 10; version-pin style of existing entries)
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Standard Stack" + §"Package Legitimacy Audit" (mcp verdict OK/Approved; no extras; no version conflicts)
  </read_first>
  <action>
    Add `mcp==1.28.1` to the `[project].dependencies` array in pyproject.toml, following the exact pinned-equality style of the existing entries (bare package, no extras — NOT `mcp[cli]`). Run `uv add mcp==1.28.1` (or edit + `uv lock`) so `uv.lock` records the resolution and the new transitive `jsonschema>=4.20`. Per D-09 and RESEARCH, this is conflict-free (httpx/pydantic/starlette/uvicorn/pydantic-settings pins already satisfy mcp's requires-dist). This is a supply-chain touch point (T-12-SC): the RESEARCH Package Legitimacy Audit already verified `mcp` as the official Anthropic/MCP SDK with an OK verdict, so no human legitimacy checkpoint is required.
  </action>
  <acceptance_criteria>
    - `grep -c 'mcp==1.28.1' pyproject.toml` returns 1 and the entry has no `[` extras bracket
    - `uv run python -c "import mcp; from mcp.server.lowlevel import Server; from mcp.server.stdio import stdio_server; print('ok')"` prints `ok`
    - `uv.lock` contains an `mcp` entry at version 1.28.1
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import mcp; from mcp.server.lowlevel import Server; from mcp.server.stdio import stdio_server; from mcp.server.streamable_http_manager import StreamableHTTPSessionManager; print('ok')"</automated>
  </verify>
  <done>mcp==1.28.1 is pinned in pyproject.toml (no extras), locked in uv.lock, and importable under uv.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 unit-test scaffolds + normalize() parity helper</name>
  <files>tests/unit/test_tool_registry.py, tests/unit/test_tool_handlers.py, tests/unit/test_readonly.py, tests/unit/test_openapi_export.py, tests/unit/test_surface_parity.py, tests/unit/test_skills_present.py, tests/unit/test_pipeline_extractions.py, tests/unit/test_input_models.py</files>
  <read_first>
    - tests/unit/test_api_search_mode.py (unit-level schema-assertion style)
    - tests/integration/test_api_new_endpoints.py:12-28 (the `_IMPORT_OK` import-guard + module-scoped fixture pattern to mirror)
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Common Pitfalls" Pitfall 2 (normalize helper spec) and §"Validation Architecture" Wave-0 Gaps
    - .planning/phases/12-agent-surfaces/12-VALIDATION.md §"Per-Task Verification Map"
  </read_first>
  <action>
    Create seven unit test files as RED scaffolds. Each imports the not-yet-existing symbol behind a module-scope `try/except ImportError` guard setting `_IMPORT_OK = False`, and marks tests `@pytest.mark.xfail(not _IMPORT_OK, reason="Wave 0 scaffold", strict=False)` so collection never errors before implementation. Coverage: test_tool_registry.py (asserts `from knowledge_lake.agent.registry import TOOLS` yields 11 tools with unique names + read/write access tags, D-11); test_tool_handlers.py (asserts each ToolDef.handler is a `pipeline/*.py` callable, not an api.app symbol, D-03); test_readonly.py (asserts `registered_tools(readonly=True)` returns only the 4 read tools); test_openapi_export.py (asserts `docs/openapi.json` is a deterministic `sort_keys=True` dump, SKILL-02); test_surface_parity.py (asserts stdio inputSchema == openai parameters == model_json_schema, normalized, SKILL-03/D-04); test_skills_present.py (asserts four `skills/*.md` exist with `name`/`description` frontmatter referencing tool names, SKILL-01); test_pipeline_extractions.py (asserts extracted `process_crawled`/`list_sources`/`stats`/`load_domain` importable with documented return shapes, D-05); test_input_models.py (asserts extended `SearchParams` + new input models import and their fields map to the target service-function kwargs, D-02). In test_surface_parity.py, implement the real `normalize(schema)` helper now (it has no implementation dependency): recursively drop every `title` key, rewrite any `$ref`/`$defs` prefix to a canonical token, and expose a `canonical(schema) -> str` returning `json.dumps(normalized, sort_keys=True)`. Do NOT place fenced implementation of production code here; only test/helper code.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_tool_registry.py tests/unit/test_tool_handlers.py tests/unit/test_readonly.py tests/unit/test_openapi_export.py tests/unit/test_surface_parity.py tests/unit/test_skills_present.py tests/unit/test_pipeline_extractions.py tests/unit/test_input_models.py -q` collects with 0 collection errors and 0 unexpected passes (all xfail or fail)
    - `normalize` and `canonical` are importable from test_surface_parity.py and `canonical({"title":"X","type":"object"})` equals `canonical({"type":"object"})`
    - Each file contains an `_IMPORT_OK` guard so a missing implementation import never raises at collection time
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_surface_parity.py tests/unit/test_tool_registry.py -q 2>&1 | grep -Eq 'xfail|error|passed|failed'</automated>
  </verify>
  <done>Seven unit RED scaffolds collect cleanly; the normalize/canonical helper is implemented and unit-usable.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 integration-test scaffolds (stdio lockdown + HTTP)</name>
  <files>tests/integration/test_stdio_lockdown.py, tests/integration/test_mcp_http.py</files>
  <read_first>
    - tests/integration/test_scrapy_subprocess.py (subprocess-launch + stdout-capture analog for the stdio self-test)
    - tests/integration/test_api_new_endpoints.py:12-28 (TestClient fixture + `@pytest.mark.integration` + import-guard pattern)
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Common Pitfalls" Pitfall 1 and §"Architecture Patterns" Pattern 2/3
  </read_first>
  <action>
    Create two integration RED scaffolds marked `@pytest.mark.integration` behind an `_IMPORT_OK` guard. test_stdio_lockdown.py (MCP-02 FIRST-TASK GATE): a placeholder that will spawn `klake mcp` (stdio) as a subprocess, send one trivial JSON-RPC request, and assert every non-empty stdout line parses as JSON-RPC (no log/banner bytes) — scaffold it xfail until Plan 04 lands the lockdown. test_mcp_http.py (MCP-02): placeholder that will build the Starlette Streamable-HTTP app and assert (a) it binds to 127.0.0.1, (b) `TransportSecuritySettings.allowed_hosts` is populated (non-127.0.0.1 Host rejected), (c) a bearer is enforced only when `KLAKE_MCP__TOKEN` is set — scaffold xfail until Plan 06. Keep both non-erroring at collection time.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_stdio_lockdown.py tests/integration/test_mcp_http.py -q` collects with 0 collection errors (xfail/skip acceptable)
    - Both files carry `@pytest.mark.integration` and an `_IMPORT_OK` guard
    - test_stdio_lockdown.py references stdout-only-JSON-RPC as its assertion intent (the first-task gate)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_stdio_lockdown.py tests/integration/test_mcp_http.py -q --collect-only 2>&1 | grep -Eq 'test_|error' && echo collected</automated>
  </verify>
  <done>Two integration RED scaffolds (stdio lockdown gate + HTTP) collect cleanly and pin their assertion intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → build env | New third-party package `mcp` enters the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-SC | Tampering | `mcp==1.28.1` install | high | mitigate | RESEARCH Package Legitimacy Audit verified `mcp` as the official Anthropic/MCP SDK (verdict OK); pin exact version, no extras, record `uv.lock`; no `[ASSUMED]`/`[SUS]` packages so no human legitimacy checkpoint required (block-on-high satisfied by the audit) |
</threat_model>

<verification>
- `uv run python -c "import mcp"` succeeds
- `uv run pytest tests/unit -q` and `uv run pytest tests/integration -m integration -q` collect with 0 collection errors; all new phase-12 tests are xfail/fail (RED)
</verification>

<success_criteria>
mcp==1.28.1 locked and importable; nine RED test files present covering every phase-12 requirement; `normalize`/`canonical` parity helper implemented.
</success_criteria>

<output>
Create `.planning/phases/12-agent-surfaces/12-01-SUMMARY.md` when done
</output>
