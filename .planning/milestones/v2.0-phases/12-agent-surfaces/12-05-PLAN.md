---
phase: 12-agent-surfaces
plan: 05
type: execute
wave: 3
depends_on: [12-02, 12-03, 12-04]
files_modified:
  - src/knowledge_lake/config/settings.py
  - src/knowledge_lake/agent/registry.py
  - src/knowledge_lake/agent/server.py
  - tests/unit/test_tool_registry.py
  - tests/unit/test_tool_handlers.py
  - tests/unit/test_readonly.py
autonomous: true
requirements: [MCP-01, MCP-02]
must_haves:
  truths:
    - "TOOLS is a list of 11 ToolDef entries, each {name, description, input_model (Pydantic), handler (a pipeline/*.py fn), access ('read'|'write')}, imported as the single source of truth (D-01)"
    - "read tools are exactly search/list_sources/lineage/stats; write tools are the other seven (D-11)"
    - "one low-level mcp.server.lowlevel.Server is built from TOOLS with list_tools + call_tool; it is the SAME object reused by stdio and HTTP (RESEARCH Pattern 1)"
    - "call_tool awaits async handlers (crawl/crawl_all) directly and calls sync handlers directly; expected ValueError/LookupError/store-fail-loud errors return isError, unexpected exceptions propagate (D-12/D-13)"
    - "registered_tools(readonly=True) returns only read tools, feeding both list_tools and dispatch (D-11)"
  artifacts:
    - src/knowledge_lake/config/settings.py (McpSettings)
    - src/knowledge_lake/agent/registry.py (ToolDef, TOOLS, registered_tools)
    - src/knowledge_lake/agent/server.py (build_server)
  key_links:
    - "each ToolDef.handler is the pipeline/* service function (not an api.app symbol) — handlers never proxy REST"
    - "inputSchema is derived from input_model.model_json_schema() — the one place a tool's shape is defined"
  prohibitions:
    - "Do NOT use FastMCP or @tool() decorators — schema must come from the shared Pydantic input_model, not a function signature (D-01, anti-pattern)"
    - "Do NOT import api.app or make an HTTP call from any handler — call pipeline/*.py directly (MCP-01 hard rule, D-03)"
    - "Do NOT use asyncio.run inside call_tool — await async service functions directly (D-12 correction, raises RuntimeError otherwise)"
    - "Do NOT add a new fetch path — SSRF guards stay inside ingest_url/crawl (validate_public_url), unchanged"
---

<objective>
Build the single tool registry and the low-level MCP server core — the heart of MCP-01. One `TOOLS: list[ToolDef]` over Pydantic input models feeds a single `mcp.server.lowlevel.Server` reused by every transport, so `stdio == http` holds by construction. Add `McpSettings` to drive the read-only posture and later HTTP bind/auth.

Purpose: this is where the ~11 curated tools become real, thin shims over the extracted/existing `pipeline/*.py` functions, with the async-bridge correction and error contract applied.

Output: `McpSettings` (settings.py), `agent/registry.py` (`ToolDef`, `TOOLS`, `registered_tools`), `agent/server.py` (`build_server`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-surfaces/12-RESEARCH.md
@.planning/phases/12-agent-surfaces/12-PATTERNS.md
@src/knowledge_lake/config/settings.py
@src/knowledge_lake/api/schemas.py
</context>

<artifacts_this_phase_produces>
This plan creates: `McpSettings`, `ToolDef`, `TOOLS`, `registered_tools()`, `build_server()`, and wires the 11 tool handlers to pipeline functions. (Full phase artifact list is in 12-01-PLAN.md.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: McpSettings nested model</name>
  <files>src/knowledge_lake/config/settings.py, tests/unit/test_readonly.py</files>
  <read_first>
    - src/knowledge_lake/config/settings.py:304-347 (SearchSettings model + env_prefix="KLAKE_" + env_nested_delimiter="__") and :431 (search: SearchSettings default_factory mount)
    - .planning/phases/12-agent-surfaces/12-PATTERNS.md §"Nested pydantic-settings for KLAKE_MCP__*"
  </read_first>
  <behavior>
    - McpSettings has token (Optional[str]=None), readonly (bool=False), host (str="127.0.0.1"), port (int=3001)
    - Settings mounts `mcp: McpSettings = Field(default_factory=McpSettings)`; env KLAKE_MCP__READONLY=true resolves readonly True; KLAKE_MCP__TOKEN sets token
    - defaults are localhost-safe (host 127.0.0.1, readonly False, token None)
  </behavior>
  <action>
    Add a `McpSettings(BaseModel)` mirroring `SearchSettings` (settings.py:304): fields `token: Optional[str] = None`, `readonly: bool = False`, `host: str = "127.0.0.1"`, `port: int = 3001`. Mount it on `Settings` as `mcp: McpSettings = Field(default_factory=McpSettings)` next to `search`. The existing `env_prefix="KLAKE_"` + `env_nested_delimiter="__"` (settings.py:346-347) make `KLAKE_MCP__TOKEN` / `KLAKE_MCP__READONLY` / `KLAKE_MCP__HOST` / `KLAKE_MCP__PORT` resolve automatically. Do not add any secret default for token.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from knowledge_lake.config.settings import Settings, McpSettings; s=Settings(); print(s.mcp.host, s.mcp.readonly, s.mcp.port)"` prints `127.0.0.1 False 3001`
    - `KLAKE_MCP__READONLY=true uv run python -c "from knowledge_lake.config.settings import Settings; print(Settings().mcp.readonly)"` prints `True`
    - McpSettings.token defaults to None (no baked-in secret)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from knowledge_lake.config.settings import Settings; s=Settings(); assert s.mcp.host=='127.0.0.1' and s.mcp.readonly is False and s.mcp.port==3001 and s.mcp.token is None; print('ok')"</automated>
  </verify>
  <done>McpSettings is mounted on Settings with localhost-safe defaults and resolves KLAKE_MCP__* env vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tool registry (ToolDef + TOOLS + read/write filter)</name>
  <files>src/knowledge_lake/agent/registry.py, tests/unit/test_tool_registry.py, tests/unit/test_readonly.py</files>
  <read_first>
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Read/write posture" (read set = search/list_sources/lineage/stats; write set = the rest) and §"Architectural Responsibility Map"
    - .planning/phases/12-agent-surfaces/12-PATTERNS.md §"7 direct shims" table (tool → pipeline fn → async?) and §"api/schemas.py — extend + add input models"
    - src/knowledge_lake/pipeline/search.py:35, ingest.py:230/337, crawl.py:244/911, export.py:243/357/449 (handler targets); pipeline/process.py, pipeline/query.py, pipeline/domains.py (Plan 02 extractions)
    - src/knowledge_lake/api/schemas.py (input models from Plan 03)
  </read_first>
  <behavior>
    - TOOLS has exactly 11 entries with unique names: search, ingest_url, crawl, crawl_all, process_crawled, add_source, list_sources, lineage, export, init_domain, stats
    - each ToolDef.access is "read" or "write"; read == {search, list_sources, lineage, stats}
    - each ToolDef.handler is a callable imported from pipeline/*.py (or the lineage resolver), never from api.app
    - registered_tools(readonly=True) returns exactly the 4 read tools; registered_tools(readonly=False) returns all 11
  </behavior>
  <action>
    Create `agent/registry.py` with a `ToolDef` dataclass `{name: str, description: str, input_model: type[BaseModel], handler: Callable, access: Literal["read","write"]}` and a module-level `TOOLS: list[ToolDef]` with the 11 tools. Map each per D-05/D-06 and the PATTERNS shim table: search→pipeline.search.search (SearchParams, read); list_sources→pipeline.query.list_sources (ListSourcesInput, read); stats→pipeline.query.stats (StatsInput, read); lineage→the lineage resolver behind lineage_endpoint (LineageInput, read); ingest_url→pipeline.ingest.ingest_url (IngestUrlInput, write); add_source→pipeline.ingest.register_source (SourceCreate, write); crawl→pipeline.crawl.crawl_source (CrawlJobCreate, write, async); crawl_all→pipeline.crawl.crawl_all_sources (CrawlAllInput, write, async); process_crawled→pipeline.process.process_crawled (ProcessCrawledInput, write); export→pipeline.export dispatch (ExportRequest, write); init_domain→pipeline.domains.load_domain (DomainLoadRequest, write). Add `registered_tools(readonly: bool) -> list[ToolDef]` returning `[t for t in TOOLS if not readonly or t.access == "read"]`. Import handlers from `pipeline/*` only — no `api.app` import (D-03/MCP-01 hard rule).
  </action>
  <acceptance_criteria>
    - `uv run python -c "from knowledge_lake.agent.registry import TOOLS; assert len(TOOLS)==11 and len({t.name for t in TOOLS})==11; print('ok')"` prints ok
    - read tools == {search, list_sources, lineage, stats}: `registered_tools(True)` yields exactly those 4 names
    - `grep -c 'api.app\|import api' src/knowledge_lake/agent/registry.py` returns 0 (no REST proxying)
    - `uv run pytest tests/unit/test_tool_registry.py tests/unit/test_readonly.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_tool_registry.py tests/unit/test_readonly.py -x</automated>
  </verify>
  <done>TOOLS is the 11-tool single source of truth with read/write tags; registered_tools filters correctly; no handler imports api.app.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MCP server core (build_server: list_tools + call_tool)</name>
  <files>src/knowledge_lake/agent/server.py, tests/unit/test_tool_handlers.py</files>
  <read_first>
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Architecture Patterns" Pattern 1 (build_server) and Pattern 4 (async bridge — await, NOT asyncio.run) and §"Common Pitfalls" (dict return auto-wrap)
    - .planning/phases/12-agent-surfaces/12-PATTERNS.md §"Expected-error contract" (catch ValueError/LookupError/store fail-loud → isError) and §"7 direct shims" (export dispatch on kind, verbatim from api/app.py:1191-1204)
    - src/knowledge_lake/api/app.py:1155-1217 (export_endpoint dispatch + TrainEvalContaminationError/ValueError handling to mirror), :1087-1136 (lineage_endpoint invocation)
    - src/knowledge_lake/agent/registry.py (TOOLS, ToolDef, registered_tools from Task 2)
  </read_first>
  <behavior>
    - build_server(tools) returns a low-level mcp Server whose list_tools emits one types.Tool per tool with inputSchema = input_model.model_json_schema()
    - call_tool validates arguments into the ToolDef.input_model, then invokes the handler: async handlers (crawl/crawl_all) are awaited directly; sync handlers are called directly
    - an expected ValueError/LookupError/store fail-loud error returns a CallToolResult(isError=True) with a readable message; an unexpected exception propagates
    - a dict return is auto-wrapped by the SDK as structuredContent + JSON text
  </behavior>
  <action>
    Create `agent/server.py` with `build_server(tools: list[ToolDef]) -> Server` per RESEARCH Pattern 1: register `@server.list_tools()` returning `types.Tool(name, description, inputSchema=t.input_model.model_json_schema())` for each tool, and `@server.call_tool()` that looks up the ToolDef by name, constructs `t.input_model(**arguments)`, unpacks via `model_dump(exclude_none=True)`, and invokes the handler using the Pattern-4 bridge: `if inspect.iscoroutinefunction(fn): await fn(**kwargs) else: fn(**kwargs)` — never `asyncio.run` (D-12 correction). For the `export` tool, dispatch on the ExportRequest dataset kind to `export_rag_corpus`/`export_pretrain_corpus`/`export_finetune_dataset`, copying the kind→fn dispatch and `TrainEvalContaminationError`/`ValueError` handling verbatim from api/app.py:1191-1204. Wrap expected errors (`ValueError`, `LookupError`, the search store fail-loud error, `TrainEvalContaminationError`) into `types.CallToolResult(content=[types.TextContent(...)], isError=True)` (D-13); let unexpected exceptions propagate (SDK turns them into protocol errors). Return dict results directly (SDK auto-wraps). The server accepts a pre-filtered tool list so `readonly` posture flows through unchanged.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from knowledge_lake.agent.server import build_server; from knowledge_lake.agent.registry import TOOLS; s=build_server(TOOLS); print(type(s).__name__)"` prints `Server`
    - `grep -c 'asyncio.run' src/knowledge_lake/agent/server.py` returns 0 (async bridge uses await)
    - `grep -c 'iscoroutinefunction' src/knowledge_lake/agent/server.py` returns >= 1 (async/sync dispatch)
    - test_tool_handlers.py asserts each handler is invoked with unpacked kwargs against a mocked service fn and that ValueError → isError; `uv run pytest tests/unit/test_tool_handlers.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_tool_handlers.py -x && uv run python -c "src=open('src/knowledge_lake/agent/server.py').read(); assert 'asyncio.run' not in src and 'iscoroutinefunction' in src; print('ok')"</automated>
  </verify>
  <done>build_server produces one low-level Server from TOOLS with list_tools/call_tool; async handlers are awaited, errors follow the D-13 contract, and no handler proxies REST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent client → call_tool → pipeline service fn | validated tool arguments cross into business logic |
| tool arg (`url`) → ingest_url/crawl fetch | user-controlled URL may target internal hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering/Info Disclosure | ingest_url/crawl handlers | high | mitigate | Handlers add NO new fetch path; the existing `validate_public_url` guard (ingest.py:99, blocks RFC-1918/IMDS) remains the boundary inside the service functions |
| T-12-07 | Tampering | call_tool input handling | medium | mitigate | Double validation: SDK `validate_input=True` (jsonschema on inputSchema) + Pydantic `input_model(**arguments)` construction before dispatch (V5) |
| T-12-13 | Information Disclosure | call_tool error contract | low | mitigate | Only expected errors surface readable messages via isError; unexpected exceptions propagate as protocol errors without leaking internals (D-13) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_tool_registry.py tests/unit/test_tool_handlers.py tests/unit/test_readonly.py -x` passes
- `uv run python -c "from knowledge_lake.agent.server import build_server; from knowledge_lake.agent.registry import TOOLS; build_server(TOOLS)"` succeeds
- no handler imports api.app; no asyncio.run in server.py
</verification>

<success_criteria>
The 11-tool registry and low-level MCP server core exist as the single source of truth; handlers are thin shims over pipeline/*.py with the async-bridge and error contract applied; McpSettings drives the read-only posture.
</success_criteria>

<output>
Create `.planning/phases/12-agent-surfaces/12-05-SUMMARY.md` when done
</output>
