---
phase: 12-agent-surfaces
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/knowledge_lake/pipeline/process.py
  - src/knowledge_lake/pipeline/query.py
  - src/knowledge_lake/pipeline/domains.py
  - src/knowledge_lake/plugins/builtin/qdrant_store.py
  - src/knowledge_lake/cli/app.py
  - src/knowledge_lake/api/app.py
  - tests/unit/test_pipeline_extractions.py
autonomous: true
requirements: [MCP-01]
must_haves:
  truths:
    - "process_crawled/list_sources/stats/load_domain exist as importable pipeline service functions returning documented dict/list shapes"
    - "cmd_process_crawled, list_sources_endpoint, load_domain_endpoint, and cmd_init call the extracted functions — logic exists in exactly one place"
    - "QdrantVectorStore.count_points(collection) returns an exact point count, 0 for a missing collection"
    - "All materialized rows are built inside the get_session() block (no ORM instances cross the session boundary)"
  artifacts:
    - src/knowledge_lake/pipeline/process.py
    - src/knowledge_lake/pipeline/query.py
    - src/knowledge_lake/pipeline/domains.py
    - src/knowledge_lake/plugins/builtin/qdrant_store.py (count_points)
  key_links:
    - "CLI + API callers must delegate to the extracted service functions (no duplicated bodies)"
    - "stats() must call QdrantVectorStore.count_points() via get_vectorstore(settings), never reach into _client"
  prohibitions:
    - "Do NOT duplicate the extracted logic in the CLI/API callers — refactor them to call the new functions (D-05)"
    - "Do NOT return ORM Source/Artifact instances across the get_session() boundary (DetachedInstanceError guard)"
    - "Do NOT reach into QdrantVectorStore._client from stats() — use the new public count_points() (Pitfall 5)"
    - "Do NOT weaken the domain-name guard `^[a-zA-Z][a-zA-Z0-9_-]{0,63}$` when extracting load_domain (path-traversal, T-12-06)"
---

<objective>
Extract the four service functions that today have no clean `pipeline/*.py` home, so the MCP tools can shim them as thin callers (D-05). Refactor the existing CLI/API callers to delegate to the extractions — one function, many callers.

Purpose: `process_crawled` logic is inline in `cli/app.py`, `list_sources`/`stats` don't exist as service functions, and the domain-load routine lives inside `_register_domain_sources`/`cmd_init`. MCP-01's "thin shim over pipeline/*.py" is impossible until these are standalone. `stats` also needs a public Qdrant point count.

Output: `pipeline/process.py:process_crawled()`, `pipeline/query.py:list_sources()` + `stats()`, `pipeline/domains.py:load_domain()`, `QdrantVectorStore.count_points()`, with `cmd_process_crawled`/`list_sources_endpoint`/`load_domain_endpoint`/`cmd_init` refactored to call them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-surfaces/12-PATTERNS.md
@.planning/phases/12-agent-surfaces/12-RESEARCH.md
@src/knowledge_lake/cli/app.py
@src/knowledge_lake/api/app.py
@src/knowledge_lake/plugins/builtin/qdrant_store.py
</context>

<artifacts_this_phase_produces>
This plan creates: `pipeline/process.py:process_crawled()`, `pipeline/query.py:list_sources()`, `pipeline/query.py:stats()`, `pipeline/domains.py:load_domain()`, `QdrantVectorStore.count_points()`. (Full phase artifact list is in 12-01-PLAN.md.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract process_crawled into pipeline/process.py + refactor CLI caller</name>
  <files>src/knowledge_lake/pipeline/process.py, src/knowledge_lake/cli/app.py, tests/unit/test_pipeline_extractions.py</files>
  <read_first>
    - src/knowledge_lake/cli/app.py:578-669 (cmd_process_crawled — the exact body to extract; note tuples materialized inside the session at ~line 632)
    - .planning/phases/12-agent-surfaces/12-PATTERNS.md §"pipeline process_crawled() — EXTRACT from CLI" (core pattern + suggested signature)
  </read_first>
  <behavior>
    - process_crawled(source_id=None, limit=100, collection="klake_chunks") returns a dict with integer counts: processed, chunks_indexed (or total_chunks), failed
    - Calling process_crawled with no unprocessed raw_document artifacts returns zeros without error
    - cmd_process_crawled produces the same user-facing summary as before, now sourced from the returned dict
  </behavior>
  <action>
    Create `src/knowledge_lake/pipeline/process.py` with `def process_crawled(*, source_id=None, limit=100, collection="klake_chunks") -> dict`. Move the body of `cmd_process_crawled` (cli/app.py:596-669) into it verbatim: the `with get_session()` block building the `aliased(Artifact)` `has_parsed_child` NOT-EXISTS query over `raw_document` artifacts, the tuple materialization inside the session (keep it — DetachedInstanceError guard), and the parse→chunk→embed→index loop counting processed/failed/total_chunks. Return `{"processed": ..., "chunks_indexed": ..., "failed": ...}`. Then refactor `cmd_process_crawled` to call `process_crawled(source_id=..., limit=..., collection=...)` and `typer.echo` the summary fields (the old progress prints become dict fields). No logic remains duplicated in the CLI (D-05).
  </action>
  <acceptance_criteria>
    - `grep -c 'def process_crawled' src/knowledge_lake/pipeline/process.py` returns 1
    - `uv run python -c "from knowledge_lake.pipeline.process import process_crawled; print('ok')"` prints ok
    - cmd_process_crawled no longer contains the extraction body — `grep -c 'has_parsed_child' src/knowledge_lake/cli/app.py` returns 0 (moved to pipeline/process.py)
    - `uv run pytest tests/unit/test_pipeline_extractions.py -k process_crawled -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_pipeline_extractions.py -k process_crawled -x</automated>
  </verify>
  <done>process_crawled() is a standalone pipeline service function returning a counts dict; cmd_process_crawled delegates to it with no duplicated logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract list_sources + stats into pipeline/query.py, add count_points, refactor list_sources_endpoint</name>
  <files>src/knowledge_lake/pipeline/query.py, src/knowledge_lake/plugins/builtin/qdrant_store.py, src/knowledge_lake/api/app.py, tests/unit/test_pipeline_extractions.py</files>
  <read_first>
    - src/knowledge_lake/api/app.py:1236-1273 (list_sources_endpoint — query + Python-side domain filter to extract)
    - src/knowledge_lake/pipeline/crawl.py:211-241 (list_sources_for_crawl_all — the session-safe materialization pattern)
    - src/knowledge_lake/plugins/builtin/qdrant_store.py:349 (internal `self._client.count(col, exact=True).count`) and src/knowledge_lake/plugins/resolver.py:250 (get_vectorstore)
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Code Examples" (list_sources body, stats shape) + Pitfall 5 (count_points)
  </read_first>
  <behavior>
    - list_sources(domain=None, limit=50, offset=0) returns a list of plain dicts (source_id, name, url, source_type, license_type, domain, created_at) materialized inside the session
    - stats(collection="klake_chunks", domain=None) returns {"sources", "documents", "artifacts_by_type": {...}, "qdrant_points", "collection"}
    - count_points("missing_collection") returns 0, not an exception
  </behavior>
  <action>
    Create `src/knowledge_lake/pipeline/query.py` with two read-only service functions. `list_sources(domain=None, *, limit=50, offset=0) -> list[dict]` — copy the merged body from RESEARCH §Code Examples: inside `with get_session()`, select `Source` ordered by `created_at.desc()`, apply the Python-side `(s.config or {}).get("domain")` filter when `domain` is set, and return materialized dicts (never ORM rows). `stats(*, collection="klake_chunks", domain=None) -> dict` — inside `get_session()` count sources, documents, and artifacts grouped by `artifact_type`; obtain Qdrant points via `get_vectorstore(settings).count_points(collection)`; return the documented dict. Add a public `count_points(self, collection: str) -> int` to `QdrantVectorStore` wrapping `self._client.count(collection, exact=True).count` with a try/except returning 0 on missing collection (Pitfall 5) — do NOT let `stats()` touch `_client`. Finally refactor `list_sources_endpoint` (api/app.py:1236) to call `list_sources(...)` and map the returned dicts to `SourceListItem` (schemas.py:651).
  </action>
  <acceptance_criteria>
    - `uv run python -c "from knowledge_lake.pipeline.query import list_sources, stats; print('ok')"` prints ok
    - `grep -c 'def count_points' src/knowledge_lake/plugins/builtin/qdrant_store.py` returns 1
    - `grep -c '_client' src/knowledge_lake/pipeline/query.py` returns 0 (stats never reaches into the client)
    - list_sources_endpoint delegates to pipeline.query.list_sources — `grep -c 'list_sources' src/knowledge_lake/api/app.py` >= 1 and the endpoint no longer inlines the Source select loop
    - `uv run pytest tests/unit/test_pipeline_extractions.py -k "list_sources or stats or count_points" -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_pipeline_extractions.py -k "list_sources or stats or count_points" -x</automated>
  </verify>
  <done>list_sources() and stats() are session-safe pipeline read functions; count_points() is public; list_sources_endpoint delegates to the extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract load_domain into pipeline/domains.py + refactor API and CLI callers</name>
  <files>src/knowledge_lake/pipeline/domains.py, src/knowledge_lake/api/app.py, src/knowledge_lake/cli/app.py, tests/unit/test_pipeline_extractions.py</files>
  <read_first>
    - src/knowledge_lake/api/app.py:1494 (_register_domain_sources — already returns {loaded_count, skipped_count, upload_required_count}) and :1574 (load_domain_endpoint)
    - src/knowledge_lake/cli/app.py:1034-1130 (cmd_init — inlines DomainLoader + register_source loop + cron validation at ~1105-1116; guard `_DOMAIN_NAME_RE` at ~1056)
    - .planning/phases/12-agent-surfaces/12-PATTERNS.md §"pipeline/domains load_domain() — EXTRACT" (A4: preserve cmd_init's cron-validation branch in the shared fn)
    - .planning/phases/12-agent-surfaces/12-RESEARCH.md §"Open Questions" #2 (return shape matches DomainLoadResponse, schemas.py:727)
  </read_first>
  <behavior>
    - load_domain(name) returns {"name", "loaded_count", "skipped_count", "upload_required_count"} (aligns with DomainLoadResponse)
    - load_domain rejects a name failing `^[a-zA-Z][a-zA-Z0-9_-]{0,63}$` with a ValueError (path-traversal guard preserved)
    - load_domain_endpoint and cmd_init both produce identical registration results to before, now via load_domain
  </behavior>
  <action>
    Create `src/knowledge_lake/pipeline/domains.py` with `def load_domain(name: str) -> dict`. Promote the body of `_register_domain_sources` (api/app.py:1494) into it, re-validating `name` against `_DOMAIN_NAME_RE` (`^[a-zA-Z][a-zA-Z0-9_-]{0,63}$`) at entry (T-12-06 path-traversal guard) and preserving cmd_init's extra cron-validation branch (cli/app.py:~1105-1116) so both callers share one code path (A4). Return `{"name": name, "loaded_count": ..., "skipped_count": ..., "upload_required_count": ...}`. Refactor `load_domain_endpoint` (api/app.py:1574) to call `load_domain(body.name)` and wrap into `DomainLoadResponse`; refactor `cmd_init` (cli/app.py:1034) to call `load_domain(name)` and echo the summary. Remove the now-duplicated inline registration logic from both callers.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from knowledge_lake.pipeline.domains import load_domain; print('ok')"` prints ok
    - `grep -Ec '\^\[a-zA-Z\]\[a-zA-Z0-9_-\]\{0,63\}' src/knowledge_lake/pipeline/domains.py` returns >= 1 (guard preserved)
    - load_domain_endpoint and cmd_init both reference load_domain — `grep -c 'load_domain' src/knowledge_lake/api/app.py` >= 1 and `grep -c 'load_domain' src/knowledge_lake/cli/app.py` >= 1
    - `uv run pytest tests/unit/test_pipeline_extractions.py -k "load_domain or init_domain" -x` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_pipeline_extractions.py -k "load_domain or init_domain" -x</automated>
  </verify>
  <done>load_domain() is the single shared domain-registration service function guarded against path traversal; both API and CLI delegate to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool arg (`domain` name) → filesystem/domain loader | init_domain/load_domain resolves a domain by name |
| service fn → Postgres/Qdrant | read aggregates over registry + vector store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Tampering | `load_domain(name)` | medium | mitigate | Re-validate `name` against `^[a-zA-Z][a-zA-Z0-9_-]{0,63}$` at entry (existing guard from schemas.py:710 / cli/app.py:1056) before any path resolution |
| T-12-03 | Info Disclosure | extracted read fns (`list_sources`/`stats`) | low | accept | Read-only aggregates over existing registry/Qdrant state; no new fetch path introduced; no SSRF surface added |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_pipeline_extractions.py -q` passes
- `uv run pytest tests/unit -q` stays green (CLI/API refactors did not regress existing tests)
- No extraction body remains duplicated in cli/app.py or api/app.py
</verification>

<success_criteria>
Four extracted pipeline service functions plus count_points() exist and return documented shapes; all four original callers delegate to them with no logic duplication.
</success_criteria>

<output>
Create `.planning/phases/12-agent-surfaces/12-02-SUMMARY.md` when done
</output>
