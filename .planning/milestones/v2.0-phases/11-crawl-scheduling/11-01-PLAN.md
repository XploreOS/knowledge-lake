---
phase: 11-crawl-scheduling
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_recrawl_gate.py
  - tests/unit/test_recrawl_sensor.py
  - tests/unit/test_set_schedule_cli.py
  - tests/integration/test_migrations.py
autonomous: true
requirements: [SCHED-01, SCHED-02]

must_haves:
  truths:
    - "Running pytest on the three new unit test files collects with zero import/collection errors while the target symbols are still absent."
    - "Each unit test module skips cleanly (SKIPPED, not ERROR) until its target symbol is importable, then auto-activates."
    - "tests/integration/test_migrations.py asserts the three new sources columns and the 0008->0009 head chain."
  artifacts:
    - tests/unit/test_recrawl_gate.py
    - tests/unit/test_recrawl_sensor.py
    - tests/unit/test_set_schedule_cli.py
    - tests/integration/test_migrations.py
  key_links:
    - "Module-level import guard sets a boolean flag; pytestmark = skipif(not flag) gates each module so the suite stays green through intermediate waves."
  prohibitions:
    - "No test scaffold may import a not-yet-created Phase 11 symbol at module top level without a try/except guard (would break collection of the whole suite)."
    - "No scaffold test may be written watch-mode or interactive."
---

<objective>
Create the RED test scaffold for Phase 11 (SCHED-01 sensor + SCHED-02 change gate) so every downstream implementation task has a concrete `<automated>` gate to turn green. Mirrors the Phase 8/9/10 Wave-0 convention: real assertions, guarded so missing symbols skip rather than error.

Purpose: Establish the Nyquist feedback loop before any production code lands.
Output: Three new unit test modules (guarded) + an extension to the existing migration integration test.
</objective>

## Artifacts this phase produces (whole-phase manifest — for downstream drift exclusion)

New symbols/files introduced across Phase 11 (this plan creates only the tests; later plans create the rest):
- Columns: `sources.crawl_schedule`, `sources.last_crawled_at`, `sources.last_content_hash`
- Migration: `registry/alembic/versions/0009_crawl_scheduling.py` (revision `0009`)
- Repo helpers: `touch_source_crawl`, `list_scheduled_sources`, `set_source_schedule`; `create_source(crawl_schedule=...)` kwarg
- Pipeline: `recrawl_source`, `_signature` (pipeline/crawl.py)
- Dagster: `dagster_defs/sensors.py` — `RecrawlConfig`, `recrawl_op`, `recrawl_source_job`, `recrawl_sensor`
- Settings: `CrawlSettings.max_staleness_days`, env `KLAKE_CRAWL__MAX_STALENESS_DAYS`
- Config model: `domains.models.SourceEntry.crawl_schedule`
- CLI: `set-schedule` command
- Infra: `infra/dagster/dagster.yaml` `QueuedRunCoordinator`
- Tests (this plan): `tests/unit/test_recrawl_gate.py`, `tests/unit/test_recrawl_sensor.py`, `tests/unit/test_set_schedule_cli.py`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-crawl-scheduling/11-RESEARCH.md
@.planning/phases/11-crawl-scheduling/11-VALIDATION.md
@tests/conftest.py
@tests/integration/test_migrations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffold for the SCHED-02 change gate (tests/unit/test_recrawl_gate.py)</name>
  <files>tests/unit/test_recrawl_gate.py</files>
  <read_first>
    - tests/conftest.py (in-memory SQLite fixture + patching conventions)
    - tests/unit/test_crawl_all.py (fake-adapter + monkeypatch patterns for pipeline/crawl functions)
    - src/knowledge_lake/pipeline/clean.py:82 `remove_boilerplate` (the normalizer the signature must reuse)
    - .planning/phases/11-crawl-scheduling/11-RESEARCH.md § "Change gate" + § "Validation Architecture" (test map rows for SCHED-02)
  </read_first>
  <action>
    Create tests/unit/test_recrawl_gate.py. At module scope add a guarded import: try to `from knowledge_lake.pipeline.crawl import recrawl_source` and set `_HAS_GATE = True`, except `Exception` set `_HAS_GATE = False`; then `pytestmark = pytest.mark.skipif(not _HAS_GATE, reason="recrawl_source pending (Plan 11-03)")`.
    Write five async tests matching the SCHED-02 test map: `test_unchanged_skips_no_raw`, `test_changed_recrawls`, `test_nonce_noise_unchanged`, `test_null_hash_forces_crawl`, `test_staleness_forces_refresh`.
    Use a fake crawler adapter whose `fetch_page(url)` returns an object with a `.markdown` attribute (controlled text). Monkeypatch `knowledge_lake.plugins.resolver.get_crawler` (or the crawl-module reference) to return the fake, monkeypatch `knowledge_lake.pipeline.crawl.crawl_source` to a mock recording calls, and monkeypatch `knowledge_lake.pipeline.crawl.validate_public_url` to a no-op spy. Seed the registry `Source` via the repo with a known `last_content_hash` / `last_crawled_at`.
    Assertions: skip path asserts the `crawl_source` mock was NOT called and `touch_source_crawl` bumped `last_crawled_at` only; changed path asserts `crawl_source` WAS called and the new hash was written; nonce test feeds two markdown strings differing only by a dynamic token line that `remove_boilerplate` normalizes away and asserts the signature is stable so the gate skips; null-hash test asserts a NULL `last_content_hash` always crawls; staleness test sets `last_crawled_at` older than `max_staleness_days` and asserts a crawl despite a matching hash. Compute the expected signature in-test as `hashlib.sha256(remove_boilerplate(text).encode("utf-8")).hexdigest()`. Do not embed the literal phrase the executor will negative-grep for; assert on the `get_crawler`/`crawl_source`/`validate_public_url` mock objects instead.
  </action>
  <acceptance_criteria>
    - File defines exactly the five named test functions above.
    - Module has a top-level `try/except` import guard and a `pytestmark = pytest.mark.skipif(...)` referencing Plan 11-03.
    - `./.venv/bin/pytest tests/unit/test_recrawl_gate.py -q` reports 5 skipped, 0 errors (symbols absent at scaffold time).
    - No production symbol is imported unguarded at module top level.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/pytest tests/unit/test_recrawl_gate.py -q -rs</automated>
  </verify>
  <done>tests/unit/test_recrawl_gate.py collects with 5 skipped tests and zero collection errors.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffold for the SCHED-01 sensor (tests/unit/test_recrawl_sensor.py)</name>
  <files>tests/unit/test_recrawl_sensor.py</files>
  <read_first>
    - tests/unit/test_dagster_e2e_job.py + tests/unit/test_dagster_retry_policies.py (how existing Dagster objects are tested in-process)
    - .planning/phases/11-crawl-scheduling/11-RESEARCH.md § "Pattern 2" (build_sensor_context, run_key, cursor, SkipReason — executed reference)
    - .planning/phases/11-crawl-scheduling/11-PATTERNS.md § "dagster_defs/sensors.py"
  </read_first>
  <action>
    Create tests/unit/test_recrawl_sensor.py. Module-scope guarded import: `from knowledge_lake.dagster_defs.sensors import recrawl_sensor, recrawl_source_job, RecrawlConfig` under try/except setting `_HAS_SENSOR`; `pytestmark = pytest.mark.skipif(not _HAS_SENSOR, reason="recrawl_sensor pending (Plan 11-05)")`.
    Write three tests from the SCHED-01 map: `test_emits_deterministic_run_key`, `test_skips_not_due`, `test_run_key_stable_within_window`. Drive the sensor with `dagster.build_sensor_context(cursor=None)`. Monkeypatch `knowledge_lake.registry.repo.list_scheduled_sources` to return a list of fake materialized source rows (namedtuple with id, url, crawl_schedule, last_crawled_at, last_content_hash, created_at, config) with tz-aware UTC datetimes.
    Assertions: due source produces exactly one `RunRequest` whose `run_key` equals `f"{sid}:{fire.isoformat()}"` where `fire = get_latest_completed_cron_tick(schedule, now, "UTC")` and whose `tags` include `{"klake/source": sid}`; a not-due source (next fire in the future) yields a `SkipReason` and no RunRequest; calling the sensor twice within the same cron window yields byte-identical run_keys (idempotent). Assert the cursor advances (context reports an updated cursor). Import the cron helpers from `dagster._utils.schedules` in-test to compute expected fire timestamps — never `import croniter`.
  </action>
  <acceptance_criteria>
    - File defines the three named tests and uses `dagster.build_sensor_context`.
    - Module guarded import references Plan 11-05 in the skip reason.
    - `./.venv/bin/pytest tests/unit/test_recrawl_sensor.py -q` reports 3 skipped, 0 errors.
    - No `import croniter` anywhere in the file.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/pytest tests/unit/test_recrawl_sensor.py -q -rs</automated>
  </verify>
  <done>tests/unit/test_recrawl_sensor.py collects with 3 skipped tests and zero collection errors.</done>
</task>

<task type="auto">
  <name>Task 3: RED scaffold for the CLI cron validation + migration column checks</name>
  <files>tests/unit/test_set_schedule_cli.py, tests/integration/test_migrations.py</files>
  <read_first>
    - tests/unit/test_cli_init_index.py (Typer CliRunner invocation pattern for klake commands)
    - tests/integration/test_migrations.py:128 `TestSourcesSchema` (the class to extend with the 3 new columns)
    - .planning/phases/11-crawl-scheduling/11-RESEARCH.md § "Cron Due-Check" (is_valid_cron_string behavior) + § Validation Architecture (Criterion 1 row)
  </read_first>
  <action>
    Create tests/unit/test_set_schedule_cli.py with a guarded import of the CLI app / `set-schedule` command handler and `knowledge_lake.registry.repo.set_source_schedule`, `pytestmark = skipif(...)` referencing Plan 11-04. Write `test_rejects_bad_cron`: invoke the `set-schedule` command via Typer's `CliRunner` with a malformed cron (e.g. `"not a cron"`) and assert a non-zero exit code and that `set_source_schedule` was NOT called (patch it). Add `test_accepts_valid_cron` asserting a 5-field cron persists via `set_source_schedule` and `test_clear_schedule` asserting `--clear` writes None.
    Extend tests/integration/test_migrations.py: add `crawl_schedule`, `last_crawled_at`, `last_content_hash` to `TestSourcesSchema.REQUIRED_COLUMNS`, and add a `TestMigrationHead` (or module-level test) asserting the current alembic head is `0009` and that `0009` down-revision is `0008` by reading the migration module's `revision`/`down_revision` attributes (no live DB needed for the head-chain read). Keep the existing Postgres-dependent column assertions as the deeper integration gate.
  </action>
  <acceptance_criteria>
    - tests/unit/test_set_schedule_cli.py defines `test_rejects_bad_cron`, `test_accepts_valid_cron`, `test_clear_schedule`, guarded + skipif referencing Plan 11-04.
    - tests/integration/test_migrations.py `TestSourcesSchema.REQUIRED_COLUMNS` includes the three new column names.
    - A head-chain assertion reads `revision == "0009"` and `down_revision == "0008"` from the 0009 module (skipif the module is absent).
    - `./.venv/bin/pytest tests/unit/test_set_schedule_cli.py -q` reports skipped, 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/pytest tests/unit/test_set_schedule_cli.py -q -rs && ./.venv/bin/python -c "import ast,sys; sys.exit(0)"</automated>
  </verify>
  <done>CLI cron-rejection scaffold collects with skips; migration test lists the 3 new columns and asserts the 0008->0009 chain (skipif 0009 absent).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/YAML → registry | Cron strings (user input) are persisted to `sources.crawl_schedule`. |
| Dagster daemon → external source host | Seed-probe + crawl fetches cross to the untrusted internet (SSRF surface). |
| Sensor → Dagster run storage | Uncontrolled `RunRequest` emission could flood the run queue. |

## STRIDE Threat Register (phase-level; this plan validates the mitigations)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-SSRF | Information Disclosure | seed probe (crawl.py) | high | mitigate | test_recrawl_gate spies that `validate_public_url` runs before `fetch_page` (implemented Plan 11-03). |
| T-11-THRASH | Denial of Service (cost) | change gate | high | mitigate | test_recrawl_gate asserts unchanged pages skip `crawl_source`/`put_raw` (Plan 11-03). |
| T-11-TICKSTORM | Denial of Service | sensor + coordinator | high | mitigate | test_recrawl_sensor asserts deterministic `run_key` dedup (Plan 11-05). |
| T-11-CRON | Denial of Service (V5 input) | set-schedule / domain-init | medium | mitigate | test_set_schedule_cli asserts malformed cron rejected pre-write (Plan 11-04). |
| T-11-WRITE | Tampering / Integrity | sensor | medium | mitigate | test_recrawl_sensor asserts no DB write in the sensor (Plan 11-05). |
| T-11-SC | Tampering (supply chain) | dependency set | high | mitigate | No new package added; Dagster's vendored cron engine used — tests import `dagster._utils.schedules`, never `croniter`. |
</threat_model>

<verification>
- `./.venv/bin/pytest tests/unit/test_recrawl_gate.py tests/unit/test_recrawl_sensor.py tests/unit/test_set_schedule_cli.py -q -rs` → all skipped, zero collection errors.
- `./.venv/bin/pytest tests/unit -q` remains green (no regression from the new guarded modules).
</verification>

<success_criteria>
Three guarded unit test modules and the extended migration test exist; the full unit suite stays green; each module auto-activates when its Phase 11 target symbol becomes importable.
</success_criteria>

<output>
Create `.planning/phases/11-crawl-scheduling/11-01-SUMMARY.md` when done.
</output>
