---
phase: 11-crawl-scheduling
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - src/knowledge_lake/registry/alembic/versions/0009_crawl_scheduling.py
  - src/knowledge_lake/registry/models.py
  - src/knowledge_lake/registry/repo.py
  - src/knowledge_lake/config/settings.py
autonomous: true
requirements: [SCHED-01, SCHED-02]

must_haves:
  truths:
    - "`alembic heads` reports 0009 as the single head; 0009.down_revision == '0008' (D-01: additive forward-only migration)."
    - "Source ORM exposes crawl_schedule (String255), last_crawled_at (DateTime tz-aware), last_content_hash (String64) as nullable mapped columns (D-01)."
    - "repo.touch_source_crawl opens its own session and updates last_crawled_at, optionally last_content_hash."
    - "repo.list_scheduled_sources returns materialized namedtuple rows only for sources whose crawl_schedule is non-NULL."
    - "repo.set_source_schedule updates the crawl_schedule column via an injected session; repo.create_source accepts a crawl_schedule kwarg."
    - "settings.crawl.max_staleness_days defaults to 30 and reads KLAKE_CRAWL__MAX_STALENESS_DAYS."
  artifacts:
    - src/knowledge_lake/registry/alembic/versions/0009_crawl_scheduling.py
    - src/knowledge_lake/registry/models.py
    - src/knowledge_lake/registry/repo.py
    - src/knowledge_lake/config/settings.py
  key_links:
    - "0009 upgrade adds the 3 nullable columns; downgrade drops them in reverse order; head chains 0008->0009."
    - "list_scheduled_sources materializes every field inside the session (DetachedInstanceError-safe)."
  prohibitions:
    - "0009 columns must be nullable — no NOT NULL, no server_default, no backfill (forward-only / WORM-safe)."
    - "crawl_schedule must be a real column, never stored under Source.config."
    - "list_scheduled_sources must not return live ORM objects that lazy-load after session close."
---

<objective>
Add the enabling schema and registry plumbing for Phase 11: the additive Alembic `0009` migration + three nullable `Source` columns (SCHED-01 schedule column; SCHED-02 hash/timestamp columns), the repo helpers the gate/sensor/CLI call, and the `max_staleness_days` setting. No behavior changes for existing sources (all columns NULL).

Purpose: Every later plan (change gate, sensor, CLI) depends on these columns and helpers existing.
Output: Migration `0009`, `Source` columns, `touch_source_crawl`/`list_scheduled_sources`/`set_source_schedule`/`create_source(crawl_schedule=...)`, `CrawlSettings.max_staleness_days`.
</objective>

## Artifacts this phase produces (relevant to this plan)
- Columns: `sources.crawl_schedule` (String(255)), `sources.last_crawled_at` (DateTime(timezone=True)), `sources.last_content_hash` (String(64))
- Migration: `registry/alembic/versions/0009_crawl_scheduling.py` (revision `0009`, down_revision `0008`)
- Repo: `touch_source_crawl`, `list_scheduled_sources`, `set_source_schedule`, `create_source(crawl_schedule=...)` kwarg, `_ScheduledSource` namedtuple
- Settings: `CrawlSettings.max_staleness_days`, env `KLAKE_CRAWL__MAX_STALENESS_DAYS`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-crawl-scheduling/11-RESEARCH.md
@.planning/phases/11-crawl-scheduling/11-PATTERNS.md
@src/knowledge_lake/registry/alembic/versions/0008_dataset_examples.py
@src/knowledge_lake/registry/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 0009 migration + Source ORM columns</name>
  <files>src/knowledge_lake/registry/alembic/versions/0009_crawl_scheduling.py, src/knowledge_lake/registry/models.py</files>
  <read_first>
    - src/knowledge_lake/registry/alembic/versions/0008_dataset_examples.py (header/revision identifiers + additive op.add_column style + reverse-order downgrade — copy verbatim, change ids)
    - src/knowledge_lake/registry/models.py:58 `Source` (place new columns after `config` at :93, before `created_at` at :96; String/DateTime/Optional/datetime already imported at :25-44)
    - .planning/phases/11-crawl-scheduling/11-PATTERNS.md § "0009_crawl_scheduling.py" and § "Source"
  </read_first>
  <behavior>
    - `alembic heads` prints `0009 (head)` (offline, no DB needed).
    - The 0009 module exposes `revision == "0009"` and `down_revision == "0008"`.
    - `Source` has attributes `crawl_schedule`, `last_crawled_at`, `last_content_hash`; all mapped nullable.
    - upgrade() adds 3 columns; downgrade() drops them in reverse (hash, then crawled_at, then schedule).
  </behavior>
  <action>
    Create registry/alembic/versions/0009_crawl_scheduling.py mirroring 0008 exactly: same `from __future__`, `typing`, `sqlalchemy as sa`, `from alembic import op` header; set `revision = "0009"`, `down_revision = "0008"`, `branch_labels = None`, `depends_on = None`. In `upgrade()` call `op.add_column("sources", sa.Column("crawl_schedule", sa.String(255), nullable=True))`, then `last_crawled_at` as `sa.DateTime(timezone=True)` nullable, then `last_content_hash` as `sa.String(64)` nullable. In `downgrade()` drop `last_content_hash`, `last_crawled_at`, `crawl_schedule` in that reverse order. No `server_default`, no NOT NULL, no data backfill.
    In registry/models.py add three `Mapped[...]` columns on `Source` after `config`: `crawl_schedule: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)` (docstring: 5-field UTC cron, NULL means not auto-recrawled, SCHED-01/D-02/D-03); `last_crawled_at: Mapped[Optional[datetime.datetime]] = mapped_column(DateTime(timezone=True), nullable=True)` (docstring: UTC timestamp of last re-crawl ATTEMPT, updated on skip and crawl alike, D-11); `last_content_hash: Mapped[Optional[str]] = mapped_column(String(64), nullable=True)` (docstring: SHA256 over normalized silver-stage seed-page text, NULL means always crawl, D-06/D-07).
  </action>
  <acceptance_criteria>
    - `./.venv/bin/alembic heads` outputs a line beginning `0009`.
    - `./.venv/bin/python -c "import knowledge_lake.registry.alembic.versions.0009_crawl_scheduling as m; assert m.revision=='0009' and m.down_revision=='0008'"` — or an import-by-path equivalent — passes.
    - `./.venv/bin/python -c "from knowledge_lake.registry.models import Source; assert all(hasattr(Source,a) for a in ('crawl_schedule','last_crawled_at','last_content_hash'))"` passes.
    - The 0009 columns carry no server_default and are nullable=True.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/alembic heads | grep -q '^0009' && ./.venv/bin/python -c "from knowledge_lake.registry.models import Source; assert all(hasattr(Source,a) for a in ('crawl_schedule','last_crawled_at','last_content_hash')); print('ok')"</automated>
  </verify>
  <done>0009 is the alembic head chained to 0008; Source exposes the three nullable columns; downgrade drops them in reverse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: repo helpers — touch_source_crawl, list_scheduled_sources, set_source_schedule, create_source kwarg</name>
  <files>src/knowledge_lake/registry/repo.py</files>
  <read_first>
    - src/knowledge_lake/registry/repo.py:55 `create_source` (extend signature + Source(...) construction)
    - src/knowledge_lake/registry/repo.py:822 `get_domain_for_source` + :843 `get_source_crawl_config` (None-guard + session.get(Source,...) template)
    - src/knowledge_lake/pipeline/crawl.py:57-87 `list_sources_for_crawl_all` (namedtuple materialization pattern to mirror)
    - .planning/phases/11-crawl-scheduling/11-RESEARCH.md § "touch_source_crawl" + § "Pattern 3"
  </read_first>
  <behavior>
    - `list_scheduled_sources(session)` returns only rows with non-NULL crawl_schedule, as `_ScheduledSource` namedtuples carrying id/url/crawl_schedule/last_crawled_at/last_content_hash/created_at/config.
    - `touch_source_crawl(sid, last_crawled_at=..., last_content_hash=None)` updates last_crawled_at always; leaves last_content_hash unchanged when the kwarg is None (skip path) and sets it when provided.
    - `set_source_schedule(session, sid, schedule)` updates crawl_schedule (None clears); returns True if the source existed.
    - `create_source(..., crawl_schedule=None)` persists the column.
  </behavior>
  <action>
    In repo.py: (1) extend `create_source` with a keyword param `crawl_schedule: Optional[str] = None` and pass it into the `Source(...)` construction. (2) Add `touch_source_crawl(source_id, *, last_crawled_at, last_content_hash: Optional[str] = None) -> None` that opens its own session via `get_session()` (confirm/add the module-level import from `knowledge_lake.registry.db`), `session.get(Source, source_id)`, early-return on None, set `src.last_crawled_at = last_crawled_at`, and only set `src.last_content_hash` when the kwarg is not None. (3) Add a module-level `_ScheduledSource = namedtuple("_ScheduledSource", ["id","url","crawl_schedule","last_crawled_at","last_content_hash","created_at","config"])` and `list_scheduled_sources(session) -> list` that selects `Source` where `Source.crawl_schedule.is_not(None)` and materializes each field into a `_ScheduledSource` before returning. (4) Add `set_source_schedule(session, source_id, crawl_schedule: Optional[str]) -> bool` following the None-guard template (session.get, early-return False, set the column, return True). Import `namedtuple`, `select`, and `get_session` as needed at module top (check what is already imported before adding).
  </action>
  <acceptance_criteria>
    - `touch_source_crawl` uses its own `get_session()` context (the Dagster op calls it without holding a session).
    - `list_scheduled_sources` filters `Source.crawl_schedule.is_not(None)` and returns namedtuples, not ORM instances.
    - `set_source_schedule` returns False when the source is missing.
    - `create_source` accepts and persists `crawl_schedule`.
    - `./.venv/bin/python -c` importing all four symbols from `knowledge_lake.registry.repo` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -c "from knowledge_lake.registry.repo import touch_source_crawl, list_scheduled_sources, set_source_schedule, create_source, _ScheduledSource; import inspect; assert 'crawl_schedule' in inspect.signature(create_source).parameters; print('ok')" && ./.venv/bin/pytest tests/unit/test_registry.py -q</automated>
  </verify>
  <done>The four helpers import cleanly, create_source takes crawl_schedule, and the existing registry unit suite stays green.</done>
</task>

<task type="auto">
  <name>Task 3: CrawlSettings.max_staleness_days</name>
  <files>src/knowledge_lake/config/settings.py</files>
  <read_first>
    - src/knowledge_lake/config/settings.py:53 `CrawlSettings` (add the field alongside max_pages/rate_limit_seconds; nested prefix KLAKE_CRAWL__ already wired at :401 via `crawl: CrawlSettings`)
    - .planning/phases/11-crawl-scheduling/11-RESEARCH.md § "Settings" (D-10)
  </read_first>
  <action>
    In config/settings.py add `max_staleness_days: int = 30` to `CrawlSettings` with a docstring explaining it forces a full re-ingest when `now - last_crawled_at` exceeds it even when the signature is unchanged (SCHED-02, D-10), read from `KLAKE_CRAWL__MAX_STALENESS_DAYS`, with a per-source override at `Source.config['crawl_config']['max_staleness_days']`. No new plumbing — the `crawl:` nested model already exists.
  </action>
  <acceptance_criteria>
    - `settings.crawl.max_staleness_days` defaults to 30.
    - Setting `KLAKE_CRAWL__MAX_STALENESS_DAYS=7` in the environment yields `settings.crawl.max_staleness_days == 7`.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -c "from knowledge_lake.config.settings import get_settings; assert get_settings().crawl.max_staleness_days==30; print('default ok')" && KLAKE_CRAWL__MAX_STALENESS_DAYS=7 ./.venv/bin/python -c "from knowledge_lake.config.settings import get_settings; assert get_settings().crawl.max_staleness_days==7; print('env ok')"</automated>
  </verify>
  <done>CrawlSettings exposes max_staleness_days with default 30 and honors the KLAKE_CRAWL__MAX_STALENESS_DAYS override.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/YAML/CLI → registry | Cron strings and crawl watermarks are persisted to the `sources` table. |
| Dagster op → registry (touch_source_crawl) | Watermark writes cross the process boundary from the crawl op. |

## STRIDE Threat Register (phase-level; this plan supplies the storage substrate)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-THRASH | Denial of Service (cost) | last_content_hash column | high | mitigate | Column stores the normalized-text signature the gate compares (used in Plan 11-03). |
| T-11-WRITE | Tampering / Integrity | touch_source_crawl | medium | mitigate | Own-session helper; the sensor never writes — only the op calls it (Plan 11-05). |
| T-11-CRON | Denial of Service (V5 input) | crawl_schedule column / set_source_schedule | medium | mitigate | Column persists only validated cron; validation enforced at the call site in Plan 11-04. |
| T-11-SSRF | Information Disclosure | (n/a this plan) | high | mitigate | Enforced in Plan 11-03 seed probe. |
| T-11-TICKSTORM | Denial of Service | (n/a this plan) | high | mitigate | Enforced in Plan 11-05 sensor. |
| T-11-SC | Tampering (supply chain) | dependency set | high | mitigate | No new package added (no install task in this plan). |
</threat_model>

<verification>
- `./.venv/bin/alembic heads` shows `0009` as head.
- `./.venv/bin/pytest tests/unit/test_registry.py tests/unit/test_settings.py -q` green.
- Once this plan lands, the Plan 11-01 migration head-chain assertion in tests/integration/test_migrations.py stops being skipped (0009 module now importable).
- Integration (requires compose Postgres `klake_test`): `./.venv/bin/pytest tests/integration/test_migrations.py -x` confirms the 3 columns exist after `upgrade head` and the downgrade/upgrade round-trip is clean.
</verification>

<success_criteria>
Migration `0009` is the additive, forward-only head; `Source` carries the three nullable columns; the four repo helpers and `create_source` kwarg exist and pass unit checks; `max_staleness_days` reads its env override.
</success_criteria>

<output>
Create `.planning/phases/11-crawl-scheduling/11-02-SUMMARY.md` when done.
</output>