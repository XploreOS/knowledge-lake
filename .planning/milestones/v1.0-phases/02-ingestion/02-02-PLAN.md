---
phase: 02-ingestion
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/knowledge_lake/plugins/protocols.py
  - src/knowledge_lake/plugins/resolver.py
  - src/knowledge_lake/ids.py
  - src/knowledge_lake/config/settings.py
  - src/knowledge_lake/storage/s3.py
  - src/knowledge_lake/registry/models.py
  - src/knowledge_lake/registry/repo.py
  - src/knowledge_lake/registry/alembic/versions/0003_crawl_jobs_states.py
  - src/knowledge_lake/crawl/__init__.py
  - src/knowledge_lake/crawl/robots.py
  - src/knowledge_lake/crawl/ratelimit.py
  - pyproject.toml
  - tests/unit/test_robots_ratelimit.py
  - tests/unit/test_put_bronze.py
  - tests/integration/test_crawl_schema.py
autonomous: true
requirements: [INGEST-04, INGEST-09]
must_haves:
  truths:
    - "crawl_states table exists with UNIQUE(job_id, normalized_url) and status in pending/complete/failed/robots_blocked"
    - "put_bronze writes bytes to the bronze zone content-addressed (WORM, hash no-op) with a parent_artifact_id linking bronze → raw (D-01)"
    - "the three-tier rate-limit resolver returns Source.config override over robots Crawl-delay over the global Settings default (D-12)"
    - "CrawlerPlugin protocol (D-02: start_crawl/poll_status/get_results multi-method interface supporting long-running crawls) and get_crawler resolver over group knowledge_lake.crawlers are importable"
    - "robots.py parses Disallow rules and Crawl-delay via Protego"
  artifacts:
    - src/knowledge_lake/registry/alembic/versions/0003_crawl_jobs_states.py
    - src/knowledge_lake/crawl/robots.py
    - src/knowledge_lake/crawl/ratelimit.py
    - src/knowledge_lake/storage/s3.py
  key_links:
    - "crawl_states uniqueness is keyed on (job_id, normalized_url) NOT content hash, so identical content under a new URL is a new state row pointing at the no-op'd existing artifact (Pitfall 4)"
    - "get_crawler resolves settings.crawler over entry-point group knowledge_lake.crawlers, injecting settings where needed (CR-03, no os.environ in builtins)"
    - "put_bronze re-uses repo.get_artifact_by_hash for hash-second no-op, mirroring put_raw"
  prohibitions:
    - "crawl_states UNIQUE MUST NOT be on content_hash (would raise IntegrityError on identical content across URLs)"
    - "builtins MUST NOT read os.environ — service URLs/config are injected from Settings"
---

<objective>
Build the durable substrate the crawler slices stand on: the `CrawlerPlugin` protocol + dataclasses, the crawl-job/`crawl_states` schema (Alembic 0003, extending the Phase-1 placeholder `jobs` table), the content-addressed bronze-zone writer (`put_bronze`, D-01 two-artifact lineage), the crawler resolver seam, and the two politeness primitives (Protego-backed robots parsing + the three-tier rate-limit resolver, D-12). No adapter and no user-facing crawl yet — those land in 02-03.

Purpose: the crawler subsystem legitimately needs this substrate before the first crawl can run end to end; the phase guidance explicitly authorizes it as a thin foundation slice. Kept to persistence + protocol + storage + politeness primitives only.
Output: CrawlerPlugin/CrawlJob/CrawlPageResult, GROUP_CRAWLERS/get_crawler, ids prefixes, CrawlSettings + crawler swap key, put_bronze, crawl_states + jobs columns, crawl/robots.py, crawl/ratelimit.py.
</objective>

## Phase Goal (slice framing)

**As a** knowledge-lake operator, **I want to** have a durable, swappable crawler substrate (persistent per-URL crawl state, bronze lineage, robots + rate-limit resolution), **so that** the next slice can run a real crawl that resumes after interruption and stays polite — without re-architecting persistence.

> Foundation slice (authorized exception, per phase guidance). Delivers no user-clickable capability alone; 02-03 completes the vertical slice by adding the adapter, orchestrator, and CLI/API.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingestion/02-CONTEXT.md
@.planning/phases/02-ingestion/02-RESEARCH.md
@.planning/phases/02-ingestion/02-PATTERNS.md
@.planning/phases/02-ingestion/02-01-SUMMARY.md
@src/knowledge_lake/plugins/protocols.py
@src/knowledge_lake/plugins/resolver.py
@src/knowledge_lake/ids.py
@src/knowledge_lake/config/settings.py
@src/knowledge_lake/storage/s3.py
@src/knowledge_lake/registry/models.py
@src/knowledge_lake/registry/repo.py
@src/knowledge_lake/registry/alembic/versions/0001_core_schema.py
</context>

## Artifacts this phase produces (new symbols — exclude from source-drift verification)

- Protocols/dataclasses: `CrawlerPlugin`, `CrawlJob`, `CrawlPageResult` (plugins/protocols.py)
- Resolver: `GROUP_CRAWLERS = "knowledge_lake.crawlers"`, `get_crawler(settings)` (plugins/resolver.py)
- ID prefixes: `crawl_job`→`job`, `crawl_state`→`cst`, `bronze_document`→`doc` (ids.py `_PREFIX`)
- Settings: `crawler` swap key (default "crawl4ai"), `CrawlSettings` (max_pages, max_depth, rate_limit_seconds, same_domain_only), `crawl` field (config/settings.py)
- Storage: `StorageBackend.put_bronze(source_id, data, ext, session, *, parent_artifact_id)` (storage/s3.py)
- Models: `CrawlState` (registry/models.py); `Job` extended with source_id, job_type (default "crawl"), crawler, config, stats, updated_at
- Repo: `create_crawl_job`, `upsert_crawl_state`, `pending_states`, `create_bronze_artifact` (registry/repo.py)
- Migration `0003_crawl_jobs_states.py` (revision "0003", down_revision "0002")
- `crawl/` package: `crawl/robots.py` (`RobotsPolicy`/`fetch_robots`, Crawl-delay parse), `crawl/ratelimit.py` (`resolve_delay`, per-host limiter)
- Runtime deps: `protego==0.6.2`, `tldextract==5.3.1`
- Tests: `tests/unit/test_robots_ratelimit.py`, `tests/unit/test_put_bronze.py`, `tests/integration/test_crawl_schema.py`

<tasks>

<task type="auto">
  <name>Task 1: CrawlerPlugin protocol + resolver seam + ID prefixes + crawl settings</name>
  <read_first>
    - src/knowledge_lake/plugins/protocols.py (EmbedderPlugin lines 118-148; VectorPoint dataclass lines 74-92 — the shapes to mirror)
    - src/knowledge_lake/plugins/resolver.py (GROUP_* lines 33-35; get_vectorstore lines 115-141 — settings-injection getter pattern)
    - src/knowledge_lake/ids.py (_PREFIX map lines 31-37)
    - src/knowledge_lake/config/settings.py (StorageSettings lines 27-49; swap keys lines 82-94)
    - .planning/phases/02-ingestion/02-RESEARCH.md (Pattern 1 — CrawlJob/CrawlPageResult/CrawlerPlugin shapes, lines 234-257)
  </read_first>
  <files>src/knowledge_lake/plugins/protocols.py, src/knowledge_lake/plugins/resolver.py, src/knowledge_lake/ids.py, src/knowledge_lake/config/settings.py</files>
  <action>
    In protocols.py add dataclasses `CrawlJob` (job_id, source_url, crawler, status="pending", config dict) and `CrawlPageResult` (url, status, html: bytes|None, markdown: str|None, error, fetched_at) plus a `@runtime_checkable class CrawlerPlugin(Protocol)` with `name: str` and `start_crawl(self, source_url, config) -> CrawlJob`, `poll_status(self, job_id) -> str`, `get_results(self, job_id) -> list[CrawlPageResult]`, keeping the thorough inline docstrings the file uses. In resolver.py add `GROUP_CRAWLERS = "knowledge_lake.crawlers"` and `get_crawler(settings)` following the get_parser no-arg `resolve(GROUP_CRAWLERS, settings.crawler)` form. In ids.py `_PREFIX` add `"crawl_job": "job"`, `"crawl_state": "cst"`, `"bronze_document": "doc"`. In settings.py add a plain `BaseModel` `CrawlSettings` (max_pages: int = 50, max_depth: int = 2, rate_limit_seconds: float = 1.0, same_domain_only: bool = True), wire `crawl: CrawlSettings = Field(default_factory=CrawlSettings)`, and add swap key `crawler: str = "crawl4ai"`. Add a regex validator posture for the crawler swap key consistent with ASVS V5.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_settings.py tests/unit/test_ids.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from knowledge_lake.plugins.protocols import CrawlerPlugin, CrawlJob, CrawlPageResult"` succeeds.
    - `python -c "from knowledge_lake.plugins.resolver import GROUP_CRAWLERS, get_crawler"` succeeds and GROUP_CRAWLERS == "knowledge_lake.crawlers".
    - `new_id("crawl_state")` returns a value starting with "cst_"; `new_id("crawl_job")` starts with "job_".
    - `get_settings().crawler == "crawl4ai"` and `get_settings().crawl.max_pages == 50`.
  </acceptance_criteria>
  <done>Protocol, resolver getter, ID prefixes, and crawl settings are importable and defaulted correctly.</done>
</task>

<task type="auto">
  <name>Task 2: crawl schema (0003) + bronze storage + crawl repo functions</name>
  <read_first>
    - src/knowledge_lake/registry/models.py (Source lines 54-99, Artifact FK/UniqueConstraint lines 116-197, Job lines 257-272)
    - src/knowledge_lake/registry/repo.py (create_source lines 34-85, _make_artifact lines 91-117, create_parsed_artifact lines 156-182, get_artifact_by_hash lines 224-243)
    - src/knowledge_lake/storage/s3.py (put_raw lines 151-243 — six-layer WORM structure to mirror for bronze)
    - src/knowledge_lake/registry/alembic/versions/0001_core_schema.py (create_table/add_column/index/constraint patterns; downgrade reverse-order)
    - .planning/phases/02-ingestion/02-RESEARCH.md (Pitfall 4 — crawl_states uniqueness on (job_id, normalized_url))
  </read_first>
  <files>src/knowledge_lake/registry/models.py, src/knowledge_lake/registry/repo.py, src/knowledge_lake/storage/s3.py, src/knowledge_lake/registry/alembic/versions/0003_crawl_jobs_states.py, tests/unit/test_put_bronze.py, tests/integration/test_crawl_schema.py</files>
  <action>
    Extend the `Job` model with source_id (FK sources.id, nullable), job_type (String, default "crawl"), crawler (String, nullable), config (_JSON), stats (_JSON), updated_at (DateTime tz). Add a `CrawlState` model: id (String(64) PK, "cst_" prefix), job_id (FK jobs.id, RESTRICT), url (Text), normalized_url (Text), status (String — pending/complete/failed/robots_blocked), raw_artifact_id + bronze_artifact_id (FK artifacts.id, nullable), fetched_at (DateTime tz nullable), created_at; `__table_args__ = (UniqueConstraint("job_id","normalized_url", name="uq_crawl_states_job_url"),)`. Create Alembic `0003_crawl_jobs_states.py` (revision "0003", down_revision "0002") adding the Job columns and creating the crawl_states table + its unique constraint + an index on (job_id, status) for the resume query, with a symmetric downgrade. Add `put_bronze(self, source_id, data, ext, session, *, parent_artifact_id)` to StorageBackend mirroring put_raw exactly but with zone prefix `bronze/`, artifact_type `bronze_document`, hash-lookup no-op via get_artifact_by_hash(..., "bronze_document"), and passing parent_artifact_id. Add repo `create_bronze_artifact` (via _make_artifact, artifact_type "bronze_document", required parent_artifact_id), `create_crawl_job`, `upsert_crawl_state` (insert-or-update on (job_id, normalized_url)), and `pending_states(session, job_id)` (ORM select WHERE job_id AND status=="pending"). Create test_put_bronze.py (bronze write sets parent_artifact_id and no-ops on repeat hash) and test_crawl_schema.py (0003 applies; UNIQUE(job_id, normalized_url) enforced; same content under two URLs yields two crawl_states rows).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_put_bronze.py tests/integration/test_crawl_schema.py tests/unit/test_registry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Migration 0003 has revision "0003", down_revision "0002"; `crawl_states` table exists with a UNIQUE constraint named `uq_crawl_states_job_url` on (job_id, normalized_url).
    - `jobs` gains source_id, job_type (default "crawl"), crawler, config, stats, updated_at columns.
    - test_put_bronze asserts the bronze artifact has artifact_type "bronze_document" and parent_artifact_id == the raw artifact id, and a repeat put_bronze of identical bytes returns the same artifact (no-op).
    - test_crawl_schema asserts inserting two crawl_states rows with identical (job_id, normalized_url) raises IntegrityError, but identical content under two different normalized_urls succeeds.
  </acceptance_criteria>
  <done>The crawl schema, bronze writer, and crawl repo functions land and their tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: robots parsing + three-tier rate-limit resolver</name>
  <read_first>
    - src/knowledge_lake/pipeline/ingest.py (tenacity retry decorator lines 107-112 — reuse config for robots fetch)
    - .planning/phases/02-ingestion/02-RESEARCH.md (Three-tier rate-limit resolver lines 438-447; Don't Hand-Roll — protego for robots; D-11/D-12/D-13)
    - src/knowledge_lake/config/settings.py (CrawlSettings from Task 1 — global default source)
    - pyproject.toml ([project.dependencies] — where protego/tldextract are added)
  </read_first>
  <files>src/knowledge_lake/crawl/__init__.py, src/knowledge_lake/crawl/robots.py, src/knowledge_lake/crawl/ratelimit.py, pyproject.toml, tests/unit/test_robots_ratelimit.py</files>
  <behavior>
    - resolve_delay(source_config, robots_crawl_delay, global_default): returns Source.config['rate_limit_seconds'] when present (tier 1); else robots Crawl-delay when present (tier 2); else global_default (tier 3). All four permutations return the expected value.
    - robots.py fetch+parse: given a robots.txt body disallowing /private/, `is_allowed("/private/x")` is False and `is_allowed("/public/y")` is True.
    - robots.py exposes the Crawl-delay for a user agent when present (feeds tier 2).
  </behavior>
  <action>
    Add `protego==0.6.2` and `tldextract==5.3.1` to pyproject [project.dependencies]. Create the `crawl/` package (`__init__.py`). In crawl/robots.py add a Protego-backed helper: `fetch_robots(base_url)` (httpx GET /robots.txt with the tenacity retry config; treat network failure per a documented policy) returning a `RobotsPolicy` object exposing `is_allowed(path, user_agent="*")` and `crawl_delay(user_agent="*")`. In crawl/ratelimit.py add `resolve_delay(source_config, robots_crawl_delay, global_default) -> float` implementing the exact D-12 tier order, plus a small per-host async limiter keyed by registrable domain via tldextract (records last-fetch time, sleeps to honor the resolved delay). Create tests/unit/test_robots_ratelimit.py: table test all four resolve_delay permutations; parse a robots.txt fixture string (Disallow /private/ + Crawl-delay) and assert is_allowed + crawl_delay.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_robots_ratelimit.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_robots_ratelimit.py -x` exits 0.
    - resolve_delay({'rate_limit_seconds': 5}, 2.0, 1.0) == 5.0; resolve_delay(None, 2.0, 1.0) == 2.0; resolve_delay(None, None, 1.0) == 1.0; resolve_delay({}, None, 1.0) == 1.0.
    - Parsing a robots body with `Disallow: /private/` yields is_allowed("/private/x") == False and is_allowed("/open") == True.
  </acceptance_criteria>
  <done>Robots parsing and the three-tier rate-limit resolver exist with green unit tests; protego + tldextract installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings → resolver → builtin | Swap keys (crawler) select code paths; must be validated, never used to read arbitrary env |
| target host → robots.py | robots.txt body is attacker-controlled content parsed before crawling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering (constraint bypass) | crawl_states dedup | medium | mitigate | UNIQUE(job_id, normalized_url) NOT on content_hash (Pitfall 4) prevents IntegrityError-on-recrawl and double-processing on resume. |
| T-02-06 | Repudiation (robots parse error) | crawl/robots.py | medium | mitigate | Use Protego (handles wildcards, `$`, Crawl-delay, agent precedence) instead of a hand-rolled parser; document the unreachable-robots policy. |
| T-02-07 | DoS (self-inflicted on target) | crawl/ratelimit.py | high | mitigate | Three-tier resolver enforces the strictest applicable delay; per-host limiter keyed on registrable domain (tldextract) so multi-host crawls throttle per host. |
| T-02-08 | Tampering (config injection) | resolver crawler swap key | low | mitigate | Regex-validate the crawler swap key (ASVS V5); no os.environ reads in builtins (CR-03). |
| T-02-SC | Tampering (supply chain) | protego, tldextract | low | accept | Both Approved in RESEARCH Package Legitimacy Audit (protego = Scrapy team; tldextract 10+ yrs). No [ASSUMED]/[SUS]/[SLOP]. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_robots_ratelimit.py tests/unit/test_put_bronze.py tests/integration/test_crawl_schema.py -q` green.
- `uv run pytest -q` full suite green (migration chain 0001→0002→0003 applies).
- Protocol/resolver/settings importable; `uv run klake --help` still lists existing commands (no CLI regression).
</verification>

<success_criteria>
- Crawler substrate (protocol, resolver seam, crawl_states schema, bronze writer, robots + rate-limit primitives) exists and is unit-tested.
- INGEST-09 primitives (robots parse, three-tier delay) are proven; enforcement in a live crawl is 02-03.
- INGEST-04 substrate (bronze lineage, crawl-job persistence) is ready for the Crawl4AI adapter.
</success_criteria>

<output>
Create `.planning/phases/02-ingestion/02-02-SUMMARY.md` when done, listing every new symbol (protocol, dataclasses, repo funcs, settings, put_bronze, robots/ratelimit) under "Artifacts produced".
</output>
