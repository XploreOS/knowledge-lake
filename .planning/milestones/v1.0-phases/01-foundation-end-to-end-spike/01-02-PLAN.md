---
phase: 01-foundation-end-to-end-spike
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/knowledge_lake/ids.py
  - src/knowledge_lake/version.py
  - src/knowledge_lake/registry/__init__.py
  - src/knowledge_lake/registry/models.py
  - src/knowledge_lake/registry/repo.py
  - src/knowledge_lake/registry/db.py
  - src/knowledge_lake/registry/alembic/env.py
  - src/knowledge_lake/registry/alembic/script.py.mako
  - src/knowledge_lake/registry/alembic/versions/0001_core_schema.py
  - alembic.ini
  - tests/unit/test_ids.py
  - tests/unit/test_version.py
  - tests/unit/test_registry.py
  - tests/unit/test_artifact_fields.py
  - tests/integration/test_migrations.py
autonomous: false
requirements: [FOUND-05, FOUND-06, FOUND-09]
must_haves:
  truths:
    - "`alembic upgrade head` builds the full core schema clean on an empty PostgreSQL database"
    - "The registry stores sources, artifacts (raw/parsed/chunk nodes), lineage events, and empty jobs/datasets tables"
    - "Every artifact row carries source id, parent artifact id, content hash, timestamp, pipeline version, and storage uri"
    - "Entity IDs are UUIDv7 with short type prefixes (src_/doc_/chk_/art_) and are time-sortable"
    - "pipeline_version resolves to package version plus short git SHA, falling back to package version alone"
  artifacts:
    - "src/knowledge_lake/ids.py (new_id helper) and version.py (pipeline_version helper)"
    - "src/knowledge_lake/registry/models.py with the self-referencing artifacts node table"
    - "src/knowledge_lake/registry/repo.py with hash-lookup, artifact-create, and query helpers"
    - "Alembic migration 0001_core_schema creating the full core table set"
  key_links:
    - "artifacts.parent_artifact_id self-FK powers the FOUND-07 recursive-CTE ancestry"
    - "UNIQUE(content_hash, artifact_type) enables the FOUND-04 registry no-op / dedup"
    - "repo.get_artifact_by_hash is the function storage put_raw calls to make re-ingest a no-op (consumed in 01-03)"
  prohibitions:
    - "No Base.metadata.create_all() or hand-written ALTER TABLE for schema — Alembic migrations only (FOUND-09, Pitfall 10)"
    - "No raw string-formatted SQL — SQLAlchemy parameterized queries/ORM only"
    - "No table-per-attribute over-normalization — core is sources + unified artifacts + lineage_events + JSONB (Pitfall 8)"
---

<objective>
Build the PostgreSQL registry that is the source of truth for identity and lineage: stable prefixed UUIDv7 IDs, pipeline_version stamping, the self-referencing `artifacts` node table (+ sources, lineage_events, empty jobs/datasets), and Alembic migration #1 that creates the whole core schema.

Purpose: Satisfy FOUND-05 (registry entities), FOUND-06 (six lineage fields on every artifact), and FOUND-09 (Alembic from the first table) — the backbone the storage no-op (01-03) and lineage query (01-05) depend on.
Output: A clean `alembic upgrade head`, a repo layer for hash-lookup and node creation, and green unit + migration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-end-to-end-spike/01-CONTEXT.md
@.planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md
@.planning/phases/01-foundation-end-to-end-spike/SKELETON.md
@.planning/phases/01-foundation-end-to-end-spike/01-01-SUMMARY.md
</context>

## Phase Goal

**As an** operator of the Knowledge Lake, **I want to** bring up the full stack with one command and flow one real document from ingest to a cited, lineage-traceable search result, **so that** the tool-agnostic foundation every later phase depends on is proven working end-to-end.

This plan delivers the operator-visible capability: the registry can persist a document/chunk with full provenance and its schema is migration-managed from table one.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package legitimacy gate for uuid-utils (blocking-human)</name>
  <what-built>Nothing yet — this is the package legitimacy gate required before installing the one [ASSUMED] third-party package this phase adds.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/uuid-utils/ and confirm the project exists, is actively maintained, and its source repo is github.com/aminalaee/uuid-utils (matches RESEARCH Package Legitimacy Audit).
    2. Confirm version 0.16.2 is a real published release and the download counts / repo look legitimate (not a typosquat).
    3. If uuid-utils looks compromised or you prefer the pure-Python alternative, approve `uuid6` (2025.0.1, github.com/oittaa/uuid6-python) instead — the ids.py helper is written to hide the choice behind one import line.
  </how-to-verify>
  <resume-signal>Type "approved: uuid-utils" (or "approved: uuid6") to authorize the install, or describe concerns.</resume-signal>
  <files>pyproject.toml</files>
  <action>Do not run any `uv add` for the UUIDv7 library until a human approves this checkpoint. Present the pypi.org verification facts for uuid-utils (project exists, maintained, source repo github.com/aminalaee/uuid-utils, version 0.16.2) and wait for the resume signal. This gate is never auto-approvable regardless of auto_advance. On approval, the approved package name is what Task 1 installs.</action>
  <verify>
    <human-check>Human has confirmed uuid-utils (or uuid6) is legitimate on PyPI and typed the approval resume-signal.</human-check>
  </verify>
  <done>A human has explicitly approved the UUIDv7 library to install; the approved name is recorded for Task 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Prefixed UUIDv7 IDs and pipeline_version helpers (D-15, D-04)</name>
  <files>src/knowledge_lake/ids.py, src/knowledge_lake/version.py, tests/unit/test_ids.py, tests/unit/test_version.py</files>
  <read_first>.planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Code Examples: UUIDv7 + type prefix; pipeline_version), src/knowledge_lake/config/settings.py</read_first>
  <behavior>
    - new_id("source") returns a string beginning with "src_" whose remainder is a valid UUIDv7; new_id for raw_document/parsed_document -> "doc_", chunk -> "chk_"
    - Two IDs generated in sequence sort in creation order (UUIDv7 time-ordering)
    - pipeline_version() returns "<pkgversion>+<shortsha>" inside a git checkout and "<pkgversion>" (no plus) when git is unavailable
    - An unknown kind passed to new_id raises a clear error
  </behavior>
  <action>After the legitimacy checkpoint is approved, run `uv add uuid-utils` (or `uv add uuid6` if that was approved). Implement ids.py with a `new_id(kind)` helper mapping kinds (source, raw_document, parsed_document, chunk, artifact) to short prefixes and appending `uuid_utils.uuid7()`; isolate the library import to this one module so the Python 3.14 stdlib switch is a one-line change. Implement version.py `pipeline_version()` reading the installed package version via importlib.metadata with a "0.0.0" fallback, appending the short git SHA from `git rev-parse --short HEAD` when available, gracefully returning package-only on any failure. Write the unit tests per the behavior block.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_ids.py tests/unit/test_version.py -x -q</automated>
  </verify>
  <done>Prefixed UUIDv7 IDs generate and sort by time; pipeline_version returns pkg+SHA with a clean pkg-only fallback; both test files green.</done>
  <acceptance_criteria>
    - new_id returns correctly prefixed, time-sortable UUIDv7 strings per kind
    - The UUIDv7 library import appears only in ids.py
    - pipeline_version() format is "<pkg>+<sha>" or "<pkg>" and never raises
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLAlchemy registry models + repo layer (FOUND-05, FOUND-06)</name>
  <files>src/knowledge_lake/registry/__init__.py, src/knowledge_lake/registry/db.py, src/knowledge_lake/registry/models.py, src/knowledge_lake/registry/repo.py, tests/unit/test_registry.py, tests/unit/test_artifact_fields.py</files>
  <read_first>src/knowledge_lake/ids.py, src/knowledge_lake/version.py, src/knowledge_lake/config/settings.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Pattern 3: Registry-First Writes + Unified Artifact Lineage Node)</read_first>
  <behavior>
    - Creating a source returns a row with an src_-prefixed id, name, source_type, url, license fields, created_at
    - Creating a raw artifact requires and persists all six FOUND-06 fields: source_id, parent_artifact_id (NULL for raw), content_hash, pipeline_version, storage_uri, created_at; artifact_type = raw_document
    - Creating a parsed artifact and a chunk artifact sets parent_artifact_id to the upstream node
    - get_artifact_by_hash(hash, artifact_type) returns the existing row when present and None otherwise
    - The UNIQUE(content_hash, artifact_type) constraint prevents duplicate nodes of the same type+content
  </behavior>
  <action>Implement the SQLAlchemy 2.0 typed models (declarative, `Mapped[...]`) in models.py: `sources` (id PK prefixed, name, source_type, url, license_type, license_url, robots_checked bool, config JSONB, created_at), the unified self-referencing `artifacts` node (id PK prefixed via new_id, source_id FK->sources, parent_artifact_id FK->artifacts nullable, artifact_type, content_hash NOT NULL, pipeline_version NOT NULL, storage_uri, mime_type, page_ref, section_path, metadata JSONB default {}, created_at NOT NULL default now, UNIQUE(content_hash, artifact_type)), `lineage_events` (id, artifact_id FK, parent_artifact_id FK, relationship, pipeline_version, created_at), and empty `jobs` and `datasets` tables sufficient to satisfy FOUND-05's enumerated set. Create db.py with the SQLAlchemy engine/session built from Settings.database_url (postgresql+psycopg). Create repo.py with create_source, create_raw_artifact, create_parsed_artifact, create_chunk_artifact (each stamping pipeline_version() and new_id()), get_artifact_by_hash, get_artifact, and list_children. All queries via ORM/parameterized — no string SQL. Tests run against an ephemeral Postgres (compose or testcontainers fixture from conftest); assert every artifact carries the six FOUND-06 fields and that the unique constraint holds.</action>
  <verify>
    <automated>uv run pytest tests/unit/test_registry.py tests/unit/test_artifact_fields.py -x -q</automated>
  </verify>
  <done>Registry persists sources and raw/parsed/chunk artifact nodes; every artifact has the six lineage fields; get_artifact_by_hash returns existing or None; unique constraint enforced; tests green.</done>
  <acceptance_criteria>
    - artifacts is a single self-referencing node table distinguished by artifact_type
    - Every created artifact row has non-null source_id, content_hash, pipeline_version, created_at and a storage_uri and parent linkage as applicable (FOUND-06)
    - get_artifact_by_hash(content_hash, artifact_type) is the dedup lookup for 01-03
    - No raw string SQL anywhere in repo.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Alembic setup + migration #1 creates the full core schema (FOUND-09)</name>
  <files>alembic.ini, src/knowledge_lake/registry/alembic/env.py, src/knowledge_lake/registry/alembic/script.py.mako, src/knowledge_lake/registry/alembic/versions/0001_core_schema.py, tests/integration/test_migrations.py</files>
  <read_first>src/knowledge_lake/registry/models.py, src/knowledge_lake/config/settings.py, .planning/phases/01-foundation-end-to-end-spike/01-RESEARCH.md (Pitfall 10; Pattern 3 schema block)</read_first>
  <action>Configure Alembic: alembic.ini pointing script_location at the in-package alembic dir; env.py wiring `target_metadata` to the models' MetaData and reading the DB URL from Settings (not a hardcoded literal). Author migration 0001_core_schema that creates the entire core table set (sources, artifacts with the self-FK and UNIQUE(content_hash, artifact_type), lineage_events, empty jobs, empty datasets) plus indexes on artifacts.content_hash, artifacts.source_id, artifacts.parent_artifact_id, and artifacts.created_at. The migration must be the sole DDL path — the app never calls create_all. Write tests/integration/test_migrations.py that runs `alembic upgrade head` against an empty database and asserts every expected table and index exists, and that `alembic downgrade base` then `upgrade head` round-trips clean.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_migrations.py -x -q</automated>
  </verify>
  <done>`alembic upgrade head` builds the full core schema on an empty DB; all core tables + indexes present; downgrade/upgrade round-trips; test green.</done>
  <acceptance_criteria>
    - Migration 0001 creates sources, artifacts, lineage_events, jobs, datasets and the four artifact indexes
    - env.py reads the DB URL from Settings, not a hardcoded string
    - No create_all path exists; Alembic is the only schema authority (FOUND-09)
    - upgrade head on empty DB succeeds; downgrade→upgrade round-trips
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → PostgreSQL | All registry reads/writes; injection surface if SQL is string-built |
| PyPI → build | uuid-utils enters the supply chain (gated by checkpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | registry SQL (repo.py) | high | mitigate | SQLAlchemy ORM/parameterized queries only; no string-formatted SQL |
| T-01-04 | Repudiation | artifact provenance fields | medium | mitigate | Six FOUND-06 fields NOT NULL where applicable; pipeline_version + content_hash stamped on every node |
| T-01-SC | Tampering | uuid-utils install | high | mitigate | Blocking-human legitimacy checkpoint verifies uuid-utils on pypi.org/project/uuid-utils before install; uuid6 approved fallback; uv.lock pins the result |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_ids.py tests/unit/test_version.py tests/unit/test_registry.py tests/unit/test_artifact_fields.py tests/integration/test_migrations.py` green
- `alembic upgrade head` on an empty DB creates the full core schema
- Grep confirms no `create_all(` and no string-formatted SQL in registry/
</verification>

<success_criteria>
- FOUND-05: registry stores sources, artifacts (documents/chunks via type), lineage events, jobs, datasets with stable IDs + hashes
- FOUND-06: every artifact records the six lineage fields
- FOUND-09: schema is Alembic-managed from migration #1
</success_criteria>

## Artifacts This Phase Produces (Plan 02)

- Files: `src/knowledge_lake/ids.py`, `version.py`, `registry/db.py`, `registry/models.py`, `registry/repo.py`, `alembic.ini`, `registry/alembic/env.py`, `registry/alembic/versions/0001_core_schema.py`
- Functions: `new_id`, `pipeline_version`, `create_source`, `create_raw_artifact`, `create_parsed_artifact`, `create_chunk_artifact`, `get_artifact_by_hash`, `get_artifact`, `list_children`
- ORM models / tables: `Source`/`sources`, `Artifact`/`artifacts`, `LineageEvent`/`lineage_events`, `Job`/`jobs`, `Dataset`/`datasets`
- Alembic revision id: `0001_core_schema`
- Test files: `tests/unit/test_ids.py`, `test_version.py`, `test_registry.py`, `test_artifact_fields.py`, `tests/integration/test_migrations.py`

<output>
Create `.planning/phases/01-foundation-end-to-end-spike/01-02-SUMMARY.md` when done.
</output>
