---
quick_id: 260715-3w5
slug: write-e2e-gap-analysis-report
description: Write E2E gap analysis report
created: 2026-07-15
mode: quick
status: planned
---

# Quick Task 260715-3w5: Write E2E gap analysis report

## Goal

Land the findings from the 2026-07-15 end-to-end test + codebase audit as a
durable, reviewable document in `.planning/`, so the remediation work that
follows has a single referenceable source with stable finding IDs (KL-01..KL-17).

## Scope

**Document only.** No source code is touched by this task. Every fix implied by
the report is deliberately deferred to its own follow-up task.

## Context

The audit was performed live against the running stack (postgres, minio, qdrant,
litellm, dagster, searxng) by scaffolding a new `aviation` domain pack via
`klake domain new`, authoring it, and driving the FAA Airplane Flying Handbook
Ch.4 (public domain, 4.6 MB, 19pp) through the full pipeline to gold-zone
outputs. Findings marked `reproduced` were confirmed by execution or a probe
script, not inferred from reading.

## Tasks

### Task 1 — Write `.planning/E2E-GAP-ANALYSIS.md`

- **files:** `.planning/E2E-GAP-ANALYSIS.md`
- **action:** Create the audit report. YAML frontmatter matching the convention
  established by `.planning/v2.5-MILESTONE-AUDIT.md` (audited date, scope,
  status, severity counts, findings list with id/severity/area/reproduced),
  followed by a markdown body containing: the headline, all 17 findings
  (KL-01..KL-17) ranked by severity with What / Evidence / Impact / Fix, a
  "what holds up" section, and a suggested fix order.
- **verify:** File exists; frontmatter parses as valid YAML; all 17 finding IDs
  KL-01..KL-17 present in both frontmatter and body.
- **done:** Report committed; no file under `src/` modified by this task.

## Must haves

- **truths:**
  - The report records only what was verified; unconfirmed claims are labelled as such.
  - Finding IDs are stable (KL-01..KL-17) so follow-up fix tasks can cite them.
  - No source code changes in this commit.
- **artifacts:**
  - `.planning/E2E-GAP-ANALYSIS.md`
- **key_links:**
  - `.planning/v2.5-MILESTONE-AUDIT.md` (frontmatter convention being matched)

## Out of scope

- Fixing any KL-## finding (each becomes its own follow-up task).
- Removing the `domains/aviation/` E2E fixture (left untracked, user's call).
