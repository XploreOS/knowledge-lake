---
phase: quick
plan: 260718-6fu
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge_lake/pipeline/quality/predicates.py
  - src/knowledge_lake/pipeline/chunk.py
autonomous: true
requirements: [CI-LINT-01]

must_haves:
  truths:
    - "ruff check src/knowledge_lake/pipeline/quality/predicates.py src/knowledge_lake/pipeline/chunk.py reports zero errors"
    - "Full repo ruff check . passes clean (no regressions introduced by the fix)"
  artifacts:
    - src/knowledge_lake/pipeline/quality/predicates.py
    - src/knowledge_lake/pipeline/chunk.py
  key_links:
    - "predicates.py import block sorted per ruff I001 (isort) rules — _LINK_PATTERN before STOP_WORDS_SET in the constants import"
    - "chunk.py import block has a blank line separating the importlib.metadata noqa-comment block from the trailing defensive-comment lines, per ruff's I001 formatting"
---

<objective>
Fix the two ruff I001 ("Import block is un-sorted or un-formatted") failures reported by GitHub Actions CI run 29630349102 on the "Lint (ruff)" job, in `src/knowledge_lake/pipeline/quality/predicates.py:33` and `src/knowledge_lake/pipeline/chunk.py:28`.

Purpose: Unblock CI — this is a pure import-formatting fix with zero behavior change.
Output: Both files reformatted by ruff's isort rule (I001) with no other code changes; `ruff check` passes clean across the repo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

The exact fix has already been verified via `uv run ruff check --diff` against both files:

In `predicates.py`, the `from knowledge_lake.pipeline.quality.constants import (...)` block needs `_LINK_PATTERN` reordered before `STOP_WORDS_SET` (ruff's isort sorts case-insensitively / leading-underscore-first, not pure ASCII order).

In `chunk.py`, the `import importlib.metadata  # noqa: F401 — defensive: ...` line is immediately followed by three more comment lines (a continuation of the defensive-comment explanation) with no blank line before `import re`. Ruff's import formatter wants a blank line inserted after the `importlib.metadata` line (before the trailing comment lines that precede `import re`), i.e. the comment block gets split by a blank line at that point. The comment TEXT itself is untouched — only a blank line is added and no import statements move in this file (the sort order is otherwise already correct: functools, hashlib, importlib.metadata, re).

No manual line editing is required — `ruff check --fix` on the two files produces exactly this diff and touches nothing else.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply ruff I001 auto-fix to both files</name>
  <files>src/knowledge_lake/pipeline/quality/predicates.py, src/knowledge_lake/pipeline/chunk.py</files>
  <action>Run `uv run ruff check --fix src/knowledge_lake/pipeline/quality/predicates.py src/knowledge_lake/pipeline/chunk.py` to let ruff apply its own I001 auto-fix. Do not hand-edit the import blocks — let the tool make the mechanical change (import reordering in predicates.py's constants import; blank-line insertion in chunk.py's import block). After the fix runs, inspect the diff with `git diff -- src/knowledge_lake/pipeline/quality/predicates.py src/knowledge_lake/pipeline/chunk.py` and confirm: (1) only import-block whitespace/ordering changed, (2) no import was added, removed, or renamed, (3) no non-import code line changed, (4) the `# noqa: F401` comment and its explanatory text on the `import importlib.metadata` line in chunk.py are preserved verbatim.</action>
  <verify>
    <automated>uv run ruff check src/knowledge_lake/pipeline/quality/predicates.py src/knowledge_lake/pipeline/chunk.py</automated>
  </verify>
  <done>Both files pass `ruff check` with zero I001 (or any other) errors; `git diff` shows only import-block reordering/whitespace changes with no semantic code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no repo-wide regressions</name>
  <files>N/A (verification only, no files modified)</files>
  <action>Run the full-repo lint check to confirm the fix did not introduce or expose any other lint violations, and run the test suite for the two touched modules to confirm behavior is unchanged (import reordering must not affect runtime — both modules are pure/deterministic per their own docstrings, so no functional regression is expected).</action>
  <verify>
    <automated>uv run ruff check . && uv run pytest tests/ -k "predicates or chunk" -q</automated>
  </verify>
  <done>`ruff check .` exits 0 (zero errors across the full repo) and the targeted pytest run passes with no failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This is a mechanical import-formatting fix with no new trust boundary, no input handling change, and no dependency change.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | predicates.py / chunk.py import statements | low | accept | Change is scoped to `ruff check --fix` output only; Task 1's verify step diffs the change to confirm no import target, semantics, or non-import code was altered. |
</threat_model>

<verification>
1. `uv run ruff check src/knowledge_lake/pipeline/quality/predicates.py src/knowledge_lake/pipeline/chunk.py` exits 0.
2. `uv run ruff check .` exits 0 (no regressions elsewhere in the repo).
3. `git diff` for both files shows only import-block whitespace/ordering changes — no import added/removed, no non-import line touched.
4. Targeted pytest run for the two affected modules passes.
</verification>

<success_criteria>
- CI's "Lint (ruff)" job (the I001 rule specifically) passes for both files.
- No behavior change: import semantics, `noqa` markers, and defensive comments are preserved exactly.
- Full-repo `ruff check .` is clean.
</success_criteria>

<output>
Create `.planning/quick/260718-6fu-fix-ci-lint-failure-ruff-i001-import-sor/260718-6fu-SUMMARY.md` when done
</output>
