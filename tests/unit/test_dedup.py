"""Tests for deduplication — exact hash and MinHash near-dup (CLEAN-03)."""
import pytest


@pytest.mark.xfail(strict=False, reason="implementation pending — Wave 0 stub")
def test_dedup_stub() -> None:
    raise NotImplementedError
